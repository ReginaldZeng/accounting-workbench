# 对接确认 · 核算侧回复 · 成本台账 → BP 定价测算 消费接口

> 2026-09-05 核算工作台线回复 BP 工作台线，对应《对接需求_BP消费接口_20260905》。
> 核算侧 §1–§3 已实现（**V2.439**，随 git 自动部署上线）。先答 §0 两件事，再列与文档**不同或需 BP 知道**的口径，最后对照验收。

## 0. 两件确认

| # | 事项 | 核算侧实况 |
|---|---|---|
| 1 | **状态命名** | **只有一档对外。** 核算侧是两个戳：成本会计**初审**（`finalized_by/at`，状态「初审」）→ 财务BP**终审**（`ack`，状态「已审核」＝前端「已审核 · 公开」）。**没有「已定稿」这个状态**——那是交接文档的旧称。「定稿指针」（`bom_quote_final` 表）指每个产品最新一次初审版，**对外只放其中状态＝已审核的**（初审未终审的绝不外发）。接口固定返回 `status:"final"`，BP 不解析中文。 |
| 2 | **物料编码** | `erpCode` 原样返回，空的返 `""`，**不过滤、不猜**；BP 同步时跳过并告警。核算侧标准台账已有「补物料编码」动作供成本会计补录，补后下次拉到。 |

## 1. 接口 `GET /api/bomcost/final`（新增，按贵方契约；原 `/api/bom/final` 保留不动）

- **选择口径**：定稿指针 + 状态＝已审核；同一 `productKey` 一版（指针即最新定稿）。
- **参数**：`channel`（接受 `电商/通品/TOB/TOC`，也接受代码 `ecom/common/tob/toc`）、`since`、`erpCodes`（逗号分隔）。
- **响应**：按文档字段；另加 `netWeightSrc`（`manual`＝成本会计确认 / `auto`＝按规格预填待确认 / `""`＝未填）、`firstReviewedBy/At`（初审）、`quotable/quoteReason`（成本会计是否建议对外报价及原因——BP 定价前请看）。
- **数值**含税、元/kg，与列表页同口径（同一 `compose` 函数）。

### 与文档不同的口径（请 BP 侧对齐）

| # | 点 | 核算侧做法 | 为什么 |
|---|---|---|---|
| a | **`finalizedAt/By` 与 `since` 按什么时间** | `finalizedAt/By` ＝ **终审**（成为 `final` 对外的时刻）；`since` 按它增量。初审时间另给 `firstReviewedAt/By`。 | 文档写按初审 `finalized_at` 增量：若一版初审早、终审晚，BP 按初审时间拉会**漏掉**它。对外时刻是终审，增量必须按终审。 |
| b | **时间格式** | 核算侧时间戳**到分**：`YYYY-MM-DD HH:MM`（如 `2026-09-05 20:51`），不是到秒。 | `since` 请**原样传接口回的 `asOf` / `finalizedAt`**（字符串比较），别自己拼到秒的串。 |
| c | **`transIncl`（运输）恒 `0.00`** | 台账「装卸费」＝源表**运输+装卸合并**入账，不单拆运输；`loadIncl` 已含运输。 | BP §4「加工费+装卸费→加工费」会把运输一并带进加工费；「运输费并入产品成本」那条对核算来源实际为 0，**不会双算**，但请勿再另加运费。 |
| d | **`netWeightKg` 可能为 `null`** | 定稿闸上线前已定稿的老行，规格解析不出的返 `null`。 | BP 同 `erpCode:""` 处理：跳过并告警；成本会计在核算侧补填后下次拉到。新定稿的行一律 >0（闸拦）。 |

## 2. 单位净重 `net_weight_kg`

- 列已加（FLOAT），迁移自动跑，服务器无需手工建列。
- 入账**不写库**，改为按 `pack_spec` **实时预填**（`(\d+(\.\d+)?)\s*(kg|g)\s*/\s*(袋|盒|瓶|包|罐)`，g 换 kg，如 `1kg/袋，10袋/箱`→1.0、`500g/盒`→0.5、`10袋/箱`→解析不出留空**不猜**），前端标「按规格预填·待确认」；成本会计在核算表右栏「单位净重(kg)」填/确认 → 存库 + `bom_quote_audit` 留痕（原值→新值）。
- **定稿闸**：空或 ≤0 **不准初审定稿**（两条定稿路径都拦，与勾稽同级），提示「单位净重(kg)未填或≤0——右栏「单位净重」填好并确认再定稿」。
- ⚠ **上线后对成本会计的实际影响**：规格能解析的单子右栏自动带值、点「确认」即可；解析不出的**必须手填才能定稿**。

## 3. 鉴权（服务间调用，不走用户 cookie）

- 放行二选一：① 请求头 `X-Internal-Token` ＝ `BOMCOST_INTERNAL_TOKEN` **且** 来源回环地址（127.0.0.1 / ::1）；② 门户登录用户。否则 **401**。
- 令牌读取：进程环境变量优先（**与 `DB_URL` 同机制**——服务器怎么给 DB_URL 就怎么给它），没有则读 `backend/.env` 的 `BOMCOST_INTERNAL_TOKEN=…`。**凭据只写两边服务器的 .env/环境变量，不进代码、不进本文档。**
- 核算 8000 / BP 8010 同机回环，无跨域。BP 调用示例：`GET http://127.0.0.1:8000/api/bomcost/final?since=2026-09-05 09:58`，头 `X-Internal-Token: <值>`。

## 5. 验收对照（文档 §5）

| # | 用例 | 结果 |
|---|---|---|
| 1 | 定稿一条、改费用再入新版定稿 → `final` 只返新版；`since` 传旧 `finalizedAt` 能拉到 | ✓（按终审时间；新版须终审通过才出现） |
| 2 | 空 `erp_code` 的定稿行 → `erpCode:""` | ✓ |
| 3 | `net_weight_kg` 空 → 定稿被拦，提示明确 | ✓ 两条定稿路径 |
| 4 | 无 token / 非回环 / 未登录 → 401；带 token 回环 → 200 | ✓（本机验：对 token+回环放行；非回环或错 token 落到登录判定） |
| 5 | `channel=电商` 只返电商行 | ✓（也可传 `ecom`） |

## 6. 换码承接（V2.440 追加，业务方 2026-09-05 定）——接口多两个字段

**场景**：BP 已按 CP0001 定价并审批通过；研发改配方换了新研发编码 CP0002，**物料编码不变**（还是同一 SKU），成本变了。

**核算侧规则**：
- CP 码正常不重复；同 CP 再来核算表，成本会计初审时**必须确认「原版是否失效」**。
- 不同 CP、同物料编码 → **后审核的替代先审核的**（按终审时间比）。旧版在新版**终审通过那一刻**退出对外（终审前 BP 仍拿旧版，不会出现没成本的空档）。
- 新版被终审退回 / 撤销定稿 / 作废 → 旧版自动恢复为当前版。

**接口变化**（`GET /api/bomcost/final`，兼容原契约，只增不改）：

| 字段 | 位置 | 含义 | BP 要做什么 |
|---|---|---|---|
| `supersedes` | 每行 | `[{entryId, cpCode, productKey, erpCode, finalizedAt, obsoleteAt}]`——本版**替代失效**的旧版（可多条、可链式）。无则 `[]`。 | 同步时凡非空：把引用其中任一 `entryId / productKey / erpCode` 的定价方案**全部打「成本已更新，N 个方案受影响」**，确认后刷新，不静默变价。按 `erpCode` 关联的物料本就会被本行覆盖；`supersedes` 是让你也能找到**按旧 CP / 旧 entryId 快照引用**的方案。 |
| `warnings` | 顶层 | `["物料编码 X 有 2 个对外版本（CP0001/CP0005），已按审核时间只发 CP0005；请成本会计…"]`——老数据两条都已审核、没走过确认时的兜底。 | 记日志/告警面板即可，**不用处理**：接口已保证同一物料编码只发一条（最新审核的），被压下的那条也列在该行 `supersedes` 里。 |

**保证**：同一 `erpCode` 永远只返回一行；`since` 增量照常按终审时间——新版终审 → `finalizedAt` 更新 → BP 拉到 → 看 `supersedes` 提示旧方案。

## 7. 身份键改 `productKey`（核算侧建议，2026-09-05，**待 BP 线确认**）

文档 §0-2 写「`erpCode` 是 BP 唯一关联键，空编码跳过并告警」。业务方补充事实：**没中试的产品本来就没有 ERP 物料编码**（中试后才建档），而 BOM 报价审的恰恰多是这类新品——按原口径它们会被 BP 整体拒之门外。建议改为：

| | 原口径 | 建议 |
|---|---|---|
| 身份键 | `erpCode` | **`productKey`**（产品名｜CP，入账起就有、终身不变） |
| `erpCode` | 准入条件，空则跳过 | **属性**：有就挂、空留空；中试建档后核算侧补码，`productKey` 不变，BP 下次同步原地更新 |
| 变更提示 | 按 erpCode 找方案 | 按 `productKey` 找方案；换码承接时用 `supersedes[].productKey/entryId/erpCode` 三个键都翻一遍 |

核算侧接口字段不用动。核算侧已把「补物料编码」接上金蝶物料档案「研发编码」反查（V2.441），建档后成本会计一点即补。

## 8. BP 只读台账配套（V2.442，已上线）

- `/api/bomcost/final` 每行加 `links{detailUrl, compareUrl, previewUrl, downloadUrl, originalUrl}`（相对门户根），顶层加 `pendingFinalCount` / `pendingFinalUrl`。
- 新增 `GET /api/bomcost/pending-final`：待终审明细 + 每行 `finalReviewUrl`（深链直开核算侧终审弹窗）。
- 导出/预览接口放行内部令牌（回环），**只准拿已审核版**。
- 核算 SPA 支持 hash 深链 `#/bomstd?entry=…[&compare=1|&final=1]`。
- 页面怎么摆、引用次数怎么算，见《对接说明_BP只读标准成本台账_20260905.md》。

## 待 BP 侧（§4，核算不动）

按文档自行做；两点提醒：**运输恒 0 不要再叠加**；台账来源物料**不叠 5% 管理费系数**（台账管理费已是星期九固定加成）。变更提示请用 `since=上次同步的 asOf`。
