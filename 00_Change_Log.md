[Change Log]
Date: 2026-07-03
Author: Claude / c
Version: V1.0
Description: 「财务核算工作台」项目（03 交接版）总变更台账。本项目由隔壁 `财务核算工作台` 交接而来，其 V1.0–V1.17 台账已整体归档至 `03_Source_Materials/隔壁项目台账_至V1.17_归档.md`（只读留存），本项目版本号从 V1.0 重新起算。

# 00_Change_Log.md — 项目总变更台账 / Master Change Ledger

> **AI_INSTRUCTION**
> This is the master change ledger for the **财务核算工作台 (Finance Reconciliation Workbench) · 03 交接版** project.
> Entries up to **V2.169** live in this file (read-only history).
> **From V2.170 onward, DO NOT edit this file.** Write a new fragment file into `00_Change_Log/`
> instead — one change, one file. See `00_Change_Log/README.md`.
> Before every new task you MUST read this file AND scan `00_Change_Log/`, determine the latest
> version, and propose the next one.
> No version may be skipped. No entry may be deleted — only superseded.
>
> **OWNER_COMMENT_CN**
> 本文件是本项目唯一的变更总账。每次开发前先读它、定版本、追加记录。
> 记录只可新增或标记作废，禁止删除或篡改历史。

> ### ⚠️ 台账分片化（2026-08-05 · V2.170 起）
>
> **本文件到 V2.169 为止，之后只读、不再续写。新条目一律写进 `00_Change_Log/` 目录，一改动一文件。**
>
> **为什么改**：本文件的版本索引表要求「最新版本置顶」。单线开发没问题；**多条需求并行开发**时，
> 每条分支都往同一张表的同一行插入新行 → **每次合并必冲突**，而且冲突发生在一个 208KB 的大表里，
> 手工解冲突极易错行漏行。分片化把「往同一处插入」变成「各自新增一个文件」，git 层面天然零冲突。
>
> - 读台账 = 读本文件（历史）＋ 扫 `00_Change_Log/`（近期，文件名自带版本号与工具线）
> - 要一份完整视图：`python 00_Change_Log/_合并视图.py` → 生成 `00_Change_Log_合并视图.md`（生成物、不入库）
> - 条目格式、防撞号规则见 `00_Change_Log/README.md`

本台账受 `00_项目治理中心.md` 治理。

> **溯源说明**：本文件在 2026-07-03 交接初始化前，内容为隔壁「财务核算工作台」项目的 V1.0–V1.17 记录，系随治理模板与代码一并交接。其原始内容已完整归档为
> `03_Source_Materials/隔壁项目台账_至V1.17_归档.md`（只读留存，不再于本项目续写）。隔壁项目代码全程只读参考、零改动。

> **分格溯源说明（2026-07-09 · V2.51）**：自 V2.51 起，`01_Current_Deliverables/` 与 `03_Source_Materials/` 内部按工具线分格（`00_平台通用` + `YYYYMMDD_工具名`，前缀=该工具需求确认书 v1.0 出稿日）。**V2.51 之前条目中引用的文档路径为迁移前旧路径**，文件名均未改动，按文件名检索即可定位现址。自 V2.51 起新条目 Description 以【工具线】标签开头（【银行对账】【物流计提】【物流对账】【成本台账】【平台】），单线追溯按标签检索。

---

## 1. 使用规则 / How To Use

每次开发任务对应**一条**变更记录，并对应 `02_Revision_History/` 下的**一个**修订文件夹，严格遵循治理九步法（读账本→定版本→提新版本→建修订文件夹→拷旧成果→开发→产出→同步→更新账本）。

> **运行形态约定**：当前可运行应用位于 `01_Current_Deliverables/app/`，项目根 `启动.bat` 双击即跑（指向该 app）。开发在 `02_Revision_History/版本/` 内进行，批准后同步覆盖 `01_Current_Deliverables/app/`；版本快照留 `02`。**一键启动始终在项目根，不埋在版本文件夹。**

---

## 2. 版本号规则 / Versioning Rule

`主.次` 两段式：
- **Major (X.0)**：架构级 / 不兼容改动 / 重大里程碑（需先出《影响分析》并获批准）。
- **Minor (x.X)**：功能新增、优化、Bug 修复等常规迭代。
- 文件夹与记录格式：`YYYYMMDD_VX.X_Description`。

---

## 3. 状态流转 / Status Lifecycle

```
Proposed → In Progress → In Review → Approved → Synced
                                  ↘ Rejected
   (已同步条目如被撤回) → Reverted / Superseded
```

---

## 4. 版本索引 / Version Index

> 最新版本置顶；每完成一次同步，在此表追加一行。

| Version | Date | Description | Author | Status | Revision Folder |
|---------|------|-------------|--------|--------|-----------------|
| **V2.169** | 2026-08-03 | **【银行对账】流水文件识别加"内容兜底"——同事随手改名后整包不再被静默跳过**。**起因**：7月流水实包中财资平台导出被改名为 `宁波+招商.xlsx`（原规则要求以「宁波银行+招商银行」开头才认），分类器判 skip → **508 笔/11 账户整包标"跳过"不并入逐笔，且不报错**——"跑得通、看着正常"的静默漏账（与天鹰坑一同族）。**改法**（`kernels/bank_import.py`）：`_classify` 保留文件名快路，认不出的再翻内容——①xlsx 前 50 行内出现「交易时间+本方账号」同行表头（与 parse_treasury 定位表头同一条件，嗅探认得出解析就必吃得下）→ 财资平台；②csv 头 4KB 含 HISQRY 独有块头「查询账号」且伴 来账/往账/总笔数 → 中行；建行/花旗原本就按内容认，零改动；支付宝/微信/抖音仍按名认（文件名即账号标识，改名风险另议）。**过程中挖出第二个坑并一并修**：该 xlsx 由银行系统生成，**sheet 尺寸声明错报成 A1:A1**，openpyxl read_only 模式信以为真把整表读成空（嗅探首版对真文件不命中、合成夹具却全过）——嗅探加 `reset_dimensions()` 重算尺寸按实际内容读；解析器走完整模式本就不受影响。**验证**：①新增 `kernels/test_bank_import.py` **7 项全过**（改名认出／改名+尺寸错报A1:A1 完整复刻件认出／原名快路不读文件／HISQRY 改名认出／建行 csv 不被误抢／乱文件仍跳过／混合目录端到端 manifest）；②**7月真实流水目录整包实测**：改名件 → 财资平台·508 笔/11 账户/7-01~7-31 全量并入，中行×2（自校验OK）/花旗×2（自校验OK）/支付宝×6 与改前一致，`宁波+招商-余额.xlsx`/理财持仓/产品列表三个 xlsx **未被误认**；③既有回归 test_reconcile_v2(28)/test_cost_ledger/test_logistics_recon/test_wealth_recon(12) 全绿。**遗留**：①支付宝另有 2 个 `…账务组合查询.csv.zip` 按既有规则跳过（只认 .xls.zip，csv.zip 疑为同数据另格式导出），要不要认领待业务方定；②修复需随下次部署生效，未部署的环境遇改名件仍跳过（急用时把文件名开头改回「宁波银行+招商银行」即可）。改动仅 kernels 两文件，回滚依赖 git。（★原提交/文件头用过程号 V2.167，与汇率 V2.167 撞号，总账归口 V2.169；代码与原提交不改） | Claude / c | Synced | 01（回滚依赖 git） |
| **V2.168** | 2026-08-03 | **【汇率录入/通知】邮件主题加日期/时间戳，防阿里企业邮折叠判重**。现象：SMTP 返回已发但收件人收不到——实测唯一主题的邮件正常收到，确诊阿里企业邮把「相同主题」邮件折叠成同一会话/判重。修：_fxrate_mail_for 正式通知主题追加发送日期(· YYYY-MM-DD，使连日的挂起/出错告警各自可见不被叠掉)；notify-test 测试主题追加 时间(月日时分秒)每次唯一。仅改邮件主题，钉钉不受影响(机器人消息不按主题折叠)。 | Claude / c | Synced | 01 |
| **V2.167** | 2026-08-03 | **【汇率录入】修防重误跳过 bug——防重改「以金蝶为准」**。现象：8-03 14:00 定时跑结7月/101，通知「写入 0 条」全部(跳过)，但金蝶里其实一条没有。定位：_fxrate_write 预载留痕台账 fx_post_log 作防重，金蝶记录被删后台账残留→ 把该期 8 条全判为已录过跳过、再也写不进。修： 不再预载台账拦重写；防重以 find_existing 对金蝶实查为准(跨组织不重叠约束)，posted 只在本次循环内防同期重复键。与看板「金蝶为准」一致；金蝶记录被删即可正常重建，幂等仍靠对金蝶实查。 | Claude / c | Synced | 01 |
| **V2.166** | 2026-08-03 | **【汇率录入/通知】「通知设置」页 UI 重做**——需求方反馈初版丑（裸 textarea、宽度不一、无分组）。改：钉钉/邮件各成一张卡片(fx-ncard，含启用开关标题)；输入框统一 fx-ta 样式 + auto-fit 等宽网格；顶部密钥状态改用彩色小标签(fx-tag)；底部口令+保存(btn-pri)+测试(btn-sec)一条 fx-fbar 操作栏；关掉某渠道时其输入框置灰禁用；全部复用工作台 CSS 变量、跟随明暗主题。build 通过。 | Claude / c | Synced | 01 |
| **V2.165** | 2026-08-03 | **【汇率录入/通知】新增「通知设置」前端页 + 手动录入也发通知**：需求方要能在前端改收发件人、且改动要口令。①FxRate.jsx 历史复核后加「通知设置」页签(仅 fxrate_post 可见)：钉钉手机号/userid、邮件收/抄/密、两渠道开关可编辑；**密钥(钉钉密钥/SMTP密码)只显示已配置/未配置、绝不下发前端**；「保存收发件人」**必须带口令**(后端 conf.ini [notify] passcode 校验，错→403)；「发送测试通知」一键测当前收件人。②收发件人存 DB(fx_notify_cfg)覆盖 conf.ini(未存过则回退、密钥恒取 conf.ini)；notifier.notify 增 dt_conf/smtp_conf/channels 注入参数。③**手动录入(post)成功也发通知**(此前仅自动批发)；autorun/autorun-now/post 三处统一走 _fxrate_send_report_notify。接口 GET/POST /api/fxrate/notify-config + POST /api/fxrate/notify-test；conf.ini.example 补 [notify] passcode。本地验证：口令读取/列表去重/DB覆盖/渠道开关/清空回退全通，前端 build 通过。 | Claude / c | Synced | 01 |
| **V2.164** | 2026-08-03 | **【汇率录入/通知】邮件新增抄送(cc)+密送(bcc)**——需求方除发件/收件外还要抄送、密送。mailer.py：load_smtp_conf 读 cc/bcc（各支持多人，任意分隔符）；send_mail 实际投递名单=to+cc+bcc（去重），邮件头只写 To/Cc、**bcc 隐藏不进头但照发**；conf.ini.example [smtp] 段补 cc/bcc 说明。单测（monkeypatch smtplib）验证：To/Cc 进头、bcc 不进头但在 envelope 投递名单里。 | Claude / c | Synced | 01 |
| **V2.163** | 2026-08-03 | **【汇率录入/通知】上线后按需求方实测意见细化通知**：①钉钉+邮件均按【月末条】/【次月区间条】两组小标题，生效/失效日拎到标题（月末条=生效失效同日；区间条=生效次月1日、失效次月末-1，供次月记账）；②每条补人行中间价公告出处链接（月末公布日+次月首个公布日，邮件可点、按url去重）——_fxrate_autobuild 存 rep["source"]、新增 _fxrate_src_links()；③邮件加「算式」列（对人民币标"公告原文照抄/原值→4位"、交叉汇率给完整除法算式），_fxrate_write 及预演结果补 basis+kind+source_date；④邮件结果列去掉金蝶内码（审核人用不上）。当日实测：发 13691683676（机器人）逐版确认；金蝶真机写 1 条（港币→美元交叉，FRATEID4454055，描述含算式与人行出处，状态B待审核）供看效果，随后撤销提交并删除、回读确认金蝶收尾干净。 | Claude / c | Synced | 01 |
| **V2.163** | 2026-08-03 | **【汇率录入/通知】通知补人行公告出处链接**——审核人可点开核对原始中间价。内核 generate_rows 早已带 month_end_ann/next_range_ann（月末公布日、次月首个公布日两份公告 URL），app.py：①_fxrate_autobuild 把出处存进 rep["source"]；②新增 _fxrate_src_links() 取两条链接（按 url 去重）；③钉钉纯文本 + 邮件 HTML（可点 a 链接）末尾均加「人行中间价公告出处（可点开核对）」。实测发 13691683676（机器人）收到含两链接全文。 | Claude / c | Synced | 01 |
| **V2.162** | 2026-08-03 | **【汇率录入】上线后微调两项**：①自动模式去掉可选结账期间，改显示「上月（自动跟随当前月）」不可选（消歧义），邮件结果列 posted→中文（commit 620cdff）；②钉钉通知改「**机器人单聊优先、工作通知回退**」——业务方为应用「风控AI数字员工」开了机器人配置，notifier.py 新增机器人 O2O 发送（api.dingtalk.com v1.0 /robot/oToMessages/batchSend，robotCode=AppKey，msgKey=sampleText）+ v1.0 token；send_dingtalk 先机器人失败再回退 asyncsend_v2。当日实测钉钉四路（gettoken／手机号查userid／工作通知／机器人）全通，默认 via=robot。 | Claude / c | Synced | 01 |
| **V2.161** | 2026-08-01 | **【平台/治理】新增 `04_部署包/` 统一收纳部署包，根目录去散落**。**起因**：项目根散落三个部署包（`finance_workbench_backend_上传腾讯云.zip` gitignore / `上传包_V2.149_20260720/`（内含 V2.149 后端 zip）未跟踪 / `汇率工具部署包_backend_20260801.zip` 未跟踪），与治理四目录+治理文档混在一起，找不清也看不出哪些算数。**改动**：①主目录新建 `04_部署包/`，三个部署包整体移入（均非 git 跟踪，纯文件操作，零 git 影响）；②`.gitignore` 把 `04_部署包/` 整夹忽略（部署包＝构建产物，源码在库随时可重打，部署史看本台账；保留根目录 `上传腾讯云.zip` 忽略行兼容打包脚本旧产出路径）；③`00_项目治理中心.md` 目录结构节 + `00_治理速查清单.md` 补登 `04_部署包/` 为**本地暂存区·非治理层级**（gitignore 故克隆里没有），避免日后有人对着治理文档看到 04_ 一头雾水。**顺带记一笔待办**：根目录 `需求确认书/`（2026-07-22 长出）在治理四目录之外，与 `01/docs`、`03/工具格` 功能重叠，是否收编另议。**内部拍平**：原 `上传包_V2.149_20260720/finance_workbench_backend_V2.149.zip` 嵌套夹拆除，zip 直接移到 `04_部署包/` 根并重命名 `finance_workbench_backend_V2.149_20260720.zip`（日期并入文件名保留）；该 zip 原为 git 跟踪，移入 gitignore 夹后记为一次删除（历史提交可取回）。本条为治理/结构调整，纯文件+文档，无修订文件夹；**改动落在主工作树、暂未 git commit，待 Owner 审后入库**。 | Claude / c | In Review | —（治理/结构调整，无修订夹） |
| **V2.160** | 2026-08-01 | **【平台/导航】新增「隐藏」模块状态**——设为隐藏的模块从左侧导航整个移除（连灰显都不显），系统设置本页仍可见、可随时改回；用来收起排期很后的占位模块。不在 NAV_OPEN 故不可进入。 | Claude / c | Synced | 01（feature/fxrate→main） |
| **V2.159** | 2026-08-01 | **【汇率录入】工具全量上线并入 main**：P1 人行中间价抓取+建汇率规则+四道机器闸门内核；P2 金蝶通用写入(save+submit「只提交不审核」，跨组织判重不覆盖)；P3 后端接口+前端页(建汇率四步/历史复核)+录入状态看板(以金蝶为准·标工具/人工来源·算式与出处盖进金蝶描述)；P4 定时自动跑批(页面开关+生产保护，次月首个工作日14:00)+邮件与钉钉工作通知。金蝶写入账号全局改「系统操作员」。★文件头在并发开发期用了 V2.157–160 过程编号(与「取数工具箱」撞 V2.158)，总账统一归口 V2.159–160。详见 feature/fxrate 各提交 + 需求确认书/001 汇率录入/。 | Claude / c | Synced | 01（feature/fxrate→main） |
| **V2.158** | 2026-08-01 | **【平台】统一取数工具箱 `kernels/_common.py` 落地（第一步：只建不换，14 个老内核一行未动）**。**起因**：全台盘点发现 `kernels/` 下 **14 个业务内核互相零复用**——没有任何一个内核引用另一个的取数函数，每条工具线都自己从头写了一套"怎么把表格里的一个格子变成能算的数"。实测差异：`_num` **5 份实现**、`_s` 4 份、`to_date` 3 份、`norm_acct` 3 份，**同一个格子 7 条线能给出 3 种答案**。先出《取数规则_跨工具对照表 v1.0》（`03_Source_Materials/00_平台通用/`）陈列全部差异 + 6 个待拍板口径，业务方 2026-08-01 拍板"按建议来"。**盘出的实际风险（对照表§1–§4）**：①**成本台账 / 物流对账遇到文本格式的 `1,234.56` 静默变 0**，不报错不提示，只要源表某列被存成文本或供应商发 csv 就中招；②**空格子两种账法**——银行对账/成本台账/物流对账当 0，物流计提/理财对账当"不算数"，金额合计相同但**笔数与单位成本会差**；③**括号负数 `(100)` 七条线全部不认**（一律当 0 或空），而境外系统/审计底稿/花旗对账单都这么写；④**账号取键两把尺子**——账户台账要≥4 位、对账程序/资金看板不限位数，且 `app.py:1556` 用长尺子造键、`app.py:1583` 用短尺子查键（表现为开户行栏空白）；⑤**日期四种认法**——只有物流对账认 Excel 序列号，银行流水导入不认单位数月份且**认不出时把原文照抄进日期栏**（"见附件"躺在交易日期里，按月筛选被静默漏掉）。**本次实现的 6 条拍板口径**：①空格子＝金额列当 0（`to_amount`）、数量/单价列当"不算数"（`to_qty`，因单价为 0 通常是没填而非真免费）；②千分位/币种符号/空格/全角数字**全认**，读不懂**不静默**——`warn` 收集器落一条带行列与原文的中文警告，空格子与会计横杠不算读不懂故不发警告；③括号负数认成负数；④账号取键**先去空格再取最长且≥4 位**（`工商银行 6228 4800 1234 5678` 现在取全整串，老实现取成 `6228` 故与《账户台账》对不上落"未映射"）；⑤日期认横杠/斜杠/点/纯数字/中文/Excel 序列号(数字与文本)/单位数月日/带时间，**认不出留空 + 警告，取消"原文照抄"**，年份不再限 20xx（历史补录可用）；⑥判平容差收到一处 `TOLERANCES`，默认值＝各线现行值原样搬（勾稽 0.01／配对绝对 1 元相对 2%／重量 0.001 吨／汇率偏离 3%），经 `configure_tolerances()` 由 app.py 从基础设置注入（**内核层不反向读 db，保持纯函数可单测**），未知键报错不默默返回 0。**顺带补上老实现漏掉的四道防护**：**NaN**（`reconcile.to_float`/`balance_dashboard.to_float` 会把 NaN 透传，一旦混进合计**整个合计变 NaN**）、**布尔值**（openpyxl 读 TRUE/FALSE 给 bool，静默当 1/0 加进合计）、**无穷**、**浮点噪声**（`100.01-100.00` 在浮点里＝`0.010000000000005` 比 1 分大，不修则"差 1 分算平"判成不平——`close_enough` 内 `round(...,9)` 抹噪声）。**测试**：新增 `kernels/test_common.py` **130 项断言全过**（含对照表点名的每一处差异做成回归用例）；现有四套回归 `test_reconcile_v2`(28) / `test_cost_ledger`(17) / `test_wealth_recon`(12) / `test_logistics_recon`(37) **全部通过，基线完好**。**本次零风险**：`_common.py` 为**纯新增文件，无任何内核 import 它**，现有行为逐字节不变。**遗留（第二步·换线）**：按风险从低到高 理财对账→物流计提→资金看板→成本台账→物流对账→银行对账，每换一条用该线现有回归比对换前换后结果，**数不一致即停**；银行对账放最后（上线最久、用户最多）。**换线前需业务方确认一处口径微调**：现行各线写的是「差 **<** 1 分算平」，统一后为「差 **≤** 1 分算平」（"容差 0.01"的自然读法），会让"正好差 1 分"的行从「需人工复核」变成「平」。**交付物**：`01_Current_Deliverables/app/backend/kernels/_common.py` + `test_common.py`；`03_Source_Materials/00_平台通用/取数规则_跨工具对照表_v1.0_20260801.md`。**版本号说明**：V2.157 已由在建的汇率录入工具占用（见提交 e73c8c0"为 V2.157 汇率工具开发隔离基线"），故本次取 V2.158。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据（续 V1.2 修订夹；修订夹 app 镜像停留在 7/20，本次为纯新增文件故直接在 01 建，回滚依赖 git） |
| **V2.156** | 2026-07-22 | **【物流对账】《天鹰物流·对账口径确认书 v1.0》+ 样机 v0.1 出稿待签署**（纯文档，未动任何代码）。天鹰口径经 V2.150–V2.155 六轮迭代已由六月真数据全程实测跑通，业务方要求固化成文签署。**定位**：本份是《物流对账工具 需求确认书 v1.2》（2026-07-08 已签）项下的**第 3 家承运商口径补充**，不改动母确认书任何已签条款；诚煜/跨越零影响。**为何单出一份**：天鹰与前两家有三处根本差异——①**按重量计费**（吨×18.5）而非按趟按件；②账单**拆多文件多表且文件数每月不固定**（六月4文件5表168行）；③要核的是"重量对不对"、真相在金蝶单据里，**必须逐物料比**，母确认书无此口径。**确认书结构**（沿用 001 汇率录入体例、财务白话）：为什么单出一份／账单实测形态／定位"是什么不是什么"+**五条铁律**（金蝶是准的·对金蝶只读·不猜不并不兜底·算不了要说算不了·跳过任何一行都必须告诉人）／**已定决策 D1–D14**（业务方拍板的 D2 文件数不写死、D3 续行并入上一行单号、D4 金蝶为准、D5 容差0、D6 少报只标记、D7 单号查无提醒就好、D8 非kg计量提示就好 逐条标黄）／六月实测结果／**三个坑必须记录在案**／本期6件待处理／待确认 Q1–Q6／对现有工作影响／签署表。**三个坑的记录（本份文档最要紧的部分，特征都是"不报错、跑得通、数字看着正常"）**：坑一**重复表让账单凭空翻倍**（`小料装货明细` 与 `小料装货明细 (2)` 逐格50行0差＝"一份带图一份不带图"，金额影响 **¥7,331**，且**合计行同步翻倍致金额勾稽照样"通过"**）；坑二**没填序号的行被丢**（`XSCKD209569`/13吨/**¥240.50** 静默漏计，同表合计行又不写"合计"二字）；坑三**拿千克比"个数"造出11条假警报**（一单多物料只拉子集 + 基本单位是 Pcs/包）。**价值论据写实不吹**：六月天鹰多收我方 ¥0.46、我方占便宜 ¥3.67，**净额我方还赚 ¥3.21**——故明确写"本工具价值不在追回运费"，而在①防我方自己算错（坑一 ¥7,331）②发现回填填错（六月 **4 处**：`XCSKD209689` 单号打反、`T0000089` 物料编码少打一个1、另2个单号查无）③把168行人工核对变成1条待办。**样机 v0.1**（单文件 HTML、双击即开、复用在用工作台 styles.css 令牌与深色自适应）：三页签——①**对账结果**（步骤条+两条真实提示横幅+6态KPI+5个切片+物料级清单，默认停「需人工复核」，多报置顶）②**本期要处理的事**（6件，标明找谁、影响）③**口径说明**（三处差异对照+五条铁律+业务方已定D2–D8）；**162 行全部为六月真实跑批结果，零编造**。**浏览器实测通过**：三页签切换正常、五切片计数正确（需人工复核1／查无未匹配4／少报40／非kg计量5／一致112／全部162）、查无4条为 XCSKD209689·XSCKD208114·XSCKD208547·XSCKD208680。**交付物**：`需求确认书/003 天鹰物流对账/` 下 `天鹰物流对账_口径确认书_v1.0_20260722.md` + `天鹰物流对账_样机_v0.1_20260722.html`。**遗留**：①按 001 汇率录入体例还需出 **.docx（供 Word 批注签字）与 .pdf（供只读转发）**——本方未先出，因 v1.0 待业务方批注、改动后须重出，**约定改动一律先改 .md 再重出另两份**，避免三份漂移；②签署通过后按治理转 Approved Baseline 移入 `01_Current_Deliverables/docs/20260708_物流对账/`；③Q1（40条少报约¥3.67要不要跟天鹰提）与 Q3（非kg计量物料要核重量需金蝶补物料重量档案或天鹰提供过磅单）需业务方与商务决断。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.155** | 2026-07-22 | **【物流对账】修天鹰解析两处静默错账：①重复 sheet 双计（金额重量翻倍）②无序号的真明细行被丢弃（漏计费用）**——业务方拿第 **4** 个天鹰账单文件（`6月份小料成品装货明细+.xlsx`，印证 V2.150"文件数不能写死"）追问："XSCKD208316 为什么解析出来是 53000"。**根因① 重复 sheet**：该工作簿有 `小料装货明细` 与 `小料装货明细 (2)` 两个 sheet，逐格比对 C1..C12 **50 行 0 处不同＝内容完全相同**（唯一差别是前者 C13 有一张 `DISPIMG` 嵌入照片，即物流部门留的"一份带图、一份不带图"）；解析器逐 sheet 全读把同一批货算两遍，故 26500kg→**53000kg**。**此错尤其危险：合计行同步翻倍，金额勾稽照样"通过"**，静默错账无从察觉。**修法**：按 sheet 明细行指纹 `(序号,单号,物料编码,吨数,金额)` 整表判重，撞指纹即跳过并记 `notices`；同 sheet 内序号唯一，故只有"整表重复"才撞、不会误伤表内合法的同料同重两笔。**同时挡住多文件上传的新风险——同一文件被误选两次**（V2.150 起可多选），实测传两遍行数金额与传一遍完全一致。**根因② 靠序号判行**：旧规则"序号必须是数字"才算明细行，而该表 R3（`XSCKD209569`／13吨／**¥240.5**／备注"系统单据为6月份"）**没填序号**，被静默丢弃＝漏计费用；同表 R53 是合计行却**不写"合计"二字**，也没被认出。**修法**：行性质改按**实质内容**判——有货物标识（产品／物料编码／单号）+ 有数值＝明细行；有数值但无任何货物标识（或含"合计"）＝合计行；`seq` 降为展示字段（可为 None），定位账单行一律用 `line`。**验证（真数据）**：**回归原 3 文件 118 行／勾稽 7205.67／待人工 0／notices 空——与修复前完全一致，未波及已验证数据**；第 4 文件 `XSCKD208316` 回到 **26500kg（1 行）**；R3 找回后**金额勾稽 7330.99 = 账单合计行 7330.99 自动对上——反向印证该行确应计入**；4 文件合并 168 行、金额勾稽 14536.66 自洽。**顺带查出承运商表自身矛盾**：第 4 文件合计行吨数 383.27 vs 明细 396.27 **差 13 吨**（合计行漏加 R3 的吨数、金额却算了）——付钱按金额不受损，但新增「账单吨数不自洽」提示要其核对，不吞掉。**notices 通道**：新增 `bill['notices']`＝提示级（不阻断解析，区别于阻断级 `warnings`），经 `/parse` 与 `/reconcile` 透出，前端在结果区上方以琥珀色横幅逐条显示——**跳过重复表这种事绝不能静默**。单测 **37/37**（新增 4 条：重复 sheet 跳过且 26500 非 53000、同文件传两次不翻倍、无序号明细行保留且不写"合计"的合计行被认出、吨数不自洽出提示）。前端构建通过（index-CF72UOWv.js）。**遗留**：①业务方需用**全 4 个文件**重跑一次对账取新口径（此前 V2.152–V2.154 的 112 行物料级结论基于 3 文件）；②第 4 文件吨数合计漏加 13 吨，请物流部门要求天鹰修表；③UI 未浏览器实测（登录态失效、本方不代输密码）——登录后应见琥珀色提示「小料装货明细 (2) 与 小料装货明细 内容完全相同…已跳过」。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.154** | 2026-07-21 | **【物流对账】重量差异按方向分态——「账单少报」独立成态只标记，「需人工复核」只留「账单多报」**（业务方定："账单少报的做标记吧，这个属于我们的优势，不需要强提醒"）。V2.153 容差归零后需复核由 1 条涨到 33 条，但**其中 32 条是账单少报**（承运商把吨数截到两位小数、少收我方钱），与「多付运费」性质相反却挤在同一个红色状态里，等于用我方占便宜的项淹掉真正要追的钱。**改法**：新增状态 `ST_MAT_UNDER＝「账单少报·我方有利」`——差异为负走此态（灰底弱化、标记留痕），`ST_MAT_REVIEW＝「需人工复核」`**只保留差异为正的账单多报**（承运商多算重量→可能多付运费→要人去追）。**少报仍计入"可核"重量汇总**（`_CMP` 含三态）——它是真实比对过的重量，漏出去总量口径就不实。**前端配套**：新增灰底「账单少报 N」切片与状态色；横幅改为「**需人工复核＝只看账单多报 N 条**（承运商多算重量，可能多付运费）· 账单少报 M 条（共 X kg，多为吨数截位）＝**我方有利，只标记留痕不必追**」；空态文案改「✓ 本期没有『账单多报』——承运商没有多算重量」；表尾口径说明按新分态重写。默认切片仍停「需人工复核」，现在它就是纯粹的待追清单。**验证（真数据+活金蝶只读）**：核对一致 72 / **账单少报·我方有利 32（合计约121kg）** / ERP非kg计量 5 / 单号查无 2 / **需人工复核 1**——即 `FBDC074225` 植物肉糜 +25kg 账单多报，复核清单回归"只有真正要追的那一条"。可核差异 −121.222kg÷379,100kg 与 V2.153 一致（分态不改口径）。单测 **33/33**（`test_zero_tolerance_splits_by_direction` 断言少报落 UNDER 态、多报落 REVIEW 态；新增 `test_under_billing_not_counted_as_review` 断言少报不混进复核数且仍进可核汇总）。前端构建通过（index-DcANLTtr.js）。**遗留**：①UI 未浏览器实测（登录态失效、本方不代输密码）——业务方登录后看：天鹰结果屏「需人工复核 1」，唯一红行 FBDC074225 +25kg；灰底「账单少报 32」切片可查明细；②32 条少报是否要求天鹰按实重而非截位计费，属商务口径待业务方与承运商议（工具已留痕，随时可导出举证）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.153** | 2026-07-21 | **【物流对账】物料级核量容差归零 + 差异清单按「账单多报」置顶排序**（业务方 2026-07-21 三条决策：①单号查无＝提醒就好、②**容差为 0**、③金蝶非kg计量＝提示出来就好）。①②③中仅②需改代码——①「单号查无」与③「ERP非kg计量」V2.152 已各自独立成态并在页面单列切片，即为"提醒/提示"，无需再改。**容差归零**：`reconcile_by_material` 默认 `tol_kg=0.0, tol_pct=0.0`——账单kg 与金蝶kg 只要对不上就落「需人工复核」，含承运商吨数舍位造成的小差。**保留形参不写死**，日后要放宽改调用处即可，不动内核。**⚠必须配套的浮点处理**：真·零容差会被二进制浮点尾巴误伤（`0.0482×1000` 在浮点下得 `48.20000000000001`，与金蝶 `48.2` 差 1e-14 会被报成差异），故比对前把差异按**克级(3位小数)四舍五入**——这不是容差，是避噪声，代码与页面均写明区分。**实测影响（真数据+活金蝶只读）**：核对一致 104→**72**、需人工复核 1→**33**。**但方向高度不对称：33 条里 32 条是「账单少报」**（−0.8~−9.44kg，天鹰把吨数截两位小数所致，少收我方钱），**「账单多报」仍只有 1 条**（`FBDC074225` 植物肉糜 +25kg）——即真正多付运费的风险点未变。**故配套改前端排序与提示**：差异清单**恒把「账单多报」置顶**（其次按差异绝对值降序），页面横幅直接标出「账单多报 N 条（多付运费风险，已置顶）· 账单少报 M 条（多为吨数截位，少收我方钱）」——容差 0 下少报条数必然远多于多报，不排序会把真正要追的那条淹掉。单测 **32/32**（`test_zero_tolerance` 改为断言舍位小差也落需复核并标"账单少报"、分毫不差才判一致；新增 `test_float_noise_not_flagged` 克级取整不误报；新增 `test_same_no_one_material_off_only_that_one_flags` 同单只有对不上的物料落复核、不牵连同单其它物料）。前端构建通过（index-BMaz3LD1.js）。**遗留**：①UI 未浏览器实测（登录态失效、本方不代输密码）——业务方登录后看：天鹰结果屏「需人工复核 33」，首行应为红色 FBDC074225 +25kg 账单多报；②32 条「账单少报」是否要求天鹰按实际重量而非截位计费，属商务口径待业务方与承运商议；③容差 0 跑几个月后如觉复核量大，可只对「账单多报」方向零容差、「账单少报」给舍位容差——形参已留，改调用处即可。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.152** | 2026-07-21 | **【物流对账】物料级核量上线——按「金蝶单号 × 物料编码」逐行比重量，金蝶为准，超容差落「需人工复核」**（业务方定："以金蝶的为准，不一致的要提醒人工复核"）。**为何必须到物料级**：V2.151 实测证明按单号汇总整张单据核重量会**大面积误报**——①一个金蝶单号常含多个物料而承运商只拉子集（`QTCK011172` 金蝶12行1017kg、天鹰只拉4个物料370kg）；②部分物料金蝶**基本单位不是千克**（印刷内袋＝Pcs、椒麻汁＝包），拿账单kg比件数无意义。单号级口径下 12 条"异常"里 11 条是假象。**①金蝶取数补两列（实账套探明，4 类单据通用，无需逐单据写死大小写）**：`FMaterialID.FNumber`＝物料编码、`FBaseUnitId.FName`＝基本单位；7 类单据字段表全部补上（PUR_MRB 用全大写变体），并入 `setdefault` 白名单——某单据不认时按既有降级机制丢列，下游按"缺则不可比"处理不 KeyError。实测 **144/144 分录两列全部取回，零降级**。**②新增内核 `reconcile_by_material(bill, docs, tol_kg=10, tol_pct=0.02)`**：账单侧与金蝶侧各按 `(单号, 物料编码)` 聚合后逐组比 kg；物料编码经 `_norm_code` 归一（Excel 数值单元格 `305000001.0` → `305000001`）；六态——**核对一致**／**需人工复核**（超容差，另标方向「**账单多报**＝多收运费风险／账单少报」）／**ERP非kg计量·重量无法核**（基本单位非千克，如实单列、**不判差异**）／**金蝶无此物料**（账单声称的物料不在该单据，提示回填单号可能填错）／单号查无／待人工配对。容差＝`max(10kg, 2%×金蝶kg)`——10kg 下限正对应承运商把吨数截到两位小数（0.01吨）的量化粒度。勾稽只在**可核口径**（一致+需复核）内汇总，避免非kg计量项再造总量假象。`PARSERS` 天鹰加 `mat_check:True` 驱动。**③接口/前端**：`/reconcile` 对 mat_check 承运商额外返回 `matrecon`（单号级结果保留不动，诚煜/跨越零影响）；`LogisticsRecon.jsx` 在 matrecon 存在时切换为**物料级差异清单**（状态/金蝶单号/单据类型/物料编码/物料/基本单位/账单kg/金蝶kg/差异kg/方向/账单金额/账单行），**默认只看「需人工复核」**（差异清单是给人复核的），另有 查无未匹配／非kg计量／全部 三个切片，单独的 CSV 导出，并在页面写明口径与容差。**验证（真数据 + 连活金蝶只读 + 端到端接口）**：物料级 112 行——**核对一致 104 / 需人工复核 1 / ERP非kg计量 5 / 单号查无 2 / 金蝶无此物料 0**（物料编码 100% 对上）；**可核差异 −121.2kg ÷ 379,100kg ＝ 0.03%**。**噪音从单号级 12 条假警报降到 1 条真差异**：`FBDC074225` 星期零香辣孜然植物肉糜 账单1075kg vs 金蝶1050kg（210袋×5kg）＝**多报 25kg·账单多报**——这正是核量要抓的东西。5 条非kg计量项（印刷内袋 Pcs／椒麻汁 包／蔬菜包 Pcs）与 `QTCK011172` 一单多物料假象全部消解。端到端 `POST /reconcile`（3 文件 multipart）HTTP 200、`mat_check:true`、matrecon 统计与内核一致。单测 **30/30**（新增物料级 4 条：六态、容差吸收舍位但抓真差异、物料编码归一、一单多物料各自比）。前端构建通过（index-DhiHLmxb.js）。**结论（回答业务方"是不是这个kg数"）**：**凡金蝶基本单位＝千克的物料，天鹰吨数与 ERP 一致，未虚报**；6月唯一实质偏差为上述 25kg。**遗留**：①UI 未浏览器实测（登录态失效、本方不代输密码）——业务方登录后看：天鹰结果屏应默认停在「需人工复核 1」；②容差 10kg/2% 为本方依吨数舍位粒度所定，跑几个月后按业务方口味调；③「单号查无」2 张（XSCKD208114／XSCKD208680）需物流部门核回填单号是否填错；④非kg计量物料若要核重量，需金蝶补物料重量档案或改由承运商提供过磅单，属口径外待议。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.151** | 2026-07-21 | **【物流对账】修天鹰解析器 BUG：有单号但无金额的拆行被误当待人工、踢出核量分组**——V2.150 落地后连活金蝶核量发现的真错。**根因**：`parse_tianying_bill` 的 `pending＝(无单号) or (无金额)`，把「有金蝶单号、有重量、只是没填费用」的拆行也标成待人工；而 `reconcile_by_backfill` 对 pending 行 `nos=[]`（不进对账组），导致同一金蝶单据的拆行重量少算，与金蝶对不上。**典型**：`XSCKD209613` 天鹰拉两行（植物鸡肉酥排 500kg 无费用 + 香辣孜然牛肉饼 650kg），金蝶该单正好 500+650=1150kg；修前只算 650（比值 0.565 假差异），**修后账单 1150 = 金蝶 1150 分毫不差**。**修法**：`pending` 只表「无单号可直查金蝶」（真·待人工）；新增 `no_amount＝billed is None`——有单号无金额的行**参与核量、不参与核价**。仅动天鹰解析器（`pending` 语义 parser-local），诚煜/跨越口径不变。**验证**：连活金蝶（只读）复跑——待人工 2→0、`XSCKD209613` 由 0.565 假差异变 1.0 对上、`CGRK172431`（有单号无重量无费用的空行）由待人工归入单号组显性待查、其余 82 单号结果与修前一致（证明精准无波及）；单测 26/26（新增无单号→真待人工、有单号无金额→no_amount 不 pending 两断言）。**顺带实测确认（回答业务方"是不是这个kg数"）**：**凡金蝶基本单位＝千克的物料，天鹰吨数与 ERP kg 完全一致或差 2% 内（天鹰截两位小数、略少报）；总比值 0.5745 是假象**——被三类拉低：①包装/小料类物料 ERP 基本单位是「张/包」非 kg（如印刷内袋 96000张、椒麻汁 60000包），拿 kg 比件数无意义；②一个金蝶单号含多物料、天鹰只拉子集（如 QTCK011172 金蝶12行1017kg、天鹰只拉其中4个物料370kg）；③真差异仅 FBDC074225 植物肉糜 天鹰1075 vs ERP1050（多报25kg）。**故要「金蝶为准·不一致提醒人工复核」须做单号+物料级核量**（下一步：金蝶取数补 `FMaterialId.FNumber` 物料编码、按 单号×物料编码 逐行比 kg、并按基本单位判定"可否用重量核"）——本条只修 BUG，未改 reconcile 判定口径。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.150** | 2026-07-21 | **【物流对账】新增天鹰物流解析方案（第 3 家承运商）＋ 账单上传打通多文件（不写死文件数）**。业务方拿来 6 月天鹰账单，**拆成多个文件、多个 sheet**（销售成品装货 / 分步调拨卸货 / 小料植物肉原辅料卸货退料），明确「**可能不止三个，看业务实际数量，不能写死**」。**①天鹰计费口径与前两家本质不同**——按 **吨数×18.5 元/吨＝含税金额**（诚煜是零担干线单价、跨越是件数），且**核价账单自洽**：118 行逐行 `吨数×18.5=金额` 分毫不差，核价不依赖金蝶，连金蝶只为核量（吨数↔单据 kg）与验单号真伪。**②两种列布局**（装货明细含「收货仓＋数量(箱)」、单号在后列 C10；卸货明细为「数量(KG)」、单号在 C9），故 `parse_tianying_bill` **一律按表头文字定位列**（子串匹配容括号差异，匹配顺序先专一键避免"数量"误吞"吨数"），不写死列号。**③单号向下填充（业务方定「续行并入上一行单号」）**——无金蝶单号的续行继承上方最近有单号的行（同一单据拆行＝对账组）；填充后 **待人工 32→2 行**（仅剩 2 行为"有单号但无金额"的真异常，如实标出不吞）；填充不动求和，三道勾稽仍 ¥7205.67 分毫不差。**④weight 存 KG（吨数×1000）**对齐金蝶基本单位数量，令 reconcile 出的毛净比≈1、每公斤费用单位正常；注册 `qty_check=False`（按吨计费、箱数≠金蝶数量，只核单号命中＋重量比，不核件数）。**⑤上传链路打通多文件（文件数不定）**：`parse_bill(carrier, data)` 改为**收单个或列表**——`multi` 方案（天鹰）吃整个文件列表、单文件方案（诚煜/跨越）传多个即**明确报错**（杜绝"多传了却只解析第一个"的静默漏账）；后端 `_recon_read_files` **两种都收**——multipart（字段 files，可多文件）与原始 body（单文件，向后兼容旧前端）；`list_parsers`/carriers 接口透出 `multi` 标；前端 `api.js` 上传一律走 FormData·files 字段，`LogisticsRecon.jsx` 对 multi 方案开 `<input multiple>`、承运商网格加「多文件」徽标、已选多个显「已选 N 个文件」。**reconcile 内核零改动**（吨位差异门控留作后续精细化——现天鹰走通用引擎＝单号直查＋单号命中＋重量比供人看）。**验证（真数据 + TestClient 端到端）**：三文件合并解析 118 行、勾稽✓ ¥7205.67、核价 0 不符、weight 抽查 0.17吨→170kg✓、单号前缀 XSCKD/QTCK/FBDC/CGRK 全落 7 类取数内；多文件 multipart HTTP 200·文件数3、单文件 raw body HTTP 200·文件数1（向后兼容）、多文件误传跨越正确拦截报错；后端 `import app` 通过、前端构建通过（index-AHsmcBPD.js）。**遗留**：①**reconcile 未连活金蝶实测**（核量吨数↔单据kg、重量比 gn_ratio 的真值比对待业务方本期跑一次）；②UI 未浏览器实测（登录态失效、本方不代输密码）——业务方登录后看：天鹰行应有「多文件」徽标、点上传对账可多选、结果屏勾稽 ¥7205.67；③退货 sheet 本月为空（将来 CGTL 采购退料是反向流，核量方向届时单独处理）；④天鹰吨位核价虽账单自洽，是否在差异清单单列「核价一致（账单自洽）」状态待业务方定。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.149** | 2026-07-20 | **【权限中枢】子管理员默认拥有其管辖工作台的全部权限（含敏感点；「系统设置」除外）**——业务方指出的真矛盾：子管理员能给别人授出管辖工作台的全部权限点（assignable_caps=57 个），自己却要逐点被授（冯辉 10/57）——**能发证的人自己没证，说不通**。**模型（动态判定、不发点）**：`db.sub_admin_caps(user)`＝管辖 ws 的全部功能码 **减 enter_settings**（业务方点名的唯一例外）；`user_can` 加分支——子管理员在该范围内**恒 True**。三个设计决策：①**恒有而非"默认可覆盖"**——账号页保存会把未勾的存成 false，若尊重显式 false，历史保存过一次默认就被压没；**要收权的正路是撤销任命**（关闭「管理本工作台账号」），与主管理员的全权同构。②**动态算不发点**——任命即全权、撤任命即收权、新加权限点自动跟上，不吃 `_backfill` 那类补发坑（V2.128 教训）。③**敏感点也在内**（封存/取数/关账等）——任命本身就是敏感动作（限主管理员），被任命=被信任；唯 enter_settings 仍须显式授。**配套四处**：①`_user_public` 把子管理员 perms 并上 sub_admin_caps=True——前端侧栏/按钮不用另写判定全部跟上；②`bp_perm_codes`：BP 子管理员（manage_bp）透传与主管理员同款（`'*'`+敏感码逐个显式——BP 侧 `'*'` 不覆盖敏感码，不带上就"管 BP 的人存不了基础数据"）；③账号页：子管理员的管辖工作台面板整块换成说明卡「👑 本工作台子管理员＝默认拥有全部权限（系统设置除外）…要收权请撤销任命」——逐点勾选与实际不符会误导（后端不看那些勾）；「进入本工作台」开关对其隐藏（恒通）；④左列徽标：子管理员由「10/57」改显「**管辖全权**」（计数失真）。**顺手修掉 V2.128 记档的平台坑**：`user_can` 原来直接 `parse_perms(user["perms"])`——`list_users` 传来的 **dict** 会 `json.loads(dict)` 抛异常 → 落 `_legacy_perms`（非敏感全给）；改用 `_pd()` 兼容 str/dict 两种形态（生产路径 str 不受影响；dict 路径从"全给"变正确值）。**验证（后端矩阵 8 项全符）**：核算子管理员——cost_ledger（显式 false 也恒有）✓/取数封存关账等敏感 ✓/enter:costledger 菜单准入 ✓/**enter_settings ·无** ✓/enter_bp、bp:edit 跨工作台 ·无 ✓；普通账号行为不变 ✓；dict-perms 坑修复实测（未授点不再全给）✓；BP 子管理员透传 `'*'`+6 敏感码 ✓；`_user_public` 展开 62 个 perms、enter_settings=False、managed_ws=['accounting'] ✓。构建通过（index-CUipawnX.js）。**遗留**：①UI 未浏览器实测（登录态失效、本方不代输密码）——业务方登录后看：冯辉左列徽标应为「管辖全权」、核算工作台面板应为说明卡；②冯辉此前被逐点授的 10 个点成了冗余数据（无害——恒 True 盖过），是否清理待定；③需求确认书 §6 权限模型如有需要可升版补记本条。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.148** | 2026-07-19 | **【成本台账】需求确认书 v1.1（补签署版）出稿——业务方拿去过会补签**（纯文档，未动任何代码）。v1.0（20260709）签署前工具已按业务方逐轮指示开发完成并部署（V2.57→V2.146），故本版把确认书从「待开发的需求」改写为「**已实现现状的逐条确认**」：①**§2 交付对照表**——v1.0 计划 vs 实际交付逐行对照（单主体→多主体、五步→八步、双通道从设想到等价，均标版本出处与实测数）；②**§5 决策补记 D1–D14**——把开发过程中业务方现场拍板的 14 条决策成文归档（双通道并列/仓库类型纯分类/多人工具须封存/新仓不猜类型/主体简码/权限四点拆分/明细独立页/占比列/共享口径修正/单价显2存6/单位按报表/列序/事务类型第④步/准入闸设二级）；③**§8 待确认清单**——v1.0 的 Q1–Q8 逐条更新状态（Q1/Q2/Q3/Q7 已实测闭环签署即确认；Q4 确认 Owner；Q5/Q6/Q8 仍待答），**新增 Q9–Q12**（混合单位合计口径／已封存期间是否补新字段·建议保持原样／岗位模板含空菜单／事务类型追单据号下钻 B 期评估）；④**§7 验收纪录**——DoD 八项全达标表（2,770 行分毫不差/勾稽差 0/44 仓全中/负结存 5 含 -27.67 元凶/尾差 7/损益一致/多主体互不串/17 单测绿）；⑤单价「取金蝶不自算」三条硬理由、单位 99.7% 即基本单位、事务类型 1.59 元金蝶自身尾差等**口径决策连同实测依据写入正文**（签的不只是功能清单，是口径）。文件：`03_Source_Materials/20260709_成本台账/成本台账工具_需求确认书_v1.1_20260719.md`；v1.0 留档同目录。**流程**：签署通过后转 Approved Baseline 移入 01_Current_Deliverables/；有批注则升 v1.2 再签。 | Claude / c | Synced | 03_Source_Materials/20260709_成本台账/ |
| **V2.147** | 2026-07-19 | **【汇率录入】新工具线立项 + 需求确认书 v1.0 出稿**（纯文档，未动任何代码；对金蝶的所有查询均为**只读**）。业务方提出每月要把人民银行月末汇率录入金蝶，要求先拆解、先读取。**①人行数据源已打通**：`pbc.gov.cn/zhengcehuobisi/125207/125217/125925/`，第 1 页 `index.html`、第 N 页 `17105-{N}.html`，共 148 页 2945 条可回溯至约 2014 年；详情页为静态 HTML，`requests` 直取，响应头未声明编码需手工 utf-8 decode；每期 25 个币种且**口径混合**（前 10 个「1（或100）外币对人民币」、后 15 个「人民币1元对X外币」，后者须取倒数归一）。**②贵司建汇率规则已由 72 条金蝶实有数据反推验证**：每月 8 条＝**月末条 5 条**（生效=失效=**自然月最后一天**，哪怕当天休市，如 5-31 周日仍写 5-31、值取 5-29；含美元/港币/英镑→人民币 + 港币/英镑→美元）+ **次月区间条 3 条**（生效=次月 1 日、失效=**次月最后一天减 1 天**，取次月第一个公布日，不建交叉汇率）；精度 4 位；类型 `HLTX01_SYS`；币别码 人民币 PRE001／港币 PRE002／英镑 PRE006／美元 PRE007。**③组织是接力关系不是并行**——101 深圳星期零建至 2026-03-30 区间条为止，107 孝感自 2026-03-31 月末条接上，现仅维护 107；遗留 101 缺 2026-03-31 那 5 条月末条（Q3）。**④回归结果 72 条对 70 条一致，查出 2 条历史错值**：2026-01-31 英镑→美元账上 1.375（精确 1.378914，应 1.3789）、2025-12-31 港币→美元账上 0.1284（精确 0.128503，应 0.1285）。**对人民币的 39 条零错误，错误 100% 集中在需人手算的 18 条交叉汇率上**——此即本工具的核心价值论据（汇率是全账套基础资料，错一条则当月所有外币凭证与调汇静默跟错，金蝶不会报警）。**⑤舍入方式判定为四舍五入**：18 条交叉汇率中 5 条精确值处于可区分位置，**5:0 全部符合四舍五入、无一符合截断**，故上述两条既非四舍五入亦非截断，是纯录入错误（初稿曾误判 0.1284 为「截断口径之争」并拟列为卡开发问题，核实后更正、降级为确认项 Q1）。**⑥本轮最该记的坑（已写入确认书 D12）**：第一版从公告 URL 截日期，而**2025-11 之前的公告 URL 是纯数字流水号**（`/125925/5903789/index.html`）**不含日期**，后果不是报错而是**静默取到错误日期**——首次全量比对因此凭空报出 18 条「与人行不符」的假警报，若未回头验算，确认书写下的就会是「历史有 18 条错账」。改为一律从链接文字「YYYY年M月D日…中间价公告」解析后，翻页自 2026-07 连续覆盖至 2025-05 无缺口，且各月公布日天数与国庆（2025-10 为 17 天）、春节（2026-02 为 14 天）休市吻合。**⑦已定决策 D1–D14**，其中 D2 数据源唯一取人行、**不接第三方财经 API**（理由是可核对性：会计要的是能对着红头文件指出来源的汇率，第三方接口会静默漂移且说不清口径）；D11 录入走**先出预览 → 人在页面确认 → 一键写入**（复用物流计提 A 期既有模式，符合「系统取数留人眼核对」既定反馈）；D13 历史复核页进一期。**⑧五条铁律**：只抄不算／交叉汇率是唯一例外且须在页面显示算式／每条必带公告出处链接／**抓不到就停绝不猜**（不用上月顶替、不插值、不用第三方补位）／写入前必须人确认且不覆盖已有。**⑨本轮无卡开发问题**（规则已实证，签署后即可开工），待确认 Q1–Q7（舍入确认／两条错值改不改及谁批／101 是否补建／新增境外主体谁通知／写入按钮准入点归属／月末跑数时点·人行约 9:15 发布／写入部分成功如何处置）。建工具线格 `01_Current_Deliverables/docs/20260719_汇率录入/`（前缀=确认书 v1.0 出稿日，依 V2.51 建格规则）。**交付物**：`01_Current_Deliverables/docs/20260719_汇率录入/汇率录入工具_需求确认书_v1.0_20260719.md`，另出**同名 .docx 与 .pdf 各一份**供转发同事阅读批注（业务方提出「发给同事要装什么才能看」，MD 记事本可开但表格会散架；docx 供批注签字、pdf 供只读转发；三份内容同源，**改动一律先改 .md 再重出另两份**，避免三份漂移）；确认书头部按业务方要求补「需求方：陈梓华」与「请仔细阅读，有修改的请使用 Word 批注模式」，签署表加需求方行、开发署名改 **Reginald**（辅助工具 Claude Code），docx 加页脚页码。**另出样机** `汇率录入工具_样机_v0.1_20260719.html`（单文件、双击即开、无需装任何东西）：复用在用工作台的 styles.css 令牌与 `.steps/.kpi/.tbl-wrap` 组件类，含**两个页签**——①本期建汇率四步流（选期间 → 取人行数 → 核对预览 → 写入金蝶），②历史复核（72 条统计 + 2 条偏差红标）；**演示数据全部为 2026-06 真实汇率与真实回归结果**，非占位符；核对预览屏逐条摊开「来源/算式」（`0.86855 ÷ 6.8109 = 0.127524`）与人行原文链接，落实确认书铁律 2/3；含「已存在则跳过」的状态标签，落实铁律 5；支持 `#step3`/`#audit` hash 直达便于把某一屏单独发人。**待业务方逐条批注签署后进开发。** | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.146** | 2026-07-17 | **【权限中枢】岗位名单维护整体搬到门户（账号管理 › 岗位模板设置抽屉）；核算系统设置降为只读展示——「岗位名单应该放到门户」（业务方定）**。V2.145 只把「新增」就近放进抽屉、改名/删除仍指回核算系统设置；业务方指出岗位是**全平台的人事概念**（BP/法务的账号也挂岗位），维护入口放在核算工作台的系统设置里本来就错位。**搬家三件事**：①**后端先下沉级联清理**——删除岗位要连带从各模块挂载摘掉，这逻辑原在核算 Settings 前端手里（delPost 顺手改 st 一起提交）；搬家后是只发 {posts} 的调用，不下沉就会在 state 里残留死岗位 key（侧栏岗位标签显示成裸 key）。/save 的 posts 段现在算 removed_keys → 从 `_NAV_KEY` 各模块 posts 里摘掉 + 从 `_TPL_KEY` 模板里清掉该岗位条目，**谁调都安全**。②**抽屉升级为完整维护**：chips 选中岗位后出现「选中岗位：[改名输入框（失焦/回车提交，改名不丢绑定）] [删除该岗位]」；删除前 confirm 说明后果（自动摘挂载与模板、已选此岗位的账号变「待认领」需重选）；至少保留 1 个岗位前端也拦。③**核算系统设置的岗位名单区块降为只读**——pills 展示 + 指路「新增/改名/删除请到 门户›账号管理›📋岗位模板设置」；该页保存改为只发 {state}（posts 不再上送），相关编辑逻辑（addPost/renamePost/delPost/postsEq/newPostKey）删除，dirty 不再算岗位——**一份数据一个改的地方**，与「仓库类型对照以数据库为唯一真相源」同一条治理原则。**验证（后端实测三步）**：建「删测岗」→ 挂到已上线模块 + 配模板 ✓ → 只发 {posts} 删除 → **挂载摘掉 ✓ + 模板清掉 ✓** → 全部还原 ✓。构建通过（index-91Kb94vx.js）。**遗留**：①抽屉 UI 未浏览器实测（登录态失效、本方不代输密码）；②需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.145** | 2026-07-17 | **【权限中枢】岗位名单答疑 + 模板抽屉里【就近新增岗位】**。业务方（在权限中枢新建账号的岗位下拉处）问：「这个岗位清单怎么新增和维护」。**答疑**：维护界面**早已存在且功能完整**——核算工作台 › 系统设置 › 岗位名单（新增/改名/删除；key 稳定 label 可改、改名不丢绑定；删除会连带从各模块上摘掉；上限 posts_max）。但业务方在权限中枢发问这个事实本身说明**发现性太差**：岗位在权限中枢里到处用（新建账号下拉/名字行下拉/模板抽屉 chips），维护却要跑去另一个工作台的系统设置页。**改法（就近补最常见诉求）**：V2.144 的「岗位模板设置」抽屉里，岗位 chips 行尾加**内联新增**——输入岗位名（≤10 字，重名拦截）回车/点添加 → `saveNavPosts(全量名单)`（新 api，只发 {posts}）→ 新岗位以**空模板**加入编辑副本并自动选中，接着就能配模板；提示语引导「先配模板、再到账号上选用」。key 生成与系统设置页同一套规则（`'p'+random36(6)`）。**改名/删除不搬**——删除要连带从各模块摘掉（那边逻辑全），抽屉里只留一行指路：「改名/删除在 核算工作台›系统设置›岗位名单」。**验证（后端实测）**：只发 {posts} 保存——新增「测试岗」落库 ✓、**state 未动 ✓、templates 未动 ✓**（V2.144 的 guard 各段独立生效）、还原成功 ✓。构建通过（index-BjZ8FgG-.js）。**遗留**：①抽屉 UI 未浏览器实测（登录态失效、本方不代输密码）；②需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.144** | 2026-07-17 | **【权限中枢】顶部工具栏新增「📋 岗位模板设置」抽屉——「按岗位一键套用」终于有了配模板的入口；顺带修掉 /save 的一颗雷**。业务方看着顶栏空位问「是不是要在这个地方加一个权限套用设置」——判断正确：后端 `/api/nav-modules/save` **早就支持收 templates**（校验岗位 key、洗 secs/mods/acts、留审计），V2.143 的套用预览也在读它，但**全站没有任何界面能编辑模板内容**（种子写死在代码 `NAV_POST_TEMPLATES_DEFAULT`，注释说"主管理员可在设置页调"却从没做过那个页）。**⚠先修雷（后端）**：`/save` 原来 `st = body.get("state") or {}` **无条件重写全部模块状态**——只发 {templates} 不带 state，会把所有模块的上线开关静默重置回 default（改个模板顺手把「已上线/待验收」全冲掉）。改为 `if isinstance(body.get("state"), dict):` 才写 state 段；既有调用方（门户管理页）总是带 state，行为不变。**实测**：只发 templates 保存后 `_nav_state()` 与保存前**逐字节相同** ✓、模板内容等价落库 ✓（后端会给缺失的 mods 键补空数组，属结构归一化非丢数据）。**抽屉（UserAdmin.jsx，仅主管理员）**：位置在「⚙敏感级别设置」左侧；复用 ua-mask/ua-drawer/ua-lvl/ua-msw 既有样式。结构＝岗位切换 chips（读岗位名单）+ 选中岗位的三组开关：①**整个板块的菜单准入**（注明"该板块现有及将来新增的菜单都给"——模板按板块写的本意）②**另加单个菜单准入**（列非 group_only 模块、标所属板块）③**动作权限（仅非敏感）**——**敏感点不列**：后端 `_template_caps` 本来就强制滤掉，UI 列了也是骗人的勾（抽屉头注明"敏感权限进不了模板，必须手工显式授予"）。保存＝新 api `saveNavTemplates(templates)`（只发 {templates}，正是踩中上面那颗雷的调用形态，故先修雷再上抽屉）；保存后刷新本地 navDef.templates——**V2.143 的套用预览立即反映新模板**，改完模板不用刷新页面。**验证**：构建通过（index-BknkdQlI.js）、`import app` OK（106 路由）；state 不被重置 + 模板等价落库两项后端实测 ✓。**遗留**：①抽屉 UI 未浏览器实测（登录态失效、本方不代输密码）——业务方登录后看：顶栏应有「📋 岗位模板设置」，点开选岗位、拨开关、保存后再点「按岗位一键套用」预览应显示新模板；②模板里"整个成本模块"仍含三个「敬请期待」空菜单（V2.143 遗留②），现在业务方可自己在抽屉里把 secs 换成 mods 单点勾——是否调整由业务方定；③需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.143** | 2026-07-17 | **【权限中枢】岗位并进名字行（李志鹏 · 成本会计）；「按岗位一键套用」挪进动作区并加【点前预览】**。业务方：「岗位挪上去，变成李志鹏 · 成本会计，一键套用好像还没有入口是怎么套用法」。**判断**：套用按钮其实一直在（藏在独立的岗位行里），业务方觉得「没有入口」的真因有二——①独立岗位行把「人是谁」劈成两行、按钮淹在中间；②**点之前完全看不出会套什么**（只有 hover title），套完也只报「勾了 N 个」不报是哪 N 个。**改法（UserAdmin.jsx）**：①岗位下拉**内联进名字行**＝「李志鹏 · 成本会计」——新样式 `.ua-postsel`：默认长得像文字、hover 才露出是控件、点开即改；「待认领」标签与「外部」标签一并挪入名字行；独立岗位行删除。②套用按钮挪进右上动作区（与 禁用/重置密码 并列），管理员不显（本就全权）。③**点前预览面板（新增）**：点按钮先展开「按『成本会计』模板将勾上：· 整个板块的菜单准入：成本模块 · 另加单个菜单：科目余额、序时账簿 · 动作权限：成本台账·查看与导出、成本台账·维护仓库类型」+ 「只加不减；敏感权限永远不会被模板给出」说明 + **确认套用/取消**——不再闭着眼睛套。预览数据源＝`/api/nav-modules` 已返回的 `templates`（secs/mods/acts 结构），前端用 sections/modules/caps 元数据翻成中文，**展示口径与后端 `_template_caps` 一致**；岗位没配模板时提示去「系统设置 › 岗位名单」配。④**套完的提示列出实际新勾的点名**（≤5 个点名、多的归总「等 N 项」）——「勾了 N 个」没人知道是哪 N 个。⑤切换账号自动收起预览面板（防止 A 的预览挂在 B 头上）。**验证（数据面，Python 模拟前端翻译逻辑）**：「成本会计」预览显示与 `_template_caps("cost_acc")` 实际勾的完全一致（enter:costledger/enter:sbal/enter:journal/enter:bomprice/enter:prodbrief/enter:tempatt + cost_ledger + cost_ledger_wh）——且 **V2.142 的二级闸模型在模板里自动生效**（模板给 enter:costledger 一个闸，仓库类型靠 cost_ledger_wh 第二道门，不再有 enter:clwh）。构建通过（index-Z7JKJlqa.js）。**遗留**：①页面 UI 未浏览器实测（登录态失效、本方不代输密码）——业务方登录后看：名字行应为「李志鹏 · 成本会计」，右上有「按岗位一键套用权限」，点开是预览面板；②成本模块整给会含 BOM报价审核/生产简报复核/临时工考勤 三个「敬请期待」菜单的准入（预览如实显示"整个成本模块"）——要不要从模板里剔掉空菜单，待业务方定；③需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.142** | 2026-07-17 | **【权限中枢】准入闸设在二级、三级不再各设准入点——「成本台账和仓库类型是一组」（业务方定）**。业务方看账号管理页第②栏：成本模块并排两个准入框（进入 成本台账 / 进入 仓库类型），指出「能不能进入的是二级菜单，而不是三级，三级应该靠右边权限去管控」。**新模型**：准入闸只有 **enter:costledger** 一个；仓库类型（clwh，三级）见不见＝**组闸 AND 动作点 cost_ledger_wh（维护仓库类型）**两道门都过。**⚠与确认书第四节的教训不冲突（那是这次设计的前提）**：当年的坑是把非敏感动作点**直接**当准入闸——default_perms 自动发放 → 菜单人人可见、D5「存量全部不给」当场失效（端到端抓过）；现在动作点只做**组闸之内**的第二道门，没组闸的人整组不可见，D5 仍成立。**实现**：①`_enter_cap(m, by_key)` 加规则——**「第三种节点」（可进入且有子项，如成本台账）的子项不生成自己的 enter:，准入跟父闸走**；group_only 组（银行对账/财务会计/物流对账）的子项维持各自准入不变（它们是并列工具页，本就要分岗授权——本次只命中 clwh 一个）。②NAV_MODULES 的 clwh 加 `act_cap: "cost_ledger_wh"`，`_all_modules()` 输出 `act` 字段；Sidebar `canEnter = (无cap或授了cap) && (无act或授了act)`。③`_nav_cap_meta()` 只登记【自己的】闸（cap == enter:<自己key>）——否则 enter:costledger 被注册两次、第二条还顶着「进入 仓库类型」的错标签（自查抓到并修）。④动作点 gate 自动归位：cost_ledger/cost_ledger_fetch/cost_ledger_close/**cost_ledger_wh** 四个的 gate 全部＝enter:costledger（第③栏归堆与级联收回跟着走）。⑤**接口同步收紧**：GET /api/cost-ledger/warehouse-types 由「仅登录」收紧为 cost_ledger_wh——菜单藏了、接口也得拦（直接打 URL 不能绕过）；已核实只有 CostLedgerWh.jsx 调它、月结核对页不碰，收紧不误伤查看者（POST 原本就是 cost_ledger_wh 未动）。**存量核查**：现库无任何账号持有 enter:costledger / enter:clwh（准入从未发过）→ **零迁移**；enter:clwh 从注册表消失后，perms JSON 里也无残留可担心。**验证**：模块输出 clwh cap=enter:costledger + act=cost_ledger_wh ✓；准入点注册表**无重复、enter:clwh 不存在**、成本模块只剩「进入 成本台账」✓；可见性矩阵五行全对——没组闸→两个都不见 / 只有组闸→只见成本台账 / 组闸+维护仓库类型→都见 / **只有动作点没组闸（当年的坑）→整组不见** / 主管理员→全见；银行对账等组子项 cap 不变（enter:reconcile…）✓。`import app` OK（106 路由）。新包 index-DJrLGMPh.js。**遗留**：①页面 UI（账号管理第②③栏实际渲染）未浏览器实测——登录态失效、本方不代输密码，业务方登录后看一眼：成本模块应只剩一个「进入 成本台账」准入框；②若业务方要把 group_only 组（银行对账等）也改成「组级一个闸」，那是权限粒度变粗的全站大改、涉及导航与权限重构确认书，需另行拍板；③需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.141** | 2026-07-17 | **【成本台账】新增第④步「事务类型透视」（收入/发出按业务类型归集）+ 导出同款「收发存·按事务类型」表；七步→八步**。业务方先问「存货收发存明细表(跨维度)的事务类型字段能取到吗」，探明后定「加多一个步骤，事务类型透视，第四步，顺便导出表格也是一样的」。**探查结论**：真·明细表 `HS_InOutStockDetailRpt` 仍被拒（「此接口暂时只支持简单账表」）；事务类型字段＝**`FBusinessType`**，只在**流水级** `RPT_INOUT_FLOW = HS_NoDimInOutStockDetailRpt` 上（107/5期 13,625 行）。候选 `FTransType`/`FBillType` 等 9 个均「字段不存在」。配套字段：`FBillNo`（单据号）✓ / `FStockName` ✓（这张表上反而有值）/ `FLotNo` ✓ / `FDate` ✗。**取数设计**：新增 `kingdee_client.fetch_business_type_summary()`——流水 13,625 行在**服务端聚合成 ~19 行**（剔「期初结存/期末结存/合计」结构行）再随 🅰 一键取数落库（落结论不落底表）；失败不拦取数（事务类型是补充视角，三道勾稽不受影响）。**⚠不碰流水表的「结存」列**——那是按物料累计的滚动结存、不按仓库/批号重置（V2.87 精炼椰子油调拨实证），算结存必错；本屏只归集**发生额**，结存以仓库透视/账实勾稽为准（页面与导出均有此注）。`btypes` 挂进 `res`（封存快照自然拍进去）；🅱 上传通道无此数据（表里没有事务类型）→ 屏上提示改用 🅰。**⚠一个 1.59 元的尾差，如实呈现、不写"同数"**：按类型逐行加总，收入合计 24,222,127.26 与汇总表**分毫一致**；发出合计 25,909,709.86 与汇总表 25,909,708.27 **差 1.59 元**——溯源＝金蝶流水报表**自身合计行就与其成员行之和差 1.59**（51,819,418.13 − 25,909,708.27 = 25,909,709.86），属金蝶报表内部尾差、非工具算错。页面与导出注脚都**动态对比**（一致显绿"分毫一致"，不一致显琥珀"差 X 元（金蝶流水报表自身尾差，非工具计算误差）"），绝不写死。**页面**：第④步屏＝事务类型 × 笔数 × 收入(数量/金额/占比) × 发出(数量/金额/占比)，负数行（红字冲销：退货/退料）标红并注明属正常业务；合计行；数量混单位注脚。STEPS 七步→**八步**，其后序号顺延（收发存明细④→⑤、异常⑤→⑥、损益⑥→⑦、台账出具⑦→⑧，「怎么走」/状态条提示同步改）。**导出**：新增「收发存·按事务类型」表（8 列同页面），插在「收发存汇总·按仓库」之后；**旧数据/🅱通道没有 btypes → 不出这张表**（同损益归集口径：缺就不出、不出空表）。**验证**：19 种事务类型（标准销售出库单 3,035 笔/发出 13,642,867.83 ← 发出的 52.7%、汇报入库 8,860,406.90、标准采购入库 8,499,379.94、分步式调入 6,741,639.96、普通生产领料 5,075,281.92…）；快照链路 `_cl_result→res.btypes` ✓；导出 **7 张表**（核对结论/账实勾稽/按类别/按仓库/**按事务类型**/异常清单/收发存明细），注脚动态文案实测正确（收入"分毫一致"、发出"差 1.59 元"）。17/17 单测绿。新包 index-5dBx-WUN.js。**遗留**：①页面 UI 未浏览器实测（重启后登录态失效，本方不代输密码）——业务方登录后过一眼第④步即可，数据链路与导出已后端实测；②存量数据需重取才有事务类型（已重取 107/2026-5）；③服务器主体档案全称待核对（V2.137）；④需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.140** | 2026-07-17 | **【成本台账】收发存明细列序按业务方指定调整**（纯列序，不动任何取数与计算）。业务方给出目标顺序：物料编码 / 物料名称 / 规格型号 / 存货类别 / 物料分组 / 仓库 / 批号 / 单位 / …（其后为 期初·收入·发出·结存 各 数量·单价·金额）。原序为 编码/名称/**分组**/规格/**单位**/类别/仓库/批号——分组插在名称与规格之间、单位夹在规格与类别之间。**改动三处**：①`/api/cost-ledger/detail` 的 `keys` 元组重排；②明细屏表头与单元格重排（物料段仍 colSpan 8，分段竖线移到「单位」之后）；③导出「收发存明细」`heads`/`TXT`/列宽重排（仍 20 列，文本列数不变故数字列起始 column 9 不动，`freeze_panes="C2"` 不变）。**附带好处**：单位现在**紧挨着后面的数量列**——看数时不用横跨半张表去确认这行是千克还是个。**验证**：导出前 8 列 = 期望顺序 ✓（全 20 列：物料编码/物料名称/规格型号/存货类别/物料分组/仓库/批号/单位/期初数量/期初单价/期初金额/收入数量/收入单价/收入金额/发出数量/发出单价/发出金额/结存数量/结存单价/结存金额），首行「强力粘鼠板 / 30*21 / 低值易耗品 / 车间周转物料 / 孝感低值易耗品仓 / 2022 / 张」；单价仍**存 6 位（0.663716）显 2 位（`#,##0.00`）**。页面实测前 8 列顺序一致 ✓，首行「桃胶 / 25kg/袋 / 原材料 / 原料 / 孝感茶饮原料仓 / 2026/04/18 / 千克 ‖ 5,000 × 57.36 = 286,821.48」。17/17 单测绿。新包 index-DYgSSsUI.js。**本版不需重取数据**（只改列序，字段与数据未动）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.139** | 2026-07-17 | **【成本台账】加【单位】列（取报表 `FUnitName`，实测 99.7% 即物料档案的基本单位）；并查实「结存数量合计」是 22 种单位混加、无物理意义**。业务方：「加多一个单位，最好是 kg」→ 追加「你就用基本单位」。**关于「基本单位」——照做会标错，故未照做（已按实测改用报表单位）**：报表的 `FBaseUnitName`/`FBaseUnitQty`/`FSecQty`/`FStockUnitName` 全被拒（500）；物料档案 `BD_MATERIAL.FBaseUnitId.FName` ✓ 可取。**但 2,770 行逐行比对：报表 `FUnitName` 与档案基本单位【一致 2,763 行 = 99.7%】、不一致仅 7 行**（急停开关 报表「个」vs档案「套」、一水柠檬酸 报表「袋」vs档案「包」、洗衣机清洁剂 报表「桶」vs档案「袋」、固态继电器 报表「Pcs」vs档案「个」、不锈钢铲子 报表「把」vs档案「个」）。**那 7 行的【数量是按报表单位计的】**——去档案取基本单位贴上来，就会把「5 个」标成「5 套」，**标错比不标更糟**。故采用 `FUnitName`：它既是这个数字的真实单位，又在 99.7% 情况下就等于基本单位。（另记：`FUnitId` 是第三个哑字段，接受但恒返回空——前两个是 `FStockName`、`FMaterialId`。）**关于「最好是 kg」——做不到，且不该硬做**：107/2026-5 实测 **22 种单位**（千克 1,504 行 54.3% / 个 567 / Pcs 317 / 套 75 / 米 37 / 根 37 / 卷 34 / 条 30 / 桶 24 / 双 23 / 瓶 22 / 块 17 / 把 14 / 盒 14 / 包 13 / 台 10 / 袋 7 / 片 6 / 升 6 / 件 5 / 张 4 / 箱 4）。个/Pcs/套/米/张/卷 等**计件计长单位物理上没有 kg 当量**；物料档案 `FNetWeight`/`FGrossWeight` **实测全为 0**（重量未维护），报表也不给换算率 → **换算即造数**。加单位列的价值正在于此：**让人一眼看出哪些行是千克、哪些不是**。**⚠顺带查实一个口径问题（本版未改，仅记录）**：页面与导出的**「结存数量合计 5,181,959.69」是把这 22 种单位直接相加**得来的——千克 + 个 + Pcs + 米 + 张 + 台…，**没有物理意义**。且 **Pcs 一种就占 4,355,091（84%）**、真正的千克只有 521,982（10%），即这个「总数量」实质是件数总和、不是重量。该数与业务方底稿一致（V2.116 曾 44/44 逐仓验证），**口径没错、是底稿本来就这么加**，故本版不擅自改；仓库透视每格数量、明细屏数量合计、导出合计行同理。**是否按单位分组小计、或标注「混合单位仅供参考」，待业务方定。****改动**：`fetch_inventory_summary` 字段加 `FUnitName` → `unit`（仍在同一次请求内，不多打接口）；cross 行 19 键 → **20 键**；`_CROSS_COLS` 补 `单位/计量单位/基本单位` 三种叫法 → `unit`（🅱 底稿列名不一，都认）、解析器补 `unit`；`/api/cost-ledger/detail` 返回 20 列；明细屏与导出「收发存明细」把**单位排在规格型号之后**（物料段 colSpan 7→8，导出 19→20 列，数字列起始 column 8→9）。**验证（真金蝶重取 107/2026-5）**：2,770 行、**结存金额 10,958,051.26 / 数量 5,181,959.69 与加列前分毫不差**、credible=True；**单位为空 0/2770**；22 种单位分布与探测一致。导出实跑：明细表 **20 列**，首行「强力粘鼠板 / 车间周转物料 / 30*21 / **张** / 低值易耗品」；单价仍是**存 6 位（0.663716）显 2 位（`#,##0.00`）**。页面实测首行「桃胶 / 25kg/袋 / **千克** / 原材料」——一眼可见这行的 5,000 是 5,000 千克而非 5,000 袋。17/17 单测绿。新包 index-sD8EZFRq.js。**遗留**：①**又要重取存量数据才有单位列**（已重取 107/2026-5，留痕仍是「管理员」）；②混合单位合计口径待业务方定（见上）；③服务器主体档案全称待核对（V2.137）；④需求确认书 v1.0 待重写；⑤台账历史遗留 **V2.60 重号**（非本次造成，待清）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.138** | 2026-07-17 | **【成本台账】字段补齐到业务方底稿的 17 列：加【物料分组】+【四段单价】；单价一律取金蝶、绝不自己算 金额÷数量（三条实测硬理由）**。业务方给出底稿列清单：物料编码/物料名称/**物料分组**/规格型号/存货类别/期初数量/**期初单价**/期初金额/收入数量/**收入单价**/收入金额/发出数量/**发出单价**/发出金额/结存数量/**结存单价**/结存金额——工具此前缺 5 列。**探明来源（只读实测金蝶，全在【同一次报表请求】里加字段，不多打接口、不增加取数时间）**：`FMaterialGroup` ✓（车间周转物料/原料）；`FInitPrice`/`FReceivePrice`/`FSendPrice`/`FEndPrice` ✓ 全有值。被拒的：`FMaterialGroupName`/`FGroup`/`FMaterialTypeName`/`FInitUnitPrice`/`FEndUnitPrice`/`FPrice`（500）。初看 `FReceivePrice`/`FSendPrice` 返回空是取样偏差——那 3 行本就没收发；全量核对：有收入量 724 行 / 收入价空 2,047 行，2770−724=2,046；有发出量 1,195 行 / 发出价空 1,575 行，2770−1195=**1,575 完全吻合** → **规律是「数量为 0 → 单价为空」**（没量就没价，合理）。**⚠核心决策：单价【取金蝶的，不自己算】。三条实测硬理由（107/2026-5，2,770 行）**：①**数量=0 而金额≠0 的「挂账尾差」7 行** → 自己算**要除零**，而金蝶照样给价（41.420118 等，加权平均结转价）；②**数量极小的 2 行**（结存量 0.0001、结存额 0.0037，青岛波尼亚仓 批号 2023/03/10）→ 金蝶单价 40.217391，金额÷数量 = 37.000000，**差 3.2174 元**——因为 0.0037 是**四舍五入后的显示值**，拿它去除以极小数量，误差被放大（另一行差 2.6648）；③**「紫糯米」有收入量 42、收入额却为空** → 自己算出 0，等于凭空造一个「单价 0」。且金蝶单价是**加权平均结转价、非简单相除**，自己算出来的数成本会计拿去跟金蝶核会对不上。全量比对 1,887 个可比行：仅上述 2 行偏差 >1e-6，其余分毫一致——**能取就取、不猜不造数**的一贯口径。**空值口径**：新增 `kingdee_client._rpt_price()` —— 空 → **None（不是 0.0）**。数量/金额可以理直气壮当 0（没动就是没动），单价不行：跟着 `_rpt_num` 返回 0.0，页面就会显示「单价 0.00」、被读成「这货单价真是零」。None → 显示「—」，同「除不动就老实说没有」口径（业务方确认）。**两通道口径再拉平**：`_CROSS_COLS` **早就写了 `"物料分组": "grp"`、解析器也一直留着 grp**——即 🅱 上传通道一直有物料分组，只有 🅰 一键取数没取（**与 V2.130 物料编码同类的第二处通道不一致**）。本版 🅰 补齐；🅱 的 `_CROSS_COLS` 补 4 个单价列（表里没这几列也不报错，老底稿照样能传）、空值同样 → None。**落地**：cross 行 14 键 → **19 键**（+grp/op/ip/dp/ep）；`/api/cost-ledger/detail` 返回这 19 列；第④步明细屏改**双层表头**（物料 | 期初 | 收入 | 发出 | 结存，每段 数量·单价·金额 并排、段间加分隔线——12 个数字列不分段会糊成一片认不出哪段是哪段）；导出「收发存明细」表 11 列 → **19 列**。**单价精度按业务方定的「显示 2 位、计算用 6 位」**：Excel **单元格存金蝶原值 6 位、number_format 只显示 2 位**——点开格子看到的就是金蝶原数，拿去核对/再计算不丢精度，表面又不刺眼；页面显示 2 位。**验证（真金蝶重取 107/2026-5）**：2,770 行、nomap=0、**结存金额 10,958,051.26 / 数量 5,181,959.69 与加字段前分毫不差**（只补字段、不动数）、credible=True；19 字段齐、17 列一个不缺；物料分组为空 **0/2770**；单价 None 比例 797/2047/1575/878 与探测阶段完全一致。导出实跑：明细表 **19 列**，单价单元格值 `92.118971`、格式 `#,##0.00`（**存 6 位显 2 位** ✓），数量为 0 的行单价写「—」✓。页面实测首行「桃胶」：期初 5,000×57.36=286,821.48 / 收入 0×—=527.08 / 发出 0×—=−256.99 / 结存 5,000×57.52=287,605.55——**这行本身就是活证据**：收入数量 0、收入金额 527.08（成本调整），自己算就是除零，金蝶给空、页面显示「—」。17/17 单测绿、`import app` OK。新包 index-c14nAEEZ.js。**遗留**：①**又要重取一次存量数据才有新字段**（本次已重取 107/2026-5，留痕仍是「管理员」；其它主体/期间需各自重取，已封存的读快照不受影响）；②服务器主体档案全称待核对（见 V2.137）；③需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.137** | 2026-07-17 | **【部署·续】科目余额取不到——主体档案【全称】是数据库数据、部署包不带库，服务器上那几条老记录与金蝶账簿对不上；把「账簿名对不上」从一句含糊提示改成指名道姓的诊断**。**现象**：业务方传 V2.136 新包后取数通了（证实 V2.136 的配置文件确是上一个坑），但「**取不到科目余额表**」。**根因（与 V2.136 同源：数据不在包里）**：`_inv_gl_balance` 自 V2.126 起按【主体档案全称**精确相等**】匹配账簿（`if book != full: continue`），而 `full` 取自 **`orgs` 表＝数据库数据**——**部署包不带库**（也不该带）。V2.125「补齐 9 个主体、全称一律从金蝶组织档案取不手抄」是在**本机库**做的，台账里就写着「数据补录，无代码改动」；服务器上那 3 条老记录（101/105/107）的全称是当初手填的，**差一个字就全量跳过 → 科目余额取空**。本机 107 全称 = `孝感市星期九食品科技有限公司`，与金蝶 GL_BALANCE 返回的账簿名一字不差，故本机一直正常、看不出问题。**真正的缺陷是提示**：对不上时前端只显示「未取到科目余额（账实勾稽跳过）」——**看不出是「名字对不上」还是「这主体本来就没存货科目」**，业务方无从下手。故本版重点不在「改数据」（那是服务器上的一次性动作），而在**让工具自己把话说清楚**。**改动**：①新增异常型 **`ClgBookMismatch`**；`_inv_gl_balance` 先把 GL_BALANCE 返回的账簿名收集成集合，`full` 不在其中即抛出——消息**把两边的名字一起摆出来**（档案里登记的是什么、金蝶那边有哪些账簿），并分两种指引：名字对不上 → 到「基础数据 › 主体档案」改成与金蝶一字不差（精确匹配非模糊匹配）；该主体本就没存货科目 → 账实勾稽跳过属正常，收发存自平与透视不受影响。全称为空时另给一条专门提示。②两个取数路由（🅰 analyze-kingdee / 🅱 analyze）都加 `except ClgBookMismatch: gl_msg = str(e)`——**已是完整诊断，不再套「科目余额取数失败：」前缀**；其余异常照旧。③前端 `gl_msg` 渲染加 `whiteSpace:'pre-line'` + `alignItems:'flex-start'`——诊断是多行的，不加会挤成一行、最该看的「金蝶那边叫什么」淹在里面。**为什么是提示而不是自动改**：账簿全称涉及主体身份，工具**只读金蝶**、也不该替业务方改主体档案（同「不猜仓库类型」「不反写金蝶档案」的一贯口径）——把两个名字摆出来，改不改、改成什么由业务方定。**验证**：模拟服务器现状（把 `_cl_org` 的 full 改成「孝感星期九」＝很可能的手填值）→ 正确抛 `ClgBookMismatch` 并列出金蝶真实的 5 个账簿（Sinkio Limited／孝感市星期九…／深圳市星期九…／深圳市星期十…／深圳市星期零…）；full 为空 → 另一条专门提示；还原后本机取到 6 个科目。`import app` OK（103 路由）、17/17 单测绿。新包 **`上传包_V2.137_20260717/finance_workbench_backend_V2.137.zip`**（29 文件），安全检查全 0，含 `cost_ledger_config.json`、两个异常守卫、前端 `pre-line`（新包 index-JtY40BtX.js）。**治理补记**：本条与 V2.136 原先误占 V2.132/V2.133——**并行会话（平台/RPA 文档线）已用掉 V2.132~V2.135**，已按「不跳号、不删条、只顺延」改为 V2.136/V2.137。另：写 V2.136 条目时在 Python 串里写 `D:\0 Claude` 导致 `\0` 被当成转义符、**往台账写进了一个 NUL 字节**（grep 一度把台账当二进制文件），已修复；同一个坑本次会话在 CostLedger.jsx 上也踩过一次。**遗留**：①**V2.131/V2.136 的包已删，以 V2.137 为准**；②**服务器上要做一次性数据核对**：到「基础数据 › 主体档案」把各主体全称改成与金蝶账簿一字不差（本机 9 条正确值见 V2.125 条目）；服务器主体档案可能只有 3 条（101/105/107），要用别的主体还需补建；③存量数据物料编码为空需各自重取（V2.130 起才带）；④需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.136** | 2026-07-17 | **【部署·事故修复】历次部署包整体排除 `sample_data/`，把成本台账的【配置文件】一起漏了——服务器上成本台账一点取数就 500（从来没能用过）；补发该文件 + 把裸 500 改成说人话**。**现象**：业务方部署后点取数 → 前端「金蝶取数失败：/api/cost-ledger/analyze-kingdee?year=2026&period=2&org=107 **500**」。**排查（本机逐项排除，全部跑通、证明不是代码逻辑问题）**：2026-2 期真金蝶取数 **2,249 行 / nomap=0**；`_inv_gl_balance` 正常；`build_cost_ledger` 正常；`_cl_save_input`/`_cl_register_warehouses`/`db.audit`/`_cl_payload` 逐个 OK；payload **JSON 可序列化、无 NaN/Inf**（Starlette 用 `allow_nan=False`，本是 500 的经典成因，已排除）；`period_inputs.payload` 列是 `LargeBinary(2**32-1)`→MySQL LONGBLOB，2 期 gzip 后仅 **89.2 KB**，装得下（BLOB 64K 上限的猜想排除）。审计表显示**本机从无 2 期记录**（`db.audit` 在取数成功后才写，故 500 不留痕）→ 判定业务方是在**服务器**上点的。**根因**：`_CL_CFG_PATH = BASE/sample_data/cost_ledger_config.json`，而 `clg.load_config` 是**裸 `open()`、无 try**；它经 `_cl_config()` → `_cl_result()` → **`_cl_payload()`** 被调到，而 `_cl_payload` 在路由的 try/except **之外** → 文件缺失＝未捕获 `FileNotFoundError` ＝ **HTTP 500，前端只看到一个光秃秃的状态码**。而**历次部署包（含 07-13 那个、含我昨天打的 V2.131）都整体排除 `sample_data/`** —— 实测 07-13 包里 `sample_data` 相关只有 `sample_data.py` 模块、**没有目录里的任何文件** → **这个配置从没上过服务器**。**为什么会漏**：`sample_data/` **混装了两类东西**，一刀切必错——`config.json` 是**运行状态**（`app.py:163` `json.dump` 会写它，内容含本机 `D:\0 Claude 数据\...` 路径，发过去会把服务器期间重置、路径还指 D 盘）→ **绝不能发**；`cost_ledger_config.json` 是**只读配置**（存货类别↔总账科目对照 + 仓库属性种子，从不被写）→ **必须发**。且 **`category_to_subject`/`extra_subjects` 只有 json 一个来源、没有数据库兜底**（V2.119 只把 `warehouse_attr` 挪到了 `app_settings`），文件缺了就没法把存货类别归到总账科目，账实勾稽无从算起。注：git 也不跟踪它（V2.57 后新增未入库），**故不能靠 git 状态分类，只能看代码怎么用**。**修复**：①**打包口径改为按文件性质逐个点名**——`sample_data/` 下只发 `cost_ledger_config.json`（白名单 `SAMPLE_SHIP`），其余（config.json/claims.json/ledger*.json/account_overrides.json/workbench.db）一律不发；②新增异常型 **`ClgConfigMissing`** + `@app.exception_handler`，配置缺失时回**可读消息**（「服务器缺少成本台账配置文件 sample_data/cost_ledger_config.json（存货类别↔总账科目对照）——它是代码配置、不是数据，请把部署包里的这个文件放到 backend/sample_data/ 下再重启」）而不是裸 500；handler **只认领这一个异常型，其余照常抛出**（不吞别人的错）。**验证**：把配置文件临时移走模拟服务器现状——`_cl_config()` 正确抛 `ClgConfigMissing`；handler 返回 500 + 上述可读消息；传入 `ValueError` 时**照常抛出未被吞**；还原后 `_cl_config()` 恢复正常。新包 **`上传包_V2.132_20260717/finance_workbench_backend_V2.132.zip`**（29 文件 / 355.5 KB）安全检查全 0：无 conf.ini / bank_uploads / 任何 .db / config.json / claims·ledger·overrides / __pycache__；`cost_ledger_config.json` 在包内且内容正确（类别↔科目对照 3 组 + extra_subjects 2 项）。**副作用交代**：排查用的诊断脚本调了 `_cl_save_input`，**实际把本机 2026-2 期数据写入了库**（审计留痕「管理员 於 2026-07-17 14:06 一键金蝶取数 孝感星期九 2026年2期」）——数据本身正确（与业务方要取的同一份），但接入人记成了「管理员」；业务方如需自己的名字留痕，重取一次覆盖即可。另注意到 **2026-5 期已由「李志鹏」於 07-16 22:29 重取并封存、22:32 由管理员解封**——本实例已是多人在用。**遗留**：①**V2.131 的包（`上传包_V2.131_20260717/`）有此缺陷，不要用，以 V2.136 为准**；②服务器上其它主体/期间的存量数据物料编码仍为空，需各自重取（V2.130 起才带）；③需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.135** | 2026-07-16 | **【平台】过会版 xlsx 升 v1.2 —— 采纳业务方重构（新增「产出」列 / 收入台账整理提前至批次一 / ⑧ 措辞订正），并订正其 3 处敏感标记自相矛盾 + 2 处算错 + 补回 2 条消失的基建 + 单位口径**（纯文档/样例文件，未动任何代码）。业务方自行重构了调研表（原表页签外新增「批次排期」页签，14 列：批次/主题/条目/主对接人/当前进度状态/优先级/预估难度/预计工时/是否含敏感操作/取数口/产出/需求调研/需求评估/日期列），交回本方复核。**①采纳业务方三项改动**：**(a)「产出」列**——本方此前只有内部黑话「敏感度★★★」，业务方直接写成「**Web确认后录入金蝶**」，一句话把「草稿态+人工确认+人在金蝶提交审核」整套安全设计说成了需求方听得懂的话，优于本方原表述；**(b) 收入台账整理由批次五提前至批次一**——业务方判断正确，本方原排期有误：该条是电商收入线【唯一不依赖外部系统】的一条（只读金蝶、只算不写），却被压在「先探 3 天」后面；且它先做能把金蝶收入侧取数趟出来，批次五的旺店通/客户对账/电商结算反过来复用它。**同时澄清「批次≠工具线」**：该条归属仍是电商收入线，只是排期提前；**(c) ⑧ 运费计提**由本方「已上线·0 天」改为业务方的「基本达到验收态，剩余＝明确做账口径（核算维度类目）与原始凭证模板」——业务方域内知识，从之。**②订正 3 处自相矛盾**（同一行 I 列与 M 列打架）：**生产简报复核** I 标「初判为仅读取」但同行写着「要写金蝶凭证」、且原表流程名即「生产简报复核**，生成凭证**」→ 改「是·写金蝶凭证」（**此条最危险：它在批次三、会上照 I 列读会得出"不敏感"的错结论**）；**成本台账** I 标「初判为仅读取」但同行写着「取数会覆盖全员共享底稿…已设敏感点 cost_ledger_fetch」→ 改「是·覆盖共享数据」；**推广平台数据** I 标「未知」但同行写着「要写金蝶凭证」→ 改「是」。并把 I 列由「是/初判为仅读取/未知」升为**四档**（是·写金蝶主数据／是·开票｜是·写金蝶凭证／是·外部登录凭据｜是·覆盖共享数据｜初判为仅读取），正好对上工作台既有的两类敏感点（写金蝶 vs 覆盖共享数据）。**③订正 2 处算错**：合计栏原「50–65」与分批次加总（55–70）**对不上，差 5 天**；批次五**少了一条（收入台账整理移出）工时反而 +3 天**（本方原估 5 条「探查3+13–16」，业务方改 4 条「探查3+16–19」，加减号反了）。现重算并做到**逐批次可加总核验**：13+8+12+6+11＝50 / 17+11+16+8+15＝67 → 合计 **50–67 天**（推广平台不可估，未计入）。**④补回 2 条基建**：业务方版本里「凭证 Model 通用化」「复核引擎」从条目中消失，但批次三长摊待摊/批次五电商结算靠前者、本批 6 条靠后者，**不排＝没底座**。现以「【基建】」前缀显式入表、灰底标记、需求调研列注「非调研表条目」，避免与需求方条目混淆。**⑤单位口径**（业务方未指定，本方按建议执行并在表内写明依据）：业务方把「均为自然日」改成了「均为**工作日**」——**该改动等于凭空加约 40% 而基准未变**：三个基准数（凭证归档 2 天／物流计提 3 天／成本台账 7 天）均为**自然日实测**（成本台账 07-09→07-16 含周末，真按工作日仅约 5 天）。改回**「自然日工作量」并把跨度另说一句**：工作量 50–67 自然日（有基准可核）＋ 因非全职、须除去 BP 常规工作时间，**日历跨度预计 2.5–3.5 个月**（取决于投入时间）——两件事分开报，会上被问「这数怎么来的」才答得出。业务方原加的「非全职…只是预估…可能建议对原有步骤升级调整」全部保留。**⑥答业务方表内提问**：「全成本人工汇总的输入是不是就是生产简报复核的产出？」——**是**，故两条必须串做。**⑦提请业务方复核一处**：其「准备出物流对账工具需求确认书」与我方记录不符——一期确认书 v1.2 **已签署**（在 `01_Current_Deliverables/docs/20260708_物流对账/`，01 只放已批准成果），已按「本条对应二期」改写，请确认。**⑧做法**：直接 `load_workbook` 业务方文件、只重写「批次排期」页签，**原表页签「调研实施需求」原封带过——逐格比对 22×8 格 diff=0**。E 列「当前进度状态」与 N 列日期列按业务方明示（「就是空的」）保持空白。表尾附【工时口径】【敏感分档】【本版改了什么】三段说明，需求评估列改动处标绿。**交付物**：`03_Source_Materials/00_平台通用/财务自动化需求_评估与批次排期_过会版_v1.2_20260716.xlsx`。**待办**：md 评估文档需同步升 v1.2（批次一/五条目与工时、单位口径、④ 归属说明），会后按批注一并回写。 | Claude / c | In Review | 无（纯文档任务） |
| **V2.134** | 2026-07-16 | **【平台】过会版 xlsx 改为「在原调研表上加列」（v1.1）；评估文档同步升 v1.1；并订正两处事实错误**（纯文档/样例文件，未动任何代码）。业务方退回 V2.133 的 xlsx：「你倒是整合一下调研表呀，只有序号，我还要去理解哪个是什么」——**批评成立**。**①病根**：V2.133 另起了一张评估表，**把原表的「具体操作步骤」整列丢了**——而那一列恰是「这条到底是什么」的唯一答案；且原表 A 列序号**本就是空的**，①②③ 是评估方自编号，业务方更无从对照。等于逼人拿序号在两张表之间来回翻。**②改法**：主表改为 `1·调研表+评估` ——**原表 B–H 共 8 列（业务模块/流程名称/具体操作步骤/涉及系统/目标/对接人/紧急程度）原样保留**（灰头灰字 + 浅底，与评估列以中粗竖线分界，头部加「原调研表字段 ← → 工作台评估」分区横幅），评估 7 列（现状/难度/工时/取数口/敏感操作/批次/说明）接在其后，**一行到底、不跨表**。**已逐格比对原表 19×7＝133 格，diff=0**（原样保留，未作任何改动）。其余各页签的条目引用一律改为**「序号 + 流程名」并排**（如「⑥ 外资/中行/微信流水 / ⑧ 运费计提凭证录入」），不再只给序号。**③订正事实错误一**：v1.0/V2.132 称「具体操作步骤」为空的是 ②③⑤⑬⑮⑯——**错**。逐格核实为 **②③⑤⑥⑬⑮**（**漏了 ⑥、误纳了 ⑯**）；⑯ 属另一种情况：填了，但填的是**系统名**「金蝶系统与税务平台」而非步骤。**⑥ 步骤为空这点有分量**——它排在批次一、且带着「导入金蝶」歧义。已在主表该列就地标黄「（原表未填 —— 表头自注「红色填充，为必填字段」）」。**④订正事实错误二（代码 bug）**：标黄提示先 `ws.cell()` 写入、随后被 `put()` 用 vals 覆盖回空，**等于没标**；改为写进 vals 再 put。回读验证：6 行标黄到位、⑯ 的「金蝶系统与税务平台」原样保留、全簿无 markdown 星号残留。**⑤评估文档同步升 v1.1**（v1.0 保留作历史版，同 `目录结构调整_影响分析` v1.0/v1.1 并存先例）：批次由 6 收敛为 5（与 xlsx 对齐，V2.133 已改 xlsx 但 md 未跟，属两份文档打架，本次修平）；**新增 §6 敏感操作**（md 此前无此切面）；§5 空白步骤清单按上述订正；⑥ 加歧义标注并把工时改标「3 天 ~ 待确认」；问题清单加「⑥ 是否直写金蝶」一问。**交付物**：`03_Source_Materials/00_平台通用/财务自动化需求_评估与批次排期_过会版_v1.1_20260716.xlsx`（6 页签，主表冻结窗格至 D 列+筛选，`5·待确认问题` 留空白「答复」列供会上当场填）、`03_Source_Materials/00_平台通用/RPA需求调研表_评估与批次排期_v1.1_20260716.md`。**待办**：会后按批注回写 md + xlsx 升 v1.2；《电商收入工具线_立项影响分析 v1.0》D1–D7 待拍板。 | Claude / c | In Review | 无（纯文档任务） |
| **V2.133** | 2026-07-16 | **【平台】《财务自动化需求·评估与批次排期·过会版》xlsx 出稿；批次由 6 收敛为 5；新增「敏感操作」维度并据此查出⑥「导入金蝶」口径歧义**（纯文档/样例文件，未动任何代码）。业务方要求：「整理成 Excel，明天和他们开会一条条过，分 3–5 批次，是否有敏感点操作」。**①交付 6 页签 xlsx**（openpyxl，微软雅黑+冷灰表头，沿用 V2.56 成本台账导出样例的样式口径）：`0·结论`（七条结论）/`1·逐条评估`（19 条 × 12 列，冻结窗格+筛选，一行一条供会上逐条过）/`2·批次排期`/`3·合并点`/`4·敏感操作`/`5·待确认问题`（留空白「答复」列供会上当场填）。**②批次 6→5**（业务方要 3–5 批）：重排后每个批次≈一个人的过会，可按人锁口径、减少答疑往返——一·在建收口(⑥⑧⑨⑪⑮，陈梓华/李志鹏，10–12天) / 二·金蝶通道与导出中心(⑦⑯⑱⑲，冯辉为主，8–10天) / 三·成本复核套件(⑫⑬⑭⑰，**全属李志鹏**，12–15天) / 四·临工考勤单独立项(⑩，李志鹏，6–8天) / 五·电商收入线(①②③④⑤，**全属黄春艳**，探3天+13–16天)。5+4+4+1+5=19 条全覆盖。**③新增「敏感操作」维度**（业务方问「是否有敏感点操作」触发，本轮前未做此切面）：按工作台既有先例分三级——★★★＝写主数据/开票（无草稿态兜底，写下去即生效）；★★＝写金蝶凭证（有草稿态+人工审核+post_log 防重）或保管外部系统凭据；★＝覆盖全员共享数据/期间封存；—＝只读只算。**结论：19 条中 7 条要往金蝶写数（②⑤⑥⑦⑧⑫⑬），占三分之一**。其中**⑦汇率写的是金蝶汇率主数据、不是凭证**——一错影响全账套所有外币业务，且无「草稿态+人工审核」兜底，**敏感度反而高于写凭证**，建议先只出 Excel 供人工导入，确需直写则须「预览→人工确认→写入」三步+留痕；**③自动开票**属强监管+开票系统集成，建议剔出；**⑯待认证进项**需税局登录+验证码，**建议不做自动登录、工具不保管税局账号密码**，改人工下载清单+上传（既避凭据风险，也是本条唯一可靠路径）。**④「敏感操作」这一问顺带查出一处真歧义**（本条最有价值的发现）：⑥原文「外资公司、中行银行、微信流水整合 EXCEL，**并导入金蝶银行对账单模块中**」——此前按「工具出 Excel、人导入」理解并估 3 天，但原文亦可读作**工具直接写金蝶银行对账单模块**；**后者是我方从未写过的新写入目标**（现有写入仅 GL_VOUCHER），工时与敏感级别均需重估。已列为待确认问题第 6 条，请陈梓华当场澄清。**⑤同步修订**：⑥ 工时由「3 天」改标「3 天 ~ 待确认」，敏感级别标「★★ 中·待确认」。**交付物**：`03_Source_Materials/00_平台通用/财务自动化需求_评估与批次排期_过会版_v1.0_20260716.xlsx`（配套 V2.132 的两份 md：评估与批次排期 v1.0、电商收入工具线立项影响分析 v1.0）。**待办**：会后按批注回写 md 两份 + xlsx，升 v1.1。 | Claude / c | In Review | 无（纯文档任务） |
| **V2.132** | 2026-07-16 | **【平台】《RPA需求调研表·评估与批次排期 v1.0》+《电商收入工具线·立项影响分析 v1.0》出稿**（纯文档，未动任何代码/文件）。业务方转来他部门《RPA需求流程收集调研表-汇总表.xlsx》（19 条、4 位对接人）要求评估，并明确「不是 RPA，后面会用 Claude 开发，他们理解错了」。**①评估结论**：19 条**无一需要 RPA**——逐条核对取数方式，金蝶导出 8 条／外部系统导出 5 条／人工上传 5 条／网页查数 1 条，**全部有数据出口**；真实形态统一为「取数→比对→出表／生成凭证」，即工作台在建能力。RPA 的适用前提（无数据出口、只能模拟点击）一条都不满足。**②重要发现·5 条已在工作台上**：⑧运费计提凭证录入＝**已上线**（物流计提 A 期「一键录入」）、⑥外资/中行/微信流水→银行对账线、⑨运费数据核对→物流对账线（v1.2 已签、二期方案已出）、⑪装卸费复核＝⑨同构可复用、⑮成本台账在建近齐活——**陈梓华填的 4 条里 3 条已在我们手上**，不应重新立项。**③自查纠错（本条最该记的）**：初评曾口头断言「至今只做过读、没做过写，凭证写入通道需 3 天」，**实测证伪**——`kingdee_client.save_voucher/view_voucher/delete_vouchers` 早已随物流计提 A 期上线（建草稿态→View 回查→`post_log` 防重→提交/审核始终人在金蝶做），且沉淀了两处实证坑位（`FDate` 不可全大写、Save 对 JSON 字段顺序敏感，顺序错即录错月份）。故⑧工时归零（仅需核对口径）、「凭证写入通道」由 3 天降至约 2 天（缺的只是把 `build_kd_model` 从物流专用泛化为通用）。**④工时基准取自本台真实交付**（凭证归档 2 天／物流计提 A 期 3 天／成本台账 7 天），据此总盘**约 50–63 自然日**；并指出真实约束是**需求方答疑带宽而非开发产能**——成本台账 7 天中编码仅约 1.5 天，其余耗在锁口径／探金蝶字段（曾返工四轮）／业务方见到成品才说出真实要求。**⑤六个合并点**：⑱+⑲＝金蝶导出中心、凭证 Model 通用化（⑤⑬⑦）、⑨→⑪对账骨架复用、复核引擎（④⑩⑫⑭⑰）、⑫→⑭串做、电商取数复用（①③④⑤）——合并省 12–19 天。其中复核引擎非本方臆造：调研表 F 列「异常行单元格标黄底颜色」出现 5 次、「保留单元格公式」2 次，**需求方心里的交付物是一张能自己往下核的 Excel**。**⑥六个批次**：一·在建收口（10–12 天，无前置可即开）／二·金蝶两条通道（5–6 天，与一并行）／三·凭证生成群（7–9 天）／四·成本 Excel 复核套件（10–12 天）／五·电商收入线（**先探 3 天再排期**，不承诺日期）／六·⑩临工考勤单独立项（6–8 天）。**⑦立项影响分析**：黄春艳填报的**全部 5 条（①②③④⑤）同属应收/收入侧**，提议立**第 6 条工具线「电商收入」**——逐条核对五条既有线（银行对账/物流计提/物流对账/成本台账/凭证归档）业务域均无法容纳；**并修正初评「只把①③⑤划新线」的错划**（④收入台账与①取数完全重叠，散挂会把同一份取数写两遍）。触及三条红线：建格（低危，纯新增目录）／新增 4 表 + `post_log` 泛化（**中危——该表是防重复记账的唯一屏障**，因金蝶草稿在列表查询里不可见）／`build_kd_model` 泛化（**高危——改已上线的写金蝶代码**；铁律＝泛化前后物流计提生成的 Save 报文**逐字节一致**方可合入、单独一版不夹带新功能）。另标记**权限机制陷阱**：`db._backfill_missing_perms` 会在启动时把新增的【非敏感】权限点**自动补给全核算组存量账号**（V2.126 已踩过一次），故 `ec_fetch/ec_post/ec_close` 一律标 `sensitive`（沿用 `cost_ledger_fetch`／`logistics_post` 先例）。**待拍板 D1–D7**：D1 立项与线名、D2 建格时点（确认书 v1.0 出稿当日、本文件先归 `00_平台通用`）、D3 泛化单独一版、D4 电商涉及 9 个核算组织中哪几个、D5 敏感级别、**D6 建议剔出②推广平台**（「具体操作步骤」全空、平台名未填，不可估）、**D7 建议③拆段**（「自动导入开发票」属税控/开票系统集成，超出核算工作台边界）。**另**：调研表 6 条「具体操作步骤」为空（表头自注必填），②③⑤⑬⑯ 的工时系带假设估算，须补齐后重核。**交付物**：`03_Source_Materials/00_平台通用/RPA需求调研表_评估与批次排期_v1.0_20260716.md`（可对外发出版，含需求方需回答的 11 个问题）、`03_Source_Materials/00_平台通用/电商收入工具线_立项影响分析_v1.0_20260716.md`。 | Claude / c | In Review | 无（纯文档任务） |
| **V2.131** | 2026-07-16 | **【成本台账】导出加「收发存汇总·按仓库」表 + 两张汇总表加结存占比列；封存入口移到台账出具；按钮样式修复（根因＝少套基类）；文案两处订正**。业务方四点。**①导出（补上 V2.130 遗留的一半）**：新增工作表 **「收发存汇总·按仓库」**——与「按类别」同格式（期初/收入/发出/结存金额），加**仓库类型**列，按仓库类型分组、组内结存金额降序、带组小计与总计（排法与页面「仓库透视」一致，两边对着看不会错行）；**两张汇总表都加「结存占比」列**（占比＝该行结存 ÷ 全部结存）。内核 `pivot_by_warehouse` 原来**只汇总结存 eq/ea**，出不了「收发存」四段——本版补 oa/ia/da/oq/iq/dq（原有键不动，纯追加）；新增 `share_of(part,total)`：**总额为 0 时返回 None**（导出写「—」）——除不动就老实说没有，不拿 0% 糊弄看的人。**②数据接入文案——业务方「还读了存货收发存明细表（跨维度）、你没写上去」这点【经实测不成立，未照写】**：重新打金蝶验证（不靠记忆）——`HS_InOutStockDetailRpt`（真·收发存**明细**表·跨维度）**仍被拒：「此接口暂时只支持简单账表!」**；`HS_INOUTSTOCKSUMMARYBYDATERPT`（按日期表）可取但 🅰 通道并未读它（故可信度带只有勾稽②③、无勾稽①）；一键取数实际只读 **`HS_INOUTSTOCKSUMMARYRPT`（汇总表·跨维度）** + 物料档案 + 科目余额，与原文案所列三样一致。**误会根源＝那张「汇总表」其实就是明细**（拆到 物料×规格×仓库×批号×库存状态＝2,770 行，第④步明细就出自它）。故文案改为**讲清这件事**而非添一张不存在的表：①条改写为「它**就是明细**：拆到…，第④步的收发存明细就出自它」，②条补上「存货类别**与物料编码**靠档案关联补上」（呼应 V2.130），并加注「金蝶另有《存货收发存明细表（跨维度）》，其接口不对外开放——但不需要它，①已含全部维度」。**③「三件要知道的」订正**：业务方「数据并不是全员共享，而是**被赋予权限的人共享**」——属实（进这页要 `cost_ledger`，V2.128 已把查看挂到导航模块 cap）。文案改为「数据在**有权限的同事**之间共享…拿到「成本台账·查看与导出」的同事看到的都是同一份（没授权的人进不来这页）」；覆盖提示同步改为「覆盖后所有**有权限的人**看到的都是新的那份」。**④封存入口移位 + 本期状态写明取数人**：业务方「每一页的本期状态可以写明是 xxx 取数，但封存本期按钮应该在台账出具页」。本期状态条改为纯报状态、**不带动作**——「进行中 · **{取数人}** 于 {时点} 通过**{一键金蝶取数/上传报表}**接入」，非导出步时右侧提示「核对完在第⑦步「台账出具」导出与封存」；**封存卡整体挪进第⑦步**（与导出并列，封存＝出完台账的收尾动作；挂在每页顶上等于每屏都催你封存、还容易中途误点），保留全部原有闸门（缺仓库类型/无权限/未过勾稽需强制封存+理由≥5字）并补「取消」。**⑤按钮样式——根因是真 bug，不是审美**：成本台账 8 处按钮写的是 `className="btn-pri"`/`"btn-sec"`，而 `border-radius:8px / height:32px / padding` 全在 **`.btn` 基类**上（别的页写的是 `"btn btn-pri"`）→ **`.btn-pri` 单独用就退化成没圆角没内边距的方块**；**`.btn-sec` 在 styles.css 里更是从没定义过**（grep 命中 0 次）→ 那 3 个按钮是浏览器默认按钮。修法不是逐处加 class，而是**把 .btn-pri/.btn-primary/.btn-sec 一并纳入基础规则**（单独用即完整按钮，不必记「必须再加个 btn」），并补 `.btn-sec` 定义（描边）、统一 hover/active（按下 1px 下沉）、主按钮加轻微投影、禁用态 `not-allowed` 且不再 hover 变色。**全站受益**（其它页原 `btn btn-pri` 写法计算样式逐项不变，无回归）。**验证（5200 + 真金蝶 + 浏览器）**：①导出实跑——工作表 **6 张**（核对结论/账实勾稽/收发存汇总·按类别/**收发存汇总·按仓库**/异常清单/收发存明细；本期 🅰 通道无货损页故无损益归集，有则 7 张）；**按仓库总计 ¥10,958,051.26 ＝ 按类别合计 ＝ 页面数**；**44 个仓占比之和 = 1.0000000000**、按类别占比之和 = 0.9999999991；`share_of(5,0)` → None。②样式——`btn-pri` 单独用的计算样式（圆角 8px/高 32/内边距 13px/投影）与旧写法 `btn btn-pri` **逐项相同**；页面四个真实按钮（一键金蝶取数/上传并核对/下载成本台账 Excel/封存本期…）全部 8px 圆角、32px 高。③封存——本期状态条 `/封存本期/` 匹配 **false**（已无按钮），台账出具页两张卡＝「⑦ 导出《成本台账》」+「封存本期」。**17/17 内核单测绿**。新包 **index-dj_59U-3.js** / **index-CNFrbuKw.css**。**遗留**：①其它主体/期间的存量数据物料编码仍为空，需各自重取一次（V2.130 起才带编码）；②主体下拉列全 9 个其中 7 个是空的；③需求确认书 v1.0 仍写「孝感星期九单主体·五步·单通道」，与现状（多主体·七步·双通道·封存·占比）差距最大，待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.130** | 2026-07-16 | **【成本台账】新增第④步「收发存明细」查询屏；并查出并修复【一键金蝶取数通道的物料编码 100% 为空】（两通道口径破裂）**。业务方两问："在平台看 XX 仓明细等等明细怎么看" / "导出的台账是不是没更新最新版本！"，随后定："单独做一个明细查询页"、放在**仓库透视后面当第四步**、透视格子**能点且带上筛选**，并强调 "**记得明细都要带物料编码啊！！！**"。**查证两处，业务方直觉均属实**：①平台上**看不了单仓明细**——仓库透视只到「仓库 × 类别」合计格、点不进去，异常稽核只列 275 条异常，2,770 行逐笔在页面上没有任何出口，只能导 Excel 自己筛；②**导出确实落后**——实跑生成的工作簿只有 5 张表（核对结论/账实勾稽/收发存汇总·按类别/异常清单/收发存明细），**V2.118 的「仓库透视」从没做进 Excel**，且「收发存汇总·按类别」只有金额四列、**没有数量**（业务方当初原话是“结存数量和结存金额都要”，数量这半边在该表丢了）。**⚠核心修复（真 bug，影响导出与页面全部明细）**：`fetch_inventory_summary` 请求字段里**根本没有物料编码**，`out` 里 `"code": ""` 写死——实测 107/2026-5 **2,770 行编码 100% 为空**，故「异常清单」「收发存明细」两张导出表的物料编码列**一直是空的**；而 🅱 上传通道把「物料编码」当**必需列**解析（`_find_header(must_have="物料编码")`）——**V2.117 宣称的“两通道并列等价”实际已破裂，🅰 静默丢编码**。**探因（只读实测金蝶）**：跨维度报表**给不出物料编码**——`FMaterialId` 被接受但恒返回空串（与 `FStockName` 同属哑字段），`FMaterialNumber`/`FNumber`/`FMaterialCode`/`FMaterialId.FNumber` 均被报表服务直接拒绝（500）。**解法**：物料档案（`BD_MATERIAL`）本来就为取存货类别在读，`FieldKeys` 顺带加 `FNumber` 带回编码，**不多打一次接口**；`fetch_material_categories` 返回值由 `{键:类别}` 改为 `{键:(类别,编码)}`，编码与类别**共用同一个关联键**（名称+规格→名称兜底）——键相同故类别能关联的行编码必然也能，未新增歧义。**验证**：修复后重取 107/5 期——编码为空 **2,770 → 0**，`nomap` 仍 0，**结存金额 10,958,051.26 / 数量 5,181,959.69 与修复前分毫不差**（只补编码、不动数）。⚠**存量数据需重取才有编码**（本次已重取 107/2026-5，7.6 秒、2,770 行、credible=true；**6 期系业务方数据，未动**）。**新增 `GET /api/cost-ledger/detail`**：按 `wh`/`cat`/`q`(名称或编码关键字)/`neg`(只看负结存) 服务端筛 + 分页（默认 100/页，上限 1000）。不把 2,770 行塞进 `/state`（每次切期间/主体都要拖一遍反而更慢）；**合计按【筛出来的全部行】算、不随翻页变**（否则翻到第二页合计变、看的人会以为账不对）；金额绝对值降序（负结存排前、不沉底）；下拉候选取自**本期真实有数**的仓库/类别（44 个仓 / 7 个类别），不是全档案。权限沿用 `cost_ledger`（查看即可查明细）。**前端**：`STEPS` 六步→**七步**（数据接入→三道勾稽→仓库透视→**收发存明细**→异常稽核→损益归集→台账出具），其后序号顺延（导出屏 ⑥→⑦）；新增第④步屏（仓库/类别下拉 + 名称·编码关键字 + 只看负结存 + 清空筛选 + 分页 + 合计带）；仓库透视**仓库名与有数的格子可点 → 跳第④步并带上筛选**；切主体/期间清空筛选与结果（否则会把上一主体的料显示在新主体名下）。**过程纠错**：初版做的是**行内下钻面板挂在整张表下方**——真机量出透视表高 2,914px、视口仅 910px，点顶部的仓、明细跑到三千像素外，**等于没做**；改行内展开后业务方指出“其实可以单独做一个明细查询页”，遂整体改为独立查询屏。**真机验证（5200 + 真金蝶 + 浏览器）**：接口——全部 2,770 种/¥10,958,051.26；孝感成品仓 234 种/¥992,677.14；× 产成品 229 种/¥944,784.57；只看负结存 5 种/−276.34 且全为负；关键字打编码 `108010109` 命中 11 行、打名称「桃胶」命中 112 行（名称模糊、编码精确，符合预期）；编码为空 0 行。**逐仓抽核 5 个仓 + 1 个格子，下钻合计与透视格子分毫一致**（含重名的孝感半成品仓 ¥129,557.79）。页面——七步条正确；明细屏编码列有数；「只看负结存」5 行标红，**内含 V2.115 那对 −13.9348 酱香南昌拌粉 / −13.7305 爆辣新疆炒米粉**（−27.67 账实差元凶，现可一眼点出）；点「孝感成品仓 × 原材料」→ 跳第④步、两筛选已填、2 种 ¥47,892.57 与透视格子一致。新包 **index-B52Lvh8t.js**。**遗留（下一步）**：①**导出仍缺「仓库透视」表、「收发存汇总·按类别」仍缺数量四列**——本版只做了页面侧，导出侧未动；②其它主体/期间的存量数据编码仍为空，需各自重取一次；③主体下拉列全 9 个其中 7 个是空的；④需求确认书 v1.0 待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.129** | 2026-07-16 | **【成本台账·前端】右上角期间改用全站共用选择器（12 期彩色状态胶囊）；顺带修好一个从来就没能用的导出按钮**。业务方："右上角的期间，能做好看点吗"（附样机截图作样式范本），并追问"**为什么右上角还有下载成本台账按钮啊**"。**改动一·期间选择器**：成本台账原用**两个原生 `<select>`**（年 + 期）拼在页头，与全站其余 9 处（银行对账/科目余额/理财对账/月结看板等）用的 `PeriodPicker` 不一致——后者点开是年度切换 + 12 期彩色状态胶囊（绿=已上传 / 琥珀=未导入 / 灰=已封存），一眼看出哪个月有数。直接套用会踩一个**真坑**：`PeriodPicker` 调的 `/api/period-statuses` 读的是 `CFG["source"]`＝**全局账本（银行对账那一套）**，而成本台账自 V2.122/V2.126 起按主体分账本存期间（`cl:<账簿代码>`）——照搬会把银行对账的月份状态显示在成本台账页上，**看着还挺权威、实则张冠李戴**。故：`/api/period-statuses` 加 `source` 参数（缺省＝全局账本，**9 个现有调用点行为不变**）；新增 `_cl_period_data_status(y,p,org)`，**沿用选择器既有的三个说法**（已封存/数据已上传/数据未上传）回答，不另造一套颜色（页面内"数据已接入/未接入"是另一回事——那讲的是取数与上传两个通道）；`api.js` 的 `getPeriodStatuses(year, source)`；`PeriodPicker` 加可选 `source` 入参并纳入 effect 依赖（切主体即重拉）；`CostLedger.jsx` 传 `source={'cl:'+org}`，且**主体清单没到位前不渲染选择器**（否则 source 缺省会回落到全局账本，正是要防的那个错）。**改动二·导出按钮**：页头「导出成本台账」与第⑥步「下载成本台账 Excel」**两个入口重复**；导出本就是六步工作流「台账出具」那一步的事，故**去掉页头那个、只留第⑥步**。**顺带查出真 bug**：第⑥步那个链接是 `/api/cost-ledger/export` —— **没带 year/period/org**。V2.126 把导出改为按页面这一期取数时，页头的链接补了参数、第⑥步这个漏了。实测 `curl` 该链接直接 **400「参数错误：导出需指定 year/period」**——即**这个按钮自 V2.126 起就一直是坏的，任何时候点都下不出东西**（不是"导出错期"，是压根导不出来）；且它绕过了前端的仓库类型硬校验闸，仓库类型没配全时也亮着、点了才报错。现已补参数 + 挂 `canExport` 闸（缺类型时置灰并注明「待补仓库类型」）+ 卡片里明写"导出的是**页面上这一期**：{主体} · {年} 年第 {期} 期"；步骤标号 ⑤→⑥（六步工作流里它是第 6 步，原文案写错）。**验证（5200 真机 + 真金蝶 + 浏览器）**：①接口分账本——`cl:107`＝5期+6期已上传 / `cl:101`＝仅 5期 / 全局(kingdee)＝仅 6期，**三者各不相同**，证明改前的张冠李戴是真的会发生（改前成本台账页会照搬全局那行，告诉你 107 的 5 期没数据，可它明明有 2,770 行 ¥10,958,051.26）。②页面——107 选择器显示「2026 年 5 期 已上传」绿胶囊、下拉里 5/6 期绿其余琥珀；**切到 101 后同样是 6 期，胶囊变琥珀「未导入」**（账本真跟着主体走）。③导出——107 链接 `?year=2026&period=5&org=107` 且**全页只此一个**导出入口，拉取 200 / 224KB xlsx；101 因「深圳办公室仓库（广宣品）」没配类型 → 按钮正确置灰；107 第 6 期 → 400「迅鸽星期零仓 没配仓库类型，不能导出」（硬校验正常拦截，该仓类型本就待配）；旧的无参写法 → 400（复现了那个坏按钮）。构建通过（FundDashboard 那条重复 key 警告为既有、与本次无关）。新包 **index-b7784v8h.js**。**遗留不变**：①主体下拉现列全部 9 个、其中 7 个是空的；②需求确认书 v1.0 仍写"孝感星期九单主体·五步·单通道"，与实现差距最大，待重写；③冯辉自动补得的成本台账权限是否收回待业务方定。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.128** | 2026-07-16 | **【成本台账·权限重分】按动作拆点：查看/取数分离；取数标敏感；顺带查出"新权限点会自动补给全组存量账号"这一机制并纠正越权状态**。业务方："把这个权限点分一下，针对不同的使用同事"。**问题**：`cost_ledger` 一个点捆了「查看+取数+上传+导出」——而 V2.122 数据改为按主体+期间落库、全员共享后，**取数会覆盖全员可见的数据**，捆在一起等于**谁能看谁就能把成本会计刚核好的数冲掉**。**参照平台惯例**（银行对账按动作分点：bank_upload/kingdee_refresh/claim/ledger_override/subject_upload/close_period；**导出无权限校验**；无"查看"点、靠准入+模块开关）。**业务方两处决策**：导出**不单设点、跟查看走**；查看**要卡**（`cost_ledger` 挂到导航模块 `clrecon` 的 cap，同「系统设置」做法）。**重分后**：`cost_ledger`「查看与导出」/ **`cost_ledger_fetch`「取数/上传（会覆盖全员数据）」新增** / `cost_ledger_wh`「维护仓库类型」/ `cost_ledger_close`「封存本期」敏感 / 解封仍限主管理员·核算子管理员（不设点，同银行对账）。取数与上传合一个点——V2.117 已定两通道并列等价，拆开只给授权添乱。`_cl_payload` 增 `can{fetch,close,wh,reopen}` 能力位，前端据此**不给按钮**（而非点了才被拒）：无取数权→两个取数按钮置灰、说明里提示"可查看与导出，要取数请找管理员授予"；无封存权→「封存本期（无权限）」；无解封权→「解封（无权限）」。**⚠查出一个平台级机制（非本方引入，但影响面大）**：`db._backfill_missing_perms()` 每次启动给存量账号**自动补齐新加的权限码**（按 `default_perms(分组)` 填），而 `default_perms("核算组")` **对所有非敏感核算点一律给 true**——即**每加一个非敏感权限点，全核算组存量账号自动就有**。实测后果：①本次拆的 `cost_ledger_fetch`（初版非敏感）**被自动补给全组，拆分等于白做**；②**陈梓华出现越权状态：`cost_ledger`(查看)=false 却 `cost_ledger_fetch`(取数)=true——能把全员数据冲掉、却打不开这个页面**；③V2.119 的 `cost_ledger_wh` 同样被自动补给了陈梓华与冯辉。溯源：陈梓华最后一次「改权限」审计为 2026-07-05（仅授 6 个点，不含任何成本台账点），其后无任何权限操作记录——确系自动补权限所致。**业务方决策一**：`cost_ledger_fetch` **代码里标 `sensitive: True`**（默认不给、须显式授予、主管理员只能加严不能降级）。与银行对账 kingdee_refresh/bank_upload（非敏感）不一致是**有意为之**并已写入代码注释：那两个是各人自己的对账口径，成本台账则是**一份全员共享的月结底稿**，被谁随手一点覆盖掉，成本会计核半天的活就没了。**业务方决策二**：陈梓华成本台账 4 个点**先全部收回**、待业务方按岗位重授——已执行（`set_user_perms` 只改这 4 个键、其它 8 个权限原样保留，并以「Claude/c（经业务方指示）」留审计）。**验证**：①权限矩阵（perms 传 JSON 串走生产路径）——成本会计 ✓✓✓··／财务经理 ✓···✓·／总账岗 ✓····／资金岗 ·····／主管理员 全✓；「只给查看的人能取数=False」「能封存=False」。②敏感标记生效——`cost_ledger_fetch ∈ _SENSITIVE_CAPS`、`default_perms('核算组')` 里取数=False（查看=True、封存=False）。③**服务重启跑过 `_backfill` 后陈梓华取数仍为 False**（backfill 只填缺失键，显式 false 不会被覆盖）——收回是稳的。17/17 单测绿、`import app` OK。新包 **index-B_wzossv.js**，已同步 01→02。**自查纠错**：首版验证脚本给 `user_can` 传了 dict 形态的 perms，`parse_perms` 只认 JSON 串 → `json.loads(dict)` 抛异常 → 落回 `_legacy_perms()`（核算组非敏感点全给），导致矩阵**全行相同、误判"权限体系形同虚设"**；改传 JSON 串后矩阵正确。附带发现 `db.user_can` 未用 `_pd()` 兼容两种 perms 形态（`list_users` 返回 dict、`get_user` 返回 str），**生产路径 `session_user→get_user` 返回 str 故不受影响**，但供他人调用时是个坑——未改（属平台公共函数，留给平台线评估）。**遗留**：①冯辉（核算子管理员）的 `cost_ledger_fetch`/`cost_ledger_wh` 同为自动补权限所得（其 `cost_ledger` 系 07-12 明确授予），是否一并收回待业务方定；②主体下拉列全 9 个、其中 7 个是空的；③需求确认书未重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.127** | 2026-07-16 | **【成本台账·前端】第①步由「两种取数方式」改为「使用说明」**。业务方指出：那块还叫"两种取数方式"，但工具早已不只是取数了——六步工作流、双通道、仓库类型前置、期间封存、多主体，新人打开这页不知道该干嘛。**改 `CostLedger.jsx`** 第①步说明卡：标题「① 数据接入 · 两种取数方式」→「**① 数据接入 · 使用说明**」，内容重写为三段：①**这个工具干什么**——把金蝶存货收发存汇总表 × 总账科目余额摆一起核，三道勾稽到分毫、异常自动成清单、出《成本台账》Excel，并明写「**全程金蝶只读，工具不往金蝶写任何东西**」（呼应[[feedback-dual-mode-human-check]]"怕领导觉得黑箱"）；②**怎么走**——右上角选主体+期间 → 二选一取数 → 顺六步看 → 第⑥步导出；③**三件要先知道的**（分隔线下）——「数据全员共享」（按主体+期间存服务器、重启不丢；已有数据时琥珀标「{接入人} 于 {时点} 接入，再取一次会覆盖它，全员可见」）、「仓库类型没配全会卡住」（不能封存/导出，新仓库自动上档标「新」但**类型要人填、工具不猜，猜错了钱会默默归错小计**，指向「成本台账 › 仓库类型」）、「封存＝把这个月定死」（拍照存死、本期只读、每主体一条封存线、要改先解封且高危留痕）。原「本期已有数据…会覆盖」提示并入第三段，不再单独浮在取数说明里。**真机验证（5200 + 浏览器截图）**：说明卡渲染正确、层次清楚（工具干什么→怎么走→分隔线→三件要知道的），下方双通道卡片不受影响；已封存态仍走另一分支（不给取数入口）。新包 **index-BwDpBj7p.js**，已同步 01→02。**遗留不变**：①主体下拉现列全部 9 个、其中 7 个选了是空的（是否只列有存货科目的几个待业务方定）；②需求确认书 v1.0 仍写"孝感星期九单主体·五步·单通道"，与实现差距最大，待重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.126** | 2026-07-16 | **【成本台账·架构】多主体——107 写死处全部参数化；账簿改按主体档案全称精确匹配；每个主体一条独立封存线**。业务方："我这里还有不同主体，这个要怎么设计"——本方此前把 **107 孝感星期九写死在六处**（封存命名空间 `cl:107`、收发存 `org="107"`、科目余额 `org_kw=("孝感","星期九")`、仓库档案 `fetch_warehouses("107")`、取数提示、导出表头），需求确认书亦只写孝感星期九。**先探查再设计（只读实测）**：金蝶 9 个核算组织中**只有 107(存货 1,099 万) 与 101(9,446.08) 有存货**；105 深圳星期九 / 103 Sinkio / 108 星期十 的 14xx 余额均为 0（105 收发存报表 0 行）；102/104/106/109 连 14xx 科目都没有。**101 实测把 org 参数化即跑通**——5 行明细、未匹配类别 0、`credible=true`、账实三科目 diff 全 0、收发存自平通过；**核算体系 `KJHSTX01_SYS`/会计政策 `KJZC01_SYS`/币别 `PRE001` 三常量跨主体通用**，无需分主体配。**改动**：①新增 `_cl_source(org)`＝`"cl:<账簿代码>"`、`_cl_orgs()`（读平台主体档案，不在工具里写死）、`_cl_org(code)`、`_CL_DEFAULT_ORG="107"`；`_cl_closed/_cl_closed_block/_cl_save_input/_cl_input/_cl_result/_cl_payload/_cl_register_warehouses/_cl_period_str` 全部加 org 参数，`_CL_SOURCE` 常量删除。②**`_inv_gl_balance` 账簿匹配由关键字改为主体档案【全称精确相等】**——原 `("孝感","星期九")` 是定时炸弹：「深圳市星期九」也含"星期九"、只是恰好不含"孝感"才没炸；而「深圳市星期零」与其「上海分公司」这种父子账簿，**任何关键字写法都会把两个圈到一起**（实测全称不等，各取各的 ✓）。③新增 `GET /api/cost-ledger/orgs`；`/state`、`/analyze`、`/analyze-kingdee`、`/close`、`/reopen`、`/closed-periods`、`/export` 全部收 org 并校验（主体档案里没有该账簿代码即拒，不猜）。④导出**文件名带主体简称**（三个主体导出来不能同名）、**表头主体全称由 `meta["org_full"]` 传入**（不再写死孝感）。⑤「仓库类型」页的仓库清单改取**所有主体仓库档案的并集**——**仓库类型对照全局共用（业务方定）**，依据：实测 101 的数据里出现 `孝感茶饮原料仓`，而它在 107 的对照里已配「工厂仓」并直接生效，说明"仓库名→类型"是**物理仓库的属性、不是主体的属性**；跨主体同名（105 与 107 都有孝感成品仓）按名归并、重名以启用的那条为准。⑥前端 `CostLedger.jsx` 页头加**主体下拉**（清单来自 `/orgs`，默认 107），切主体即重读 `/state`；第①步说明与封存面板文案随主体走。**验证**：①后端脚本 5 项全过——9 主体清单读档案；账簿精确匹配（101=9,446.08 / 105=0 / 107=10,994,036.02，父子账簿区分 ✓）；独立封存线 `cl:101`/`cl:107`；101 与 107 各跑一遍**账实三科目 diff 全 0、互不串**；**封存 101 后 107 与银行对账均不受影响**（101 封存清单['2026-05']、107 []）。②真机（5200 重启+真金蝶+浏览器）——主体下拉 9 个、默认孝感星期九；切 101 取数→期末结存 **¥9,446.08** / 总账 ¥9,446.08 / 勾稽平 / 0 异常，硬校验认出其自有新仓库「深圳办公室仓库（广宣品）」并卡住封存与导出；切回 107 显示「数据未接入」（**101 的数据没串过来**，旧全局单槽在此必然把 9,446 显示成 107 的数）；107 取数→**¥10,958,051.26 / 2,770 行**，导出链接 `export?year=2026&period=5&org=107`。17/17 单测绿、`import app` OK。新包 **index-BBGlsOTG.js**，已同步 01→02。**遗留**：①105/103/108 现余额为 0，可选但无数据；102/104/106/109 无 14xx 科目，选了也是空——是否在主体下拉里只列"有存货科目"的几个，待业务方定；②需求确认书 v1.0 仍写"孝感星期九单主体"，与本版差距最大，下一步重写。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.125** | 2026-07-16 | **【平台·基础数据】主体档案补齐至 9 个（成本台账多主体的前置）**。V2.126 探查发现：金蝶 `ORG_Organizations` 有 **9 个核算组织**，而平台主体档案只有 3 个（101 深圳星期零 SZL / 105 深圳星期九 SZJ / 107 孝感星期九 XGJ）——其中 **103 Sinkio、108 深圳星期十有 14xx 存货科目却不在档案里**，成本台账要按主体档案取主体清单，缺档即选不到。**业务方决策**：先答"把星期十/Sinkio 也补进主体档案"，再追加"一并补齐 9 个"。**简称/简码**：`code` 会印在凭证册标签上、**一经生成过册号即锁死**（`org_code_locked`），故不由本方擅定——**108 深圳星期十=SZS、103 Sinkio=SNK 由业务方直接指定**；其余 4 个本方按现有惯例（深圳=SZ＋星期X拼音首字母）拟稿并经业务方确认「就用这套」：**102 深圳星期八=SZB、104 Starfield=SFD、106 星期零上海分公司=SHF、109 Starfield PB=SFP**。**全称一律从金蝶组织档案取，不手抄**；`book_code`＝组织编码（沿用现有 3 条的口径）；标签色只用于凭证册标签纸预览、不参与任何计算。经 `db.save_org` 写入（走既有校验：简称非空、简码 2–4 位大写字母数字、简码/账簿代码唯一）。**结果**：9 个主体全部在档（101 SZL/102 SZB/103 SNK/104 SFD/105 SZJ/106 SHF/107 XGJ/108 SZS/109 SFP）。**注意**：主体档案是**全平台共用**基础数据——凭证归档取简码作册号首段、物流计提取账簿代码写金蝶；本次只新增、未改动原有 3 条。 | Claude / c | Synced | —（数据补录，无代码改动） |
| **V2.124** | 2026-07-16 | **【成本台账】新仓库自动上档 + 「没配仓库类型不许封存/导出」硬校验 + 数据接入文案按实测重写**。业务方三条要求之一二。**①新仓库自动上档（业务方定：自动上档、类型留空等人配）**：`_cl_register_warehouses` —— 取数/上传时，本期数据里出现但台账没见过的仓库，自动登记进 `app_settings.cost_ledger_wh_attr.seen`（记首次出现期间），「仓库类型」页据此打「✦ 新」徽标 + 行尾「自 2026-06」+「✦ 新仓库待配 N」切片。**只上档、不猜类型**——按名称规则猜（如"孝感*"→孝感内仓）会把钱默默归进错的类型小计，比不归更危险，延续「对照缺失不硬归」口径。保存路由原样带过 `seen`（本页不提交，不能丢）。**②硬校验（业务方定：卡封存 + 卡导出）**：`_cl_missing_wh(res)` 取 `pivot_wh_type.missing_attr`；`/close` 与 `/export` 均拦，**不给 force 绕过**（补一条对照几秒钟，没有"就是配不上"的正当场景，不同于勾稽差异那种真说不清的）；已封存读快照、封存时已过校验故导出不再拦。`_cl_payload` 增 `missing_wh`/`can_seal`；前端 `CostLedger.jsx` 加琥珀告警条、封存 chip 变「封存本期（待补仓库类型）」不可点、导出按钮由 `<a>` 降级为灰 `<span>`（不给点，而非点了才被拒）。**③数据接入文案按实测重写**：实测两轮稳定——登录 0.4s / 收发存汇总表(跨维度)2770 行 **5.2s**（含物料档案 BD_MATERIAL 取存货类别的 join）/ 存货类科目余额 1.8s / 仓库档案 0.7s，**合计约 8 秒**。🅰 改为「免导表 · 约 10 秒」并逐条列明读哪三样（收发存汇总表跨维度≈5秒 / 物料档案·补存货类别 / 科目余额14xx≈2秒），按钮态「取数中…（约 10 秒）」；🅱 改为「用你手头已导的表 · 约 5 秒」，说明存货类别取表里那列、只再读科目余额≈2秒，并点明**只有🅱能做损益归集**（🅰 无货损/盘盈亏页）。**④删「本期有数据」概念（业务方："删掉这个子弹"）**：「仓库类型」页无期间选择器，"本期"指代不清——实测竟指向 6 期（`_cl_latest_period` 取最近有数据的一期），而月结核对页停在 5 期，两页对不上。删掉该 chip、「本期」列、及基于它的告警；哪一期缺谁由月结核对页硬校验明确拦住，本页不重复。顺带省掉每次进页面 build 一整期结果的开销。**真实案例验证（非合成）**：业务方 17:45 自行取过的 **2026-6 期真数据里冒出新仓库「迅鸽星期零仓」(XQJ62)**，未配类型、**4.5 万结存正落在「（属性缺失）」里**——V2.124 前这 4.5 万会静悄悄进一个没人看的桶、台账照导、期照封。现真机实测：6 期告警「1 个仓库没配仓库类型…迅鸽星期零仓」、导出按钮灰掉无 href、封存 chip 变「待补仓库类型」；5 期 `missing_wh=[] can_seal=true` 正常放行。**一次性回填**：V2.124 前落库的期间没走过自动上档，按已存输入补登记——精准只补 1 个（迅鸽星期零仓→2026-06），5 期 44 个未重复；刷新后「✦ 新仓库待配 1」+ 行显「✦新 自 2026-06」。往后每次取数自动上档，无需再回填。**修 3 个自引入问题**：①`CostLedgerWh.jsx` 里 `const ALL = ' 全部'` 的前导空格**落盘时变成了 NUL 字节**（文件被 `file` 识别为 data、grep 当二进制），且"前导空格防撞名"本就脆弱 → 哨兵改为 `ALL=null` / `UNSET=''`（与 map 里未配的真实取值一致）；②改哨兵时多行匹配因 CRLF 失败，留下对未定义 `LBL` 的引用 → **页面整屏白**，真机点开才发现（console: `ReferenceError: LBL is not defined`），已修为 `LBL_ALL`/`LBL_UNSET`；③`in_use`/`used_period` 随「本期」概念一并从前后端拆净。17/17 单测绿、`import app` OK。新包 **index-DXyukfLD.js**，已同步 01→02。**未清理（业务方数据，非本方测试痕迹）**：2026-6 期输入数据为业务方 17:45 自行取数所留，本方未删；`迅鸽星期零仓` 的类型**本方未代配**——不知该归外仓还是代工厂仓，待业务方/成本会计定。**遗留**：需求确认书 v1.0/样机仍未同步（下一步）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.123** | 2026-07-16 | **【成本台账·决策留痕】月结看板不纳入成本台账封存状态——V2.122 遗留①【决定不做】，非待办**（口径澄清，无代码改动）。V2.122 各封各的落地后本方提出："月结看板是否该分工具显示各自封存状态（银行对账已封存/成本台账进行中）"。**业务方定：「月结看板只管月结里面的东西，不管报表与台账」**——成本台账挂在导航「报表与台账」组下，不属于「月结与结账」组，故**不进月结看板**、看板也不显示其封存状态。成本台账的封存入口就在其自身页面（V2.122 已实现），`source="cl:107"` 独立一条线本就与银行对账互不可见，与此决策一致、无需改动。**结论**：V2.122 遗留①关闭；月结看板（并行会话地盘）本方全程未动。其余遗留不变：②「（属性缺失）」仍只提示不阻断；③需求确认书 v1.0/样机未同步（六步 + 仓库透视 + 仓库类型页 + 封存）。 | Claude / c | Synced | —（无代码改动） |
| **V2.122** | 2026-07-16 | **【成本台账·架构】期间化 + 封存——拆掉进程内全局单槽缓存（多人浏览下的串数据 BUG），复用平台 periods/period_inputs/period_snapshots**。业务方定性："如果这是多人可以直接浏览、而不是每次使用一次的工具，就需要做封存动作"——这也回答了代码审查里长期挂着的「单人 vs 多人」前提。**先坐实缺陷（非理论风险）**：`_COST_LEDGER_CACHE = {}` 是进程内全局字典、**不带期间键、不落盘**——①张三跑 5 期、李四跑 4 期会互相覆盖，张三点导出**拿到李四的 4 期数据**（屏幕显示 5 期、导出却是 4 期，属"算错钱"簇）；②服务重启后谁都导不出；③「仓库类型」页的"本期有数据"跟着最后一个人跑的结果走。**封存粒度（业务方拍板：各封各的）**：`periods` 唯一键是 `(source,year,period)`、**不带工具维度**，加维度要重建表（SQLite 尤甚）且动的是银行对账活数据、与并行会话冲突风险高 → 改用 **source 命名空间**：成本台账固定 `source="cl:107"` 自成一条封存线，**零改表、零影响银行对账**（`_period_where`/`list_closed_periods` 均按 source 精确过滤，实测两条线互不可见）。不跟 `CFG["source"]` 走：成本台账不论平台切 sample/kingdee 都真取金蝶 107，跟着切会把同一份真数据劈到两个命名空间。**后端**：新增 `_cl_save_input`（本期输入整体落 `period_inputs` kind=`cl:input`，含 cross/bydate/loss/disp/gl/src）、`_cl_result`（**已封存读快照、进行中由输入现算**——结果不落库，避免结果与输入不同源）、`_cl_payload`（一整份本期状态，页面直接读）、`_cl_closed_block`（已封存拦截）、`_cl_latest_period`；`db.py` 新增 `list_period_inputs(source,kind)`、能力码 `cost_ledger_close`「成本台账·封存本期」(sensitive)。新增路由 `GET /state`、`POST /close`（勾稽全过直封；未过需主管理员/核算子管理员填≥5字理由强制封存）、`POST /reopen`（高危：限主管理员/核算子管理员、理由≥5字、留痕）、`GET /closed-periods`；**封存顺序照搬银行对账：先存快照、再落封存标记**（中途挂了本期仍进行中，不会"已封存却读到空快照"）。`analyze`/`analyze-kingdee` 加封存拦截并改为落库；**`/export` 改为按显式 year/period 取数**（修"屏幕与导出对不上"）；「仓库类型」页的"有数据"改取**最近有数据的一期**。**顺手补权限漏洞**：`analyze-kingdee` 此前**只要登录就能触发金蝶取数**（上传通道却要 `cost_ledger`），两通道口径不一致，已补 `_require_perm`。**前端 `CostLedger.jsx`**：进页面/切期间 `useEffect` 读 `/state`（秒开、不取金蝶、全员同一份）；页头加期间状态胶囊（已封存/数据已接入/数据未接入）；封存条（封存人·时间·**数据取回时点**·说明）+ 封存/解封面板；已封存时第①步**直接不给取数入口**（不是点了才被拒）；未封存但已有数据时提示"再取一次会覆盖、全员可见"；导出链接带 `?year&period`。**自查修一处自引入 BUG**：`res` 改为无数据时也非空（要保留 closed 状态），导致 `clickable = 'import' || res` 让空数据也能点进内屏、读 `ties.two_reports` 崩溃 → 全部内屏守卫改为 `res.has_data &&`。**验证**：①后端脚本 8 项全过——无数据→数据未接入；落库 2770 行/credible/10,958,051.26；**4 期仍无数据（期间隔离，旧单槽必串）**；重启从库读回（带落库人/时点）；封存快照 114,166 字节；封存后 `from_snapshot=true`、金额与封存前一致、取数被拦；**成本台账已封存 ['2026-05'] / 银行对账 [] 互不影响**；解封后数据仍在。②真机（5200 重启+真金蝶+浏览器）——胶囊「数据未接入」→取数后「数据已接入」+状态条"数据由 管理员 于 17:38 接入（一键金蝶取数）。全员看到的是同一份"→封存后胶囊「已封存」+🔒条"管理员 于 17:39 封存 · 数据取回时点 17:38"+第①步**取数按钮消失**→切 4 期「数据未接入」、取数按钮回来、封存条消失→切回 5 期秒开仍「已封存」、导出链接 `export?year=2026&period=5`。**测试痕迹已清理**（5/4 期数据与封存标记均已清除，两条封存线均为空）。17/17 单测绿、`import app` OK。新包 **index-B2Vk5hvD.js**，已同步 01→02。**遗留**：①月结看板未纳入成本台账封存状态（各封各的后，看板可分工具显示，待定）；②「（属性缺失）」仍只提示不阻断；③需求确认书 v1.0/样机未同步。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.121** | 2026-07-16 | **【成本台账·仓库类型页·定稿】类型改横向切片器（纯分类）；仓库的启用/禁用日期与操作取消，合并为一条「备注」**。**本方 V2.120 理解有误**：业务方原话"启用禁用日期操作都是针对具体仓库的"＋"仓库类型的分类是没有任何启用日期禁用日期和操作的"，并给出参考图（应收单类型横条切片器）。V2.120 把日期/操作做到了类型主档上，方向反了；本版按业务方定稿重做，且业务方进一步简化：**"算了，删掉启用和禁用，就合并成一个备注吧"**——故仓库侧也不做启用/禁用与日期，只留自由填写的备注。**数据模型**（`app_settings.cost_ledger_wh_attr`）：`types` 由 `[{name,on,off}]` **降维为 `[类型名]`**（读取端 `_cl_wh_types` 兼容旧结构，实测旧库可正常读出并在首次保存后自动迁移）；新增 `notes:{仓库:备注}`（单条限 200 字、空串不落库）；`map` 不变。相应删除服务端日期盖章逻辑（该能力随日期一并取消）。**前端 `CostLedgerWh.jsx` 重写**：①「仓库类型」卡片＝**横向 chips 切片器**（全部/（未配）/各类型，带计数，点选即筛下方仓库；计数按**编辑中**的对照实时算），下方保留「新增类型」输入框（回车可提交、重名拦截）；②仓库表列改为 **仓库 | 编码 | 仓库类型 | 备注 | 本期**（删除启用日期/禁用日期/操作/结存金额）；原「仓库类型」下拉切片器与「待配类型/已禁用」切片撤销（功能已被类型 chips 覆盖），保留「只看本期有数据」与仓库名/编码搜索，三者叠加并带「清除筛选 · 当前 X/Y」。**自加的一处小能力**：没有仓库在用的类型，chip 上带「×」可删——防止手滑新增的错别字类型永久留在切片器里（业务方只提了"新增"，此项**请复核是否保留**）。**真机验证（5200 重启 + 浏览器）**：类型切片器 `全部140 | （未配）96 | 代工厂仓16 | 内仓1 | 外仓15 | 孝感内仓9 | 工厂仓3` 渲染正确；**点「工厂仓 3」精准筛出 3 行**（孝感半成品仓 XQJ04/孝感茶饮原料仓 XQJ11/孝感茶饮成品仓 XQJ13）——**补验了 V2.120 未验完的切片筛选**；备注填写→脏计数 1、行琥珀高亮→保存后落库 `notes:{"孝感半成品仓":"..."}`、`types` 自动迁为 `["代工厂仓","内仓","外仓","孝感内仓","工厂仓"]`、map 44 条未受影响。**测试痕迹已清理**（备注已清空，现存 5 类型 / 44 对照 / 0 备注）。17/17 单测绿、`import app` OK。新包 **index-B7kaeZeo.js**，已同步 01→02。**遗留**：①「（属性缺失）」仍只提示不阻断；②需求确认书 v1.0/样机未同步（六步 + 仓库透视 + 仓库类型页）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.120** | 2026-07-16 | **【成本台账·仓库类型页】仓库类型升级为可维护主档（成本会计自己增/禁用，带启用·禁用日期）＋ 按类型切片器；仓库表去掉结存金额**。业务方在 V2.119 基础上提要求：①要能自己新增仓库类型；②要能按某个仓库类型切片；③仓库表不放本期结存金额，改放启用日期/禁用日期，状态列改为操作。**先澄清再动手（业务方拍板）**：日期与操作放在**新的「仓库类型」主档表**上（不是仓库表）；「禁用」＝**本工具软禁用**（禁后新配时选不到、已配该类型的仓库保持不变），**不反写金蝶仓库档案**（另一选项会改 ERP 主数据、影响全公司业务，已明确排除）。**后端 `app.py`**：新增 `_cl_wh_types(attr)` —— 类型主档 `[{name,on,off,n}]`，未维护过时用现有对照里的类型作种子（启用日期空＝「初始内置」），并把"对照在用但主档没有"的类型兜底补进来（避免在用却不可见）；`GET /warehouse-types` 的 `types` 由字符串数组升级为对象数组（含日期与在用仓库数）；`POST` 收 `types:[{name,on,disabled}]`，**启用/禁用日期一律服务端盖章**（不信任浏览器本地时间）——新类型补 `on`＝今天、`disabled` 且未禁用过则 `off`＝今天、改回启用则清空 `off`；已禁用过的保留原禁用日期；对照在用但未提交的类型不凭空消失。**前端 `CostLedgerWh.jsx` 重写**：①「仓库类型」主档卡片——类型名称/启用日期/禁用日期/在用仓库数/操作(禁用⇄启用)，底部输入框 + 「新增类型」(可回车)，重名拦截；配着已禁用类型的仓库单独提示（历史保留、不自动清）。②仓库表——**删「本期结存金额」列**，「状态」列简化为「本期(有/无数据)」，金蝶禁用改为仓库名后的「金蝶已禁用」徽标；类型编辑由自由输入 `datalist` 改为**下拉 `select`**（只列未禁用类型；若该仓已配的正是已禁用类型则保留该项可见，避免一编辑就被悄悄清掉）。③**类型切片器**：`仓库类型` 下拉（全部类型/（未配）/各类型，已禁用的标注「（已禁用）」），与原有切片、搜索**叠加**生效。**修 2 个自引入 BUG（实测发现）**：①新建类型的启用日期误显示为「初始内置」（应为「保存时记录」）——加 `fresh` 标记区分"本次新建未保存"与"初始内置"；②**保存成功提示看不见**——`save()` 先 `setMsg` 再 `await load()`，而本版新加的 `load()` 里有 `setMsg('')` 会把提示擦掉，改为先 `load()` 后设提示。**真机验证（5200 + 真金蝶 + 浏览器）**：类型主档 5 类、在用仓库数正确（代工厂仓16/外仓15/孝感内仓9/工厂仓3/内仓1＝44）；新增「保税仓」→ 启用日期显「保存时记录」、切片器即时含该项、脏计数正确 → 保存后**服务端盖章 2026-07-16**、提示「已保存：6 个仓库类型、44 条仓库对照」正常显示；点禁用 → 行标「已禁用」、禁用日期「保存时记录」、操作翻为「启用」、切片器标注「保税仓（已禁用）」→ 保存后**启用 2026-07-16 / 禁用 2026-07-16**。**未验**：类型切片器的筛选结果未读回（仅验到选项渲染正确，筛选逻辑与既有切片同为纯前端过滤）。**测试痕迹已清理**：验证用的「保税仓」已从 `app_settings` 移除，现存 5 类型 / 44 条对照＝原 json 种子内容。17/17 单测绿、`import app` OK。新包 **index-rgBiCbpl.js**，已同步 01→02。**遗留**：①初始 5 个类型保存后启用日期被盖为当日（主档建档日，非业务实际启用日），若需可维护的真实启用日再议；②「（属性缺失）」仍只提示不阻断；③需求确认书 v1.0/样机未同步（六步 + 仓库透视 + 仓库类型页）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.119** | 2026-07-16 | **【成本台账】新增「仓库类型」维护页 + 成本台账做成三级菜单；对照真相源由 json 迁到数据库**。业务方要求：仓库类型要能在页面上维护，并挂到成本台账的三级菜单下（此前只能改 `cost_ledger_config.json`，业务方碰不到）。**导航（照 V2.113 银行对账的做法）**：`costledger` 改为**纯分组父项**（`group_only`），其下两个三级子项——**月结核对** `clrecon`（即原成本台账页）/ **仓库类型** `clwh`（新页，`cap: cost_ledger_wh`）；`App.jsx` 路由与 `CODED_VIEWS`、首个可进入模块兜底、`Sidebar` 图标映射同步。**权限**：`db.py` 新增能力码 `cost_ledger_wh`「成本台账·维护仓库类型」（非敏感——仓库类型只影响仓库透视分组，不参与勾稽、不改金额）。**持久化**：对照存 `app_settings.cost_ledger_wh_attr`（`{map,by,at}`），`_cl_config()` 改为 **json 种子 ← 数据库覆盖**；一旦在页面存过，即以数据库为唯一真相源、改 json 不再生效（避免"两处可改、不知谁生效"）。**后端**：`kingdee_client.fetch_warehouses(org)` 读仓库档案 `BD_STOCK`（`FUseOrgId.FNumber='107'`，取 FNumber/FName/FForbidStatus）；新增 `GET/POST /api/cost-ledger/warehouse-types`——GET 返回 金蝶档案 ⋃ 现有对照 ⋃ 本期出现过的仓库，每行带 已配类型/本期结存金额/是否禁用/是否在档案内，并回 `can_edit`；POST 全量覆盖保存（类型留空＝不配、不硬归）。**前端 `CostLedgerWh.jsx`**：切片「待配类型/本期有数据/全部/已禁用」+ 仓库名·编码搜索；类型用 `datalist` 可选已有类型亦可新建；改动行琥珀高亮、按钮显脏改动数；**本期有结存却没配类型**的仓单独琥珀告警（这些会直接落进「（属性缺失）」）；无权限时只读并说明。**修 BUG（实测发现）**：107 档案 **143 条 / 140 个仓库名——3 个仓库重名双编码**（旧的禁用、新的启用：孝感半成品仓 XQJ04(A)+XQJ17(B)、北京诚煜仓 CK063(B)+CK117(A)、百卡弗广州 CK069(B)+CK110(A)）。初版按名归并"后者覆盖前者"，会把**本期在用的孝感半成品仓（结存 ¥129,557.79）误标为"已禁用"**；改为**重名时以启用的那条为准**，已禁用数 38→**37** 修正。收发存报表只给仓库【名称】、只能按名归并，此约束已在代码注释留痕。**真机端到端验证（5200 重启 + 真金蝶 + 浏览器截图）**：三级菜单「成本台账 › 月结核对 / 仓库类型」层级正确；页面 140 仓 / 切片「待配类型 59 · 本期有数据 44 · 全部 140 · 已禁用 37」；跑过月结核对后本期结存联动出数（孝感半成品仓 ¥129,557.79 · 孝感茶饮原料仓 ¥3,366,780.51 · 三个重名仓均取到启用编码 XQJ04/CK117/CK110 且类型正确）；**保存链路双向实测**——新增 1 条→「已保存 45 条」、署名「管理员 于 2026-07-16 16:55」、待配 59→58；再清空该条→「已保存 44 条」、待配回 59（**该测试值为临时验证，已还原；数据库现存 44 条＝原 json 种子内容**）。`_cl_config()` 实测取数据库、44 条、孝感半成品仓→工厂仓。17/17 单测绿、`import app` OK。新包 **index-CXIjmQg4.js**，已同步 01→02。**遗留**：①「（属性缺失）」现仅提示不阻断，是否要在仓库透视上做硬校验待业务方定；②需求确认书 v1.0/样机仍写"五步"、且未含本页，待过会同步。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.118** | 2026-07-16 | **【成本台账】新增「仓库透视」屏——仓库 × 存货类别交叉表（金额+数量），补齐需求确认书"类别/仓库透视"的另一半**。承 V2.116/V2.117：仓库维度已可取、后端 `pivot_wh_type` 早已返回但**前端无任何组件渲染**（V2.117 自查发现的缺口）。业务方明确要求："每个仓库的仓库类型、存货类别分别是多少、总计是多少，结存数量和结存金额都要"。**内核 `cost_ledger.py`**：新增 `pivot_wh_category(recs, wh_attr)` → `{cats(列，按金额降序、（未分类）殿后), rows(每仓：wh/type/cells/total), types(仓库类型小计，含分类别 cells), cat_total(列合计), total(总计)}`；数量金额双度量；仓库为空→「（无仓库）」、类别为空→「（未分类）」、仓库无属性→「（属性缺失）」，**均不硬归**（延续对照缺失不猜的口径）。行按仓库类型分组、组内按结存金额降序。`build_cost_ledger` 输出 `pivot_wh_category`；**`app.py` 两条路由（analyze / analyze-kingdee）同步返回**。**前端 `CostLedger.jsx`**：五步工作流→**六步**，在「三道勾稽」后插入 **③ 仓库透视**（原③④⑤顺延为④⑤⑥；步骤态靠 key 索引自动计算、无硬编码序号）。表：`仓库 | 仓库类型 | 7 个存货类别 | 合计`，**每格上行＝结存金额（主）、下行＝结存数量（弱化 11px 灰）**，仓库类型分组小计行灰底加粗、仓库行缩进、末行总计；「（属性缺失）」时给琥珀提示列出仓库并指向基础数据维护；底部标注四舍五入口径。数量用 `qty()` 格式化（最多 2 位小数、不补零，避免 1,953,324.45 显示成 ...4500）。**真机端到端验证（5200 重启加载新后端 + 真金蝶 + 浏览器截图）**：44 个仓库 / 5 个仓库类型 / 7 个类别全部渲染；**逐仓对底稿**——孝感茶饮原料仓 3,366,780.51 / 2,468,102.78、孝感半成品仓 129,557.79 / 8,800.97、**总计 10,958,051.26 / 5,181,959.69** 与成本会计底稿一致；属性缺失提示未出现（V2.116 补的 `武汉诚煜仓` 生效，44 仓类型全配齐）。**闭合性实测**：行合计/列合计/类型小计/单元格和 与总计差 ≤0.02（各格独立四舍五入的显示尾差；合计与总计均按未舍入值算，故**总计与账实勾稽分毫一致**，已在页面底部向用户说明）。**单测 14→17 绿**（新增 `TestPivotWhCategory` 3 例：单元格/行列合计/类型取自对照表、类型小计横竖闭合、缺属性与无仓库不硬归且仍计入总计）。新包 **index-CFmo1NHR.js**，已同步 01→02。**遗留**：工作流由五步变六步，需求确认书 v1.0 与样机（4 屏）未同步更新，待与成本会计过会时一并确认。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.117** | 2026-07-16 | **【成本台账·前端】双通道文案订正——删除已被证伪的三处不实描述，两通道改为并列（业务方决策）**。承 V2.115/V2.116：后端已订正，但前端仍在向成本会计陈述**已不成立的事实**，属工具误导用户，必须改。**业务方决策**：两通道**并列、不分主次**（"不替成本会计定习惯，他爱用哪个用哪个"），故不做"API 转正/上传降备用"的定位调整。**改动 `CostLedger.jsx`**：①🅰 卡片——"存货收发存汇总表（按日期·**物料级**）"→"（**跨维度**）"，删除琥珀色警示"**物料级（无仓库维度）；个别物料口径差异会致账实勾稽微差**"，副标"免上传·快速核对"→"**免导表**"；②🅱 卡片——副标"精确归零·正式底稿"→"**用你手头已导的表**"（不再暗示只有上传才能归零），正文补"原表在手可逐行核对"（呼应[[feedback-dual-mode-human-check]]"怕领导觉得黑箱"）；③取数后提示条——删除"**库存商品科目可能有微差**""**仓库级明细与精确归零的正式底稿请用🅱上传通道**"，改为陈述真实差异：**本通道不含货损/盘盈亏页故不做损益归集**（API 通道 loss_rows/disposal_rows 恒为 None，这是两通道**唯一**的真实能力差）；④第①步引导语补"两种方式取的是同一张表、结果一致"。**自查纠错**：初稿曾写"类别与**仓库透视**"，自查发现**前端并无仓库透视 UI**（后端 `pivot_wh_type` 已返回但无任何组件渲染），属新的过度承诺，已删除该措辞——**仓库透视 UI 缺口另记，待开版本**。**真机端到端验证（5200，真金蝶+浏览器）**：新文案上屏正确；点🅰→2,770 行明细、期末结存 ¥10,958,051.26、账实三科目 **0.00「✓勾稽平」**、判定「本期台账可信·可出台账」；**异常明细表「仓库」列首次有值**（此前 API 通道恒空）——V2.115 找回的那 2 行负结存现带仓库落在清单里（酱香南昌拌粉 −0.87 / 爆辣新疆炒米粉 −0.84，均**昆山吉波（新）**仓），即"一笔查无来源的 27.67 账实差"变为"某仓两个物料负结存、关账前清理"的可行动项。新包 **index-DXvKRP4F.js**，已同步 01→02。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.116** | 2026-07-16 | **【成本台账·重要能力订正】仓库维度一直可 API 取到——此前"取不到仓库"是看错字段，44/44 逐仓实测通过**。承 V2.115，业务方追问"跨维度明细表能否算出各仓库结存数量金额"并指出**成本会计底稿只有 3000 来行**——此关键信息推翻了本方"跨维度只有 movement 级流水表"的假设（13,625 行那张是 `HS_NoDimInOutStockDetailRpt`＝单据流水级，非底稿用表）。**根因**：仓库字段是 **`FStockId`（返回仓库【名称】）**，而本方一直读 `FStockName`——**该字段是哑字段、恒返回 null**，故长期误判"报表无仓库维度/核算体系不含仓库维度"。**订正后口径（三条）**：①报表改用 `HS_INOUTSTOCKSUMMARYRPT`（存货收发存汇总表·跨维度，含全部维度）；②`FCOMBOTotalType="3"` → 2771 行＝2770 明细＋1 行总计（总计行各列为空，被 V2.115 的"库存状态为空=小计"判据自然剔除）；③每个(物料×规格×批号×库存状态)恰好落在唯一仓库，加仓库维度**行数不变**，可直接按仓库汇总——这正是底稿约 3000 行的由来。**改动**：`kingdee_client.py` FieldKeys 增取 `FStockId`、报表 FormId 改 `RPT_INOUT_CROSSDIM`、`FCOMBOTotalType="3"`、`wh` 字段由硬编码空值改为真实仓库、取值下标顺延；`cost_ledger_config.json` 补 `武汉诚煜仓=外仓`（5 期结存为 0、上传通道从未暴露，缺属性会落入"（属性缺失）"）；`app.py` analyze-kingdee 文档串更新。**真金蝶实测（107 孝感星期九 2026 年 5 期）**：2770 行 / 未匹配类别 0 / **仓库 44 个、无仓库行 0**；`credible=true`，账实三科目 diff 全 0；`pivot_wh_type` **missing_attr 为空**（孝感内仓 4,064,169.13 / 工厂仓 4,226,130.55 / 外仓 2,376,334.85 / 代工厂仓 287,562.25 / 内仓 3,854.49）；**逐仓对成本会计底稿 44/44 全中**，总计 5,181,959.69 / 10,958,051.26 分毫不差（孝感茶饮成品仓 729,792.24 vs 底稿 729,792.25，1 分钱四舍五入尾差，在 0.05 容差内）。14/14 单测绿、`import app` OK。**影响**：**V2.87 "仓库级 API 取不到、待金蝶顾问配报表方案" 的结论作废**——无需金蝶顾问介入；API 通道（🅰一键金蝶取数）现已具备与🅱上传通道**完全等价**的能力（含仓库维度），"正式月结必须走上传"的限制已无技术依据（前端双通道文案待业务方确认后另开版本调整）。**探查纠错留痕**：本轮曾据错误假设启动"全量流水(736,023 行)按仓库净额还原"的绕路方案，发现真因后已中止（未并入代码）；另实测账簿 107 在 2020-01-01 前无业务（2015/2018/2020 起点行数均 736,023）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.115** | 2026-07-16 | **【成本台账·重要数据修正】小计行判据改错致误杀真实明细——27.67 账实差结案，API 通道实现分毫不差**。业务方要求验证"跨维度明细表能否算出各仓库结存"，深挖中发现更根本的问题。**根因**：`kingdee_client.fetch_inventory_summary` 剔小计行的判据是「**无批号=小计行**」（V2.61 起），但收发存报表里**存在"有库存状态、无批号"的真实明细行**——2026 年 5 期恰好有 2 行（星期零拌拌粉-酱香南昌拌粉风味 结存 −0.87/−13.9348、爆辣新疆炒米粉风味 −0.84/−13.7305，存货类别均为**产成品→库存商品科目**），被判据误杀，合计 **−27.67**。这正是 V2.87 记录在案、并据此判定"正式月结只能走上传通道"的那笔**库存商品账实差 27.67**——**与"物料档案类别口径不一致"无关，是判据 BUG**（V2.87 结论就此更正）。**修复**：判据改为「**库存状态为空=小计/合计行**」（明细行必带库存状态，批号可为空），FieldKeys 增取 `FStockStatusName`、取值下标顺延。**真金蝶实测（107 孝感星期九 2026 年 5 期）**：老判据 2768 行/10,958,078.92（差 +27.66）→ 新判据 **2770 行/结存数量 5,181,959.69/金额 10,958,051.26，与成本会计底稿分毫不差**；未匹配存货类别 0；三道勾稽 **credible=true**，账实**库存商品 4,153,701.90 差 0.00 / 原材料 6,364,996.13 差 0.00 / 周转材料 439,353.22 差 0.00 全部 pass**（此前库存商品差 27.67）。14/14 单测绿、`import app` OK。**影响**：API 通道（🅰一键金蝶取数）已可精确归零，"正式月结必须用🅱上传通道"的限制具备摘除条件（待业务方确认后另开版本调整前端文案）。**探查副产物（只读、无代码改动）**：①真·「存货收发存明细表(跨维度)」`HS_InOutStockDetailRpt` **API 取不到**——接口明确返回"此接口暂时只支持简单账表"（分组树形账表不支持）；②`HS_INOUTSTOCKSUMMARYRPT`(跨维度汇总表) 可取且**字段含 `FStockName`，但值恒为空**，`FDimType` 给任何值行数均不变（疑该核算体系核算维度=批号+库存状态，不含仓库）；③`HS_NoDimInOutStockDetailRpt`=**单据流水级**（5 期 13,625 行），其结存列为**按物料累计的滚动结存、不按仓库/批号重置**（调拨实证：精炼椰子油 175→157.5 跨仓续算），故"按仓库取末行结存"是错的（更正 V2.87"聚合翻倍"的表述）；④该报表全量求和=底稿×2，因含 1 行总计行；⑤账簿 107 在 2020-01-01 前无业务（2015/2018/2020 起点行数均 736,023），期初为 0。**各仓库结存还原仍在验证中**（按仓库净额法），未并入本版本。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.105** | 2026-07-12 | **【平台】侧栏视觉重设计（贴边版，参照财务BP工作台）**。业务方给了一版导航设计稿，想让侧栏更有层次。据此**重写 `Sidebar.jsx`**：①组头做成带**竖向强调色条**的分组标题（月结与结账/报表与台账/常规对账/其它小工具），可折叠；②选中项带**强调色竖条+浅色底**，父子项用**嵌套引导竖线**区分层级；③模块名后挂**状态标记**（待验收=琥珀胶囊、开发中/敬请期待=灰字）与**服务岗位胶囊**；④新增**折叠成图标轨**模式（窄栏只留图标、hover 弹出飞出菜单）。**关键调整**：初版把侧栏＋主区做成"浮在灰桌面上的圆角白卡片"，业务方否掉——"取消圆角和悬浮空白，参照BP工作台贴边，但竖向色条/字体/行高保留"。遂改 `styles.css`：`.shell` 回纯白、`.aside` 回**贴边面板**（`--bg-sub` 底＋右边线、`sticky` 满高、`z-index:20` 让图标轨飞出菜单不被裁），撤掉桌面渐变/圆角/阴影/外边距；侧栏组件内部的色条/字号/行高全部保留。**验证**（1340 宽）：侧栏贴左边、无圆角无悬浮，四组组头竖条、选中项强调条、嵌套引导线、待验收/岗位胶囊均在；主区贴边白底。纯前端，改 Sidebar.jsx/styles.css，新包 index-DcV_9kLt.js／index-uO3wTPTT.css，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.114** | 2026-07-12 | **【平台·权限】「基础设置」改名「系统设置」+ 加权限点，默认仅主管理员可进**。业务方：基础设置改名系统设置、做成权限点、默认只有主管理员能进。**后端** `db.CAP_META` 新增权限点 `enter_settings`(标签「进入系统设置」，ws=accounting，`sensitive:True`)——敏感点＝新账号默认不给、须显式授予；`NAV_MODULES` 的 settings 标签「基础设置」→「系统设置」并挂 `cap:"enter_settings"`。**前端**：Sidebar 加 `canSee(m)＝无cap或主管理员或已授权`，据此过滤 bottomMods/itemsOf/childrenOf(有 cap 的模块无权者看不见)；App.jsx 加 `canSettings`，`view==='settings'` 无权时渲染「仅主管理员可进入」占位页(纵深防御)；Settings 页标题「设置」→「系统设置·仅主管理员可进入」；占位提示里「基础设置›导航模块上线管理」同步改「系统设置›…」。**权限语义**：主管理员 `user_can(enter_settings)=True`(role=admin 全权)、普通账号默认 False；主管理员可在「账号管理」勾选授予他人(在 WS_CAPS.accounting 内)。系统设置内的敏感写操作(导航模块管理=主管理员、金蝶连接等)本就各自受控，此点只管「进入」入口。**验证**(Python 逐条)：enter_settings 已注册且 sensitive；新核算组账号默认 False、主管理员 True、普通默认 False；可授予=True；NAV 标签=系统设置/cap=enter_settings。**后端改动，需重启后端(启动.bat)生效**。改 db.py/app.py/Sidebar.jsx/App.jsx/Settings.jsx，新包 index-DKFVFktf.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.113** | 2026-07-12 | **【平台】银行对账做成三级菜单：银行对账(分组) › 对账程序/理财对账/资金看板/账户台账**。业务方：把「对账程序」本身也显式放进三级菜单，与理财对账/资金看板/账户台账并列。原来 `reconcile`(银行对账)既是二级项又是逐笔稽核程序本身、点它只能展开、程序没有显式入口。**改** `NAV_MODULES`：新增纯分组父项 `bankrecon`(标签「银行对账」，二级，点击只展开不进入)；`reconcile` 改标签「对账程序」并挂 `parent=bankrecon`(逐笔稽核5步程序不变、App 路由不变)；`wealth/fundboard/ledger` 的 parent 从 reconcile 改到 bankrecon；**顺序＝对账程序·理财对账·资金看板·账户台账**(childrenOf 按 NAV_MODULES 顺序)。Sidebar 给 bankrecon 配银行图标。父子从属仍走后端单一真相源(V2.101)，侧栏与上线管理表都据此三级缩进。**验证(5200)**：`/api/nav-modules` 四子项 parent=bankrecon、顺序正确；侧栏 月结与结账›银行对账›[对账程序/理财对账/资金看板/账户台账] 三级缩进；点父项「银行对账」只展开/收起、标题不变(不误跳)；点「对账程序」进逐笔稽核(624笔)、点「资金看板」进资金看板，均正常。**后端 NAV_MODULES 改动，需重启后端(启动.bat)生效**。改 app.py/Sidebar.jsx，新包 index-CiLyR0SE.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.112** | 2026-07-12 | **【资金看板·后端·重要数据修正】期末余额还原——原来取的是"期初"不是"期末"**。业务方一针见血指出"你取的是期初余额，不是期末余额"。**根因**：金蝶 GL_BALANCE 接口在**凭证未过账期间**，「期末/本期发生」字段停在期初/返回 0（实证孝感招商一般户 712900412410308：期初=期末=8,906,394.12、本期借贷=0，而真实本期发生 4141 万在序时账里），本期真实发生额只在**序时账**中。逐笔稽核/余额调节/科目余额表早已用「期初＋本期序时账净」还原（V1.7/V2.35 逐户验证一致），唯独资金看板直读 GL_BALANCE 的 `FEndBalanceFor` → 显示成期初。**修复**：`balance_dashboard.load_balance(rows, vou_rows)` 新增序时账入参，给了就按 **期末＝期初＋本期序时账净还原**（原币＝期初原币＋Σ原币净、本位币＝期初本位币＋Σ本位币净，各自还原兼容境外美元簿；维度编码 FF100002 跨主体唯一，据此把序时账对回账户）；`_fund` 传入定格的 `gl_subjects`（四类科目序时账）；样例无序时账→传 None 走原期末字段（样例自带真实期末）。**验证（真金蝶6月）**：孝感交易性金融资产 115,390,781.93(期初)→**20,000,000**(期末，9500万宁银理财本期已赎回)、与科目余额表逐分一致；孝感银行存款还原为 **134,794,588**（招商一般户 890万→3596万、宁波86041 →9469万，发生额全部反映）；集团各科目大类期末＝科目余额表口径（境外美元簿按账簿本位币加总、不折人民币，属既有口径）；护栏 集团合计=Σ科目大类 且 =Σ主体 均 true。**后端改动，需重启后端(启动.bat)生效**。改 kernels/balance_dashboard.py + app.py，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.111** | 2026-07-12 | **【资金看板·后端·重要数据修正】交易性金融资产双重计数修复（父级汇总科目误计）**。业务方给出孝感真实金蝶截图排查，发现工具把孝感交易性金融资产算成 **230,781,563.86**，实际应为 **115,390,781.93**（近翻倍）。**根因**：金蝶科目余额表对**有子科目的科目**（1101 交易性金融资产 下挂 1101.01 成本 / 1101.02 公允价值变动）会同时返回【父级 1101 汇总行（核算维度空白）】与【子级明细行（核算维度＝理财产品）】，原 `load_balance` 两者都计＝父＋子重复；父级空白行还在页面显示成"看不到是什么账户"的 1.15 亿幽灵户。1002/1012 无点号子科目故不受影响、之前对得上。**修复**（`balance_dashboard._collapse_subaccounts`）：①**丢父级汇总行**——同一主体下若存在"本科目编码+."开头的子科目，本行余额已被子科目覆盖，删除；②**按(主体·核算维度·币别)合并子科目行**——同一理财产品的 成本＋公允价值变动 相加成一行（＝该笔理财公允市值）。**验证**（真金蝶6月）：孝感交易性金融资产 230,781,563.86→**115,390,781.93**（与金蝶逐分一致）；信益嘉321号6单元＝2000万+10万公允=**2010万**、宁银理财=9500万+29.08万=**95,290,781.93**、幽灵空白户消除；护栏 集团合计=Σ科目大类 且 =Σ主体 均 true；1002 银行存款不受影响（每户核算维度唯一、无子科目，合并/排除均无操作）。另加排查接口 `/api/debug/balance-raw?prefix=1101`（管理员登录态，看某科目原始 GL_BALANCE 行/期初/本期/期末，定位核算维度落哪个字段）。**后端改动，需重启后端(启动.bat)生效**。改 kernels/balance_dashboard.py + app.py，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.110** | 2026-07-12 | **【资金看板·后端】交易性金融资产(1101)账号显示完整核算维度名，不再丢成一串数字**。业务方给出真实样式："63672948信益嘉321号6单元"——1101 在金蝶按理财产品/项目名记，核算维度名带项目名。原 `balance_dashboard.build_dashboard` 账号取 `norm_acct`(抽最长数字串)，会把它显示成光秃秃的"63672948"、名字全丢。改：**科目大类＝交易性金融资产 时，账号列直接显示完整核算维度名称**(`acct_raw`)；银行/其它货币仍显示抽出的数字账号；数字键(norm_acct)仍用于内部匹配，不影响新增判断/开户行反查。**验证**：单元模拟 1101 行「63672948信益嘉321号6单元」→ 账号显示＝该完整名、科目大类＝交易性金融资产；银行户分支不变。**重要前提**：取数对四类科目读的是同一核算维度字段 FF100002(银行账号)，此修复生效的前提是**贵司 1101 也挂在"银行账号"这个核算维度**上；若 1101 用的是另一个核算项目(如理财产品维度)，当前取数读不到该名，需在真实刷新后核对、必要时补取该维度字段。**后端改动，需重启后端(启动.bat)生效**。改 kernels/balance_dashboard.py，无前端变更(仍 index-Ts7PutB3.js)，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.109** | 2026-07-12 | **【资金看板】按开户行归组细化：第三方支付/理财各自成组，"其他"只留未识别开户行的银行户**。业务方澄清：①「其他」里那些多半是**交易性金融资产(1101)**——金蝶里 1101 通常**不挂银行账号核算维度**（按理财产品/明细科目记），取数按「核算维度·银行账号」摊户时账号为空，故落"其他"；②第三方支付应像库存现金一样，归到「第三方支付」这个"开户行"。据此改 `FundBoard.jsx` 的 `bankKey`：**有开户行→用开户行；无开户行的按性质各自成组（库存现金/第三方支付/理财 本就无银行）；只把"是银行户、开户行没识别出来"的留在"其他"**（＝真正需去出纳主数据补开户行的）。**验证**（5200 样例6月）：样例无第三方支付、其唯一理财户（花旗1037204028）本有开户行归花旗，故样例"其他"仍＝两个1012其它货币户（2088/01，¥7.3M）、可见结果不变（预期）；真实金蝶里第三方支付/无账号理财将各自拆出、"其他"随之缩小。构建通过无报错，下钻仍正常。纯前端，改 FundBoard.jsx，新包 index-Ts7PutB3.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.108** | 2026-07-12 | **【资金看板】图例可点下钻——看清「其他」里是哪些账户**。业务方问"按开户行的『其他』那么多，都是什么账户"。原因：「其他」＝按开户行归组时**没识别出开户行、又不是库存现金**的账户（金蝶开户行字段本空、靠出纳银行账号主数据反查，主数据没覆盖到的账号就补不出开户行，真实数据开户行覆盖约 126/177，剩余 ~50 户全落此组；另含第三方支付/电商货币户这类本就无银行的）。改 `FundBoard.jsx`：资金构成图例每一组**可点下钻**（`catSel` 态），点后**账户明细只筛出该组**、卡头显示「只看：X ✕」可一键取消；切换书签（性质/开户行）时自动清除下钻。让业务方点一下就能核到"其他"里具体是哪些账号。**验证**（5200 样例6月，JS 读表）：切「按开户行」→ 点「其他」→ 明细只剩 集团·2088、集团·01 两户（均"其它货币"、开户行"—"，合计 ¥7.3M 与该段一致），「只看：其他 ✕」标记正确。纯前端，改 FundBoard.jsx，新包 index-PRV_wsuR.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.107** | 2026-07-12 | **【资金看板】资金构成加「按开户行」书签切换**。业务方：资金构成环形图现按账户性质分，想再加一个按开户行看的口径——不新增页面，只做一个可切换的书签。改 `FundBoard.jsx`：①资金构成卡头放**书签式二选一切换**（按账户性质／按开户行），切的是同一张环形图，选项记 localStorage `fw_fund_catby`；②按开户行聚合时，**库存现金归到「库存现金」这个开户行**（现金无银行），无开户行的（如电商第三方货币户）归「其他」；③配色：按性质走固定 `NAT_VAR`，按开户行无固定色→按金额排名依次取 `--cat-1..8`（前 8 上色其余落灰）。**验证**（5200 样例6月）：切「按开户行」＝宁波52.1%/招商39.6%/其他4.8%/花旗3.0%/库存现金0.5%/中国银行0%，库存现金独立成组正确；切「按账户性质」＝银行存款91.7%/其它货币4.8%/理财3.0%/库存现金0.5%；两口径集团总资金均¥151,200,000 一致、颜色分明。纯前端，改 FundBoard.jsx，新包 index-CGAgtTAk.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.106** | 2026-07-12 | **【平台】基础数据/基础设置钉到侧栏底部（参照 BP 工作台）**。业务方：这两项原先跟在最后一组「其它小工具」后面、随导航内容流走，希望像 BP 工作台那样固定在底部。改 `Sidebar.jsx`：把 `基础数据/基础设置` 移出可滚动的分组区，放进滚动区与页脚之间的**固定块**（分隔线 + 两项），无论上方分组多长都恒定贴底；展开态与折叠图标轨两种模式都同步处理（图标轨里同样移出滚动区、飞出菜单 overflow 可见）。**验证**（5200 实机 1340×860）：进项税核对之后有留白→分隔线→基础数据/基础设置紧贴页脚上方，与 BP 一致。纯前端，改 Sidebar.jsx，新包 index-DC_FxzKQ.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.104** | 2026-07-12 | **【资金看板】资金构成卡布局优化：环形图放大 + 不留空**。业务方："饼图有点拥挤，旁边又有点空"。根因：资金构成卡与旁边"各主体资金"卡在同一 grid 行被拉到等高，但环形图内容短→底部一大块空白；且环形偏小(168px)显拥挤。改 `FundBoard.jsx`：①环形图 168→**200px**(viewBox 200、rad 76、w 26、中心字号加大)；②grid 加 `alignItems:'start'`——两卡按各自内容高度排、不强行拉平(资金构成卡 255px 贴合内容，各主体 291px)；③环形与图例 gap 16→20、居中，图例行距 5→7px。**验证**：环形 200px、资金构成卡 255px(不再被拉到 291)、底部空白消除；截图确认平衡。纯前端样式，新包 index-B-1_SFer.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.103** | 2026-07-12 | **【资金看板】主体/开户行拆两列 + 开户行取自出纳银行账号 + 科目大类换成「账户性质」**。业务方三点。**①开户行来源**：实证金蝶「开户行」字段基本为空，但**出纳管理·银行账号**里 `银行账号` 名带前缀（招商银行基本户…）、`账户类型`＝基本户/一般户/通知存款/结构性存款/资本金户、`类别`＝银行账户/电商渠道。刷新时用 `al.kd_accounts_from_cn(fetch_bank_accounts())` 解析出 {账号→开户行+账户类型+类别} 定格入库（`kd:bank_master`，124 户）；`_fund` 据此填 `开户行`（台账/名称解析兜底）。**②账户性质**：新增 `_acct_nature()`＝库存现金 / 第三方支付(电商渠道·支付宝微信抖音…) / 理财(交易性金融资产·结构性存款·理财产品) / 银行账户按出纳账户类型(基本户/一般户/通知存款/资本金户) / 兜底其它货币，**替换原「科目大类」**（资金看板跨科目、按性质更贴业务）。**③前端** `FundBoard.jsx`：账户明细「主体/开户行」合列拆成**两列**；环形图、筛选下拉、明细列全部由 科目大类→**账户性质**；配色扩到 8 槽(`--cat-1..8` 加进 styles.css 含深色)。**验证（真金蝶6月，需先解封本期才能刷）**：出纳取回 124 户；孝感逐户 招商/宁波银行 + 基本户/一般户正确；全量性质分布 理财80/一般户28/通知存款25/第三方支付16/其它15/库存5/基本5/资本金2/NRA1；开户行覆盖 126/177；环形「按账户性质」理财83.4%；样例无主数据走兜底(银行存款/其它货币/理财/库存现金)也正常。**注意**：账户性质要细分到基本户/一般户，须金蝶模式且**刷新过（出纳主数据）**；样例只到粗类。改 app.py/styles.css/FundBoard.jsx，新包 index-DWiJ4Lx4.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.102** | 2026-07-12 | **【平台】侧栏服务岗位胶囊改为跟在模块名同行末尾（不另起一行）**。业务方：已上线模块挂的服务岗位原先显示在模块名【下方另起一行】，希望改到模块名【后面同一行】末尾。`Sidebar.jsx` 抽出 `postPills(it)`，放进导航行内（`marginLeft:auto` 右对齐、`nowrap`+`overflow:hidden` 一行放不下横向省略、不撑高，hover 整组看全部岗位），删除原来行下的独立 `div`；与「待验收/已封存」标记互斥（已上线才有岗位，右侧空着正好放）。**验证**：挂 1 个岗位（常见）——「银行对账·总账岗」「资金看板·资金岗」同行末尾、行高 36px 正常、完整显示；挂 2 个也不换行（行高仍 36px，超宽横向省略）。测试岗位分配已清库恢复默认。纯前端，新包 index-BQX3a08r.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.101** | 2026-07-12 | **【平台】资金看板转正式模块 + 父子从属做成后端单一真相源（侧栏与上线管理表都缩进）**。业务方发现：资金看板在侧栏是「银行对账」的子项，但它是**合成快捷入口、没登记成模块**，所以「基础设置›导航模块上线管理」表里没有它、没法设状态；且父子关系**写死在前端 Sidebar**。**改**：①资金看板转**正式模块** `fundboard`（`NAV_MODULES` 加条目，默认已上线，自动进 `_BUILTIN_KEYS` 受保护、进上线管理表可设状态）；App 的 fundboard 视图改按 `modOn('fundboard')` 门禁（此前借用 reconcile）。②**父子从属挪到后端**：`NAV_MODULES` 给 fundboard/ledger/wealth 加 `parent:"reconcile"` 字段，随 `_all_modules()` 透传前端；Sidebar 去掉硬编码 `SUB_OF`，改由 `navDef` 的 `module.parent` 现算 `subOf`（单一真相源）；`Settings` 上线管理表按 parent **重排+缩进**（父项在前、子项紧随其后带 └ 缩进 18px）。**验证**：`/api/nav-modules` 三子模块 parent=reconcile；侧栏 资金看板/账户台账/理财对账 缩进 16px、科目余额表/物流计提 0；设置表 └资金看板/└账户台账/└理财对账 缩进 18px、其余 0；截图确认。改 app.py/App.jsx/Sidebar.jsx/Settings.jsx，新包 index-Ctc2dIq8.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.100** | 2026-07-12 | **【平台】侧栏板块「报表与台账」调到「常规对账」之前**。`app.py` 的 `NAV_GROUPS` 顺序由 月结与结账/常规对账/报表与台账/其它小工具/通用 → 月结与结账/**报表与台账/常规对账**/其它小工具/通用；`_all_groups()` 与前端 navDef 直接读它，侧栏随之重排。**验证**：`/api/nav-modules` groups 与侧栏 `.grp` 实测顺序均为 月结与结账→报表与台账→常规对账→其它小工具。纯后端一行常量，无前端/数据改动，静态包不变。同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.99** | 2026-07-12 | **【银行对账·改】资金看板快速筛选「余额为0」改为「隐藏0余额户」**。业务方澄清：不是要"只看0余额"，而是要**一键把0余额账户从看板清掉、只看有钱的户**（金蝶模式下有 100+ 个 0 余额户，很吵）。改：chip 文案「余额为0」→「隐藏0余额户 N」（N=当前0余额户数），筛选逻辑由 `qZero→只留0户` 翻转为 `qHideZero→排除0户`（`!isZero(a)`）；state `qZero`→`qHideZero`。当月新增 chip 不变。**验证**：样例实测——点击前 10 行含中行0户，点「隐藏0余额户 1」后 9 行、中行0户消失、环形中心仍¥151,200,000（0户不贡献余额，总数不变）。新包 index-CbkWCXjK.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.98** | 2026-07-12 | **【银行对账】资金看板加两个快速筛选：当月新增(*New) / 余额为0**。**后端**：`_fund` 给每个账户加 `新增` 标记——**本期账号在【上一期金蝶科目余额】里不存在＝本月首次出现**；靠上期【已取数】的持久化余额(`period_inputs kd:gl_balance`)判断，上期没取过数则 `新增可判断=false`(不标、前端给提示)；样例源无上期数据，指定 755953100010001 一户标新增供演示。**前端** `FundBoard.jsx`：筛选条加两个一键 chip「✦ 当月新增 N」「余额为0 N」(带全量计数)，与主体/开户行/科目/搜索**叠加**生效、图表明细一起联动；`*New` 徽标标在明细表主体名后；`新增可判断=false` 且勾了当月新增时提示"上一期没有金蝶数据"。余额为0＝`|期末余额(本位币)|<0.01`。**验证**：样例实测——chip 显「当月新增 1 / 余额为0 1」、表内 1 个 *New；点当月新增→只剩 755953100010001、环形中心随之¥41,200,000；点余额为0→只剩中行 769272660074(¥0)、清除链「当前 1/10 户」；截图确认。金蝶模式因上期未持久化→新增不可判断(设计如此，取过上期数即可对比)。新包 index-BTN-6yxD.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.97** | 2026-07-12 | **【银行对账】资金看板加筛选条（主体/开户行/科目 + 账号模糊搜索，图表联动）**。`FundBoard.jsx` 顶部加同项目 `.fbar` 样式筛选条：主体/开户行/科目 三下拉（选项从账户去重）+ 账号关键字**模糊搜索**（`String(账号).includes`）+ 清除筛选（带「当前 X/Y 户」计数）。**关键：图表与明细都从【筛选后】账户重新聚合**——`total/科目大类环形/各主体横条/明细表` 全部由 filtered accounts 按本位币口径 `agg()` 现算，筛选一变全联动（不是只筛表格）。**验证**：样例数据浏览器实测——选「交易性金融资产」→环形中心总资金¥4,500,000、主体条 1 根、明细 1 行、清除链「当前 1/10 户」；账号搜「1037」→精准命中花旗户 1037204028、明细 1 行；清除后复原。截图确认观感。新包 index-CXENwBXL.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.96** | 2026-07-12 | **【银行对账】资金看板独立成页 + 可视化（环形图/横条/明细）**。业务方要资金看板独立、加可视化。原先「资金看板」只是余额调节页的标题装饰，`_fund` 早已返回 集团合计/科目大类/主体/accounts 却从没画出来。**新页 `views/FundBoard.jsx`**（view=`fundboard`，侧栏「银行对账›资金看板」直达）：①**集团总资金** 大数字（环形中心）；②**资金构成·按科目大类** 环形图（4 类·库存现金/银行存款/其它货币资金/交易性金融资产，2px 段间隙，图例带金额+占比%）；③**各主体资金** 横条（单色量级·降序·直接标金额·4px 圆角）；④**账户明细** 表（主体/开户行/账号/科目大类带色点/币别/期末余额原币+本位币/生效状态）。**配色走 dataviz 技能校验**：分类色 `--cat-1..4`(蓝/青/黄/绿)写进 styles.css 并带**深色模式**步；`node validate_palette.js` 通过（CVD ΔE 24.2 ≥12；青/黄低对比→靠图例直接标签补足=relief 满足）。数据源 `/api/fund-dashboard`（未取数/错误态友好横幅）。**配套**：`RECON_VIEWS` 恢复含 fund（余额调节仍是银行对账四步之一）、`EXTRA_CHILDREN` 资金看板改指 `fundboard`；`App.jsx` 加 `fundboard` 视图路由 + **加进 `CODED_VIEWS`**（否则"未接代码模块"兜底会在页底叠一块 fundboard 占位——已修）；旧四步第 3 步页标题「余额调节·资金看板」改「银行存款余额调节」去重名。**验证**：样例数据浏览器实测——环形 4 段(银行存款91.7%/其它4.8%/交易性3.0%/现金0.5%)、集团总资金¥151,200,000、主体 6 条降序、明细 10 行；底部占位已消除、3 卡干净；截图确认观感。新包 index-CkXuoA0U.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.95** | 2026-07-12 | **【平台】理财对账、资金看板一并收进「银行对账」子项**。承 V2.94：业务方续定——理财对账也是银行对账一环、资金看板也归进去。**做法**：`Sidebar.jsx` 重构 item 渲染为「父项 + 紧随其后的缩进子项」，抽出 `renderNav(it,sub)` 复用；`SUB_OF = { ledger:'reconcile', wealth:'reconcile' }`（账户台账+理财对账嵌入）；`EXTRA_CHILDREN = { reconcile:[资金看板→fund视图] }`——**资金看板本是银行对账四步的「余额调节」步、无独立模块，故做成合成子项直达 fund 视图**。银行对账主项高亮集从含 fund 改为 `['import','reconcile','result']`（`RECON_VIEWS`），避免进资金看板时银行对账/资金看板两处同时高亮。科目余额表、物流计提保持平级。**验证**：DOM 实测 银行对账(缩进0)下依次 资金看板/账户台账/理财对账(均16px)、科目余额表/物流计提(0)；点资金看板→落「余额调节·资金看板」页、仅资金看板高亮、银行对账不亮。纯前端，未动后端注册表。新包 index-BuNUPiXF.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.94** | 2026-07-12 | **【平台】账户台账收进「银行对账」做缩进子项**。业务方觉得账户台账更像银行对账的一部分（它是银行账户主数据＋每户稽核方案＝银行对账的配置，非独立月结环节）。**做法**：`Sidebar.jsx` 加 `SUB_OF = { ledger: 'reconcile' }`，渲染时账户台账缩进(marginLeft16+左连接线)、视觉归到银行对账下；科目余额表/理财对账/物流计提保持平级（业务方定：只收账户台账，科目余额表仍平级）。**纯前端视觉嵌套**，未动后端模块注册表(navDef)——避开并行会话对导航系统的重构；账户台账仍是独立路由，点击照常。**验证**：DOM 实测 月结看板/银行对账/科目余额表/理财对账/物流计提 缩进=0、**账户台账 缩进=16px**。新包 index-Cvbygf0I.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.93** | 2026-07-12 | **【银行对账】上传流水加「月份不符」警告（防选错月份存错期）**。业务方在期间＝2026-04 时上传了 6 月流水，工具按所选期间存进了 4 月（流水按当前选中期间入库，工具原不校验流水实际月份）。**修**：上传端点解析后统计逐笔 `交易日期` 占比最高的 YYYY-MM＝流水实际月份，与所选期间比对；不符则返回 `period_mismatch/bank_ym/sel_ym`、并把 `流水月份` 写进 bank 元信息。**前端**：上传后不符时弹**红字**「⚠ 月份不符！你选的期间是 X，但流水主要是 Y 的，已按 X 存入——若选错请把期间切到 Y 再重传」；一致时提示「流水月份 Y，与所选期间一致」。状态卡也常驻显示「⚠ 流水月份 Y 与本期不符」，切期间/重看都在。**不阻断存储**（可能有跨月流水的合理场景），只重警告让人自己判断。**验证**：合成 6 月流水行→选4月判不符=True、选6月=False。新包 index-LckF701N.js，同步 01→02，部署包已更新。**（业务方现象根因）**：其截图流水文件名均为 `20260601-20260630`（6月），却存进了 4 月＝当时期间停在 4 月——正确操作＝先把期间切到 6 月再上传；此警告即为防此。仍需业务方**重启后端**跑最新版（承 V2.92）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.92** | 2026-07-12 | **【银行对账】修数据接入状态卡上传/取数后不翻绿（后端旧版兜底）**。业务方反馈：上传流水(已并入622笔)+从金蝶更新(624笔)后，顶部「本期数据状态」卡与右上角胶囊仍显「未上传/未取数」。**根因**：状态卡读的 `bank_meta`/`kd_synced_at` 是 V2.88 才加进后端的字段，业务方运行的**后端进程未重启到最新**→不返回这些字段→卡片认不出已备（下方清单/manifest 能显示是因走旧字段）。**兜底**：`DataImport` 判定改为——`bankReady = 有精确元信息 或 并入笔数>0`、`kdReady = 有取数时点 或 金蝶笔数>0`；文案里"谁/何时"仅在拿得到时显示，拿不到就只显「已上传/已取数 + 笔数」，保证**只要有数据就翻绿**、不因后端旧一直显未。②金蝶取数区的「本期未取数」同步改用 kdReady。**验证**：注入 kd 462/科目余额400→ data-sources 返回 kd_count=462，`kdReady=（时点∥笔数>0)=True`。新包 index-BkzP_70g.js，同步 01→02，部署包已更新。**⚠根治仍需业务方重启后端**：数据接入页已兜底可绿，但其他页胶囊（读 `cfg['数据状态']`）与「按期间持久化/取数一次定格」等全套 V2.88–V2.91 能力，都要后端跑到最新版才生效——线上/本地那个服务进程重启一次即可。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.91** | 2026-07-12 | **【平台】期间选择器改自定义下拉·12个月做成彩色状态胶囊**。承 V2.90：业务方要「下拉里做成彩色胶囊、旁边独立胶囊去掉」。原生 `<select><option>` 只能纯文本、塞不了颜色，故 `PeriodPicker` 改为**自定义下拉**：触发按钮显「2026 年 5 期 · 已上传（当前月状态小胶囊）+ ▾」；点开面板＝年度切换(2025/2026/2027 chip) + **12 个月彩色胶囊**（2 列网格，底色＝该月状态：**灰蓝=已封存 / 绿=数据已上传 / 琥珀=数据未导入 / 浅灰=样例**，当前月带 accent 描边），底部三色图例；点月即选并关闭，点外部关闭（mousedown 监听）。年度/本期状态变即重拉 `/api/period-statuses`。全项目 7 处期间选择器统一生效，独立外挂胶囊取消（状态并入选择器本身）。**验证**：造 1-4封存/5上传 分布→浏览器实测下拉 12 胶囊文案与底色＝「1-4期·已封存(灰蓝rgb(241,245,249))/5期·已上传(绿rgb(232,244,238))/6-12期·未导入(琥珀rgb(254,243,199))」、触发钮「2026 年 5 期·已上传」、点外部关闭正常；截图确认面板布局(年度行+彩色月网格+图例)。测试注入的 periods/period_inputs/审计已清库、本地 config 复位 sample/6月。仅前端组件重写，无后端/数据模型改动。新包 index-veKVsF80.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.90** | 2026-07-12 | **【平台】期间下拉逐月标注数据状态**。业务方要「把状态胶囊放进期间筛选器里」——如 1–4月已封存、5月已上传、6–12月未导入，选月时一眼看清哪月备齐/封了/没导。**后端**：新增 `GET /api/period-statuses?year=YYYY` 返回该年 12 个月各自 `_period_data_status`（复用 V2.89 逻辑，本源）。**前端**：`PeriodPicker` 在年度变或本期状态变（上传/封存后）时拉该年 12 月状态，把期间下拉每个 `<option>` 文案改为「N 期 · 已封存/已上传/未导入」（样例源不加后缀）；右侧当前月的彩色胶囊保留。原生 `<select>` 选项只能纯文本、不能塞彩色胶囊，故用「· 状态」文字后缀实现，月下拉加宽到 110px 容纳。**验证**：后端造 1–4封存/5上传 分布→ `/api/period-statuses` 返回逐月正确；前端 DOM 实测期间下拉 12 项文案＝「1 期·已封存 … 5 期·已上传 … 6 期·未导入」与业务方示例一致。测试注入的 periods/period_inputs/审计已清库、本地 config 复位 sample/6月。仅新增只读接口 + 组件渲染，无数据模型改动。新包 index-CEMAPdkm.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.89** | 2026-07-12 | **【平台】期间选择器改「年度＋期间(12项)」+ 数据状态胶囊 + 数据接入页右上角加选择器**。业务方三点。**①拆两段**：`PeriodPicker.jsx` 由原来「年×12=一长串」的单下拉改为**年度下拉 + 期间下拉**，期间只 1–12、**升序**排列（原是 12→1 倒序）；年度选项 2025/2026/2027。全项目 6 处共用（逐笔稽核/余额调节·资金看板/科目余额表/账户台账/理财对账/月结看板）一改全生效。**②状态胶囊**：后端 `_period_data_status()` 按本期算一句话——**已封存 / 数据已上传 / 数据未上传 / 样例数据**（closed→已封存；sample→样例数据；有 period_inputs(bank 或 kd)→数据已上传；否则数据未上传），塞进 `/api/config` 的 get 与 set 响应（`数据状态` 字段）；`PeriodPicker` 收 `status` prop 显彩色胶囊（封存/未上传=琥珀，已上传=绿，样例=灰），各页传 `status={cfg['数据状态']}`。上传流水/刷新金蝶后 `DataImport` 调 `getConfig()` 回灌 `onChange`，胶囊与侧栏封存态即时刷新。**③数据接入页右上角**：`DataImport` head 加 `PeriodPicker`（此前该页无期间选择器，只能靠别页切）；`App.jsx` 给 DataImport 传 `onPeriod={changePeriod}`。**验证**：后端四态实测——样例数据 / 金蝶6月「数据未上传」/ 注入流水→「数据已上传」/ 封存→「已封存」。前端 DOM——逐笔头部年度下拉[2025/2026/2027]、期间下拉[1..12 升序]、胶囊「数据未上传」；数据接入页右上角 2 下拉+胶囊且在右侧、下方「本期数据状态」卡在位。测试注入的 period_inputs/periods/审计已清库、本地 config 复位 sample/6月。仅前端组件 + 后端 config 响应加字段，无数据模型改动。**（同版补丁）业务方反馈数据接入页看不到胶囊**——因其运行的后端进程尚未加载到本版（`数据状态` 字段是本版才加进 `/api/config` 的；上一版就有的「本期数据状态」卡仍正常，故只有胶囊缺）。修：`DataImport` 的胶囊改为**优先用 `cfg['数据状态']`，取不到（旧后端）就本页从 `d`（bank_meta/kd_synced_at/封存）自行推算**，不重启也一定显示；宽视口(1900)实测胶囊右边缘 1880≤1900 未被裁、可见。其余页仍读 `cfg['数据状态']`（后端重启后即全有）。新包 index-CQ_0Khnp.js。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.88** | 2026-07-12 | **【银行对账】数据按期间持久化：上传一次/取数一次就定格，根治"5月却用6月流水"+ 每次重解析**。业务方两点：①样例是6月、金蝶刷5月，怎么还用到6月流水；②上传流水/金蝶取数不该每次进页面重跑，应上传/取数一次就一直是那样。**病根（实证）**：银行流水读的是**同一个全局文件夹**、不跟会计期间走（金蝶侧按期间对、银行侧不对→5月金蝶×6月流水）；且解析结果只在**内存缓存**、重启/换期/刷新即废，每次进来重解析5.5万行、重打金蝶。**架构改造**：新表 `period_inputs(source,year,period,kind,payload gzip,meta,updated_by,updated_at)`——按期间存的输入数据，重启不丢、全员共享。**金蝶取数一次定格**：`_kd_get(call_key)` 读本期已存；没取过抛 `KdNotFetched`（页面转"本期未取数"友好提示，不再 500、不再拿别月凑）；`POST /api/kingdee/refresh` 改为**唯一真取数处**，一次预取并落库 科目余额/1002/1012/四类科目序时账，返回各笔数与时点；`_balance_rows/_real_bank_kd/_fund/_balance_adjust/_data_sources/_channel_adjust/_subject_balance` 全改走 `_kd_get`（wealth/cost-ledger 各自独立流未动）。**银行流水按期间持久化**：上传端点解压到**按期间专属目录** `bank_uploads/<源>_<年>_<期>/` → 解析 rows+manifest+第三方渠道 channels → `db.set_period_input(...,'bank',...)` 定格入库、写审计；`_period_bank()` 严格读本期已上传的，本期没传→空+"本期未上传流水"（不再串月、不再退样例）。**样例锁6月**：`SAMPLE_YM=(2026,6)`，切样例源即锁6月、启动纠正历史 config 里 sample+5月 的错位。**月结看板对齐**：金蝶取数时点改从 `_kd_synced_at()`（period_inputs meta）读，不再用旧的内存 `_SYNC_AT`。**前端**：`DataImport.jsx` 顶部加「本期数据状态」卡（银行流水：谁/何时上传 or 未上传；金蝶数据：取自何时 or 未取数；两态胶囊绿/琥珀，未备齐红字提示）；`Reconcile/FundDashboard/SubjectBalance` 在 `未取数` 时显琥珀横幅+「去数据接入 ›」跳转，不再空表。**真金蝶端到端验证（5200）**：5月未取数→逐笔"未取数"；刷新5月→取回 科目余额400/1002=462/1012=24/四类=490；5月逐笔=462笔；**切6月未刷新→"未取数"、绝不用5月的462笔**（根治串月）；切回5月→462笔仍在、0.03s读库不重取（取数定格）。浏览器实测：6月数据接入卡显示"银行流水未上传/金蝶未取数"、6月逐笔显示未取数横幅、5月逐笔加载真数据无横幅。测试写入的 period_inputs(4行)/审计已清库、本地 config 复位 sample/6月。**注意**：新表 period_inputs 重启自动建；旧全局 `bank_import_dir` 字段保留兼容（渠道等仍读它当本期目录）。改动 db.py/app.py + 4 前端视图，新包 index-BrpbownD.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.87** | 2026-07-11 | **【成本台账】前端加「一键金蝶取数」按钮——物料级收发存 API 直取（双通道）**。承 Q7 深挖闭环：存货收发存汇总表可经**报表接口 `GetSysReportData`** 取数（非 ExecuteBillQuery），实测打通并接进工具。**①后端 `kingdee_client.py`**：新增 `fetch_inventory_summary(year,period)`——调 GetSysReportData 取「存货收发存汇总表(按日期)」`HS_INOUTSTOCKSUMMARYBYDATERPT`（固定口径：核算体系 `KJHSTX01_SYS`/组织 `107`/会计政策 `KJZC01_SYS`/币别 `PRE001`/日期区间+年期；字段 期初FInit·收入FReceive·发出FSend·结存FEnd×Qty/Amount），金额去千分位逗号、剔除无批号的小计行；+ `fetch_material_categories()` 从物料档案 `BD_MATERIAL` 取 (名称,规格)→存货类别 `FCategoryID.FName`（按日期报表无存货类别字段，靠档案关联），按(名称+规格)join。**②后端 `app.py`**：新增 `POST /api/cost-ledger/analyze-kingdee`——API 取物料级收发存 + 科目余额(`_inv_gl_balance`)→ `build_cost_ledger` → 返回三道勾稽/透视/异常，带 `source:kingdee`。**③前端 `CostLedger.jsx`**：第①步数据接入改**双通道卡片**——🅰一键金蝶取数(免上传·物料级)/🅱上传报表(精确归零·带仓库维度·正式底稿)；三道勾稽屏在 source=kingdee 时显条状说明(物料级/类别口径微差/仓库级走上传)。`api.js` 加 `analyzeCostLedgerKingdee`。**口径与限制（实测）**：API 物料级=**收发存自平 0、原材料/周转材料科目账实精确归零**；**库存商品科目差 27.67 元**——个别产成品/自制半成品物料的存货类别在物料档案与存货核算口径不一致（无可用物料编码做精确 join，只能名称+规格），故 API 通道的账实勾稽做不到分毫、正式月结仍用上传通道；**仓库级明细取不到**（按日期报表无仓库维度；跨维度只有"明细表"HS_NoDimInOutStockDetailRpt=单据movement级、运行结存不按批号重置、聚合翻倍，无法还原仓库结存——待金蝶顾问配"带存货核算类别+按仓库分组"的报表方案再上）。**真机端到端验证（5210 dev，浏览器+真金蝶）**：点「一键金蝶取数」→ 2768 行/0 未匹配类别、收发存自平✓、账实原材料/周转0·库存商品−27.67、期末结存¥10,958,078.92、总账¥10,994,036.02、source 说明条正确显示。**在 V2.86 基线上增量开发**（并行会话已把工作台重构为月结看板/常规对账/报表与台账/其它小工具+模块门禁，成本台账挂"报表与台账·待验收"），我的改动纯新增未碰其重构。已同步 01（kingdee_client/app.py/api.js/CostLedger + 新包 index-D-3PbPwQ.js；01 import OK、三路由在位）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.86** | 2026-07-11 | **【平台】账号管理升级为「三工作台统一权限中枢」+ BP 板块级准入 + 敏感级别棘轮 + 岗位字段**（业务方：BP 有一个板块要给外部门看、但不能看 BP 其它内容；且账号一多分不清、权限想一处统管三台）。**核心主张=中台发证、各家验票**：核算门户「账号管理」成为三工作台（核算/BP/法务）唯一发证处，BP（独立进程，反代 /bp/）降级为「听中台的话、来查证放行」的执行方。**①后端 `db.py`**——`CAP_META` 重构为**单一注册表**（每条带 `ws/kind/group/sensitive/plan`，加权限点只改这一处、账号页自动渲染）；`_SENSITIVE_CAPS`/`_ENTER_CAPS`/`WS_CAPS` 全部从注册表**派生**（不再手维护并列清单）；补 BP 板块码 `bp:board:<navTree叶子key>`（报表工作台/电商定价/驾驶舱/销售预算/基础数据…，与 BP 侧合同码一一对齐）+ `kingdee:fetch`；`default_perms` **收紧**为「只默认开本人所属工作台的非敏感功能权限 + 本组准入」，跨台板块/准入与敏感一律默认不给（外部协作账号=全不给，逐项授）；`_legacy_perms` 老账号兼容也只给核算非敏感、堵住跨台；`users` 表加 `post`（岗位·识别标签不影响权限）+ 老库自动迁移；新增 `bp_perm_codes`（透传给 BP 的板块码集，主管理员=`*`、无 enter_bp=空）、`escalated_caps`/`is_sensitive`/`set_cap_escalated`（敏感级别加严，存 `app_settings['sensitive_escalated']`）、`_backfill_missing_perms`（存量账号补 `bp:board:*` 缺键、为反代透传做准备，幂等）。**②后端 `app.py`**——新增 `/api/bp-authz`（Nginx auth_request 探针：校验 BP 准入、把该用户 BP 权限码回吐到响应头 X-BP-User/X-BP-Perms；未登录 401、登录但无 enter_bp 403=**堵掉"登录即进整个 BP"窟窿**）；新增 `/api/perms/sensitivity/toggle`（仅主管理员，单向加严/降回，代码定死项拒绝——棘轮）；`/api/perms/caps` scope 加 `escalated`；建号接口收 `post`；启动跑 `_backfill_missing_perms`。**③前端 `UserAdmin.jsx` 全量重写**（对齐样机 v6）——全屏三工作台版；每台块头=**准入总闸**（关则下面板块权限整块收起、开才放出）+ 任命子管理员开关（敏感）；板块权限**横排勾选**、常规/敏感高亮并区分 🔒敏（代码定死）/⤴敏（经理加严）；左栏账号按 **5 类分区可折叠**（主管理员/子管理员/普通/外部门=分组「外部协作」判定/禁用一律沉底）+ 搜索置顶；新建含**岗位**（建议+手填）；主管理员专属「⚙ 敏感级别设置」抽屉（🔒系统固定锁定 + 常规可单向升敏）。`api.js` 加 `setCapSensitivity`。**④BP 侧接线（改隔壁「01 财务BP工作台集成」项目，记 BP 自己台账）**：`auth.py` 注册 `bp:board:*` 板块码；`main.py` 各 router 挂 `require_perm(板块码)`（master/journal/finnote/revenue/revenue-ledger/pricing/performance/budget，receivable 不网关因写口已受 kingdee:fetch）；`App.jsx`+`Sidebar.jsx` 按 `canSee`（读 /api/me 的 X-BP-Perms）隐藏无权板块并落地到首个有权板块。**因 BP `FALLBACK_PERMS="*"`，网关为惰性——未接头前全放行、接头后才生效，落地零风险。** **⑤Nginx**（配置已写入 `03_Source_Materials/00_平台通用/服务器部署_反向代理接入BP_清单.md`，**待服务器手动贴 + reload**）：auth_request 由 `/api/me` 改 `/api/bp-authz`，`auth_request_set` 取出 X-BP-Perms 覆盖注入到 /bp/api/（剥离客户端伪造头），并按 enter_bp 放行 /bp/；生产给 BP 设 `BP_FALLBACK_PERMS=""`。**验证（本机 5200 实登 管理员/admin888 端到端）**：账号页三工作台/准入总闸开合/5 类分区（含真实外部账号 姚少玲·外部协作 正确归外部门）/岗位显示均正常；BP 网关 TestClient——仅授 budgetCockpit 的用户访 master **403**、访 performance **放行**、无头 fallback* 放行；`/api/bp-authz`（admin）返 200+`X-BP-Perms:*`；`bp_perm_codes` 外部→`[budgetCockpit]`、BP组→全板块、核算无准入→`[]`；棘轮——升 bank_upload→chip 变 amber+⤴敏（DOM 实测 `ua-chip on sens`/文本含 `⤴敏`）、降代码定死项 close_period **被拒**；测试用加严已还原、真配置 `escalated:[]` 干净。两前端(核算+BP) build 均通过。新包 index-CNpI3Oyx.js，同步 01→02。**待办**：a) 服务器贴 Nginx 新配置启用板块隔离（外部门才真锁死）；b) 敏感级别加严 UI 已含；c) BP 侧改动登其自身台账。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.85** | 2026-07-11 | **【平台】自建模块表单补「分组可选 + 状态可选」**（业务方："为什么只能归属其它小工具、状态应可选"）。承 V2.84——后端 add-module 本就接受任意 grp 与任意 status，是 V2.84 前端图省事写死了（grp 默认「其它小工具」的 datalist 看着像不可改、status 硬编码「开发中」）。**改 `Settings.jsx` 自建表单**：①分组由 datalist 改为 `<select>`（列出全部现有组 月结与结账/常规对账/报表与台账/其它小工具 + 「＋新建分组…」选项→展开文本框输入新组名）；②新增状态 `<select>`（五态全给：敬请期待/开发中/待验收/等待部署/已上线，选色，默认开发中）；③提示语按所选状态动态说明（选到「待验收/已上线」这类可进入状态会提示"可进入但没接代码会显示规划占位"）。`nm` state 加 `newGrp`/`status`（仍在早退前声明，守 hooks 规则）。**验证**：真机 5200 实登端到端——表单分组下拉出四组+新建分组、状态下拉出五态；实测新增「预算管理」到「月结与结账」组·状态「敬请期待」→落库 grp=月结与结账、status=敬请期待、不可进入、侧栏归入月结与结账组；删除清理、库无残留（含清掉早前测试误建的 key='key'）。**踩坑**：脚本首测抓到别的模块行的状态 select（每行都有状态下拉、都含"敬请期待"），改为限定在自建表单容器内定位后正确——代码无误，是测试选择器需按容器收窄。新包 index-C0QxlNcX.js，同步 01→02。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.84** | 2026-07-11 | **【平台】导航模块清单改为「代码持有内置 + 主管理员自建」的单一真相源（业务方要求"设置页能自己增删模块、不用每次找我写代码"）**。**根因**（承 V2.83）：侧栏(前端硬编码 `SECTIONS`)与管理面板(后端 `NAV_MODULES`)是两份清单、易漂移。**本次重构**：**①后端**——模块清单 = `NAV_MODULES`(内置、代码持有、`builtin:True`) + 用户自建(存 `app_settings` 的 `nav_user_modules`，`builtin:False`)，`_all_modules()`/`_all_groups()` 合并；`/api/nav-modules` 返回带 builtin 标记的合并清单；新增 `/api/nav-modules/add-module`(key 限 `^[a-z][a-z0-9_]{1,23}$` 当路由用·自动小写·唯一·不撞内置，默认「开发中」不可进入) 与 `/del-module`(仅删自建，**内置不可删**——要隐藏内置设「敬请期待」)，均限主管理员。**②前端**——`Sidebar.jsx` 改为从 `navDef`(API 下发的 modules+groups)渲染工具组（图标按 key 映射、自建落默认图标，reconcile 子页映射保留，底部基础数据/设置按 `通用` 组排除后单独钉底）；`App.jsx` 存 navDef 传侧栏 + 为"自建但未接代码"的可进入视图兜底「规划中」占位（不白屏）；`Settings.jsx` 加「+ 自建模块」表单(名称/key/分组 datalist)与每行删除链接(内置显示「内置」不可删)。**踩坑（真机验证抓出，纯看代码没发现）**：①`Settings` 新增的 `useState(nm)` 误放在 `if(!d)return` 早退**之后**→React error #310(hooks 顺序)白屏，移到组件顶部修复；②侧栏折叠 `toggle` 用 `!o[grp]` 而默认态改为"未记录=展开"，导致首次点折叠要点两下，改为 `cur=o[grp]!==false; !cur`。**验证**：①后端 HTTP 全链路（清单带 builtin、add 进清单且开发中不可进入、新组插通用前、key 大小写/撞内置/重复校验、设已上线→可进入持久化、删自建成功、删内置被拒）；②**真机 5200 实登（管理员/admin888）端到端**：侧栏从 API 渲染出四组+凭证归档、设置页加「测试规划模块」即时现于侧栏(开发中·灰显·cursor:default 点不进)、UI 删除链接删除成功、库无残留。新包 index-CppU2mHl.js，同步 01→02。**今后新增工具：我加内置 key 一处即侧栏+管理面板都有（不再漂移）；业务方可自助加规划占位/软删除，无需等开发。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.83** | 2026-07-11 | **【平台】导航模块上线管理补登凭证归档 + 基础数据/基础设置**（业务方反馈"设置里导航模块上线管理没法根据开发新增/删除"——命中真缺陷）。**根因**：模块清单是**两份各自维护**——侧栏 `Sidebar.jsx` 的 `SECTIONS`（前端，带图标）与后端 `app.py` 的 `NAV_MODULES`（管理面板数据源），新工具须两处同步；V2.77 加凭证归档进侧栏时漏了 `NAV_MODULES`，导致设置里管不到它（虽因 `modOn` 对未知 key 默认放行、能进，但状态不可控）。**本次修**：`NAV_MODULES` 补 `archive`(凭证归档@其它小工具·待验收) + `basicdata`(基础数据@通用·always) + `settings` 标签「设置」→「基础设置」；`NAV_GROUPS` 加「其它小工具」。设置页是读 `/api/nav-modules` 渲染，故纯后端改、免 build。**验证**：`_nav_state()` 现返回 archive={待验收·可进入·可挂岗位}、basicdata={已上线·always}，组含其它小工具。**说明「新增/删除」的真实语义**：模块与代码绑定，业务侧不能凭空加一个空模块；面板管的是**状态**（敬请期待→开发中→待验收→等待部署→已上线，只有后两者可进入）——把某工具设「敬请期待」即等于从侧栏隐藏（软删除）。真正的隐患是上述**两份清单易漂移**，已在台账标注；根治方案=让侧栏从 `/api/nav-modules` 取模块清单（图标仍前端按 key 映射），单一真相源，**待业务方确认后另立一版重构**。同步 01→02。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.82** | 2026-07-11 | **【凭证归档】增量四：Excel 批量导入（期初存量）+ 号段体检（接金蝶实测通过）**。至此工具功能齐活。**①批量导入**：`db.import_volumes(rows)` 逐行校验号段（每行即时提交，故库里天然累积本批已登行——校验与册序直接读库、无需批内缓存）、自动生成册号、`ensure_location_path` 按「档案室B › 3号柜 › 第1层」路径逐级建/找位置树（分隔符 ›>»/ 都认，类型按层级猜库房/柜/层/箱）；失败行不提交、逐行报因，改完重传不重复。接口 `/api/archive/import`（读 xlsx raw body、openpyxl 解析、限 archive_edit）+ `/api/archive/import-template`（openpyxl 生成模板，列=主体/年/月/凭证号起/凭证号止/存放位置/凭证类型/备注，带 2 示例行，`Response(content=bytes)` 下载，同既有导出模式）。**②号段体检**：`/api/archive/checkup?total=N` 用 `range_checkup` 找缺口，`total` 双模式来源——**手工填（会计眼见为实，合 feedback-dual-mode-human-check）** 或从金蝶取；`/api/archive/kingdee-count` 走新增 `kingdee_client.fetch_voucher_count`（取 GL_VOUCHER 该账簿该期间 FVOUCHERGROUPNO 最大值=当月凭证张数，只读 best-effort，连不上则报错回退手工）。**③权限**：导入限 archive_edit（总账会计）。**④前端**：`Archive.jsx` 加第五 tab「批量导入·体检」——左＝下载模板/上传/成功失败清单（含建位置数），右＝号段体检（选主体年月+填金蝶张数→绿"无缺口"或红列缺口段+"漏装订或册子丢了"提示）。**踩坑**：首版册序 `next_seq(db_seqs)+len(批内缓存)` 因即时提交导致双计（第2本变 -03），去掉批内缓存后连号正确（01/02）；号段重叠提示也因缓存重复列 4 次，一并修好。**验证**（真 HTTP 全链路，临时库）：①模板下载可 openpyxl 打开、表头正确；②上传 3 行 xlsx→2 成功(SZL2026-05-01/02 连号)+1 跳号失败+自动建位置树；③体检金蝶=120→缺 101–120、=100→无缺口；④**`/api/archive/kingdee-count` 实连本机金蝶成功、账簿101 2026-05 返回 603 张**（金蝶集成真机验证，非模拟）。前端 build 编译通过。新包 index-Cb_Kh648.js，已同步 01→02。**凭证归档五屏全功能上线：找凭证/登记新册/位置与转移/标签打印/批量导入·体检。确认书 v1.3 的 A 期范围全部交付。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.81** | 2026-07-11 | **【凭证归档】标签分「外壳横向 / 书脊竖向」两版**（业务方提"外壳做横向、编码最大、号段小字在下；册子沿用竖向那版"，采纳）。`Archive.jsx` Labels 重构为两种标签渲染：**书脊 SpineLabel**（竖向，默认 30×50mm，简码放大+尾号+号段拆行，承 V2.80）；**外壳 ShellLabel**（横向，默认 50×30mm，整册号 `SZL2026-03-02` 一行最大、号段/主体/期间小字在下——横向够宽故一行不拆）。外壳册号字号**按标签宽自适应**（`f=clamp(2.6, (shW-7)/(len×0.62), 5.5)`，`whiteSpace:nowrap` 防溢出）。UI：加「打哪种」切换（两种/只书脊/只外壳）+ 书脊、外壳各自宽高可调；预览分两组、打印时两组各自成页（`break-before:page`），且 `.lbl-noprint` 隐藏组标题、`.lbl-card` 去外框、白底黑字不变。**验证**：Edge headless `scale=3` 出**真实尺寸**三联图（书脊竖向 / 外壳横向 / 同本两张并排），首轮外壳 6mm 字号仍换行→压到 5mm+nowrap 后单行放下，目检两版均清晰；图归档 `03_Source_Materials/20260710_凭证归档/凭证归档_标签_书脊竖向+外壳横向_真实尺寸.png`。**验证边界**：本会话预览基础设施切到扩展代理模式、仅暴露 5200（正式库，无测试账号密码），临时后端 5207 扩展够不到，故**未在真应用内点击走查**；已凭真实尺寸出图（字号公式与代码一致）+ build 编译通过 + 该组件上一版(V2.79)曾真机验证，风险低。新包 index-CwheE7ty.js，已同步 01→02。**建议下次预览可用时补一次真应用截图；日常动线四屏功能不受影响。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.80** | 2026-07-11 | **【凭证归档】标签可读性修正——册号拆两行、简码放大、字号随宽缩放**（业务方质疑"30mm 塞得下 SZL2026-03-02 吗"，问得对）。**根因**：V2.79 的真实代码把册号 `SZL2026-03-02`（13 字符）挤成**一行**、字高仅 3.4mm，一米外发糊；而 V2.71 样机本是拆「SZL / 2026-03-02」两行的，落地时图省事塞回一行、是回归。**改法**（`Archive.jsx` 的 Label）：从主体档案取简码 `codeOf`，把册号切成 **简码行**（`SZL`，≈8.5mm 高、`letter-spacing`、最粗）+ **尾号行**（`2026-03-02`，≈4mm mono）；号段 `51–100 号` 加粗单列（找某号时扫这行）；主体/期间/册序/类型依次缩小。**字号随标签宽度线性缩放**（以 30mm 为基准，`k=clamp(0.75,w/30,1.4)`）——宽度调 40mm 字自动变大、调窄不会破版。合起来仍拼出完整册号，且每行都清楚。**验证**：Edge headless `--force-device-scale-factor=3` 出**真实 30×50mm 尺寸**对比图（旧一行挤 vs 新两行放大 + 30mm 毫米尺），目检新版简码醒目、尾号与号段清晰可读；图归档 `03_Source_Materials/20260710_凭证归档/凭证归档_标签可读性对比_30x50真实尺寸.png`。另附建议：书脊多在 4cm 以上可把宽度设 40mm。新包 index-nCulyjvR.js，已同步 01→02。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.79** | 2026-07-11 | **【凭证归档】增量三·标签打印屏（书脊+外壳双标签、按主体色预览、真实 mm、浏览器打印，真机通过）**。承 V2.78，纯前端（`Archive.jsx` 加第四 tab「标签打印」，无后端改动）。**功能**：①左侧尺寸面板——宽/高/每行几枚**可调不写死**（默认 30×50mm、5 列），显示「每本打印 2 张（书脊+外壳）」；②选册子——主体下拉 + 年份筛选 + 册号 chip 点选（点掉不打的，划删线）；③预览区——每本出**相邻两张**（书脊/外壳，内容相同），标签用**真实 mm 尺寸**渲染、按主体标签纸色着色（黄=星期零、蓝=深圳星期九…色来自主体档案）；④`window.print()` 打印，`@media print` 只显 `#lbl-sheet`、`.lbl-print{background:#fff!important}` 强制**白底黑字**（stylesheet `!important` 压过内联色，同物流计提打印屏成熟模式）+ `print-color-adjust:exact` + A4 8mm 边距。**落主张**：预览显色只为「该抽哪种纸」参考，实际彩色靠换标签纸、打印不喷底色。**验证**（真机临时库，两主体三册）：标签屏出 6 枚=3 本×2、每本书脊+外壳成对、SZL 黄 SZJ 蓝着色正确；筛主体「深圳星期零」→4 枚；点掉一本 chip→2 枚；改宽 30→70mm 首标签实测 width:70mm 生效。截图工具遇金蝶请求挂起，用 preview_eval 读 DOM 验。新包 index-DtZiLihW.js，已同步 01→02。**至此凭证归档四屏闭环：找凭证 / 登记新册 / 位置与转移 / 标签打印，日常动线可用。剩：Excel 批量导入（期初存量）+ 号段体检接金蝶（这两项为增量四）。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.78** | 2026-07-11 | **【凭证归档】增量二：位置与转移屏（位置树维护 + 批量转移 + 借出归还 + 销毁双人，真机端到端通过）**。承 V2.77。**①后端新增**（`db.py`）：`borrow_volumes`（借出→状态借出中、位置转「外借›借出人」自动建节点、记 loc_before）/`return_volumes`（归还→在库、位置自动回借出前）/`destroy_apply`（销毁申请→建终态「销毁批次」节点、册子转待销毁、必填审批单号）/`destroy_cancel`（撤回→回在库）/`destroy_execute`（执行→已销毁终态）；`_ensure_borrow_node` 幂等建「外借」根+借出人子节点。**②接口**：`/api/archive/{borrow,return,destroy/apply,destroy/cancel,destroy/execute}`——前四个限 `archive_edit`（总账会计），**销毁执行单独限 `archive_destroy`（财务经理）**，两人四眼落到权限层。**③前端**（`Archive.jsx` 加第三 tab「位置与转移」）：左＝位置树（按 path 深度缩进、节点类型徽标、每节点在册数、点选筛选、底部建位置节点表单）；右＝该位置册子表（勾选）+ 操作面板（批量转移选目标+原因 / 借出填借出人应还日期+归还 / 销毁填审批单号+申请+撤回+仅财务经理见执行键）；**并发冲突整单拦截卡**（列冲突册号、刷新按钮）；成功 toast。销毁执行键仅 `canDestroy` 可见、点击二次确认。**踩坑**：`archiveVolumes('','','')` 拼出 `?year=` 空串，后端 `year:int` 校验 422 → 列表空白；改 api 用 `URLSearchParams` 只带非空参。**验证**：①后端借出/归还/销毁全流程单测（借出→外借节点、归还→自动回原位、申请→待销毁且不参与普通转移、撤回→回在库、执行→已销毁终态且不可再转移、轨迹5段完整）；②真机 UI（临时库：建四级位置树→勾两本转移到箱→状态自动变「已装箱」+toast→借出一本→借出中@外借节点→归还→自动回箱复原已装箱）。截图工具遇金蝶请求挂起，用 preview_eval 读 DOM。新包 index-BjslZWoU.js，已同步 01→02。**下一增量：标签打印（书脊+外壳双标签按主体色）+ Excel 批量导入 + 号段体检接金蝶。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.77** | 2026-07-11 | **【凭证归档】工具本体·增量一：内核 + 数据层 + 找凭证/登记新册两屏（真机端到端通过）**。确认书 v1.3 签署后正式开发。**①数据层**（`db.py`）：新增三张表 `arch_locations`（位置树·邻接表，任意深度）/`archive_volumes`（凭证册，表名与 `org_code_locked` 探测一致）/`arch_transfers`+`arch_transfer_items`（转移单+明细），纯新增不改既有表。编排函数：`list/save_location`（含从根拼 path、按节点统计在册数）、`register_volume`（自动生成册号/算册序张数到期/登记前号段校验/写登记入库轨迹）、`find_volume`（主体+年月+号→册）、`range_checkup`（对金蝶找缺口）、`transfer_volumes`（乐观锁：提交前比对现况位置，冲突整单拒、一本不动；待销毁/已销毁不参与；终态位置拒入）、`volume_trail`（轨迹）。**②内核**（`kernels/archive.py`，纯函数便于单测）：`make_vol_no`（简码+年+月+册序，转大写）、`next_seq`、`check_range`（重叠/跳号，出会计白话）、`range_gaps`（号段体检）、`display_status`（在库+箱→已装箱，派生不存储）、`keep_until`（+30 年可配）、`transfer_conflicts`、`can_transfer_into`。**③权限**：CAP 新增 `archive_edit`（登记/转移/借出/销毁申请=总账会计）、`archive_destroy`（销毁执行=财务经理，列入 `_SENSITIVE_CAPS` 默认不授），并入 `WS_CAPS.accounting` 供子管理员分配。**④接口**：`/api/archive/{orgs,find,volumes,period-info,register,locations,locations/save,transfer}` 八个，写操作限 `archive_edit`。**⑤前端**：新建 `views/Archive.jsx`（找凭证＝三件套查询→命中大卡：30px 册号+位置+身份栏+轨迹；登记新册＝起号自动接上一本、人只填「止号」、册号与张数实时预览、标签按主体色着色预览），挂侧栏「其它小工具」组、**删除 `_soon`「敬请期待」占位**，`App.jsx` 加 `archive` 路由与落地页白名单。**验证**：①内核 24 条断言全绿（册号/号段重叠跳号/体检缺口/派生态/冲突/终态）；②数据层端到端（建四级位置树→登记两本→重叠与跳号与无简码三拦截→查第85号命中03-02带位置轨迹→号段体检缺口→批量转移成功+终态拦截+并发冲突整单拒→轨迹2条）；③真机 UI（临时空库，管理员补简码 SZL+黄→进凭证归档→登记新册填止号自动出册号 SZL2026-03-03/50张/起号101→保存成功→找凭证查第120号命中该册带身份+位置+轨迹）。截图工具遇金蝶请求挂起，用 `preview_eval` 读 DOM 验。新包 index-DOvGY4Cf.js，已同步 01→02。**下一增量：位置树维护 + 批量转移 + 借出归还 + 销毁双人 + 标签打印 + Excel 批量导入 + 号段体检接金蝶。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.76** | 2026-07-10 | **【凭证归档】样机标签屏更新为「书脊+外壳双标签 + 按主体色预览」**（设计稿，无代码改动；补 V2.75 遗留的样机屏）。`凭证归档工具_样机_v1.html` 的 `#labels` 屏三处改：①尺寸面板加「每本打印＝书脊+外壳各一张（共2张）」与「标签纸颜色＝星期零·黄（来自主体档案）」两行，打印按钮改「打印 15 本·30 枚」；②1:1 放大区由单张改为**书脊/外壳两张并排**（内容完全相同、均 30×50mm、都着黄纸色，各带「贴·书脊」「贴·外壳」角色胶囊），配文点明"两张一模一样只是贴处不同""一盒装 N 本就在盒上贴 N 张、那排即箱内清单""颜色只是纸色、打印仍黑字、靠换纸不喷底"；③A4 排版由单张网格改为**成对**（每本相邻两张=书脊+外壳）并**按主体着色**（星期零黄/深圳星期零蓝/上海分公司绿），顶部加色板图例，底注"预览着色只为分纸参考、实际打印黑字"。CSS 新增 `.bigl.paper/.tag.paper`(--paper 底色)、`.rolechip`(角色胶囊)、`.swatch`(色板条)。重出 `凭证归档_样机_标签打印屏_1920.png`(1920×1426)，目检通过：双标签同色并排、A4 三主体三色成对、图例与文案齐。至此样机与 V2.75 代码口径一致。**下一步：正式开发凭证归档本体（内核 archive.py + 找凭证/登记新册屏）。** | Claude / c | In Review | —（设计稿任务，无修订文件夹） |
| **V2.75** | 2026-07-10 | **【平台/凭证归档】主体档案加「标签纸颜色」+ 确立「一本两张标签（书脊+外壳）」口径**（本条=颜色代码落地；两张标签为设计口径，样机屏后补）。业务方提两点：①每个主体配一种颜色，对应一种颜色的标签纸，便于区分（预览显色、打印黑白靠换纸）；②一本册子打两张——一张贴册子书脊、一张贴所在外壳；1:N 时（一盒装多本）就在外壳上按本贴 N 张。**①颜色**：`orgs` 表加 `color`(#RRGGBB) 列，`_ensure_org_color_column()` 幂等给 V2.74 已建的老表补列（create_all 不改既有表，照 `_ensure_perms_column` 先例）。后端色板 `LABEL_PALETTE`（白/黄/粉/蓝/绿/橙/紫/灰 八种市售浅色，经 `/api/orgs` 下发，前端不另抄）。三道校验：`#RRGGBB` 格式、**颜色唯一（两家同色纸就分不出）**、**WCAG 对比度护栏**——黑字压底色对比度须 ≥7:1(AAA)，深色被拦（八色板实测 15.6–21:1 全过，#1A1A1A 这类被拒并提示"黑字看不清"）。**明确"打印不出底色"**：纸本身彩色，再喷底既费墨又把黑字压灰——预览显色、打印只出黑字。前端 `BasicData.jsx` 加 `ColorPick`（色板九宫格+不设色+自定义 #hex 兜底），行左侧 4px 色条便于扫视，页内说明补颜色与"一本两张"用法。**②两张标签口径**：外壳那张本质是位置信息（册子换盒要撕旧贴新），不违反"标签只印身份"（撕的是外壳那张、册子那张永不动）；故设计上转移完成后提示"为新箱补印 N 张外壳标签"、支持只补印外壳份。两张内容相同、均 30×50mm，一次出 2N 枚、两两相邻便于揭贴。**验证**：①内核用例——老库补列后可读、八色板对比度、存 #fff3b0 转大写回读、非hex/过深/撞色三拦截；②临时空库端到端——色板随接口下发、点选色器选「黄」保存后落库且行左色条变 rgb(255,243,176)、给第二主体也选黄被拦且库未污染。截图工具遇金蝶请求挂起（同前），用 `preview_eval` 读 DOM 验。新包 index-DmdYxW2T.js，已同步 01→02。**下一步：样机标签屏补「书脊/外壳双标签 + 按主体色预览」，及物流计提账簿代码切换（增量二·六项回归）。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.74** | 2026-07-10 | **【平台】基础数据 › 主体档案（增量一：表 + 页 + 种子）**。依《主体档案_设计与影响分析 v1.1》，D1–D3 获批（编辑限主管理员 / 银行对账主体归一后置 / 简码 `[A-Z0-9]{2,4}`）后开工，**本增量只做主体档案，不碰物流计提**（切换与六项回归留增量二）。**①数据层**：`db.py` 新增 `orgs` 表（全称/简称/简码/金蝶账簿代码/别名/启用/备注/留痕，`short_name` 唯一约束）+ `list_orgs`/`save_org`/`delete_org`/`book_code_lookup`/`org_by_alias`/`org_code_locked`/`seed_orgs`。校验：简称非空且唯一、简码 `[A-Z0-9]{2,4}` 且唯一（存前自动转大写）、账簿代码唯一、**别名不得撞上他人的简称或别名**（撞了会把两家公司认成一家）。`book_code_lookup()` 把简称与每个别名都映到同一 `book_code`——台账写「星期零」也能取到 101（为增量二的 R-5 铺路）。`seed_orgs()` 幂等迁入物流计提原硬编码三条（深圳星期零=101/深圳星期九=105/孝感星期九=107），全称与简码留空待超管补。**②简码锁死**：`org_code_locked()` 查 `archive_volumes` 是否已有该主体的册子——**有则简码不可改、该主体不可删**（册号已印在标签上贴到书脊）；凭证归档表尚未建时 try/except 返回 False，不影响现状。**③接口**：`/api/orgs`（登录可读，附 `locked` 标记）、`/api/orgs/save`、`/api/orgs/delete`（均限主管理员 `_admin`，走 `audit` 留痕）。**④前端**：新建 `views/BasicData.jsx`（tab 骨架 + 主体档案行内编辑表，锁定行简码格渲染为 `🔒 SZL` 只读带 title 提示，非管理员整表只读）；`Sidebar.jsx` 底部新增「基础数据」并把「设置」显示名改为「**基础设置**」（`view` 键 `settings` 不变）+ 新增 `IC.basicdata` 图标；`App.jsx` 挂 `basicdata` 路由并加进"模块下线时不误踢"的白名单；`api.js` 加三个封装。**踩坑**：`_split_aliases` 原只收字符串，而 `list_orgs()` 返回的 `aliases` 是列表——前端把整行原样回传时 `str(list)` 被当串切逗号，别名会碎成 `['星期零'`。改为列表/字符串都收，并补 round-trip 回归。**验证**：①内核用例全绿（种子幂等、别名归一、七条校验拦截逐条命中、锁死后其余字段仍可改、锁死后拒删、停用主体不进 lookup）；②起临时空库实例端到端点验——侧栏渲染出「基础数据/基础设置」、种子三行落表、页面填 `szl`+中文逗号别名保存后落库为 `SZL`+两个别名、撞简码与撞别名均被拦并弹红条且**库未被污染**、塞入一本册子后该行简码格变 `🔒` 且直接调 API 改简码同样被拒（不只是 UI 挡）；③主库启动种子写入三行、`users`(5) 与 `logistics_tax_rates` 未受影响。截图工具遇金蝶请求挂起（同 V2.67），改用 `preview_eval` 读实际 DOM。新包 index-Dj9bpOuL.js，已同步 01→02。**下一步=增量二：物流计提 `BOOK_CODE` 硬编码切到本表 + 六项回归（核心 R-3：切换前后凭证 JSON diff 为空）。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.73** | 2026-07-10 | **【平台】《主体档案 · 设计与影响分析 v1.0→v1.1》出稿 + 【凭证归档】确认书升 v1.3**（纯文档，未动任何代码）。起因：业务方要求把凭证归档确认书 v1.2 的「待补 D · 主体简码对照表」**做成可维护页面**。**探查发现工作台根本没有统一的主体档案，主体口径散在三处且互不相识**：①银行对账权威台账 `主体` 列是自由文本；②物流计提把 主体→金蝶账簿代码 **硬编码**在 `kernels/logistics_accrual.py:68`（`BOOK_CODE={"深圳星期零":"101","深圳星期九":"105","孝感星期九":"107"}`，写金蝶时取 `FACCOUNTBOOKID`）；③凭证归档若自建简码即成第三份真相。**关键事实**：BOOK_CODE 的键是「深圳星期零」，台账里可能写「星期零/深零」，两边现靠人脑对号——而册号是要**印在标签上贴到书脊**的，印错即几百张重贴。**故方案=平台级 `orgs` 表**（全称/简称/简码/金蝶账簿代码/别名/启用，三者各自唯一 + 别名不得跨主体冲突）。**业务方四项批注**：**D4** 主体档案不塞进「设置」页，而是**照财务BP工作台骨架新建独立「基础数据」页**（侧栏底部与「基础设置」并列，现有「设置」改名；主数据用 tab 承载，主体档案为首个 tab）——分野=「设置是工作台怎么跑，数据是公司长什么样」；**D5** 物流计提「税率表」**不收进基础数据**（费用类型税率属工具口径参数、非组织主数据，且会计就地维护不必跳页），`LogisticsAccrual.jsx` 本次零改动；**D6** **本次即把硬编码账簿代码切到新表**——`logistics_post` 在权限体系里标为 `_SENSITIVE_CAPS`（写金蝶正式账套），`FACCOUNTBOOKID` 取错=凭证录进别的公司的账，故**不做静默回退**（表缺该主体即拒绝录入、报原文案，绝不 fall back 旧硬编码）+ 幂等种子写入三条既有映射 + **六项回归清单，核心 R-3「切换前后凭证 JSON diff 为空」**；**D7** **简码一经生成过册号即锁死**（保存时若该主体已有册子则 `code` 变灰只读）→ 推论 **R10：主体档案定稿是凭证归档的开发前置依赖，执行顺序不可颠倒（先建档案→再切物流计提过回归→才开册号生成）**。银行对账台账主体归一**不搭本次车**（牵动已验证的 `account_ledger.match_account` "账号数字+主体"消歧桥），列后续独立评估。风险新增 R9–R14（R14=导航改名/新增仅动显示标签，`view` 键 `settings` 不变、不影响 `App.jsx:31` 进入判定与导航状态机）。文档落 `03_Source_Materials/00_平台通用/主体档案_设计与影响分析_v1.0/v1.1_20260710.md`；凭证归档确认书同步升 **v1.3**（待补 D 划掉改为前置步骤、§9 补平台级配套与回归要求、风险补 R10、签署栏加 v1.3 增补同意），v1.3 存 01，v1.2 归档回 03。**D1–D3 待批（主体档案编辑权限=仅超管？银行对账归一是否确认后置？简码格式 `[A-Z0-9]{2,4}`？）；批准后按影响分析 §7 三步执行。** | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.72** | 2026-07-10 | **【凭证归档】需求确认书 v1.2 签署通过 + 工具定位落定 + 样机侧栏改版**（文档签署 + 设计稿，无项目代码改动）。业务方批示「可以了，这个归属于核算工作台的其它小工具」→ **两项最终批注**：①**工具定位由"与银行对账/物流计提/成本台账并列的主工具线"改为挂侧栏「其它小工具」组**（与月结与结账/常规对账/报表与台账三组并列），本工具为该组首个真实成员；②**去掉该组现有 `_soon`「敬请期待」占位项**（`Sidebar.jsx:37`）。样机侧栏据 `Sidebar.jsx` 真实 `SECTIONS` 结构重绘（原样机自拟的「凭证档案/ERP对账程序」组名与现行不符，已改为四组真实结构 + V2.66/V2.67 的可折叠底色条样式），凭证归档挂「其它小工具」下高亮，五屏高清图全部重出并目检通过。**确认书 v1.2 = v1.1 + 定位调整 + §4.1「两条录入通道」（Excel 批量导入=期初存量一次性；登记新册=日常装订完当场录，起号自动接上一本、全表人只填一个"止号"、存前三条体检）+ 受影响文件补 `ArchiveRegister.jsx` 与 Sidebar 占位删除 + 签署栏**。**状态置为 ✅ 已签署基线（Approved Baseline · 2026-07-10）**，按治理转入 `01_Current_Deliverables/docs/20260710_凭证归档/`；v1.0/v1.1 草稿与样机留存 `03_Source_Materials/20260710_凭证归档/`。至此凭证归档线过会材料集齐：确认书 v1.2（签署）+ 五屏样机 + 五张 1920 图。四条「待补」（标签纸规格/法定保存期年数/AB期切分/主体简码对照表）经业务方确认不阻塞开发，各有兜底方案。**下一步：提交开发六件套（版本提案/受影响文件/开发计划/风险/交付物/变更记录）等批准后开工。** | Claude / c | Synced | —（文档签署 + 设计稿，无修订文件夹） |
| **V2.71** | 2026-07-10 | **【凭证归档】样机补第 5 屏「登记新册」+ 高清图**（设计稿，无代码改动）。业务方看图后指出：**Excel 导入解决存量，那当下装订完的册子怎么录？**——确认书 §4 虽已列「单本录入 + Excel 批量导入」，但样机只画了一个「登记新册」按钮、未出屏，属样机疏漏。补 `#new` 屏（分段器插到第 2 位，五屏为 找凭证/登记新册/册子台账/位置与转移/标签打印）。**核心不是做一个普通表单，而是把"唯一会错的地方"压到最小**：装订完的那一刻系统已知上一本止于几号，故——顶部四步动线条（凭证装订完成→①录这一本→②系统生成册号→③打印标签→④贴上书脊放进柜子）；①「这一本是谁」自动带出「该主体该期间已登记 1 本 SZL2026-07-01 收录 1–62 号 → 本册起号自动接 63、册序自动排第 2 册」，**起号/张数(=止−起+1)/册序/凭证类型/保存到期全部自动填（灰色虚线框标注），全表人只填一个数字：这一本装到第几号为止**；②「放到哪里」位置树选择 + 三枚快捷片（沿用上一本的位置/选别的位置/先不放暂存待上架，连录多本时位置只选一次）；③「存之前系统先体检」三条实时校验（✓号段不重叠——不会"一个凭证号查出两本"；✓号段不跳号——上一本止 62、本册起 63 首尾相接；ⓘ金蝶核对——该期间共 118 张，本册收至 118 号，本月号段完整闭合无漏装订）；④册号预览大卡 `SZL2026-07-02` 带四段拆解 + 「保存并打印标签」「保存并继续录下一本」双出口。右栏＝标签实时预览（边填边变）+ 两张白话卡：「**Excel 批量导入=只用一次搬存量；登记新册=天天用，录入发生在装订那一刻，台账才不会失真**」「为什么起号要自动填——人翻册子会翻错，系统一定知道」。新增高清图 `凭证归档_样机_登记新册屏_1920.png`(1920×1017)，其余四屏因分段器变化一并重出。目检五屏全通过。 | Claude / c | In Review | —（设计稿任务，无修订文件夹） |
| **V2.70** | 2026-07-10 | **【凭证归档】静态样机 v1 + 四张 1920 高清图**（纯设计稿，无代码改动；治理「UI 前置」流程，配合确认书 v1.1 签署）。单文件自包含 HTML `凭证归档工具_样机_v1.html`（双击即看，URL # 切屏），设计令牌与银行对账样机 v3 / 成本台账样机 v1 完全一致（同 `:root` 变量、冷灰 Linear 风、侧栏新增「凭证档案」组与既有三组并列）。**四屏**：①**找凭证**（主屏）——三件套查询（主体+年月+凭证号，标 `*` 不可省，附白话说明"每个主体每年都有自己的第1号，少填一项会查出好几本"）→ 命中大卡：34px 等宽册号 `SZL2026-03-02` + 位置面包屑（档案室B › 3号柜 › 第2层 › 2026年03箱，末节点高亮）+ 双栏「这一本的身份（终身不变·印在标签上）」/「这一本去过哪里（4 段真实感轨迹：初次登记→借出天健事务所→归还自动回原位→装箱）」+ 底部 5 个 KPI；②**册子台账**——顶部红色《号段体检》告警（星期零 2026-01 第 96–100 号无册覆盖，配三段式号段条，斑马纹缺口块）+ 状态切片器 + 8 行册子表（含借出逾期 12 天、号段缺口红态、外仓箱、他主体 SZS）+ 白话注解「已装箱不是人工填的状态，是系统算出来的」；③**位置与转移**——左侧任意深度位置树（库房/柜/层/箱/外仓/临时点/外借/销毁批次·终态，带本数徽标 + 拖放提示条「整箱 42 本一起搬走」）+ 右侧本层册子表 + 批量转移抽屉 + **并发冲突拦截卡**（"其中 5 本在你勾选之后被李出纳挪走了…若继续提交台账就开始骗人了"，列出 5 个册号 + 三个补救按钮）+ **销毁两人四眼卡**（①陈会计申请·销毁字第7号 ✓ →②待财务经理复核确认 →③置为已销毁·只读留痕）；④**标签打印**——左侧尺寸面板（预设 30×50mm，宽/高/每行/每列/页边距/间距**全部可调不写死**，输出通道 A 期=浏览器打印、B 期=标签打印机）+ 1:1 放大标签（四行：册号/主体期间/凭证号区间/册序，配卡尺标注 30mm×50mm）+ 绿框主张「标签上刻意不印存放位置」+ 三行核对清单（✓册号 ✓号段 ~~✗ 位置——不印，去系统查~~ 划删线）+ A4 不干胶排版预览（5列×10行=50枚/页，15 枚实样 + 空位）。**踩坑**：标签格子类名 `.lb` 与表单标题类名 `.lb` 撞名，`aspect-ratio:30/50` 把查询三件套撑成三个大方块——首轮出图即目检发现，标签格改名 `.tag` 后复现正常（教训：单文件样机里的短类名要防碰撞）。高清图 Edge headless 1920 宽出图、PIL 按背景色自动裁到内容底边：`凭证归档_样机_找凭证屏/册子台账屏/位置与转移屏/标签打印屏_1920.png`（976/976/976/1364 px 高），四屏均目检通过。产出落 `03_Source_Materials/20260710_凭证归档/`。**待业务方看图反馈后升版样机或直接签署确认书 v1.1 进开发。** | Claude / c | In Review | —（设计稿任务，无修订文件夹） |
| **V2.69** | 2026-07-10 | **【凭证归档】需求确认书升版 v1.1 —— Q1–Q8 首轮批注落定**（纯文档，未动任何代码）。业务方逐条批注：**Q1** 目前只归档记账凭证，册号不加类型段（维持四段 `SZL2026-01-02`），表内保留 `凭证类型` 字段备扩展；**Q2** 书脊实测 3–5cm，标签默认 **30×50mm**，且**尺寸不写死、设置页可自定义**宽/高/页边距/行列数并实时预览——B 期对接标签打印机时排版逻辑零返工；**Q3** 登记/转移/借出归还=总账会计，**销毁=总账会计申请 + 财务经理执行**（两人四眼，拆为「待销毁」可撤回中间态 + 终态「已销毁」，新增权限位 `archive_edit`/`archive_destroy`，职责分离）；**Q4** 借出归还登记纳入 A 期（含借出人/应还日期/逾期标红/归还自动回原位），理由=不登记则「被借走」与「丢了」无法区分，而那正是最需要工具的时刻；**Q5** 业务方要求保留「已装箱」共五种状态——**采纳但改为由位置派生、不存字段**：存储四态（在库/借出中/待销毁/已销毁），显示态在「在库 且 当前位置节点类型=箱」时自动呈现「已装箱」，筛选器照常可筛。理由=若独立存储，取出箱子改位置忘改状态则系统一边说「已装箱」一边说「在3号柜」，两个都是系统说的、无从取信；派生后事实来源唯一，永不打架（记为 R7）；**Q6** 到期提醒纳入 A 期，保存期年数做成**按凭证类型可配置项、不硬编码**（我记为 30 年但不替业务方拍板，待其按《会计档案管理办法》财务口径核实，列为待补 B）；**Q7** 业务方反问"打通金蝶目的是否为搜索摘要"——**澄清为两个用途**：①号段漏装订校验（拿金蝶该期间实际凭证号 1–100 比对已登记册号段，指出「78–80 号无册覆盖」=漏装订或册子丢了；只需起止号、代价轻）→ **入 A 期**；②按凭证摘要反查凭证号（会计脑中是"付给极鲜达那笔运费"而非"3月85号"，链路可通、`fetch_gl_voucher_subjects` 现成，但须维护凭证明细本地副本并跟红冲反过账）→ **列 B 期第一优先**，待实际高频再做；**Q8** 存量册数业务方暂不确定 → 按「几百本」设计，Excel 批量导入模板列为 A 期第一交付物，建议先录近 3 年、历史分批补，若确认上千本则另评估用金蝶号段自动生成候选册。新增 §6.3 借出归还、§6.4 到期提醒、§7 金蝶用途分析、§11 待补事项 A–D（标签纸规格 / 法定保存期年数 / A-B 期切分认可 / 主体简码对照表）。风险表增至 R1–R8。v1.0 保留归档，v1.1 落 `03_Source_Materials/20260710_凭证归档/`。**待最终签署后按 UI 前置出线框图与样机，再进开发。** | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.68** | 2026-07-10 | **【凭证归档】新工具线立项 + 需求确认书 v1.0 出稿**（纯文档，未动任何代码）。业务方提出装订凭证册越堆越多、找一本要挨个柜子翻，需按主体+凭证号快速定位、按标签锁定册子，且存放位置要能批量转移（搬办公室/装箱/送外仓/销毁）。**四项口径经问答锁定**：①册子唯一键=主体+年月+凭证号区间，同时保留「第X册」册序（两者都要）；②位置=自定义深度的树，另给每本编一个显式唯一识别码（形如 `SZL2026-01-02`）印在标签上；③标签**人眼识读、不扫码**（二维码/扫码枪本期不做；册号已唯一，日后加码无需改数据结构）；④**多人共用、必须留痕**。**核心设计主张=「标签只印身份、位置只进系统」**——位置会变而身份不变，位置一旦印上标签则每搬一次要重贴一次、台账必然失真；解耦后转移只改一条记录，整层/整箱/整柜可一次搬移。**三张新表**（`archive_volumes` 册 / `archive_locations` 位置树·邻接表实现任意深度 / `archive_transfers`+items 转移单），纯新增，不改既有表与 API，对四条既有工具线零影响。**多人场景两处必答**：转移乐观锁（提交前校验册子当前位置=勾选时所见，冲突则拒绝并列出冲突册）、销毁独立通道（审批单号+二次确认+仅超管+记录永久保留）。**风险 R1=期初录入是最大落地成本**（存量数百上千本），故 A 期首个交付即 Excel 批量导入模板；**R2=台账失真**（搬了不登记，工具"假装很准"比没有更危险），解药=转移三步极简+盘点表打印现场逐本打勾+轨迹可追到人。含**决策点 Q1–Q8 待批注**（凭证类型是否占册号一段 / 书脊与标签规格与打印机型号 / 三级权限 / 借出归还是否入 A 期 / 册子状态取值 / 保存到期年份与法定保存期核实 / 号段是否从金蝶取 / 存量册数）。建工具线格 `03_Source_Materials/20260710_凭证归档/`（前缀=确认书 v1.0 出稿日，依 V2.51 建格规则）。**待业务方逐条批注签署后，按 UI 前置出线框图与样机，再进开发。** | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.67** | 2026-07-10 | **【平台】侧栏一级板块加底色**。`styles.css` 的 `.grp`（**仅 Sidebar 在用**，已 grep 确认，可安全整体改）由无底色小标签改为**带底色的可折叠条**：`background:var(--gray-bg)` + `1px solid var(--line)` + `border-radius:7px` + `padding:7px 10px` + `margin:9px 0 4px`，色号取自设计令牌故**深色模式自动适配**（暗底上变亮条，不必另写一套）；`:hover` 转 `var(--line)` 底、`var(--ink)` 字，带 .12s 过渡，配合 V2.66 的折叠交互给出可点反馈。二级项保持透明底，与一级条拉开层次。**验证**（`preview_screenshot` 因金蝶请求挂起超时，改用 `preview_eval` 读实际渲染值）：4 条一级条各 191×31px、`rgb(238,240,243)` 底压在侧栏 `rgb(250,251,252)` 上、边框 `rgb(233,235,239)`、圆角 7px、字色 `--ink-2`；`.nav` 背景仍为 `rgba(0,0,0,0)`。**另记**：视口 <900px 时 `.aside{display:none}` 是既有响应式规则（`styles.css:211`），排查时一度误以为侧栏塌了——非本次引入。仅前端样式，无逻辑/后端/DB 改动。新包 index-qGZIm65Y.js + index-C358QCyC.css，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.66** | 2026-07-10 | **【平台】侧栏四个一级板块改为可折叠**。点一级组名收起/展开其下的二级项（▾ 展开 / ▸ 收起），全收起后侧栏只剩四个一级 + 设置。`Sidebar.jsx`：`.grp` 由静态标签改为可点行（flex + 右侧箭头 + `cursor:pointer` + `userSelect:none`）；折叠态记 `localStorage['fw_nav_groups']`（**默认全展开**，仅显式收起才记 false，老用户无感）；`grpOfView()` + `useEffect([view])` 让**当前所在板块自动展开**（否则从别处跳进来会不知道自己在哪），但**不硬顶**——人主动收起当前板块仍收得掉。岗位胶囊随所属板块一起收起。**验证**：默认全展开；收「月结与结账」后其 6 个二级消失、箭头转 ▸；四个全收起后侧栏只剩一级 + 设置；`localStorage` 写入 `{"月结与结账":false,...}`；刷新后三个仍收起、而当前页（银行对账）所在的「月结与结账」自动展开；主动再收起该板块可收；跳到板块外的「设置」不会强行展开任何板块。截图复核 ▸/▾ 对应正确。仅前端，无后端/DB 改动。新包 index-CuhdhGiV.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.65** | 2026-07-10 | **【平台】岗位名单改为可维护 + 岗位胶囊上侧栏**。业务方两点：①岗位别写死在代码里；②岗位做成胶囊挂到导航。**后端**：岗位名单从常量 `NAV_POSTS` 改为存 `app_settings['nav_posts']`（`NAV_POSTS_DEFAULT` 五个只作首次种子）；`_clean_posts()` 去重去空、单个≤10 字、总数≤20（`NAV_POSTS_MAX`）；`_nav_posts()` 读名单，`_nav_one()` 读模块岗位时**按当前名单过滤**——所以**删掉一个岗位，挂在各模块身上的那条自动消失**，不会留下指向已删岗位的脏数据。`POST /api/nav-modules/save` 改为一次收 `{state, posts}`：先落名单（删除项写审计 `岗位名单`），再按新名单过滤各模块的 posts；空名单拒绝（至少留 1 个）。GET 增返 `posts_max`。**前端**：设置页模块表上方加「岗位名单」卡（胶囊带 × 删除、输入框回车/点「添加」新增、显示 `n/20`、非主管理员只读）；模块表的可勾岗位改读**编辑中的名单**（新增的岗位不用先保存就能勾）；删岗位时同步从本地各模块 posts 摘掉，界面即时一致。**侧栏**：`Sidebar` 在模块名下方渲染服务岗位胶囊（9.5px、`accent-soft` 底、缩进 36px 对齐模块名、自动换行；仅「已上线」且挂了岗位的模块才有）——侧栏 216px 宽放得下 2 个/行。**验证**：内核层——去重去空限长正确；挂「总账岗+资金岗」后从名单删掉「资金岗」，模块上自动只剩「总账岗」。界面层——新增「出纳岗」后名单 5/20→6/20 且该行立刻可勾、勾选保存后落库正确、侧栏出现胶囊；名单里删「出纳岗」→ 模块行的已选立刻摘掉、保存后落库与侧栏胶囊同步消失。测试写入的 `app_settings`(2 行) 与审计(7 行) 已清库，回到默认名单/默认状态/无岗位。新包 index-B3YtNNUo.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.64** | 2026-07-10 | **【平台】导航模块「开关」升级为「状态机 + 岗位标签」，收紧到仅主管理员，整页宽**。业务方三点反馈同步落实。**①状态机**：`NAV_STATUSES = 敬请期待 → 开发中 → 待验收 → 等待部署 → 已上线` 取代 V2.63 的布尔开关；`NAV_OPEN = ("待验收","已上线")` 才可从导航进入（**待验收要让人进得去才验得了**，这是有意为之），其余状态侧栏灰显该状态名、不可点；主区二次守卫的占位页标题也改显具体状态。**平滑迁移**：`_nav_one()` 认旧布尔值（True→已上线 / False→敬请期待），老库升级不炸。**②岗位标签**：`NAV_POSTS = 成本岗/应收岗/总账岗/资金岗/财务经理`，**仅「已上线」的模块可勾**（改为非已上线时前后端都清空 posts；后端还过滤非法岗位名）。**⚠这是标签不是权限门**——只说明模块服务谁，挡人仍用账号管理里的权限点（页面两处写明）。**③权限收紧**：`_nav_can_edit` 从「主管理员或核算子管理员」改为**仅 `db.is_super`**；子管理员现在 `可编辑=false` 且保存返回 403。**④整页宽**：`Settings.jsx` 的 `.body` 去掉 `maxWidth:660`，模块表横向铺满（模块 180px ｜ 状态 130px ｜ 岗位 flex:1），下方数据源/会计期间/金蝶连接三张小卡仍限宽 660 免得输入框拉长。UI 另加「撤销改动」按钮、状态色胶囊（灰/蓝/琥珀/紫/绿）、「设置」行因 always 不存岗位故岗位列显「—」。**默认状态如实反映当前进度**：银行对账·账户台账·科目余额表·理财对账·物流计提=已上线；月结看板·成本台账=待验收；电商三方核对=敬请期待；设置=始终开启。**验证**：接口层——子管理员 `可编辑=false` 且保存 **403**、主管理员放行；旧布尔格式迁移正确（False→敬请期待、True→已上线）；「待验收」挂岗位被清空、「已上线」只留合法岗位名、`settings` 强制已上线。界面层——8 个状态下拉、卡片宽 713/主区 753（已铺满）、银行对账勾「总账岗+资金岗」保存后落库正确、电商改「开发中」后侧栏灰显「开发中」、月结看板改「已上线」后侧栏「待验收」标签消失、非已上线行岗位列为「—」、「设置」行为「—」、无改动时保存禁用。测试写入的 `app_settings` 与审计已清库、临时授予「陈梓华」的子管理员权限已收回。新包 index-guvYT2PJ.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.63** | 2026-07-10 | **【平台】设置页新增「导航模块上线管理」（对齐财务BP工作台同款）**。业务方给出 BP 工作台截图，要求核算工作台照做。**数据层**：新表 `app_settings`（k/v/updated_by/updated_at，值存 JSON）——通用全局配置槽，首个用户是导航开关，后续别的全局设置可复用。`get_setting/set_setting`（坏 JSON 回退默认，不让整页 500）。**后端**：`NAV_MODULES` 注册表（9 项，带 `grp` 分组与 `dev`/`always` 标记，分组沿用 V2.62 的 月结与结账/常规对账/报表与台账/通用）；`GET /api/nav-modules`（返回 modules+groups+state+`可编辑`）、`POST /api/nav-modules/save`（限**主管理员或核算工作台子管理员**，写 `audit_log`）。**语义**：未设过 = 默认全开（新装即全量可用）；`always` 的（设置）强制开启、传 false 也无效；关掉的模块侧栏**灰显「未上线」不可点**；开着但 `dev=False` 的（电商三方核对）进去是**「规划中」占位页**。**⚠明确边界**：这是**上线开关不是权限**——只管导航显示，**不挡接口**；挡人仍用账号管理里的权限点（页面上已写明，免得有人当权限用）。**前端**：`Settings.jsx` 顶部新增卡片（自绘 `Switch` 组件、按组渲染、非管理员只读并提示、保存前 `dirty` 判断禁用按钮、保存后 `onModsChanged` 即时刷新侧栏）；`Sidebar.jsx` 收 `mods`（null 时按全开渲染，避免先灰后亮闪一下）；`App.jsx` 拉取开关 + 主区二次守卫（被下线的模块即使直达也只显示「该模块未上线」，四部曲 import/fund/result 三视图统一归属 `reconcile` 模块）+ **默认落地页守卫**（`reconcile` 被下线时自动改落第一个还开着的模块，不让人一进来就撞见「未上线」）。**验证**：接口层——9 模块 / 默认全开 / 普通账号 `可编辑=false` 且保存 **403** / 管理员保存生效 / `settings` 传 false 仍强制 True；界面层——8 开关+1「始终开启」、初始 `保存` 禁用、改动后可点、保存提示「✓ 已保存，全员生效」、关掉「理财对账」后侧栏出现「未上线」且点击无跳转、下线「银行对账」后落地页自动变「月结看板」、复原后侧栏无「未上线」。测试写入的 `app_settings` 与 8 条审计已清库（回到默认全开）。新包 index-BWJbBz_e.js，同步 01→02，部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.62** | 2026-07-10 | **【平台】侧栏信息架构按业务性质重排为四个一级板块（两级到底，业务方定）**。V2.61 把月结看板升为一级后，业务方进一步给出分法：一级 = **月结与结账 / 常规对账 / 报表与台账 / 其它小工具**，其余全部作二级挂在下面。**关键判断（业务方）：银行对账整条链都是「月结的一环」**——原「ERP对账程序」下的 对账程序·账户台账·科目余额表·理财对账 四项，连同 物流计提，**全部平移进「月结与结账」**；「ERP对账程序」这层中间层随之**取消**（它本就是按工具而非按业务性质分的产物）。电商三方核对不卡月结 → 归「常规对账」；成本台账属结账**后**的报表 → 归「报表与台账」（呼应 V2.61 里"成本台账不进月结清单"的同一判断）；「其它小工具」暂空，放灰色「敬请期待」占位（不可点）。子项「对账程序」更名 **「银行对账」**（与门户卡片口径一致），选中态仍覆盖四部曲四个视图（import/fund/reconcile/result）。`Sidebar.jsx` 重写为数据驱动（`SECTIONS` 常量 → 一级 grp + 二级 nav），去掉两处折叠状态与 `.nav-tool/.subs` 用法；新增 5 个内联 SVG 图标（科目余额表/理财/物流/成本/占位），全部离线不依赖 webfont。封存标记仍挂在总纲「月结看板」右侧。**验证**：浏览器逐项点击 9 个二级项路由全通（月结看板·期间封存 / 银行–金蝶稽核 / 账户台账 / 科目余额表·账面核对 / 理财产品对账 / 物流计提 / 电商三方核对 / 成本台账·存货月结核对 / 设置）、「敬请期待」不可点、侧栏 933px 不溢出视口无需滚动、截图确认观感。新包 index-B8nXLyf6.js，同步 01→02，部署包已更新。仅前端，无后端/DB 改动。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.61** | 2026-07-10 | **【银行对账】月结看板 + 期间封存（补上"一期结账任务"的缺口）**。业务方提问"每次结账前是不是开启新的一期结账任务、结账后关闭这一期，不然好乱"——查证确认工具**根本没有期间生命周期**（9 张表无任何期间状态/封存/归档；期间只是设置里一个全局 (年,月)，所有结果都是即时算的）。由此三个真问题：①**底稿不可复现**（7月再导6月底稿，数字会因金蝶补凭证/有人改认领而变，审计问"当时凭什么这么调节"答不上来）；②本月做到哪一步全靠人脑记；③已出报表的月份照样能改认领、撤计提凭证，账与底稿两张皮。**数据层**：新增 `periods`（期间状态机 open↔closed，记封存人/时间/说明/金蝶取数时点）+ `period_snapshots`（gzip(JSON) 存 `LargeBinary(2**32-1)`→MySQL LONGBLOB；TEXT 仅 64K 装不下 600+ 笔）。**封存语义**：把 逐笔稽核 / 余额调节+钩稽 / 月结清单 三份结果拍照存死 → 该期转**只读**：**15 处**写入与取数接口（6 个 sync、金蝶刷新、认领、未达原因、流水上传、科目表上传、计提录入与撤销**按台账记的落账月**判断）一律 **409** 拦下；读接口改走快照（**0.07s，不再打金蝶**）；导出底稿即封存那一刻的版本，汇总页抬头标注封存人/时间/取数时点。**月结看板**：9 项清单，`kd/bank/guard/claim/badj/fix` 六项未过**挡住封存**，`cover/accrual/export` 三项仅提示；全绿方可封存；未全绿时主管理员/工作台子管理员可勾「强制封存」+ 填理由（≥5字）放行——真实月结总有说不清的小差异。**解封**限主管理员或核算子管理员 + 必填理由 + `audit_log` 留痕，解封后清缓存回到实时算。**无"开启新一期"动作**：上一期封存后下一期天然进行中（与金蝶期末结账同理）。**正确顺序**：工作台对平→差异去金蝶改干净→出底稿→**工作台封存**→**金蝶期末结账**（改差异要金蝶可写，故金蝶结账必须最后）。新权限点 `close_period`（「月结看板·封存本期」，月结与结账组，**列入 `_SENSITIVE_CAPS` 默认不给**，并入 `WS_CAPS.accounting`）；`/api/config` 与 `set_config` 返回带 `封存` 态，侧栏常驻「本期已封存·只读」徽标（可点击直达看板）。前端新增 `views/PeriodClose.jsx` + 侧栏「月结与结账 › 月结看板」+ `api.js` 三个接口（用 `jpSoft` 保留 403/409 的 msg，不被 `j()` 抛掉）。**验证（真金蝶 2026-06，非样例）**：清单如实报出「差异已全部处理✗、余额调节已对平✗」两项阻断；`force+理由` 强制封存成功→**624 笔存成 33,217 字节快照**；认领/未达原因/金蝶刷新/逐笔sync/余额调节sync/资金看板sync **6 个接口全部 409**；GET 逐笔稽核 `封存=true`、**0.07s** 走快照、624 笔与 21 账户完整；导出底稿 200 / 53,350 字节；理由 <5 字的封存与解封均被拒；解封后 sync 恢复 200。浏览器实测：侧栏徽标、只读卡片、解封区、历史封存表、"清单未全绿时封存按钮禁用/勾强制封存后可点"均正确。端到端测试在金蝶只建删草稿、在本地库留下的 periods/快照/审计痕迹**已全部清理**。**注意**：`close_period` 是敏感权限，现有账号（含老账号）默认**没有**，需主管理员逐个授予；本地测试库 `管理员` 密码被重置为 `admin888`（仅本地 workbench.db，服务器走 MySQL 不受影响，且该文件不入部署包）。改动文件：`db.py` `app.py` `App.jsx` `api.js` `Sidebar.jsx` `views/PeriodClose.jsx`(新)。**（同版内 IA 修正，业务方当场指出）**：初版把「月结看板」塞进侧栏「月结与结账」工具组里，而「ERP对账程序」是它的**平级兄弟**——但封存作用域是整个 (源,年,期)、一封锁全站，**月结才是总纲、银行对账只是其中一环**，总纲不该嵌在自己管辖的兄弟节点内。修：①月结看板**升为侧栏一级**（新分组「本期月结」，置于「工具合集」之上，封存时 nav 右侧显「已封存」标记；撤掉底部重复的状态胶囊）；②清单**按环分组**（`数据准备` / `环① 银行对账` / `环② 物流计提`，item 加 `环` 字段 + 返回 `分组` 顺序；前端按组渲染并显示各组"N 项未完成/全部完成"；老快照无 `环` 字段时归入单组，向后兼容）；③**成本台账不列入清单**——业务方判定它属结账**后**的报表系列，非封账前必做项（页面下方注明）。物流计提项标题改「计提凭证已录入」。新包 index-DIBx8l63.js，浏览器复验分组渲染正确。**踩坑**：launch.json 的 workbench-01-live 无 `--reload`，改后端后必须 `preview_stop`+`preview_start` 重启，否则读到旧进程（本次分组一度不生效即此因）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.60** | 2026-07-10 | **【平台/成本台账】代码审查·第一档四个小改动（多人使用确认后开工，堵大洞·低风险）**。承全量对抗性代码审查（102 确认问题，17 高危）；业务方确认"未来多人使用"，先做四个小改动。**①依赖**：`requirements.txt` 补 `pandas>=2.0`（app.py suppliers-check 与 logistics_accrual.process_workbook 实际 import，缺则干净环境 ImportError 起不来）。**②脱敏台账数据**：`git rm --cached` 撤除 5 类运行时数据文件（`ledger_authoritative/ledger/account_overrides/claims/config .json`）跨 01/V1.1/V1.2 共 **12 份**出版本控制（含真实银行账号与账户映射，本属 .gitignore「本地运行数据」范畴却误入库）；`.gitignore` 加 `**/sample_data/{五文件}` + `scratch_*.json` + `~$*`；留**脱敏** `ledger_authoritative.sample.json`（6 行示例主体，结构参考）+ `.README.txt` 说明。真实文件留本机、不入库、部署包本就排除 sample_data → **应用零损**（os.path.exists 兜底，实测仍读 153 行）。**遗留**：历史提交中仍含真实账号，需 `git filter-repo` 清理（破坏性、改写共享历史，单独审批，本次未做）。**③权限点**：`db.py` CAP_META 增 `cost_ledger`（"成本台账·上传核对与导出"，月结与结账组，非敏感）+ 并入 `WS_CAPS.accounting`；`app.py` 给 `/api/cost-ledger/analyze`、`/api/cost-ledger/export` 挂 `cost_ledger`、`POST /api/config` 挂 `enter_accounting`（数据源/期间是全局共享态，挡住非核算账号）。**注意**：现有**有显式 perms 的普通账号**需管理员补授「成本台账」方可用（主管理员与老 NULL 账号自动有）。**④set_config 类型校验**：改为"先全部校验后一次性写入"——非法 year(2000–2100)/period(1–12)/source(sample|kingdee) 直接返回 **400 且绝不部分写入全局 CFG**，修复"一个坏请求把 CFG 污染成字符串→此后 _period_str 全站 500 需重启"的 DoS。**验证**：`import app` OK（58 路由）；直调端点实测 period=x/13、year=20x6、source=evil 均 400 且 CFG 未污染（坏请求后 _period_str 仍正常）、合法 200；BP 账号 set_config 与 cost-ledger export 均 403、admin 放行；cost_ledger 内核 14/14 绿；01↔02 三源码文件（app.py/db.py/requirements.txt）逐字节一致。**已同步 01**（app.py/db.py/requirements + sample/README）。**未提交 git**（交业务方审阅后一并 commit）。**下一档**：重复记账簇（一键录入互斥锁 + DB (year,period,摘要) 唯一约束 + view/save_voucher 三态返回）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.60** | 2026-07-09 | **【成本台账】纠正 Q7：存货收发存汇总表可直接 API 取数（实测打通）**（只读探查+文档，无代码改动）。业务方指出 API 可读并提供接口文档，**推翻 V2.59 "报表无 API" 的误判**（此前只试了单据查询 ExecuteBillQuery）。实测：报表取数走 **`GetSysReportData`** 接口（服务 `...DynamicFormService.GetSysReportData.common.kdsvc`）+ FormId `HS_INOUTSTOCKSUMMARYBYDATERPT`(按日期)。逐步试出全部必填参数：核算体系 `KJHSTX01_SYS`、核算组织/库存组织 `107`、会计政策 `KJZC01_SYS`(查 BD_ACCTPOLICY 得)、币别 `PRE001`、日期范围+年期；字段名 期初`FInitQty/FInitAmount`·收入`FReceiveQty/FReceiveAmount`·发出`FSendQty/FSendAmount`·结存`FEndQty/FEndAmount`+`FMaterialName/FModel/FUnitName/FStockName`（逐字段试出，物料编码字段未定 FMaterialId 返 null）。**真数据验证**：取回 4456 行，行值与底稿一致（一次性白色丁晴手套 期初45/882.26·发出20/392.11·结存25/490.14）；全行结存和 21,922,397.30 = 底稿 10,958,051.26 **×2**（报表含每物料小计行）。解析坑：金额带千分位逗号、需过滤小计行。文档：确认书 Q7/§13/头批注三处**再纠正为"API 可取、已打通"**（V2.59 的"走上传"作废，上传保留为双通道兜底）。**下一步（可选增量）**：把 GetSysReportData 封进 kingdee_client + cost-ledger analyze 加"一键金蝶取数"通道（免上传）；跨维度报表 FormId 待业务方补文档同法接入。 | Claude / c | In Review | —（探查+文档，无修订文件夹） |
| **V2.59** | 2026-07-09 | **【成本台账】金蝶报表 API 深探 + 确认书 Q7 标注实测结论**（只读探查 + 文档批注，无代码改动）。业务方追问物料级收发存能否直接读金蝶。**深探（只读）**：①即时库存 STK_Inventory 全字段——只有数量（库存量/可用量/预留量）**无任何成本/金额字段**（库存模块只管数量、成本在存货核算模块）；②批量探 20 个核算/收发存对象（STK_InvBalance/Cal_InvBalance/SP_InventoryBalance…）**全部"业务对象不存在"**，能查的仅单据（STK_MISDELIVERY 其他出库/IV_SALESIC 销售出库/STK_InStock 入库）。**定论：金蝶暂无开放「报表分析」板块 API，物料级带成本收发存明细取不到 → 走手工上传**（会计导出报表→上传，兼作人眼可核，合 [[feedback-dual-mode-human-check]]）；账户级科目余额 API 直读已实现（账实勾稽/趋势看板）。自行从单据重算需复刻加权平均成本、脆弱难零容差，不采。**文档**：`成本台账工具_需求确认书_v1.0` 四处标注（文件头批注 + §11 D1 + §13 风险由"待验证"改"已定" + §14 Q7 → ✅ 已实测确认 + §15 签署清单）；若要省掉上传须金蝶顾问后台配报表引出接口。 | Claude / c | In Review | —（探查+文档，无修订文件夹） |
| **V2.58** | 2026-07-09 | **【成本台账】A 期第二增量：接 API 路由 + 五步工作流前端屏（真机端到端验证）**。承 V2.57 内核。**后端 app.py**：新增 `import cost_ledger as clg` + `_inv_gl_balance()`（存货类 14xx 科目余额按(账簿,科目)去重取孝感星期九，同 V2.53 实证）+ `_cl_read_workbook()`（上传工作簿自动识别 跨维度/按日期/货损/处置 四类 sheet，按内容非名字）+ `POST /api/cost-ledger/analyze`（上传收发存汇总表 xlsx→三道勾稽/透视/异常/损益，科目余额金蝶只读）+ `GET /api/cost-ledger/export`（`_build_cost_ledger_xlsx` 出 6 表：核对结论/账实勾稽/收发存汇总/异常清单/损益归集/收发存明细）。**前端**：新增 `views/CostLedger.jsx`（五步工作流：数据接入上传→三道勾稽可信度带+账实勾稽表+KPI→异常稽核切片器+清单→损益归集→台账出具导出）、`api.js` 加 analyzeCostLedger、`App.jsx` 挂 view=costledger、`Sidebar.jsx`「月结与结账」下加「成本台账」子项。**真机端到端验证（5210 dev，浏览器+真金蝶）**：登录→月结与结账→成本台账→上传 5 月底稿→analyze 返回 credible=true、**账实勾稽 pass（金蝶实取科目余额去重后 book_total=10,994,036.02 与底稿一致）**、负结存5/挂账尾差7、损益 -4874.95/8912.37；UI 渲染 可信度带三✓+"本期台账可信·可出台账"+KPI（待处理异常 12=负结存5+尾差7，成本调整 262 另列"提示"不算待处理·本次修正）+异常表 274 项负结存置顶+护栏 2496+274=2770；**导出 GET 200 返 213KB 6-sheet xlsx**；无 console 报错。**已同步 01**（app.py/api.js/App/Sidebar/CostLedger + static 新包 index-CYo3FeG7.js；01 侧 import app OK + 14/14 绿）。**下一增量**：手工上传通道细化（多文件/金蝶报表导出格式适配）+ 趋势看板与异常问询接线（V2.53/54 样机）+ 权限位（参照 logistics）+ 对照表/仓库属性界面维护。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.57** | 2026-07-09 | **【成本台账】A 期第一增量：成本台账内核 + 5 月真底稿全 DoD 达标**（业务方"可以先开始开发、后续再改"，从设计转开发）。**新增** `kernels/cost_ledger.py`（解析存货收发存汇总表跨维度/按日期，按表头文字定位列容忍漂移 → 三道勾稽①两表互勾②收发存自平③账实勾稽（类别↔科目对照，含对照缺失兜底）→ 类别/仓库/仓库类型透视（挂仓库属性维表）→ 异常扫描 负结存/挂账尾差镜像/对照缺失/成本调整提示 → 损益归集 货损→管理费用·处置→营业外支出 → 顶层 build_cost_ledger 出可信度结论）、`kernels/test_cost_ledger.py`（**14/14 绿**：解析/自平通过与失败/两表互勾/账实勾稽与对照缺失/四类异常/计数不重不漏/损益/可信度）、`sample_data/cost_ledger_config.json`（类别↔科目对照＋仓库属性维表种子，5 月逆向验证）、`tools/run_cost_ledger_5月.py`（真底稿 DoD 跑分器）。**5 月真底稿跑分全 DoD 达标**：两表互勾四项全 0、收发存自平 0、账实勾稽三组差 0＋总账合计 10,994,036.02、类别透视 7 类逐类与底稿一致、负结存精确 5 个、损益 −4,874.95/8,912.37 分毫不差、可信度=可信；**挂账尾差实扫 7 个**（底稿"数量0金额≠0"真为 7 行，样机仅示意 2）。**已同步 01**（kernels×2/sample_data/tools，01 侧 14/14 绿＋import app OK；**未接 app.py 路由，对运行中应用零影响**）。铁律落地：对照缺失不硬归、成本调整负数行仅提示不算异常。**下一增量**：API 路由 + 五步工作流前端屏 + 导出 xlsx（复用 V2.56 导出样例结构）+ 手工上传通道（收发存报表 API 取不到，走上传）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.56** | 2026-07-09 | **【成本台账】《成本台账》导出样例 xlsx（模拟"台账出具"输出）**（样例文件，无代码改动）。业务方问导出的台账长什么样，直接用 5 月真数据生成 `成本台账_导出样例_2026年5月.xlsx`（openpyxl，微软雅黑，冷灰表头，229KB）。**6 个工作表**：①核对结论（封面：主体/期间/生成时间＋三道勾稽全过可信度报告＋关键指标＋异常计数——工具独有的新增价值页）；②账实勾稽（科目↔类别对照，差异列=公式 B-D 恒 0）；③收发存汇总·按类别（7 类期初/收入/发出/结存，自平列=公式 B+C-D-E 恒 0，合计=SUM）；④收发存明细（2,770 行真实跨维度数据，物料×仓库×批号，5 个负结存行红标+异常列）；⑤异常清单（5 负结存＋2 镜像尾差，白话说明＋建议动作）；⑥损益归集（货损→管理费用 −4,874.95、处置→营业外支出 8,912.37，业务↔总账互核）。数据取自底稿明细＋已验证勾稽数字；合计/勾稽用公式（Excel 打开即算，逐条低风险），源数据为金蝶只读取回值。openpyxl 回读校验：6 表齐、公式引用正确、明细完整（Windows AF_UNIX 限制 recalc.py 不可用，公式为 SUM/减法零风险已人工验）。落 `03_Source_Materials/20260709_成本台账/`。 | Claude / c | In Review | —（样例文件，无修订文件夹） |
| **V2.55** | 2026-07-09 | **【成本台账】过会导航中心页**（设计稿，无代码改动）。把成本台账线的 3 样机 + 2 文档用同目录相对链接串成一个 hub，过会时点着走。`00_成本台账_过会导航.html`（工作台冷灰风，双击即开）：顶部演示动线（月结核对→趋势看板→异常问询→确认书签署），卡片区——①月结核对样机带 4 个子屏直达（#tie/#anomaly/#pnl/#detail）、②趋势看板、③异常问询，各含"打开"按钮；支撑文档区=需求确认书 v1.0 + 存货趋势分析。落 `03_Source_Materials/20260709_成本台账/`。至此成本台账线过会材料集齐：需求确认书 + 月结核对样机(4屏) + 趋势看板(真数据) + 异常问询(QA闭环) + 趋势分析 + 导航页。**下一步待成本会计过会反馈**（确认书 Q1–Q8 + 三个异常追问 + 看板/问询是否纳入 A/B 期）。 | Claude / c | In Review | —（设计稿任务，无修订文件夹） |
| **V2.54** | 2026-07-09 | **【成本台账】存货异常问询（QA 闭环）样机**（设计稿，无代码改动）。业务方提出把趋势分析的异常点挂到 web 上、做成"成本会计作答、领导可见"的问答形式。**方案=复用银行对账 `balance_notes`（未达原因·会计填供领导核查·持久化）成熟模式，升级为可多轮的异常问询闭环**：①系统自动识别异常并列数据证据 →②成本会计就地作答（或标"已核实·数据正常"/"确需调整转异常清单"）→③管理层看板直接可见、可标已阅或追问 →④一问一答成串、带人名+时间戳留痕；权限沿用账号体系（成本会计作答/管理层追问核阅/他人只读），右上「成本会计视角↔管理层视角」切换。样机 `成本台账_异常问询_样机_v1.html`(+_1920.png)，用 1–5 月真数据分析出的三个真实异常演示三种状态：5 月原料领用比 18%异常（待说明·红）、5 月去库存 142 万（已说明待核阅·蓝，答"旺销拉动正常去库存"）、原材料占比升+周转 52.6 天（管理层追问中·紫，曾禹锡追问锁价明细）。价值=静态分析变"活"记录，领导自助查看不必追人问。落 `03_Source_Materials/20260709_成本台账/`。**建议纳入看板功能：异常问询作为趋势看板的批注层。** | Claude / c | In Review | —（设计稿任务，无修订文件夹） |
| **V2.53** | 2026-07-09 | **【成本台账】金蝶多期存货取数实证 + 存货趋势看板样机（真数据）**（只读探查 + 设计稿，未改项目代码）。业务方问"能否结合前几期做可视化看板"。**①金蝶只读实测（本机直连 xingql.ik3cloud.com 成功，沙箱这次未拦）**：存货收发存汇总表是"报表分析类"，`ExecuteBillQuery` 无对应业务对象（SP_InvBalance/STK_InvInboundDetail 等均返回"对象不存在"）——Q7 落定：**物料级/仓库级收发存明细 API 取不了、走手工上传**；即时库存 `STK_Inventory` 可查（结存快照，无期间流量）。**但发现账户级多期真数据可取**：存货类科目（14xx）科目余额表**按期可取**，`fetch_gl_balance` 现成可用。**踩坑=同 V2.35 的重复行**：GL_BALANCE 对每(账簿×科目)返两行（币别=空的本位币汇总行＋币别=人民币明细行，金额相同），直接 SUM 翻倍（库存商品被 ×2、周转材料 ×4）；**按(账簿,科目)去重后 P5 期末合计 10,994,036.02 与 5 月底稿分毫一致**，口径验证通过。拉回 2026 P1–P5 六科目（材料采购/在途/原材料/库存商品/委托加工/周转材料）期初/收入(借)/发出(贷)/期末（P6 未结账、期末=期初，排除）。**②趋势看板样机**（standalone `成本台账_趋势看板_样机_v1.html` + `_1920.png`，纯内联 SVG 无依赖，工作台冷灰令牌）：KPI（5月结存¥1,099万/环比−14.0%/5期均值¥1,229万/库存商品周转6.7天/呆滞预警待账龄表）+ 四图（存货结存趋势线/类别构成堆叠/库存商品收发流量/周转天数双线 库存商品5–10天·原材料16–53天）。**看板全部真数据**，底部注明"已能做=账户级趋势（科目余额按期取）｜还需补=物料级明细与库存账龄（呆滞/跌价预警，报表类走手工上传）"。落 `03_Source_Materials/20260709_成本台账/`。**结论进路线图**：账户级趋势看板 A 期即可做（甚至先于全量明细）；建议月结存快照攒历史。 | Claude / c | In Review | —（探查+设计稿，无修订文件夹） |
| **V2.52** | 2026-07-09 | **【成本台账】静态样机 v1 + 三张 1920 高清图**（纯设计稿，无代码改动；UI 前置流程，配合需求确认书 v1.0 与成本会计过会）。单文件自包含 HTML `成本台账工具_样机_v1.html`（双击即看，URL # 切屏），设计令牌与银行对账样机 v3 / 工作台冷灰 Linear 风完全一致（同 :root 变量、五步条/可信度带/KPI/切片器/表格套件），侧栏挂「月结与结账」组与物流计提并列。**三屏**：①三道勾稽（主屏）——可信度带三道全过 + 账实勾稽表（库存商品/原材料/周转材料三组差异 0.00 + 在途/委托加工单列轧平 10,994,036.02）+ 按类别收发存自平表；②异常稽核——切片器（负结存5/挂账尾差2/对照缺失0/成本调整提示12/正常2,752）+ 异常清单（白话说明+建议动作+认领处理，护栏 Σ各态=2,771 行）；③损益归集——货损→管理费用 −4,874.95 与资产处置→营业外支出 8,912.37 双卡 + 业务↔总账互核尾差 0.00；④**明细下钻**（同日按业务方"能不能看明细"补充）——2,771 行收发存明细（跨维度原样只读）随查屏：下钻路径面包屑（账实勾稽›库存商品›明细）+ fbar 筛选（搜索/类别/仓库/批号/只看异常行）+ 明细表（金额/数量双行显示，真实原行含 马蹄包心圆两批号、成本调整 −1,996.53、金枪鱼肉糜负结存红底高亮、同料两批号分行的丁晴手套）+ 分页 185 页 + "点汇总看明细、点明细看原报表行，随时与金蝶界面人眼互核"说明。**页面全部数字取自 2026 年 5 月真实底稿**（含 5 个真实负结存物料、±0.0011 镜像尾差）。高清图 Edge headless 按屏定高出图：`成本台账_样机_勾稽屏/异常稽核屏/损益归集屏/明细下钻屏_1920.png`，四屏均目检通过。产出落 `03_Source_Materials/20260709_成本台账/`。**待成本会计过会反馈后升版样机或直接进确认书签署。** | Claude / c | In Review | —（设计稿任务，无修订文件夹） |
| **V2.53** | 2026-07-17 | **【平台】账号页三栏化——先答「能不能进」、再答「进去能干什么」**（承接 V2.52，业务方看样机后拍板方案 B′）。**背景**：V2.52 加了 22 个菜单准入点后，账号管理页把准入点与动作点平铺混排、还各用一套词汇（准入点按业务板块、动作点按工具线），管理员看到「进入 对账程序」与「上传资金流水」得自己在脑子里连线；且有历史挂错（「上传科目余额表」挂在银行对账下，可科目余额已搬去报表模块）。**①每个工作台内三栏**：① 块头两开关＝能不能进工作台（准入总闸＋任命权，敏感）；② tier=nav 准入点＝看得见哪些菜单（按业务板块，顺序与侧栏一致）；③ tier=act 动作点＝进去能干什么。**②第③栏只出第②栏勾了的菜单的动作**，按菜单归堆并标出处（「上传科目余额表」→ 科目余额·报表模块）；进不去的菜单其动作根本不渲染——矛盾组合从「能勾但不该勾」变成「压根不存在」。**③取消某菜单准入 → 底下已授动作跟着收回**（业务方定）：前端即时同步 + 后端 `_cascade_revoke` 强制兜底（绕过前端直接调 /api/users/perms 硬塞也照收，杜绝屏幕看不见却仍授予的幽灵权限）。**④后端新增纯展示字段** `tier`(nav/act) 与 `mod`(动作归属菜单 key)，**独立于 kind**（kind 驱动 default_perms 默认给不给，动它会连带改 BP 存量账号默认板块，故另开字段）；同时把 14 个核算动作点、6 个 BP 功能点一一标注归属，**顺带修正「上传科目余额表」「月结看板·封存本期」的历史挂错位置**。**样机**（Edge headless 1920 出图，业务方选定）落 `01_Current_Deliverables/docs/20260717_导航与权限重构/账号页三栏样机_方案B改_第3栏只出进得去的_20260717.png`；确认书补第十节。**端到端验证**（本地库，临时账号验完即删）：第②栏板块序与侧栏一致 ✓；第③栏只出进得去的 9 菜单、按菜单归堆 ✓；取消准入→动作整堆消失、勾回→堆回但内部为空不自动还权 ✓；绕过前端硬塞→后端强制收回 3 个动作 ✓；BP 动作认到自己板块门控 ✓。**顺带发现（记录）**：一键套用岗位模板只给菜单准入点、不含「进入工作台」总闸（总闸 kind=enter 不在模板 secs/mods 范围）；真实核算组账号建号时 default_perms 已给总闸故无碍，但总闸缺失的账号套模板会「给了一堆菜单却整台进不去」，若要更稳可让套用连带补 enter 总闸。 | Claude / c | In Review | —（在 01 原地迭代，无修订文件夹） |
| **V2.52** | 2026-07-17 | **【平台】导航与权限架构重构——一级按业务板块重组 + 菜单级准入权限**（依《导航与权限架构重构_确认书_v1.0_20260717》，D1–D12）。**背景**：改造前菜单层几乎没有权限——三级里只有 3 个设了门槛（系统设置/月结核对/仓库类型），其余 11 个人人可进，管控全压在动作层，即"门大开、柜子上锁"；一级无权限概念、且不是实体（从模块 `grp` 字符串反推，改不了名排不了序）；菜单按业务性质分、权限点按工具线分，两套分类法打架。**①菜单树**：一级升格为实体 `NAV_SECTIONS`（报表/总账/应付/成本/应收/其它 + 通用钉底），可增删改名排序；模块挂 `sec`+`parent`+`order`，**内置模块位置存覆盖值**（代码只给默认，否则这次搬完以后照样搬不动）；建模块接口收 `parent` → **二级可建三级**。**②准入点自动生成**（`kind="nav"`，`enter:<key>`，22 个）：菜单树能自建，准入点就不能靠改代码——`db.CAPS` 常量改为 `db.caps()` 函数（静态动作点 + 菜单派生准入点），app 启动注册 provider、进程内缓存变更即失效。动作点仍由 `CAP_META_STATIC` 持有，**敏感地板纹丝不动**。**③岗位 key 化**（D11）：`{key,label}`，改名不丢绑定；老名单 5 个字符串岗位按别名表平移 key、label 换成业务方新命名，并补齐至 8 个（财务经理/总账会计/成本会计/应付会计/应收会计/税务会计/资金专员/实习生）；账号岗位由手打自由文本改为下拉选，存量认不出的原样留着显示「待认领」不静默清空。**④岗位模板**（D4，岗位不判权）：按板块/模块写而非按权限码写，菜单树一变模板自动跟上；**敏感点一律滤掉**；一键套用只勾不取消。**⑤菜单落地**：3 处改名 key 不动（付款对账→账单核对、单据物流成本→单据运费+挪入应付、电商三方核对→电商对账）；**月结核对层取消、内容上提**（点成本台账直进七步工作流页，`clrecon` 退休，非删功能）；仓库类型做成本台账三级；月结看板**暂留**（D10：它是封存/解封唯一入口，`_closed_block` 拦截遍布全台，现摘＝某期封存后永远解不开，待"封存拆到各二级"验收后再下线）；新增 9 个内置占位。**⑥侧栏第三种节点**：可进入且有子项（点文字进页面、点箭头只展开），展开态/收起态图标轨/悬停飞出三处同改。**⑦存量迁移＝全部不给**（D5）。**开发中实测推翻两处纸面设计**：(a) 确认书原定"复用 `cost_ledger`/`cost_ledger_wh` 当准入点"**是错的**——那是非敏感动作点，`default_perms` 会自动发给每个核算组账号，复用＝这两个菜单人人可见、D5 当场失效（实测：零权限账号侧栏赫然出现成本台账），已改为自动生成；(b) Q1"税务会计没有工具"判断有误——DB 里早有自建占位 `tax`「进项税核对」。**并修掉三个既有缺陷**：`_legacy_perms()` 会把菜单准入点白送给 `perms=NULL` 老账号（已加 `kind="nav"` 双排除）；准入点原本只挡侧栏不挡页面（零权限账号正文照样渲染整个对账程序，已改落地页按"能进的第一个"选 + 越权视图给明确占位）；「基础数据」因 `always` 被误当权限闸门差点谁都进不去。**Q2 按业务方既有标注落地**（对账程序+理财对账=总账岗、资金看板+账户台账=资金岗，本就挂在模块上，已平移）。**端到端验证**（本地 SQLite，临时账号验完即删）：D5 全 0 ✓；零权限侧栏空+指引 ✓；套用成本会计模板新勾 7 点、侧栏只出报表+成本 ✓；点成本台账直进七步工作流 ✓；第三种节点四种交互全对 ✓；岗位改名绑定不丢 ✓；建三级准入点自动出现 ✓；8 个模板敏感点泄漏 0 ✓。**已知限制**：准入点是导航级、只在前端挡（后端仍只对动作点强制校验，与确认书定位一致）；岗位模板暂无设置页编辑界面（后端接口已支持，待 Q1–Q3 定后补）。**待业务方签署确认书**；Q1（`tax` 归哪个板块）/Q2（汇率录入·月结看板归谁）/Q3（实习生只读范围）/Q4（存量岗位认领）均不卡开发。 | Claude / c | In Review | —（在 01 原地迭代，无修订文件夹） |
| **V2.51** | 2026-07-09 | **【平台】目录结构调整执行完成——01/03 按工具线分格**（依《目录结构调整_影响分析_v1.1》，D1–D5 全部批准：D3 由"归档"改为业务方批示"删除"）。**①迁移**（全程 `git mv`，文件名零改动）：01 根 3 份文档入 `docs/20260703_银行对账|20260708_物流对账/`；03 建 `00_平台通用` + 四条工具线格（20260703_银行对账/20260705_物流计提/20260708_物流对账/20260709_成本台账，前缀=确认书 v1.0 出稿日），25 份文件按映射入格；空夹「需求确认书草稿归档」「样机」撤销。**②删除**（业务方批示）：历史部署包 V2.22/V2.25/V2.26 ×3 zip + login.html（现行部署包留根）。**③制度**：02 的 V1.2 夹内新增 `README_本夹说明.md`（本夹=V2.x 时代持续开发夹，夹名保留）；治理中心（目录结构条款+九步法第4/5步）与速查清单（四大目录表+九步）同步修订转正"一个 Major 时代一个开发夹+文档任务无修订夹"+建格规则；台账头部加分格溯源说明并约定新条目【工具线】标签（本条起）。**④执行前快照提交**隔离 V2.44–V2.50 在途改动，迁移为独立提交可整体回滚。验收：01 根=app+docs、03 根=5 格、启动.bat 运行链路实测、git rename 完整性、全库 grep 无活引用指向旧路径——结果见本条同步说明。 | Claude / c | Synced | —（结构调整，无代码开发） |
| **V2.50** | 2026-07-09 | **《目录结构调整·影响分析》升版 v1.1 —— D1 按业务方批注锁定**（纯文档，未动任何文件）。批注内容：除 `00_平台通用`（`00_` 前缀永远置顶）外，工具线格子命名加**开发开始日期前缀** `YYYYMMDD_工具名`，文件夹默认排序即工具开发时间线——`20260703_银行对账`（交接立项+确认书 v1.0 同日）/ `20260705_物流计提`（说明书 v0.1，V2.20）/ `20260708_物流对账`（确认书 v1.0，V2.41）/ `20260709_成本台账`（确认书 v1.0，V2.48），日期均以总台账立项/首份文档出稿日为据。治理建格规则同步进 §4：新工具在需求确认书 v1.0 出稿当日建格、前缀取该日。01/docs 与 03 两处映射表同步更名；其余内容（引用核查/风险/执行清单/D2–D5）承 v1.0 不变。v1.1 落 `03_Source_Materials/目录结构调整_影响分析_v1.1_20260709.md`，v1.0 移入草稿归档。**待批 D2–D5 与签署后执行迁移（记 V2.51）**。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.49** | 2026-07-09 | **《目录结构调整（按工具线分格）· 影响分析 v1.0》出稿**（纯文档，未动任何文件）。背景：合集已 4+ 工具（银行对账/物流计提/物流对账/成本台账），01/03 根目录文档混堆、02"一版本一修订夹"名存实亡（V2.20–V2.47 共 28 版实际全在 V1.2 夹迭代）。**方案二获业务方口头认可**：顶层 01/02/03 治理分区不变（成果隔离保留），01 内新增 `docs/<工具线>/`、03 内按 `00_平台通用`+四条工具线分格（理财对账并入银行对账线）；02 制度按实际做法转正（一个 Major 时代一个开发夹+文档任务无修订夹，既有夹不改名仅加 README）；总台账保持唯一、新条目加【工具线】标签、历史行不动。**引用依赖已逐项 grep 实测**：启动.bat/launch.json/config.json/部署包全部指向 01/app（原地不动）→ 运行链路零影响；过期引用仅存在于历史文档的路径表述，按台账不可篡改原则不回改，靠文件名不变定位。迁移 17+3 份文档纯 `git mv` 不改名，执行前先快照提交隔离在途改动，独立提交可一步回滚。文档落 `03_Source_Materials/目录结构调整_影响分析_v1.0_20260709.md`，**含决策点 D1–D5 待批注**（工具线定义/四期路线图归属/项目根历史部署包与 login.html 处置/02 制度/台账标签法）；批准后执行记 V2.50（含治理三件套修订）。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.48** | 2026-07-09 | **新增《成本台账工具（存货月结核对）需求确认书 v1.0（草稿）》**（纯文档，无代码改动）。起因：业务方提供 5 月真实底稿 `成本台账-星期九5月.xlsx`（网络盘 5月数据包/成本），逆向分析摸清现行手工月结流水线——两张金蝶报表（存货收发存汇总表 按日期 17 列 / 跨维度 23 列，孝感星期九 2026 年 5 期，各 2,771 行明细）→ 类别/仓库/仓库类型透视（挂现成维表「仓库属性列表」104 行 7 类）→ **与总账科目余额三组勾稽全 0**（库存商品=产成品+自制半成品+委外半成品 4,153,701.90；原材料=原材料+包材 6,364,996.13；周转材料=低值易耗品+广宣品 439,353.22；总账存货合计 10,994,036.02 = 收发存结存 10,958,051.26 + 在途 4,465.13 + 委托加工 31,519.64）→ 盘盈亏货损归集（存货盘盈亏 -4,874.95→管理费用；资产处置 8,912.37→营业外支出，业务↔总账互核）。**实测异常**：5 个物料负结存（孝感成品仓/昆山吉波（新））、昆山吉波↔山东新飞达仓 ±0.0011 镜像尾差、隐藏页残留 3 月旧数据（账龄/制造费用/制费环比，公式区 #DIV/0!）、VLOOKUP 引用区拖拽漂移——均写入确认书作 DoD 基准测试集。确认书含：三道勾稽（两表互勾/收发存自平/账实勾稽，零容差）、异常五态模型（勾稽不平/负结存/挂账尾差/对照缺失/成本调整提示）、口径 R1–R10、A/B/C 分期（账龄与制造费用推 B 期）、待确认 Q1–Q8（类别↔科目对照、盘盈亏归属、负结存清理流程、主体范围、**报表类 API 可行性=开发首步实测、不通则手工上传兜底**）。文档落 `03_Source_Materials/成本台账工具_需求确认书_v1.0_20260709.md`，**待成本会计确认签署**；签署后转入 01 并另行立项定开发版本。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.47** | 2026-07-09 | **一期方案升版 v1.1 —— 维度模型按业务方反馈重构**（纯文档/样表）。业务方两点反馈：①先业务线、后费用归属（出库/入库/仓储/研发/设备调拨），且税率可能随费用归属不同；②对账需拆更细（样品/真实销售/成品调拨/原料调拨）。**v1.1 修订**：维度模型= 账期×付款主体×物流商×**业务线×费用归属×业务性质** 三维分列下拉——业务线定核算维度与 TO C 判断；费用归属定 科目/部门/费用项目/**税率**（维表键升级为 供应商×费用归属，工具 logistics_tax_rates 本就是此结构零改造；实证案例=极鲜达运输9%票/仓储6%专票，报价单原文）；**业务性质**（销售/样品/成品调拨/原料调拨/内部领用）为对账拆细落点，与金蝶四单据类型一一对应（销售出库/其他出库/分步式调出入/采购入库，均已实测可读），三期定稿按此粒度回填、并可反向校验（计提说样品但匹配到销售出库单→提示口径不符）。旧「费用归属」混合标签→三维**翻译对照表**（机械重键不重验映射；入库类的业务性质=原料/成品调拨待业务方逐项确认 D3a）。样表重做 `物流费用计提表模板_新版草案_v1.1.xlsx`（桌面）：维表带 极鲜达×仓储6% 差异化税率示例行、明细表税率公式两级取数（简称×费用归属精确→简称默认兜底）、汇总表增按业务线小计块，公式验算通过（v1.0 样表被 Excel 占用未删、已口头作废）。方案落 `03_Source_Materials/物流计提表_一期格式优化方案_v1.1_20260709.md`（v1.0 归档），决策点更新为 D1–D6+D3a 待批注。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.46** | 2026-07-09 | **一期开工：《物流计提表·一期格式优化方案 v1.0》+ 新版模板样表草案**（纯文档/样表，未动代码与在用表）。输入=旧格式《2026年计提.xlsx》（110 列宽交叉表+电商仓/非月结两附区，一月一 sheet）+ 用户新模板草案《物流费用计提表模板（2026年7月份）.xlsx》（扁平明细+汇总+维表雏形，方向认可）+ Web 工具 V2.29 现状。**方案核心**：①扁平《计提明细表》为唯一手填表（每行仅填 付款主体/物流商/费用归属/结算类型/含税金额 5 列，其余公式或工具回填；电商仓与非月结货拉拉并入明细表按"结算类型"区分，取消附区）；②**费用归属沿用旧口径标签**（映射表 1-6 月序时账已实证，不拆两列重验）；③「是否山姆项目」不设手填列（工具按实证规律自动挂 TO C）；④《物流商汇总表》100% SUMIFS 自动生成，带状态推进列 已计提/对账定稿/已开票/已请款/已付款（对齐四期钉钉付款）；⑤维表单一事实源=Web 工具税率维表，Excel 维表 sheet 为只读快照；⑥预留 **对账定稿金额/计提差异/差异处理** 三列（三期对账工具回填）；⑦迁移=7 月新模板试运行、新旧解析器并存、历史封存。**样表**：`物流费用计提表模板_新版草案_v1.0.xlsx`（桌面）——三 sheet 带公式/下拉/手填蓝字·公式灰底·工具回填绿底配色，用 5 月真数据做 8 行示例；修复 sheet 名含 & 的公式引用需加引号问题（105 处），公式逻辑人工验算通过（recalc.py 在 Windows 上 AF_UNIX 限制不可用）。方案文档落 `03_Source_Materials/物流计提表_一期格式优化方案_v1.0_20260709.md`，**含决策点 D1–D6 待业务方批注**（含税口径/按月分文件/费用归属沿用/维表以工具为源/附区并入/状态五档）。批注定稿后：出正式模板 + Web 工具新格式解析器（一期开发项）。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.45** | 2026-07-09 | **《物流费用管理·四期路线图 v1.0》确认落档**（纯文档）。物流计提+物流对账统一排期，业务方当日确认：**一期**=优化计提表/汇总表格式（口径与对账对齐：承运商×费用归属×主体×账期，预留 计提数/对账定稿数/差异 三列）；**二期**=物流部门已回填金蝶单据号的账单，工具**拿单号直查金蝶比对**数量/重量/金额（复用 V2.44 内核核价核量，匹配引擎不启用；新增单号查无/重复/不符校验；低风险先建信任）；**三期**=原始账单核对上线（五步屏+价格卡/别名表界面+五信号匹配引擎启用+5 家承运商铺开+**结算定稿**锁定留痕）；**四期**=钉钉付款申请（人工触发绝不自动发、审批单带差异清单/回填账单证据链附件、**发票金额=定稿金额卡点**；需协调钉钉审批模板与应用凭证）。路线图取代对账确认书 v1.2 §12 的 A/B/C 分期表述，口径基线（R1–R14/9态/零容差/账期=发货月按账单）不变。文档落 `01_Current_Deliverables/物流费用管理_四期路线图_v1.0_20260709.md`。下一步：一期开工，待业务方提供现行计提表/汇总表样表+痛点清单。 | Claude / c | Approved | —（文档任务，无修订文件夹） |
| **V2.44** | 2026-07-08 | **物流对账工具 A 期第一增量：对账内核 + 金蝶四单据取数 + 极鲜达 6 月真数据全 DoD 达标**。前置：需求确认书 v1.2 当日经业务方签署（"可以，开工"）转 Approved Baseline 移入 01。**新增** `kernels/logistics_recon.py`（账单解析极鲜达 4-sheet 模板 / 五信号匹配含 S1 内部单剔除·S2 收货地址线路·S3 日期±2·S4 按客户计费重折算·S5 别名表 / **山姆全局分桶配对**（城市×件数×品规分桶+保序最小日期差 DP 对齐——账单发货时间与金蝶单据日期有 ±2~4 天漂移且窗口缓冲混入多余单据，逐行就近贪心会抢错，实证 3 行）/ **对账组 1:N**（同客户多单合计重量）/ 零担·快运分档·山姆板折·首衡仓储四套价格重算（零尾差）/ 体积重行降级仅提示（备注箱规或计重小数位>2）/ 9 态归因 / 三道勾稽 / 差异清单+单号回填 Excel 导出）、`kernels/test_logistics_recon.py`（**23/23 绿**：四种计价/折算/内部剔除/别名确认与未确认宁标不猜/对账组/保序对齐胜贪心/9 态/勾稽）、`tools/run_recon_jxd.py`（端到端跑分器，--cache 支持）、`sample_data/logistics_recon_极鲜达.json`（价格卡种子：42 城零担价+22 城快运分档+山姆项目+首衡仓储+折算规则+17 条别名含 confirmed 标记）；`kingdee_client.py` 增 `fetch_outbound_docs()`（四单据只读，缺列自愈降级）。**真数据跑分（极鲜达 6 月账单 181 行 × 金蝶 14,120 分录）**：三道勾稽全过（明细↔汇总页三段全平）；**DoD-1 山姆回填 102/102 与物流部门人工一致**；DoD-2 零担快运自动匹配 52/69=**75%**（≥70% 达标，未匹配行均给候选）；DoD-3 手工原型 712 元疑似差异**全部自动捕获**且多抓 3 笔（夏晖专车+170/长沙米陀加急快运+22/新疆快运少收-154），差异态合计 750 元；覆盖率（按金额）自动核 **93.2%**；9 态分布：一致 140/待人工 18/规则未配置 5/重量不符 6（真差异：账单计重高于 ERP 实重 8~25%，如江门 132vs98）/多收 12。产出 `极鲜达_差异清单_2026-06.xlsx`、`极鲜达_单号回填版账单_2026-06.xlsx`（桌面）。**已同步 01**（kernels×2/tools/sample_data/kingdee_client，01 侧 23/23 绿 + import app OK；未接 app.py 路由，对运行中应用零影响）。下一版本：API 路由 + 五步工作流前端屏（按 §5/§7.6）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.43** | 2026-07-08 | **《物流对账工具需求确认书》升版 v1.2 —— 真数据实测闭环**（文档任务 + 只读探查，无代码改动）。当日两轮实测并入：**①材料实测**：极鲜达报价单（零担城市价/快运分档价/整车/仓储四表）+ 26年6月对账单（4 sheet 210,073.27 元）；手工原型重算发现疑似多收约 712 元/月（西安/郑州 +0.1 单价、天津 +0.05、山姆广州京东价差、首衡短驳 260vs200），零尾差可行（60/60 行运费=计重×单价分毫吻合，极鲜达逐行不取整仅汇总四舍五入→Q7 已答）。**②金蝶只读实测**（scratchpad 探查脚本，未动项目代码）：销售出库/其他出库/分步式调出/调入四单据全部可读；XSCKD=销售出库单，山姆 sheet 物流部门手工回填的 102 个单号 102/102 命中（客户=沃尔玛中国）；出库单自带实重（基本单位=千克）与**收货地址（有值）**；承运商/运输单号字段存在但未填；物料主数据重量体积全 0。**③匹配引擎实测定型（五信号）**：内部单剔除（孝感九↔星期零对开双胞胎，6月 4919 张剔后仅 376 张外部单）/收货地址线路/日期±2/按客户计费重折算/客户别名表（提议→人工确认→记住）；非山姆 73 行零配置自动唯一匹配 60%，"鱼你在一起"经线路反查=杭州熠陪你供应链且计重=实重×1.1 恰好吻合；折算规律四套全部确定性实测（实重 1:1 / ×1.1 / 6.16kg 箱折 / 650kg 板折 / 13kg 京东箱 / 体积重）。**④口径落定**：R6 账期=**发货月、以物流账单为准**（业务方 2026-07-08 批注）；R1 匹配主力改五信号特征匹配（运单号路线废）；新增 R13 内部交易剔除、R14 入向物流标识；新增产出物《单号回填版账单》与客户别名映射表；DoD 以极鲜达 6 月账单为基准测试集（山姆回填 102/102、非山姆自动匹配 ≥70%、712 元差异全捕获）。v1.2 落 `03_Source_Materials/物流对账工具_需求确认书_v1.2_20260708.md`，v1.1 归档。仍开放：余 4 家承运商（Q1/Q2/Q7）、加急与山姆+0.1 书面依据（R8 依据待补卡）、状态模型表态（Q8）、流程改进两项决策（Q9：物流补录承运商字段、物料档案补箱规体积）。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.42** | 2026-07-08 | **《物流对账工具需求确认书》升版 v1.1**（纯文档，无代码改动）。按业务方当日批注面板反馈落定：✅范围边界（A/B/C 分期）与五步工作流通过；✅对账组窗口 **±2 天**（R2）；✅**重量零容差**（R3，业务方强调重货/抛货计费重换算是命门，换算规则逐家按合同录入并抽单验证）；✅**金额零尾差·一分都要对**（R5）；✅账期口径**自然月**（R6）。金蝶出库单三字段（运单号/体积箱规/承运商）初步答复"都有"、列为 D3 开发首步实单验证；常用承运商已定 1/5=**极鲜达**。**新增 Q7 计费取整规则**（零尾差下各家合同取整方式直接决定假异常，随合同附件确认）、Q8 状态模型表态（批注未涉及）。风险表新增"零容差+换算/取整不准→假异常淹没清单"为最大失败风险。v1.1 落 `03_Source_Materials/物流对账工具_需求确认书_v1.1_20260708.md`，v1.0 移入 `03_Source_Materials/需求确认书草稿归档/`（沿用银行对账草稿归档惯例）。仍开放：余 4 家承运商+账单样例+合同附件（Q1/Q2）、取整规则（Q7）、状态模型表态（Q8）、动态条款（R8）。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.41** | 2026-07-08 | **新增《物流对账工具需求确认书 v1.0（草稿）》**（纯文档，无代码改动）。基于当日头脑风暴：新工具「物流对账」两大核对目标——①按合同价格卡重算核价（零担阶梯/首重续重/整车/冷运四种计费，含体积重系数与生效日期）②账单×金蝶出库数据核数量重量（精确匹配 + **对账组 1:N** 解样品汇总发货；无单号账单 A 期标暂不核对、B 期模糊匹配）。约 20 家承运商分层：常用 5 家 A 期自动核、长尾 15 家登记+人工核。差异 9 态自动归因（多收·单价/多收·附加费/重量不符/数量不符/账单单边/ERP 单边/规则未配置/待人工配对/核对一致）出《差异清单》交人工判断，工具边界到清单为止、金蝶只读。含口径 R1–R12、三道勾稽（笔数/金额/覆盖率）、A/B/C 分期与待确认清单 Q1–Q6（承运商名单与账单样例、合同价格附件、金蝶出库单运单号/体积/承运商字段、容差参数、账期口径）。文档落 `03_Source_Materials/物流对账工具_需求确认书_v1.0_20260708.md`，**待业务方签署**；签署后转入 01 并另行立项定开发版本。 | Claude / c | In Review | —（文档任务，无修订文件夹） |
| **V2.40** | 2026-07-07 | **修复物流计提录入月份错乱（凭证全录到当前期间而非指定月）·两个金蝶 Save 坑**。用户反馈"选了7月却录到6月"。**实证根因**（View 回查每张凭证实际 Date/PERIOD）：`build_kd_model` 的日期字段有两个坑，缺一即被金蝶忽略→凭证钉到"当前会计期间"(6月)：**① 字段名必须 `FDate` 不是 `FDATE`**（全大写金蝶不认，记忆里本记对是 FDate，但代码错写成 FDATE）；**② 金蝶 DynamicFormService.Save 对 JSON 字段【顺序】敏感——`FDate` 必须排在 `FACCOUNTBOOKID`/`FEntity` 之后**，放最前会被忽略。修：`build_kd_model` 返回改为 FACCOUNTBOOKID/FVOUCHERGROUPID/FEntity 在前、FDate/FYear/FPeriod 在后。**验证**(5200 真金蝶 View 回查)：指定6月→PERIOD6✓、7月→PERIOD7✓；8月→金蝶正常报错"只允许录入未关闭期间…请先期末结账"(会计规则、错误已如实抛出)。**影响**：此前所有经工具录入的凭证(含 V2.29 起"测试成功"的)其实都落在当前期间6月——用户可用 V2.39 撤销功能删掉重录(撤销按内码走、不受期间影响)。仅改 kernels/logistics_accrual.py，无前端/DB 变更，静态包不变(index-BfAdlQid.js)。同步 01↔02、部署包已更新。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.39** | 2026-07-07 | **物流计提·撤销录入（删除已录金蝶草稿）做进工作台**。用户测试录了几张凭证要删，且希望删除也在工作台里做。**安全边界**：只删①本工具录入的(有 `logistics_post_log` 台账记录，绝不碰其它凭证)②仍是草稿态的(View 查 DocumentStatus，已提交B/已审核C 不删、提示去金蝶反审核)③需 `logistics_post` 权限。**后端**：db 加 `list_logistics_posts(year,period)`/`get_logistics_post(id)`；app 加 `GET /api/logistics-accrual/posted`(列本期台账+View 查金蝶实时状态/可撤销)、`POST /api/logistics-accrual/unpost`(逐张 View→草稿则 delete_vouchers+清台账/金蝶已无则只清台账/已提交已审核拦下，写审计)。**前端**：`LogisticsAccrual.jsx` 第④步加「撤销/删除已录草稿」区——查看本落账期已录凭证表(勾选/凭证号/摘要/录入人/时间/金蝶状态)，已审核灰行禁勾，`撤销选中·删除草稿` 红按钮+二次确认。**验证**(5200 真金蝶全链路)：录 1 张(记-493,草稿)→列出(草稿·可撤销)→撤销"草稿已删除"→复查 0 张(金蝶草稿删、台账清)；物流页渲染冒烟正常。新包 index-BfAdlQid.js，同步 01↔02。**部署注**：仅 app.py/db.py/LogisticsAccrual+static，无新表(复用 logistics_post_log)。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.38** | 2026-07-07 | **账号管理升级：分级管理员（主管理员 / 工作台子管理员）**。用户需求：管理员分主管理员 + 各工作台子管理员(各主主管，可对相应工作台建号、配权限)。**设计**：主管理员=现 role=admin(全权、能任命子管理员、能删账号)；子管理员=普通用户 + 新能力点 `manage_accounting/manage_bp/manage_legal`(勾哪个=管哪个工作台，可多个如总监管三个)。用户拍板：**子管理员看不到别工作台账号(完全隔离)、不能删账号只能禁用**。**后端 db.py**：CAP_META 加「工作台管理权（子管理员）」组三点(入 _SENSITIVE_CAPS 默认关)+ WS_CAPS/GRP_OF_WS/MANAGE_CAP/WS_LABEL 映射 + is_super/managed_workspaces/can_admin_accounts/assignable_caps/manageable_grps/can_manage_user 判定；**修 parse_perms 安全洞**(NULL 老账号原"全开"会误开 manage_*/logistics_post，改为敏感能力一律不给)。**app.py**：账号管理端点从"仅 admin"改分级——list 子管理员只返可管账号+scope；create 子管理员限本组·普通·可授权限(越权字段过滤)；perms 子管理员只改可授能力(合并保留范围外，碰不了 manage_*/别工作台)；active/reset 限 can_manage_user 范围；**delete 仍仅主管理员**；/api/me 与 /api/perms/caps 返回 is_super/can_admin/managed_ws/assignable/manageable_grps。**前端**：UserAdmin.jsx 按 scope 适配(建号组下拉限管辖组、角色锁普通、权限编辑只显可授组、无「管理权」组不能任命、删除按钮仅主管理员、标题带身份徽章)；Portal.jsx「账号管理」入口对子管理员放行(门户管理仍仅主管理员)、身份显示 主管理员/工作台子管理员。**验证**：db 判定自检全过；**API 端到端**(主管理员建核算子管理员→子管理员登录建核算组账号越权字段被过滤、建BP组被拒、删账号403、碰主管理员403、只见核算组普通账号)；**DOM**(子管理员：有账号管理无门户管理、组下拉仅核算组、无角色下拉、权限组仅核算三组无管理权组、无删除按钮；主管理员：全组+管理权组+删除+门户管理)全通。新包 index-CLhgouV-.js，同步 01↔02。**部署注**：重启自动加 3 能力点(存 users.perms JSON)，无新表；现有 admin 自动=主管理员。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.37** | 2026-07-07 | **逐笔稽核状态「金蝶单边·待查」改名「金蝶单边·疑似做错」+ 打服务器部署包**。承 V2.36 以银行为锚点口径：金蝶单边语义已是"疑似金蝶做错(重复/错账户/错记)"，故状态名对齐。改 `kernels/reconcile.py`(_CN/summarize/文档注释)、`views/Reconcile.jsx`(ST/CNT/CHIPS/KPI切片器/可信度带提示"疑似金蝶做错·待核，或银行流水未导全")、`test_reconcile_v2.py`(9处summary键)——状态 key `kd_only` 不变、仅显示名与 summary 中文键改。单测 28/28 通过；DOM 实测切片器显示"金蝶单边·疑似做错"、无旧名残留。**打服务器部署包** `finance_workbench_backend_上传腾讯云.zip`(项目根，20文件/0.21MB)：含 backend 全部 .py+kernels+static(index-C1AyCJRX.js/index-BCH1ozoG.css)+requirements，**排除 conf.ini/sample_data(config.json/workbench.db/claims等运行数据)**——不覆盖服务器金蝶授权与配置。新包 index-C1AyCJRX.js，同步 01↔02。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.36** | 2026-07-07 | **余额调节·核心口径改为「以银行流水为锚点」（重大逻辑修正）**。用户指出稽核逻辑方向错了：银行流水是真实的、金蝶账面要向银行看齐；金蝶单边很大概率是金蝶做错(做错账户/重复/错记),要金蝶去改,不能拿银行凑金蝶。**旧逻辑的错**(0308 实例)：金蝶把利息 21737.46 记两次(银行仅一次)、又把资金调拨记本户实走他户,这些「金蝶单边」被当成"企业已记银行未记"的合法未达、**加到银行侧凑成假对平**——掩盖金蝶账面虚高该更正的事实。**新口径**：银行对账单余额=锚点；金蝶单边净=**「金蝶待更正」**(疑似做错,应冲/改)、漏账+内部往来净=**「金蝶应补记」**；更正后账面=金蝶账面−待更正+应补记，应=银行余额，`对银行差额`应0。**有金蝶单边的户不再算对平**，状态改为 `账实相符 / 待金蝶更正 / 不明差异 / 缺账面`。用户拍板：`kd_only`(真金蝶单边)+`kd_xfer`(内部划转·对应他账户=记本户实走他户)**都算金蝶待更正**。落地：`_balance_adjust` 账户循环重写(kd_fix/kd_add/book_fixed/diff/status)+`钩稽`环②改计数；`FundDashboard` 表改 12 列(银行余额锚点高亮+金蝶待更正/金蝶应补记/更正后账面/对银行差额/状态胶囊)、标题计数与钩稽②同步、列 tip 全改；导出底稿列同步换。**验证**(5200 真数据 21 户)：账实相符18/待金蝶更正2/不明差异1(旧版假对平20/不平1)；**0308 现「⚠待金蝶更正」·银行锚点35,960,955.82·金蝶账面34,234,703.61·金蝶待更正-1,726,252.21·更正后=银行·差额0**(不再假对平)；Sinkio港币户差0.03(外币舍入,暂列不明差异,待放宽容差)。新包 index-B3po4XyC.js，同步 01↔02。**部署注**：仅 app.py+FundDashboard+static，无DB变更。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.35** | 2026-07-06 | **余额调节·钩稽关系面板（做法2）+ 补回丢失的后端改动 + 修港币户原币账面老 bug**。用户问"余额调节最终调节数要跟金蝶科目余额表核对，这个钩稽关系能在核对时呈现吗"。**①先修地基**：核实发现之前 V2.34 的**后端 app.py 余额调节改动实际没落盘**（`_acct_info` 仍 2 返回值、无币别/账户名称/未达原因/本位币字段、无 `/api/balance-adjust/note` 路由；前端/db/kingdee 却已有 → 前后端不一致、这些功能实为坏的）。已补回：`_acct_info` 改 4 返回值、`_balance_adjust` 加 open_bal_base/kd_move_base/book_base + 每户 币别/账户名称/未达原因/金蝶账面本位币/有未达、note 路由、导出加列；前端补「本位币账面」列（此前也漏建）。**②修实证老 bug**：金蝶科目余额表对外币户返回**两行**——币别=空的本位币合计行（原币栏其实是本位币）+ 币别=具体的原币明细行，原去重按(科目,维度)只留先到的一行、顺序不定，导致港币户"金蝶账面原币"在 7049.22(对)与 898.41(错混本位币)间飘。修：去重时**优先保留具体币别行**（原币才准），本位币两行一致。验证：港币户原币 7049.22 港币→本位币 898.78 美元（比值 0.1275 ✓）。**③钩稽面板（本次核心）**：`_balance_adjust` 返回 `钩稽` 三环 + `FundDashboard` 加可视核对链面板 `TieRow`：环①笔数勾稽(账证：银行622/金蝶626 全归类✓)、环②余额调节对平(账实：覆盖21户 对平20/不平1)、环③**对金蝶科目余额表(账账★)**：1002 共 27 户有余额 = 已纳入调节 21 户 + **未纳入 6 户逐个列出**（有金蝶余额但没导银行流水=核对缺口，如孝感九宁波¥4840.91、深零宁波¥14.12…，多为星期八测试户+零星小额）；口径=期初+序时账净，与「科目余额表」视图一致。**验证**（5200 真数据全链路 API+DOM）：钩稽三环数值正确、本位币列港币行 898.78美元、note 路由 200、逐户对平；后端 py_compile+import OK。**⚠过程教训**：本会话中段 export_report 区曾因编辑错位损坏(已恢复重做)，且部分中间验证结果不真实——本版所有结论均以真实 API/DOM 复核为准。新包 index-Cg3dgW8v.js，同步 01↔02。**部署注**：仅 app.py/kingdee_client.py/FundDashboard.jsx + static，db 的 balance_notes 表重启自动建。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.34** | 2026-07-06 | **逐笔稽核头部精简 + 余额调节表增强（币别/完整户名/未达原因/主体筛选）**。用户三点：①逐笔稽核头部删多余「认领人」(左下侧栏已有登录人)、「刷新」→「刷新金蝶数据」+悬停说明、删「每5秒自动刷新」字样(逻辑保留)；②余额调节表账户名此前被 `slice(0,10)` 截断→改完整显示(主体+账户名称+开户行)、加「币别」列(台账 CNY/HKD/USD→前端映射中文，外币蓝字，与逐笔稽核统一)、加「未达原因/说明」列(有「认领/处理差异」权限的会计填/改、显示填写人+时间、供领导核查，持久化=新表 `balance_notes` 按 期间×账号 upsert、`POST /api/balance-adjust/note`)；③余额调节表加**主体筛选器**，用与逐笔稽核**同款 `.fbar`**(筛选·主体下拉·清除筛选)，选主体收窄表格+对平计数联动。db.py balance_notes 表/CRUD、kingdee_client GL_VOUCHER 币别字段(V2.32起)已在位。**注**：本条对应改动的后端 app.py 部分在 V2.35 才真正补齐落盘(见 V2.35 说明)。新包同 V2.35。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.33** | 2026-07-06 | **逐笔稽核·切片器合并同类态精简**。用户反馈 V2.31 联动后切片器好用但偏多（13 个含全部），选定「合并同类」方案。落地 `views/Reconcile.jsx`：把三对相近状态各并成一格——**晚记**=晚记·本月+跨期晚记、**内部往来**=未做账+对应他账户、**账户缺数据**=缺银行流水+缺金蝶数据；「汇兑·账面调整」单列（无可配对同类，外币期末重估特有）。切片器 13→10。**只合并切片器层，不丢细分**：`KPI` 每格加第 5 位状态集 `sts`，`SLICE_ST` 展开为筛选/计数依据，合并格计数=所辖状态之和（`sumSt`），**表格行状态标签仍按 `r.status` 显示细分**（点「内部往来」表格里如实分「内部往来·未做账 / 内部划转·对应他账户」两标签）；合并格标 `⧉` 记号 + 悬停「合并态，明细状态见表格」。计数仍随筛选条联动、显示「当前/全量」小分母（复用 V2.31，改按状态集汇总 `fullCnt`/`sliceCnt`）。**验证**（5200 DOM·真数据 638 行）：10 格=全部638/疑似漏账3/做错·金额0/晚记⧉43/金蝶单边6/内部往来⧉17(=9+8)/汇兑·账面调整2/账户缺数据⧉0/账号对不上0/已匹配567；点「内部往来」表格出两种细分标签、当前提示 17 条；选主体 Sinkio → 全部17/638、晚记4/43、汇兑2 联动小分母正确。纯前端改动。新包 index-CI2h-_Pd.js，同步 01↔02。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.32** | 2026-07-06 | **逐笔稽核·区分币别（原币↔本位币）+ 新态「汇兑损益·账面调整」**。用户点题：金蝶有美元户/港币户/人民币户，要区分原币和本位币。**实证根因**（真数据核查）：GL_VOUCHER 的 FDEBIT/FCREDIT 是**账簿本位币**——境外三簿（Sinkio/Starfield×2）**本位币=美元**，美元户此前能对上纯属"原币=本位币"巧合；**港币户、境外簿人民币户两边不同币根本对不上**（银行 113.92 港币 ↔ 金蝶借 14.52 美元），此前被拆成 疑似漏账+金蝶单边 两堆假异常；且余额调节此前"期初原币+本期本位币"混口径。**修法**：①`kingdee_client.GL_VOUCHER_FIELDS` 补 FAMOUNTFOR(原币金额)/FCURRENCYID.FName(币别)/FEXCHANGERATE(汇率)；②`kernels/reconcile.py` 金蝶侧按**原币**归一对账（Rec 加 currency/amount_base/rate/fx），行输出加 币别/本位币金额/汇率，币别按账号回填银行侧行；**无原币字段（样例数据/老单测）自动退回本位币，向后兼容**；③新状态 `fx_adjust`「汇兑损益·账面调整」——原币=0 而本位币有数的期末汇率重估行，本无银行流水，单列不参与配对（此前误进金蝶单边·待查）；④新增 `kd_delta_for()`（原币净发生，借+/贷-，重估行天然不计），**余额调节 kd_move 与 1012 渠道 kd_net 改用之**——修正"港币期初+美元发生"的混口径；⑤前端 `Reconcile.jsx`：加「币别」列（外币蓝字）、外币金额=原币不带¥号、外币行金额下挂"本位币 X"灰字、重估行标"本位币口径(原币0)"、详情面板列 原币/本位币/汇率、切片器加「汇兑·账面调整」、页脚补口径说明。**验证**：老单测 28/28（兼容路径）+ 新币别单测（港币配对/重估单列/原币汇总/退回本位币）全过；**真数据（643→638 行）**：港币户 2 笔按原币配上（晚记·本月，本位币 12.75/14.52 另列）、汇兑重估 2 笔（港币1.4/人民币0.58）单列、**疑似漏账 8→3、金蝶单边 13→6（消掉 12 笔假异常）**、已匹配 565→567、护栏两侧各归一类仍 True；UI 币别列/外币无¥/本位币小字/汇兑切片 DOM 实测全对。新包 index-Bnd_r1Uq.js，同步 01↔02。**部署注**：替换 backend 3 个 .py + static，重启即可，无 DB 变更。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.31** | 2026-07-06 | **逐笔稽核·切片器计数随筛选条联动**。用户需求：顶部 12 个状态切片器的张数要跟着下方筛选条（主体/开户行/账号/关键字搜索/认领情况）变化，此前切片器一直显示后端全量汇总、与筛选后的表格对不上。落地 `views/Reconcile.jsx`：新增**底数集** `base` ＝除状态切片外全部筛选命中的记录集，12 个切片器计数改为按 `base` 前端实时计数（`sliceCnt`），表格 `rows`＝`base`∩当前状态切片——切片器与表格口径严格一致；**有筛选且数字被收窄时切片器显示「当前/全量」小分母**（如 疑似漏账 5/8，`styles.css` 加 `.kpi .kv .kt` 灰色小字），悬停提示"当前筛选下 N 笔·全量 M 笔"，清除筛选即复原纯数字。**验证**（5200 DOM 实测·金蝶真数据 643 笔）：选主体 Sinkio → 全部 22/643、疑似漏账 5/8、金蝶单边 7/13、已匹配 9/565 且分状态和=22 与表格一致；叠加搜索"利息"→ 全部 1/643、金蝶单边 1/13；点"已匹配"切片(0) → 表格 0 条口径一致；清除筛选 → 复原 643 无分母。纯前端改动。新包 index-D0UDxTj8.js / index-BCH1ozoG.css，同步 01↔02。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.30** | 2026-07-06 | **物流计提页面重构：对标「对账程序」骨架——全宽自适应 + 每步独立成页**。用户两点要求：①拉满一个屏幕、自适应调节 ②每个步骤单独成页。落地 `views/LogisticsAccrual.jsx`：**布局**改用工作台标准 `.head`(标题/副题)+`.body` 骨架，去掉原 `maxWidth:1180` 限宽——1920 下侧栏 216px+内容 1704px 拉满、1100 窄屏无页面横滚（复核清单在 `.la-tblwrap` 自身容器内滚动、控件条 flex-wrap 自动换行）；**步骤条**复用对账程序全局 `.steps/.step` 样式（当前步高亮、完成步打✓、全部可点跳转、副标题实时联动：①显示文件名·月份 ②已维护 N 条 ③N 张·借=贷 N/N ④成功 N 张），**五步各自独立成页**（内部 step 状态，数据在父组件共享跨步不丢）：①导入与排查(月份/上传/供应商核对) ②税率维护(维表 CRUD+缺税率/不一致提醒+一键采纳) ③解析复核(指标卡+只读复核清单) ④勾选录入(勾选列清单+落账期间+录入结果) ⑤打印附件(打印按钮+清单+58张凭证卡片，`#la-print` 仅本步挂载)；未就绪步骤给白话空态引导(「先在③解析…去处理›」)+每步尾「下一步›」按钮。**顺带修一真 bug**：复核清单原写成组件内嵌 `<ReviewTable/>`，组件身份逐次渲染变化 → React 整表卸载重建，勾选框连点第二下失灵、丢滚动位置；改为调用式渲染函数 `reviewTable(withCheck)` 后修复（实测连点 3 张 58→55、表头全选复原 58）。**验证**（5200 DOM 实测）：五步切换/空态引导/文件注入解析 58 张/勾选连点/全选/打印页 58 卡/1920 拉满/1100 无横滚全通过；纯前端改动、后端/DB 零变更。新包 index-DDBpygM0.js，同步 01↔02。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.29** | 2026-07-06 | **物流计提 A 期落地：5 步工作流 + 权限分离 + 税率维表 + 一键录入金蝶（真写实证）**。承说明书 v0.2 §11（B 期应收单/费用率不含，留 B 期灰字占位）。**①权限分离**：`db.py` CAP_META 加「月结与结账」组两权限点——`logistics_upload`(上传计提表·维护税率)/`logistics_post`(一键录入金蝶)；**录入为写正式账套的敏感操作，新账号默认不给**（`_SENSITIVE_CAPS`），老账号也不会自动获得（parse_perms 缺键=False），须管理员在账号管理显式勾选；parse/suppliers-check/税率写 挂 upload 权限、post 挂 post 权限，无权限 403 白话提示。**②税率维表**：新表 `logistics_tax_rates`(供应商×费用类型→税率，不含税口径，费用类型空=该供应商默认) + CRUD 路由 + 前端维护面板（增删、缺税率红条、维表↔计提表税率不一致琥珀提醒、「计提表税率一键采纳入表」）；解析取数口径=维表(精确)→维表(默认)→计提表格内→缺税率（缺则该格不可勾选录入）。**③写金蝶编码实证补全**（一次性脚本从 1-6 月序时账 889 行批量反查，非手填）：**孝感账簿=107（纠正记忆中的 104，104 实为 Starfield 英文账簿）**、部门/费用项目编码三账簿一致、供应商分组全=供应商009；**产品项目 TO C(CPXM017) 实证规律=业务线「鲜食」或费用归属带「山姆」的费用行全挂**（61 行鲜食+31 行山姆全挂、其余全不挂——比说明书 v0.2 只写山姆系宽，按账上口径实现）；同名供应商多档案（极鲜达有押金档+物流档）按「分组=供应商009 优先」取编码。**④一键录入**：`kingdee_client.py` 加 `save_voucher`/`delete_vouchers`/`view_voucher`/`supplier_code_map`；`logistics_accrual.py` 加 `build_kd_model`(凭证→Save Model，配方=已验证的 FDetailID 对象+完整 FDETAILID__FFLEXn 键)+`check_voucher_for_post`(服务端重校验，不信任前端回传)；`/api/logistics-accrual/post`(勾选凭证+落账年/期间可指定)。**关键新实证：草稿凭证在 ExecuteBillQuery 列表查询里完全不可见（连摘要都查不到），但 View 按内码可读、且保存即分配记-字号(VOUCHERGROUPNO)**——故凭证号回传走 View；**防重录双闸**=金蝶已提交/已审核同摘要凭证查询 + 工具自建录入台账 `logistics_post_log`(同年/期/摘要幂等，台账命中先 View 自愈：草稿被人在金蝶删了则放行重录)。**⑤前端 5 步工作流**：`LogisticsAccrual.jsx` 重构——步骤条(①导入排查②税率③解析④勾选录入⑤打印)、复核清单加勾选列(全选/单选，缺税率行禁勾、打印时隐藏)、落账 年+期间 选择(默认=会计月份，会计月份扩到 1-12)、录入结果面板(成功/跳过/拦下逐条白话原因)、凭证号自动回填清单与打印卡片、TO C 挂维标签、无权限按钮置灰+提示。**验证**：内核断言全过；5 月真表回归 58 张/借=贷 58/未税一致 58/挂 TO C 8 张规律正确/58 张 Save 报文借=贷全平；**真写实证（5200 服务 HTTP 全链路）**：录 1 张(顺丰·鲜食·66 元，期间 7)→金蝶回「记-490」→同张重录被台账拦→无权限用户 parse/post/税率 403→金蝶草稿删除+台账/税率/测试账号全清（金蝶库干净）；对账内核单测 28/28；UI(5200)：5 步条/税率面板增删/权限组「月结与结账」在账号管理自动出现。新包 index-NJw1dYBL.js，已同步 01↔02。**部署注**：重启自动建 2 新表；管理员需给核算同事勾「上传物流计提表·维护税率」，「一键录入金蝶」按人单独授。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.28** | 2026-07-06 | **物流计提·打印加「单凭证卡片」+ 并发构建冲突修复**。①核算组意见：生成 PDF = 复核清单(先) + 每张凭证一页卡片(后)，卡片「一物流商一卡、高亮当前凭证行」。落地 `views/LogisticsAccrual.jsx`：`#la-print` 内复核清单后加 `.la-cards`(屏隐藏、`@media print` 显示)，`groups.flatMap(items)` 每凭证一页 `.la-card`(`page-break-before:always`)——卡头 主体/凭证号/期间/供应商；①该供应商本月全部计提明细表(列与复核清单一致，当前凭证行 `.hl` 琥珀高亮)；②本张凭证生成分录(借费用[部门/费用项目/业务线]+借暂估进项税+贷其他应付款，合计借=贷)+摘要+签字位。验证：58 卡、每卡高亮1行且卡头凭证号=高亮行凭证号(极鲜达11张509→509…)、②为当前凭证分录。②**并发修复**：本工作与另一会话 V2.25(逐笔稽核筛选/内部划转)/V2.26(花旗PDF+建行CSV)/V2.27(稽核序号)并行；先前误用**落后的开发夹源码**重打前端包、同步 01 时**回退了他们 V2.25 前端(fbar 等)**。修法：确认 01 源码为完整超集(物流卡片 + fbar + FundDashboard + citi_pdf/reconcile)后，**在 01 就地重打**得 index-Bxq4uGOe.js/index-CCDBGTcx.css(含双方所有功能，CSS 哈希与他们 V2.25 一致)，并把完整源码反向同步回开发夹使两处一致；核对 bank_import.py 同时含我 V2.24 RAR 回退与他们 V2.26 花旗/建行解析、未互相覆盖。**教训**：多会话并行务必以 01 现源为准构建、勿从可能落后的开发夹盲目覆盖。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.27** | 2026-07-06 | **逐笔稽核序号改为「按账号分组·组内日期升序」**。用户需求：序号(左列全局稳定号)原按 `状态优先级(疑似漏账置顶)→账号→日期` 编，改为**按账号分组、组内按银行流水日期升序**——每账户流水连成一段、由早到晚，序号在账户内连续不断号，便于"XX户是第N到M号"沟通。`kernels/reconcile.py` 排序键 `(_ORDER[status],账号,日期)` → `(账号, 日期 or '9999-99-99', _ORDER[status])`(同账号同日仍按状态优先级稳定兜底；无银行日期的金蝶单边用其日期字段沉到组内靠后)。放弃"漏账置顶"，但切片器一键筛「疑似漏账」仍可优先看。前端 `_seq` 随后端顺序，无逻辑改动（仅更新注释）。**验证**(5200)：组内日期升序✓、每账户连续不跨插✓、序号全局连续；单测 28 通过。**无DB变更**，替换 kernels/reconcile.py + 重启即可（前端无需重打）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.26** | 2026-07-06 | **花旗 PDF + 建行 CSV 对账单解析·补齐全部"账户缺银行流水"缺口**。用户反馈"有些流水没读到"。排查(逐笔稽核 no_bank_acct + 数据接入清单):导入器只按文件名认 2 类源(财资 宁波+招商 xlsx / 中行 HISQRY csv=596笔)，其余跳过；花旗 4 户 + 建行 1 户共 5 户金蝶挂账、银行侧 0，流水在 PDF/异名 CSV 里未适配。**排除"假缺口"**：`交易查询…共139笔.xlsx`(8户)经逐户笔数比对**已被财资文件覆盖**(每户笔数一致)，导入会双计，确认不并入。**①花旗**：新增内核 `kernels/citi_pdf.py` 解析两种版式——A 亚洲对账单(中文列，按词坐标分列判借/贷方向，排除页顶摘要金额)、B US 报告(英文定宽 DEBITS/CREDITS)；逐户余额自校验(期初+Σ贷−Σ借=期末)、仅并入通过账户。**②建行**：`bank_import.py` 加 `is_ccb_csv`/`parse_ccb_csv`(gb18030、按表头名取列、余额连续性自校验)。两者金额均原币(与金蝶花旗/建行户口径一致)。`bank_import.py` 加内容嗅探(文件名多为 Unsaved-*/异名，不能靠名)+ load_bank_dir 分支 + manifest 逐户展示自校验。**验证**(5200·真数据)：花旗 2 PDF 并入 25 笔(5户自校验OK)+ 建行 1 笔(结息 0.26↔金蝶记-398 matched)；银行笔数 596→622；**「账户缺银行流水」5 户→0 户**；缺流水账户全部进入逐笔稽核。**无前端/DB 变更**；PyMuPDF 已在 requirements，无新增依赖。改 backend/kernels/{citi_pdf.py(新),bank_import.py} + 重启即可。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.25** | 2026-07-05 | **逐笔稽核筛选强化 + 跨账号内部划转识别 + 余额调节可读性**。①逐笔稽核加筛选条：**主体→开户行→账号** 三级联动收窄 + **认领情况**(未认领/已认领/已认未调整) + **对方户名·交易备注 模糊搜索**(一个关键字同搜两字段)，与状态切片器 AND 叠加、清除一键复位(`views/Reconcile.jsx`+`styles.css .fbar`)。②对账内核新增**第七态 `内部划转·对应他账户`**(`kernels/reconcile.py` `_flag_kd_crossacct`)：判"金蝶单边"前，对含划转特征的金蝶单边做跨账号对手腿甄别——A 同额同向命中别账户未匹配银行流水(钱实走他账户、本户金蝶挂账)，B 同额反向命中别账户金蝶单边(两腿均入金蝶、缺银行流水)，附对应账户；余额调节口径守恒(`app.py` 仍按企业已记银行未记计银行侧未达)。③余额调节：加**未达账项白话折叠说明块** + **删除与科目余额表重复的"各账户余额明细"**(`views/FundDashboard.jsx`)；④数据接入页去 `maxWidth:1120` 改全宽，与逐笔稽核/余额调节对齐。**验证**(5200·金蝶真数据)：账号…0308 原 8 笔金蝶单边 → 7 笔内部划转(实走 7736/与…7061 对开)+1 笔真单边(结息 21737.46 疑重复入账，无划转特征正确留待查)；余额调节 15 户全对平不变；单测 28 通过(+5 断言含负例)。新包 index-ggi6Pwje.js / index-CCDBGTcx.css，同步 01。**无DB变更**，替换 backend .py+static + 重启即可。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.24** | 2026-07-05 | **银行流水 RAR 解压多工具回退修复**。部署到腾讯云(OpenCloudOS)时 RAR 流水包解不了：服务器 7z 不带 RAR 编解码(`7z i` 无 Rar)，而原 `_extract_rar` 逻辑「7z 一失败即抛错、不退到 unrar」，导致装了 unrar 也用不上。改 `kernels/bank_import.py`：`_extract_rar` 依次尝试 **7z→unrar→unar→bsdtar** 全部可用工具，谁成算谁；不再用 `_find_unrar` 只取首个，改为逐个 `which` 收全(unrar-free 解不了 RAR5 时可退到 unar)；密码错立即抛，全部失败才报错并列出已试工具。**注**：服务器最好装 `unar`(免费且支持 RAR5)，unrar-free 对 RAR5 支持有限。**未改前端/无DB变更**，只需替换该 .py + 重启。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.23** | 2026-07-05 | **部署依赖修复·openpyxl 版本下限**。上线腾讯云时物流计提/银行流水解析报「Pandas requires version '3.1.5' or newer of 'openpyxl' (3.1.0 installed)」。`requirements.txt`(开发夹+01) 将 `openpyxl>=3.1` 改为 `openpyxl>=3.1.5`，免重装退回旧版。服务器即时修复：`python3.11 -m pip install --upgrade --no-cache-dir "openpyxl>=3.1.5"` 后重启。另记录部署环境问题：服务器 urllib3 1.26.19 残缺(`No module named urllib3.packages.six`)→ 已升级 2.7.0(idna 3.7 为 rpm 装、pip 删不掉但无害，用 --upgrade 不带 --force-reinstall 规避)。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.22** | 2026-07-05 | **物流计提·复核清单对齐 Excel v3 版式**。用户反馈：缺物流商小计行与加粗外框、主体列位置、核算维度列。落地 `views/LogisticsAccrual.jsx`：①列序重排为 序/物流商/物流商全称/含税/税率/未税/税额/借方科目/**部门·费用项目·业务线(核算维度归一处)**/**主体(倒数第二)**/凭证号；②按 (主体×物流商) 分组，每组末加**灰底小计行**(含税/未税/税额)，整组四周**加粗外框**(`bd()` 逐格算 outer/inner 边)，连续序号；③小计灰底打印保留(`.la-sub` + print-color-adjust)。**验证**(5210)：屏显与强制打印样式上屏均见 每物流商一整块加粗框+灰底小计、主体在凭证号前、核算维度三列相邻；29 个物流商小计。新包 index-BSb4ZPtL.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.21** | 2026-07-05 | **物流计提·复核清单打印修复**。用户反馈「打印复核清单」变成打印整页。落地：`views/LogisticsAccrual.jsx` 加打印专用 CSS——把复核清单标题+表格包进 `#la-print`，`@media print` 下 `body *` 隐藏、仅 `#la-print` 可见并绝对定位到页首；侧栏/控件/指标卡/供应商面板/展开分录行(`.la-noprint`·`.la-detail`)打印时隐藏；A4 横版、表头 `table-header-group` 跨页重复、蓝底表头 `print-color-adjust:exact`；打印时显示「物流费计提复核清单 · 2026 年 N 月」标题。**验证**(5210，强制打印样式上屏)：仅复核清单表格 + 标题输出，其余全部隐藏。新包 index-CrWs6Byd.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.20** | 2026-07-05 | **门户「月结与结账」·物流计提 一期（内核+预览+复核清单，不写金蝶）**。承接 `03_Source_Materials/需求确认书草稿归档/物流计提工具_说明书与一期方案_v0.1_20260705.md`（规则均由 1–6 月 K3Cloud 序时账实证反推）。落地：新增 `kernels/logistics_accrual.py`——解析老格式宽交叉表(按表头文字定位、容忍列漂移/残缺块、月结/非月结分区)→ 映射表(主体×费用归属→科目/部门/费用项目/业务线，业务线=产品分类FF100010，B可留空)→ 分录生成(借 费用[部门/费用项目/业务线] + 借 2221.01.07 暂估进项税[供应商]，贷 2241.02 其他应付款-供应商往来[供应商])→ 自校验(借=贷、未税+税额=含税)；`kingdee_client.py` 加 `fetch_suppliers()`(BD_Supplier 只读)做供应商核对闸门；`app.py` 加 `/api/logistics-accrual/*`(上传解析/供应商核对/复核清单/导出)；前端 `views/LogisticsAccrual.jsx` 挂「月结与结账」。**本版不含金蝶写入**(一键录入+回传凭证号列 V2.21，需先验证 Save 权限)。**验证**(5210 浏览器全链路)：登录→核算工作台→侧栏「月结与结账 › 物流计提」→ 上传 2026物流计提表.xlsx(5月)→ ①供应商核对：计提表15家供应商全部已在金蝶建档(金蝶3361家)；②解析生成凭证：58张，借=贷 58/58、未税与源表一致 58/58、未覆盖映射 0、异常 0、含税合计 533,271.36、非月结5(单列不做账)；点行展开分录(借费用[部门/费用项目/业务线]+借暂估进项税+贷其他应付款，借=贷)；含税总额与计提表内合计一分不差。新包 index-a8ySd-Xs.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.19** | 2026-07-05 | **门户管理 CMS（工具卡片可视化维护，管理员自助）**。用户提议：门户工具卡片(功能/状态/概述/通用技能/AI技能)应能后台管理，不必改代码。落地：**卡片改为数据库驱动**。`db.py` 加 `portal_tools` 表(lane/name/status/icon/descr/gen/ai/sort) + `list/save/delete_portal_tool` + `seed_portal_tools`(首启种入当前5工具)；`app.py` 加 `/api/portal/tools`(登录可读，供门户+CMS)、`/api/portal/tools/save`·`/delete`(仅管理员，写审计)、启动调 seed。`Portal.jsx` 工具卡片改为 `getPortalTools` 接口读取(LANES 只留结构，法务仍占位骨架)、dim 由 status==='soon' 推导；导航加「门户管理」(管理员)→ 新组件 `PortalAdmin.jsx`(深色 pa- 主从：左按工作台分组列工具、右编辑表单 所属台/名称/状态/概述/通用/AI + **门户效果实时预览** + 新建/保存/删除)。保存后回调 `loadTools()` → 门户即时刷新。**验证**(5210)：种子5工具入库；管理员见「门户管理」→分组(核算3/BP2/法务0)→编辑 银行-金蝶稽核 状态→保存→**回门户卡片即变「成熟可用」且计数胶囊同步**→已还原。新包 index-C1d60H-4.js，同步 01。**部署注**：老库重启自动建 portal_tools 表并种子。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.18** | 2026-07-05 | **门户内容更新（工具列表/状态 + 底部行 + AI灯联动模型配置）**。按用户定的门户展示：核算组=银行-金蝶稽核(即将上线)/月结与结账(即将上线)/电商平台对账(敬请期待)；财务分析组=管报编制&经营分析(即将上线)/电商专项管报(敬请期待)（`Portal.jsx` LANES 工具与 status 重写；描述/通用/AI 标同步给合理默认，待门户管理 CMS 落地后改为可编辑）。泳道下方加一行居中「其它功能陆续上新中，敬请期待」(`.pt-more`)。**右上「AI 模型」指示灯改为联动模型配置**：新增 `aiReady`(现无模型配置=false→显示琥珀色「AI 模型 · 待配置 / 点此前往模型配置」)，点击整块→切到「模型配置」页签；`aiReady` 预留 `user.aiReady`，待模型配置后端落地后由真实状态驱动。**验证**(5210)：三/二工具与状态正确、底部行在、AI灯待配置态(amber)、点击跳模型配置且导航高亮。新包 index-D10sdCZP.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.17** | 2026-07-05 | **加「工作台准入」权限组 + 门户准入改按权限**。承 V2.16 主从两栏，加第 2 组权限「工作台准入」：`enter_accounting`/`enter_bp`/`enter_legal`（进入 核算/BP/法务 工作台）。`db.py` CAP_META 增 3 条(group=工作台准入)；`default_perms(grp)` 改**按组给默认准入**——核算组→只开 enter_accounting、BP组→enter_bp、法务→enter_legal，动作类仍默认全开；`create_user` 传 grp。**门户准入从"按 grp 匹配"改为"按权限"**：`Portal.jsx` 各泳道加 `enterCap`，`canEnter=管理员||user.perms[enterCap]`、`isMe=canEnter`——管理员可精确授予"谁能进哪个工作台"，与组解耦。**验证**：db 单测(核算组默认只 enter_accounting、BP组只 enter_bp、可授权叠加)；前端(建 BP组「王五」→账号编辑器两组 工作台准入(进BP✓/进核算✗/进法务✗)+核算工作台(5全开)、徽章6/8；**以王五登录门户→仅 BP 可进、核算/法务🔒、无账号管理导航**)。将来加法务工作台/BP内细权限只需 CAP_META 追加。新包 index-TA7azpv6.js，同步 01。**注**：法务工作台尚未建(泳道仍占位)，enter_legal 为前瞻预留。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.16** | 2026-07-05 | **账号管理改左右两栏主从（为权限扩展铺路）**。用户预期权限会越来越多（将并入"能否进 BP/法务工作台""进去后能干什么"），故 `UserAdmin.jsx` 重做为**主从两栏**：左=账号列表（状态点+组/角色+权限计数徽章，可选中高亮，含可折叠「+新建」），右=选中账号详情（姓名/组/角色/状态+操作 禁用·启用/重置密码/删除）+ **按 group 分组的权限**（勾选+「有未保存改动」提示+保存；管理员显示"拥有全部权限"）。权限分组数据由后端驱动：`db.py` 权限清单改 `CAP_META`（key/label/**group**），`/api/perms/caps` 直接返回——将来加"工作台准入"等新组只需在 `CAP_META` 追加，前端自动分组渲染。深色样式沿用 V2.15（`ua-` 作用域，门户内页签）。功能/后端校验逻辑不变。**验证**（5210）：门户「账号管理」两栏并排+门户顶栏在；建李四→自动选中→右栏"核算工作台"组5勾选；取消"上传资金流水"→保存→`/api/users` 确认 bank_upload=false 其余保持。新包 index-CwgS58aE.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.15** | 2026-07-05 | **账号管理页深色化，风格并入门户**。V2.14 把账号管理搬门户，但当时是踢到工作台**浅色 shell** 渲染，与深色门户风格相撞（用户反馈"偏离前面的风格"）。改为：账号管理作为**门户内页签**（`tab='admin'`，不再走 App `zone='admin'`），保留门户深色顶栏+背景+缩放；`UserAdmin.jsx` 整体重做为**深色自洽样式**（`ua-` 作用域 <style>，portal 调色板：深卡片/半透明输入框/紫渐变主按钮/浅字，不再借用工作台 styles.css）。`Portal.jsx` 导入并在 admin 页签渲染 UserAdmin、`tabOf` 加 `账号管理→admin`；`App.jsx` 移除 zone='admin' 分支与 UserAdmin 导入。功能与权限逻辑不变（V2.14 后端校验照旧）。**验证**（5210）：门户点「账号管理」→ 门户顶栏仍在、内容深色(.ua-root 文字#EDEAF6/输入框深底/主按钮紫渐变)、权限列/编辑器在。新包 index-_zLO7ETw.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.14** | 2026-07-05 | **账号管理搬门户 + 细粒度权限（管理员专属）**。用户要求：账号开设从核算工作台拉出、放门户；门户按账号编辑细粒度权限（谁能上传流水/更新金蝶等）；仅管理员可添加账号和权限。**推荐集 5 权限**（用户确认）：上传资金流水/从金蝶更新/认领处理差异/账户台账改动/上传科目余额表。**后端**：`db.py` users 加 `perms`(JSON)列 + `_ensure_perms_column()` 老库自动迁移(ALTER TABLE，SQLite/MySQL 通用) + `CAPS/CAP_LABELS/default_perms/parse_perms/user_can`（NULL=全开向后兼容、管理员恒全能）；`create_user` 收 perms、`set_user_perms`、`list_users` 带 perms。`app.py`：`/api/login`·`/api/me` 用户信息带 perms(管理员=全True)；新增 `/api/perms/caps`、`/api/users/perms`(仅管理员)；`create` 收 perms；**5 敏感端点强制校验**（bank-import/upload·kingdee/refresh·reconcile/claim·account-ledger/override·subject-balance/upload）无权限回 403。**前端**：账号管理从核算侧栏移除，改由**门户导航「账号管理」**（仅管理员可见）→ App `zone='admin'` 整页渲染 UserAdmin(styles.css 全局可用)+「返回门户」；`UserAdmin` 加权限列(n/5)+每行「权限」编辑器(5 勾选+保存)；`DataImport` 按 `user.perms` 禁用「上传流水」「从金蝶更新」并提示。**验证**：db 单测(建/改权限/管理员全能/NULL兼容)；API 端到端(受限账号传流水/认领 403、有权限更新金蝶 200、/me 带正确 perms)；前端 DOM(门户见账号管理→进入→权限列/编辑器5项/返回门户；核算侧栏已无账号管理)。新包 index-BA8W4Dyf.js，同步 01。**部署注**：老库(V2.13前)重启即自动补 perms 列。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.13** | 2026-07-05 | **门户点亮「进入BP工作台」（BP 已上线，翻开关）**。BP 工作台已作为独立子应用部署腾讯云、经 Nginx 反代挂 /bp/、共享核算登录态并验收通过（服务器/BP/Nginx 本次均不动）。核算门户侧只改一处：`views/Portal.jsx` 的上线开关 `BP_LIVE` 从 `false`→`true`（V2.10 预置）——此后 BP 泳道「进入工作台」按钮点击执行 `window.location.href='/bp/'`（**整页跳转**，非新 tab、非内部路由；跳转目标 `GROUP_URL.bp='/bp/'`）。**权限无需改动**：`canEnter=!blank&&(admin||grp含'分析'/'BP'/'bp')`（V2.10 已就位）——BP 组账号与管理员放行、核算组仍「🔒无权限进入」。**自测**：前端 `npm run build` 通过（index-CoxY_DBw.js）；本地 5210 管理员登录→点 BP 泳道→URL 整页跳到 `/bp/`（本地无 BP 走核算 SPA 兜底，线上由 Nginx 转 BP）。仅改 1 文件 1 常量。同步 01。**注**：需把本版部署到服务器后，线上 BP 组账号才真正点亮（本次遵嘱不动服务器）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.12** | 2026-07-05 | **流水包同时支持 RAR + ZIP（自动识别）**。用户真流水包是**加密 .rar**（V2.11 的 zip 解法读不了——RAR 是不同格式）。`bank_import.py` 新增 `extract_archive(path,dest,password)` 统一入口：按**文件头**识别（`PK`=zip / `Rar!`=rar / `7z`），ZIP 走内置 pyzipper（零系统依赖），RAR/7z 走 `_extract_rar()`——优先 **7-Zip**（`_find_7z` 查 PATH+常见安装路径）、退路 unrar/unar/bsdtar，都支持密码，`stdin=DEVNULL` 防交互卡死，错误分「密码错」「无工具(NO_RAR_TOOL)」。`app.py` 端点改调 `extract_archive`，无工具时提示装 7-Zip/p7zip 或改存 ZIP。`DataImport.jsx` 文件框 `accept=".zip,.rar,.7z"` + 文案更新。**验证**（直接单测）：sniff 正确识别 zip/rar；extract_archive 走 zip 解密+中文名+内容全对（回归）；无 7-Zip 时 rar 抛 NO_RAR_TOOL→友好提示。**真 RAR 解密需装 7-Zip 后验**（本机/服务器均须装：Windows 7-Zip、Linux p7zip）。新包 index-BIxsIwJS.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.11** | 2026-07-05 | **加密银行流水包 自动解密上传**。用户的出纳流水包带密码，应用原 `zipfile` 解不了（尤其 AES）。改造：`bank_import.extract_zip(zip_path,dest,password=None)` 新增密码参数 + `_open_zip()`（有密码优先用 **pyzipper**（同解 AES 与老式 ZipCrypto）、无 pyzipper 退标准库、无密码走标准库不变；GBK 中文文件名修正逻辑保留）；`app.py` 上传端点 `/api/bank-import/upload` 从**请求头 `X-Zip-Password`** 读密码（不进 URL/日志，前端 encodeURIComponent、后端 unquote）传入，并把"密码错误/AES不支持/需填密码"映射成友好提示；`api.js uploadBankZip(file,password)` 加密码头；`DataImport.jsx` 上传行加「压缩包密码（无则留空）」password 输入框 + 加密说明。`requirements.txt` 加 `pyzipper>=0.3`。**验证**：直接单测——造 AES 加密 zip(内含中文名文件)→ 正确密码解开且内容/中文名正确、错密码 RuntimeError、无密码开加密包 RuntimeError，端点关键词匹配命中友好提示；前端 DOM 确认数据接入页密码框(password 类型)+加密说明渲染。新包 index-DyxvG_4A.js，同步 01。**部署须知：服务器需 `pip install -r requirements.txt`（装 pyzipper），否则 AES 包仍解不了。** 若实际是"Excel 本身带打开密码"（非 zip 加密），需再加 msoffcrypto 层，待用户拿真包验证。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.10** | 2026-07-05 | **门户预置「BP 接通开关」（默认关闭，备而不用）**。为 BP 工作台经反向代理接入(/bp/)提前布好线：`Portal.jsx` 加 `const BP_LIVE=false` + `GROUP_URL={bp:'/bp/'}`；进入按钮点击逻辑改为——`BP_LIVE && GROUP_URL[组]` 时整页跳转该外部工作台地址，否则维持现状（走 App 内 zone 占位"建设中"）。**默认 false=行为与现在完全一致，纯预置不影响线上**；待服务器 Nginx 反代 + BP dist 就位验收后，改 `BP_LIVE=true` 重新构建同步即点亮"进入BP工作台→跳/bp/"。配套集成文档已归 `03_Source_Materials/服务器部署_反向代理接入BP_清单.md`、`服务器部署_逐步操作指南_接入BP.md`。**验证**（5210）：管理员登录→点 BP 泳道"进入工作台"→URL 未变、出现"建设中"占位（=开关关闭态正确）。新包 index-B1PJssAa.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.9** | 2026-07-05 | **门户大屏等比放大填满（2.5K/4K 不再空旷）**。承 V2.8：用户指出最大宽度居中会让 2.5K/4K 屏两侧大片留白、"不自适应"。改为**混合缩放**——`Portal.jsx` 加 `.pt-scale` 包裹层 + `useEffect` 监听 resize：窗口≥设计基准(1560px)时 `zoom=窗宽/1560` 整体等比放大填满（CSS zoom 会按目标尺寸重排文字→保持清晰，非拉伸变糊），窗口<基准时宽度回 100% 交给 auto-fit 网格重排（半屏/小本→两列/一列）。顶栏/hero/lanes 的 max-width 居中与 V2.8 一致，只是外层再套一层缩放。**DOM 实测**（截图工具本沙箱对宽视口只抓左上角，以 DOM 为准）：4K(3840)→zoom2.46填满无横滚；2.5K(2560)→zoom1.64填满；1080p(1920)→zoom1.23填满且横竖不滚；1100(半屏)→zoom1自动两列。新包 index-DgQGslxG.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.8** | 2026-07-05 | **门户响应式自适应（对齐 1080p 主力屏）**。用户反馈门户要"随桌面大小缩放"，确认走**自适应重排+最大宽度居中**（非等比缩放——1080p 笔记本常开 125%/150% 系统缩放或半屏用，等比会把字压小）。`Portal.jsx` CSS：`.pt-lanes` 改 `grid-template-columns:repeat(auto-fit,minmax(340px,1fr))` + `max-width:1560px;margin:0 auto`（宽屏三列自动→中屏两列→窄屏一列，字号恒清晰）；`.pt-hero` 同 max-width 居中 + `flex-wrap`（AI 指示灯窄屏下自动换行）；左右 padding 用 `clamp(20px,4vw,34px)`；`.pt-top` 用 `max(30px,calc((100%-1560px)/2+34px))` 让顶栏内容与居中主体对齐；删旧 1080 断点、加 ≤520px 标题缩字。**验证(DOM实测,截图工具本会话持续抓不全故以DOM为准)**：1920→居中三列各484px/两侧留白180px/无横滚；1100→自动两列498px/无横滚。新包 index-BYT7Qzf4.js，同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.7** | 2026-07-05 | **工作台门户（Hub）落地为真页面 —— 三组分离 + 组级权限门**。按用户逐轮评审定稿的门户草稿（`03_Source_Materials/设计稿_门户Hub_三组工作台.{html,_1920.png}`，参考电商站 cw.xingchiec.com 的"智能体卡片+进入工作台"结构、套星期零深紫品牌）实现为 `views/Portal.jsx`：登录后**先落门户**（不再直进核算工作台），三组泳道（财务核算组/财务分析BP组/法务部），**进入按钮在组一级**——有权限→「进入工作台」，否则→「🔒无权限进入」；每组头三档**计数胶囊**（成熟可用/即将上线/敬请期待，按组内工具状态自动统计）；组内工具**纵向列示**（图标+名称+说明+通用能力灰标+**AI能力青绿高亮标 ✦**，不显示业务数据量）；右上**「AI 模型已就绪」青绿指示灯**（为后续接大模型预留）；导航按普通账号收为 工作台门户/使用指南/模型配置（后两者门户内占位页）。**法务部整栏占位**（用户要求：敏感、待问法务部）——保留三个工具位但**文字全清空**、渲染为骨架占位，组头显示"工具规划中·待与法务部确认"。`App.jsx` 加 `zone` 状态（portal/accounting/bp/legal）：登录→门户；核算组"进入工作台"→现有 shell；BP/法务进入→建设中占位（均可返回）；`Sidebar` 加**「返回门户」**入口+品牌区可点返回。权限：`user.role==='admin'` 放行全部、否则按 `user.grp` 匹配组关键词（核算/分析·BP/法务）。样式 `pt-` 前缀作用域隔离。**验证**（5210修订区）：管理员登录→落门户/三泳道/法务3占位0文字/进入按钮权限（核算·BP可进、法务锁）/AI指示灯/三项导航；进核算工作台→侧栏"返回门户"→回门户 闭环通过。新包 `index-WrqMkzcI.js`，同步 01（5200 冒烟：新包+登录门正常）。门户深色 vs 内页浅色为有意分层（门户=品牌门面、内页=作业区）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.6** | 2026-07-05 | **理财产品对账单独流（1101/1012理财腿 + 6xxx收益，产品维度聚合 · 含 PDF OCR）**。银行对账最后一条稽核流。**为何单独**：一笔理财赎回在金蝶跨科目拆多腿（本金1101.01或1012理财腿 / 公允价值变动1101.02 / 投资收益6xxx / 申购1101.01借），逐笔对不齐，故按**理财产品维度聚合**勾稽（口径：对账单赎回额 ≈ 金蝶本金退回＋投资收益；纯月末公允价值变动确认排除，见 `03_Source_Materials/理财对账单独流_设计分析_20260704.md`，用户确认 产品维度/OCR做进工具/拉6xxx对/含申购含信托基金）。①**OCR 内核** `kernels/wealth_statement.py`（移植隔壁 V1.15 成熟解析，渲染换 **PyMuPDF**免 poppler 系统依赖 + **rapidocr** 离线中文OCR多dpi并集；识别对账单/交易凭证 主体/产品/账号/净值/交易明细，OCR坑全处理）。②**对账内核** `kernels/wealth_recon.py`：金蝶按产品维度聚合（`kd_wealth_by_product` 本金退回/申购/公允价值/投资收益/纯估值；**排除电商1012腿+银行账号6603利息**只留理财）；产品匹配（`match_dim` 产品代码前缀→账号数字→品牌+号数→归一化子串）；`reconcile_wealth` 出 已勾稽/有差异/金蝶未记/对账单缺/持仓·无交易。③`kingdee_client` 加 `fetch_gl_voucher_income`（6101公允价值损益/6111投资收益/**6603财务费用**——本账套理财利息收入冲减6603.02）。④后端 `/api/wealth-recon`(GET秒回不触发OCR) + `/sync`(跑OCR，含 mtime 磁盘缓存 文件没变不重复OCR)；`wstmt.find_wealth_pdfs` 从流水目录找理财PDF。⑤前端 `views/WealthRecon.jsx`（侧栏「ERP对账程序 ▾」下新子项；汇总带+产品对账表+下钻看两侧凭证）。**关键发现**：理财赎回收益不在6101、而在 **6603.02 利息收入**（记393 同凭证冲减财务费用）——拉6603后浦银**完整对平到0.00**。**真数据端到端验证**（4份理财PDF OCR × 真金蝶）：浦银理财天添鑫93号 已勾稽（对账单35,160,943.15 = 金蝶本金35,000,000 + 利息160,943.15，差0✓，下钻见1012记393+6603.02记393两腿）；宁银理财81号 有差异 -834,868.06（对账单94,165,131.94 vs 金蝶本金95,000,000，日期错开6-22/23 vs 记392 6-25，交核算组）；基金791377 对账单缺；信托信益嘉321号 持仓·无交易（账号63672948匹配金蝶维度、6月无申赎）。内核单测 12/12；浏览器渲染+下钻验证。requirements 补 PyMuPDF/rapidocr（**服务器/生产机需 pip 装这两个 OCR 依赖，理财对账才可用**；本机已装）。新包 index-Dbta5m4_.js，已同步 01（保留 config/db）。设计稿归 03。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.5** | 2026-07-04 | **科目余额表视图（账面核对·API+手工上传双模式）+ 侧栏固定 + 逐笔稽核分页**。①**科目余额表视图**（侧栏「ERP对账程序 ▾」下新增子项，落 `views/SubjectBalance.jsx`）：四类资金科目（库存现金1001/银行存款1002/其它货币资金1012/交易性金融资产1101）按科目分组+账户级明细+小计+总计；**账面数用「期初(GL_BALANCE去重)＋本期序时账逐笔净发生」还原实时期末**（金蝶接口"期末"字段凭证未过账时停在期初、本期发生返0，不可直接用，V1.7 已验证此公式=金蝶报表期末）。新建内核 `kernels/subject_balance.py`（build_rows_kingdee/build_rows_sample/parse_report_xlsx/compare）+ `kingdee_client.fetch_gl_voucher_subjects`（序时账带科目编码，一次拉四类）。**②人眼核对（用户既定「API+手工上传双模式，留人眼核对」，见 feedback-dual-mode-human-check）**：页内「和金蝶报表对一遍」上传金蝶导出的《科目余额表》Excel→逐科目对 期初/本期借方/本期贷方/期末，一致打✓、有出入红字列差额；解析器容错认列（单金额列 与 借/贷两行表头 都认、合并单元格向右填充、编码 .0 后缀清洗）。后端 `/api/subject-balance`(+/sync)、`/api/subject-balance/check`、`/api/subject-balance/upload`。**内核单测全过**（样例构建/参考xlsx解析/自比一致/两行表头模拟）；**真金蝶端到端**（400 余额行×643 序时账→170 行，1002 各户期末与已验证报表数一致：157061=456,220.33、深零=2,719,381.62、上海分=112,498.27）。**③侧栏固定**（用户要求）：`.aside` 改 `position:sticky; height:100vh; overflow-y:auto`——右侧内容再长，侧栏及左下角「当前登录人」都钉在视口不随滚动。**④逐笔稽核分页**（用户要求）：每页 **15 笔**，表格上方右对齐（悬在「操作」列上方）加分页条「共 X 笔 · 每页15笔 · 第 N/M 页 + 上/下一页」，换筛选自动回第1页。DOM 验证（5224）：侧栏滚到底仍 top:0、登录块留在左下角；分页翻页 15 行/页、可翻至第11/43页、分页条 bottom 436 悬于表格 top 452 上方且盖住操作列；新包含双皮肤登录(fw_login_theme)未误伤 V2.4。**（补·序号列）逐笔稽核加「序号」列**（用户提，便于办公室对号沟通）：首列**全局稳定序号**（按稽核结果固定顺序编，1..N；**不随筛选/翻页改变**——同一笔的号固定，谁筛哪个状态、翻到第几页都指同一条）；导出对账底稿的「差异清单」「全部逐笔明细」两表也加同一套全局序号（差异清单保留全局号故有间隔）。DOM 验证：全部视图 1-15/16-30 连号；已匹配筛选序号从 112 起(证明是真全局号非重编)；导出 xlsx 序号=结果全局位置(单测差异清单 1,3,5 / 全部 1-5)。新包 index-Ca8ISHOd.js。新包 index-CQYdyXoa.js→**index-Ca8ISHOd.js**，已同步 01（保留 config/db）。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.4** | 2026-07-04 | **登录页双皮肤可切换（轻松版⇄风控正式版）+ 平台化文案**。设计稿 `03_Source_Materials/设计稿_登录页_风控正式版.html`（高清图 `_1920.png`，Edge headless 出图）经用户评审通过后并入 `Login.jsx`：右上角「切换 · 正式版/轻松版」按钮，选择存 localStorage(`fw_login_theme`) 记住。**正式版**（领导/对外场景）：深茄紫面板+细网格+超大Ø水印(5%)+**星图**（LOGO六图案作星座点连线=风险图谱）；主文案「让每一笔业务，都经得起审计。」；三支柱**平台化**（用户指正：本工具只是平台一环，核算组/分析BP组/法务部都将进来）→ 一站式工具集(核算·分析BP·法务)/审计留痕/分级授权(组间数据隔离)；法务要素：《内部数据安全与保密规范》勾连、异常登录将被记录、底部审计声明、V2.4版本角标；按钮/焦点态改品牌深紫 #4A3A8C。**轻松版**（默认，团队日常）：小猫版原样保留。实现：`.lg-formal` CSS 覆盖层+条件渲染，动画 effect 依 theme 重挂、正式版无猫场景自动跳过。**验证**（5210）：切换双向/持久化/正式版内容(星图·大Ø·主标题·支柱)/正式版下真登录(管理员)全过；小猫动画代码零改动（切回轻松版场景恢复；无头验证时发现**预览标签页后台 visibilityState=hidden 会暂停 RAF**——环境伪象非代码问题，前台即恢复）。新包 `index-BdRv1OjY.js`，同步 01。设计稿+高清图归档 03 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.3** | 2026-07-04 | **登录页品牌化重设计（星期零风控中心AI赋能中台）**。按用户逐轮评审定稿的样机（项目根 `login.html`，Approved）移植进 React：`views/Login.jsx` 整页重写——左=品牌紫面板(#2B1A46 与官方 LOGO 图同色)：官方 LOGO「六图案+Ø」（从 `03_Source_Materials/星期零LOGO让健康更好吃2022-03.jpg` 裁标志区+透明底处理、base64 内嵌、3x 高清）+ 中文名/一行英文 `STARFIELD RISK CONTROL CENTER · AI-POWERED MIDDLE OFFICE`（中台=Middle Office 前中后台标准译法）+ 三行文案 + **大黄猫与三个几何小伙伴互动动画**（RAF 引擎：鼠标视差跟随/输用户名全员逐字追光标+猫倾身伸长瞳孔放大/输密码猫举爪捂眼伙伴闭眼/点显示密码猫扒开爪全员偷看+随机眨眼；`window.__forceMode` 测试钩子）；右=表单（安全登录徽章/姓名+密码/显示密码眼睛/记住我 localStorage/忘记密码提示/错误抖动），接真 `/api/login`，登录中禁用态。样式 `lg-` 前缀+CSS 变量作用域隔离，不污染应用全局。**端到端验证**（5210 修订区）：登录页渲染/LOGO 加载/管理员 admin888 真登录进应用（认领人=登录人）/登出回登录页/错密码提示/记住用户名/捂眼-偷看-瞳孔动画数值全对。新包 `index-CT3AuN7b.js`，同步 01（保留 01 的 config/db），5200 冒烟通过。样机归项目根 `login.html`（可独立双击演示） | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.2** | 2026-07-04 | **阶段2：登录 + 账号系统**。`db.py` 加 `users`(姓名/组/角色/pbkdf2密码/启用)+`sessions`(DB存token)表 + pbkdf2密码哈希 + 账号CRUD/会话函数 + `seed_admin`(首启无账号→种 管理员/admin888)。`app.py`：认证中间件 gate `/api/*`(白名单 login/logout/me/health，未登录401)；`/api/login`(设httponly cookie)、`/api/logout`、`/api/me`；管理员接口 `/api/users`(列表)、`create`、`active`(禁用/启用，禁用即踢下线)、`reset-pwd`、`delete`(每步写审计)；**认领 operator 改用登录用户**(不再信前端传值，防冒名)。前端：`Login.jsx`(姓名+密码)、`UserAdmin.jsx`(建/禁用/删/重置密码，仅管理员)、App 加登录门(getMe→登录页/应用)、Sidebar 显当前用户+退出+管理员见「账号管理」、Reconcile 去掉操作人下拉→显「认领人：登录名」。**验证**：未登录401、管理员登录200、建账号(李志鹏/黄春艳)、禁用后登录被拦+踢下线、启用恢复、认领记为登录人、登录页/退出正常。新包 index-DN-607aD.js，已同步 01。**部署更新须知**：服务器更新代码重启后，MySQL 会自动建 users/sessions 表并种 管理员/admin888，用它登录→建团队账号→改管理员密码。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.1** | 2026-07-04 | **阶段1部署验收通过：腾讯云宝塔实跑 MySQL + 多机共享认领 + 逐笔稽核自动刷新**。用户在腾讯云宝塔（`111.229.72.116:8000`）实际部署：建 Python 项目、排查两道防火墙（腾讯云安全组+宝塔系统防火墙均需放行8000，此前均未放行故手动进程本机curl通、外网502）、修正 `DB_URL` 环境变量（MySQL密码含特殊符号导致连接串解析错位，改为纯字母数字密码后连接成功）。**验收结果**：`/api/health` 返回 `"db":"mysql+pymysql"`，逐笔稽核页两位操作人（李志鹏“已认领”、黄春艳“已调整”）状态互相可见——**阶段1目标（多机共享认领存 MySQL）达成**。**逐笔稽核加自动刷新**（用户提出）：`Reconcile.jsx` 改用 `setInterval` 每 **5 秒**调用只读缓存的 `getReconcile()`(非重取的 `syncReconcile`，避免金蝶模式下高频拉数)，页头加"● 每5秒自动刷新"透明提示；验证间隔精确5000ms、筛选/展开状态不受打断。新包 index-0biAeCsV.js，已同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V2.0** | 2026-07-04 | **【架构级·多人服务器版基础】数据层 JSON → 数据库（阶段1）**。方案见 `03_Source_Materials/服务器部署方案_v1.md`(用户批准:腾讯云宝塔测试服+MySQL 5.7.44,生产财务部PC)。新建 `db.py`(SQLAlchemy 2.0，**`DB_URL` 环境变量一套代码两库**：本地默认 SQLite `workbench.db`、服务器指 MySQL `finance_workbench`)，建表 `claims`(认领)/`account_overrides`(账户覆盖)/`audit_log`(**审计留痕**,财务必备)。app.py：认领(reconcile_claim)、账户覆盖(override) 全改走 DB→**多人共享**(认领状态存服务器库,多台电脑实时可见);每次动作写审计(谁/何时/认领·调整·识别有误·撤销·账户覆盖);`_migrate_json_to_db` 首启把旧 JSON 平滑导入 DB(升级不丢数据);`/api/health` 加 db 后端信息。requirements 补 SQLAlchemy/PyMySQL/requests/openpyxl。**本地验证**:app 走 SQLite,认领→已调整、覆盖、审计三表读写全通;01 冒烟 health `db:sqlite`、operators 核算组7。前端无改动(API 不变,免重构建)。已同步 01(排除 workbench.db,首启自建+迁移)。**下一步**:用户按方案文档「阶段1部署步骤」把 backend 传腾讯云宝塔、建 Python 项目、设 DB_URL 指 MySQL、验证多机共享认领;通过后进阶段2(登录/账号系统)。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.10** | 2026-07-04 | **认领工作流·轻版（逐笔差异三态处理 + 当前操作人）**。给逐笔稽核差异加处理状态：**待认领 → XXX已认领 → XXX已调整 / 识别有误**（"识别有误"=引擎判错的反馈回路，IT据此修BUG）+操作人+时间+备注，本地 `claims.json` 持久化(不写金蝶)。后端：`ROSTER` 名单(**认领=账务处理，只核算组7人**：小朋友A/B·李志鹏·陈梓华·黄春艳·冀欣欣·冯辉；**BP组曾禹锡/吴卓文不涉及账务、不进认领**)+`/api/operators`；`_assign_keys` 给每条结果配稳定内容哈希key；`/api/reconcile/claim`(认领/已调整/识别有误/撤销)；reconcile GET **现读claims叠加认领态**(即时生效不吃缓存)。前端 Reconcile：顶部"当前操作人"下拉(localStorage记住、未选描边提示)+ 操作列按状态给认领动作("识别有误"弹窗填说明给IT)。**验证**：认领→已调整→识别有误(带备注)→撤销 全通；操作人下拉核算组7人BP组排除；浏览器点认领→"李志鹏·已认领"+冒[标已调整][识别有误]。新包 index-BzNsg6Ul.js。**已同步 01**。真登录/账号系统(多岗位·RBAC·上服务器)为后续独立一期。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.9** | 2026-07-04 | **支付宝等第三方渠道余额勾稽（1012）**。渠道海量微交易(支付宝5.5万笔) vs 金蝶汇总(22条)逐笔对不了→**核对总额**：渠道对账单期末余额+本期净 vs 金蝶1012该维度账面(期初+序时账)。`bank_import` 加 `parse_alipay`(流式解析 25MB SpreadsheetML/.xls.zip，只抽汇总不留逐笔，2.9s跑完5文件)+ `_read_broken_zip`(手工deflate解压：部分支付宝 .xls.zip 中文内名被错标UTF-8致 zipfile 崩，从本地头后取deflate流绕过)+ `parse_channels`(按查询段去重、按账户合并多日期段)。后端 `_channel_adjust`+`/api/channel-adjust`：按 email 匹配金蝶核算维度、**本期净核对**+期末余额差。前端 FundDashboard(余额调节页)加"第三方渠道余额勾稽"表(与银行调节表并列)。**真数据效果**：3个支付宝户，zhifubo(净60,341.32=金蝶淘宝zhifubo,期末169,414.31=账面,**差0对平**)、wuzw(净357.70,**差0对平**)净一致✓；estore@starfield.cn 金蝶维度名不含该email→标"待映射"(核算组确认对应维度)。新包 index-B0BT0mPV.js。**已同步 01**。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.8** | 2026-07-04 | **四部曲第4步「结果出具」：一键导出对账底稿 Excel（C），四步工作流走完**。后端 `GET /api/export/report`：openpyxl 生成 4 表 xlsx（**对账汇总**：期间/各状态笔数/护栏/对平户数；**差异清单**：非"已匹配"的逐笔=疑似漏账·做错·晚记·内部往来未做账·金蝶单边；**银行存款余额调节表**；**全部逐笔明细**），带表头样式/冻结窗格，Content-Disposition 附件(RFC5987 中文名)浏览器直接下载。新增 `views/ResultExport.jsx`（结果出具页：本期结果概览卡 待处理差异/已匹配/对平/不平 + 导出按钮）；`Steps.jsx` 第4步 result 可点(→result视图)、四部曲四步全可导航；逐笔稽核页"导出差异清单"按钮改"导出对账底稿"并接同一接口。**验证**：真数据导出 xlsx 4表(汇总18/差异清单112/余额调节16/全部逐笔641行)有效(PK头/53998字节)，01冒烟通过。新包 index-BRxEEq8g.js。**已同步 01。四部曲 数据导入→逐笔稽核→余额调节→结果出具 全部完成、可端到端跑。** | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.7** | 2026-07-04 | **四部曲第3步「余额调节」做实：银行存款余额调节表（B）**。新增 `/api/balance-adjust`(+/sync)+`_BADJ_CACHE`：每户 **银行对账单余额**(银行流水末笔余额，`bank_import` 补 `余额` 字段——财资"账户余额"列 / 中行HISQRY 带±号金额后的无符号运行余额) vs **金蝶账面余额**，用逐笔稽核的未达账项两边调节 → 对平。**未达口径**：金蝶单边=企业已记银行未记(调银行侧)、疑似漏账+内部往来未做账=银行已记企业未记(调账面侧)。**两个关键修正**：①GL_BALANCE 返回重复行(同科目+同维度)必须按(科目,维度)去重，否则相加→账面翻倍(全不平真因)；②账面**不能用 GL_BALANCE 期末**(凭证未过账时期末=期初、不含本期变动、与未达口径打架)，改用 **期初＋本期序时账净发生**——与逐笔稽核口径一致。修后**真数据 15 户全部对平、差额 0.00**，验证整条稽核链自洽(逐笔找出未达→余额调节把银行/账面调到相等)。前端 FundDashboard(余额调节步)加调节表(主体/账号/银行余额/账面余额/银行侧未达/账面侧未达/调节后/对平)+资金看板余额明细降为下方子区。浏览器验证对平15/不平0。新包 index-AkzKcVj1.js。**已同步 01**。剩四部曲第4步「结果出具」(差异清单导出)。**（补·经用户核对金蝶科目余额表截图确认）账面数正确**：调节表"金蝶账面余额"与金蝶科目余额表期末**逐户一致**(157061=456,220.33、743=2,719,381.62、121941=112,498.27…)；金蝶GL_BALANCE接口"期末"字段因本月未过账停在期初、本期发生返回0，用「期初＋本期序时账」还原=金蝶实时期末(与金蝶报表同公式)。表头加 ⓘ hover 小字(列标题保持短)注明各列口径(账面/银行余额/银行侧未达/账面侧未达)。新包 index-DVIMwR1m.js。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.6** | 2026-07-04 | **新增稽核态「内部往来·未做账」+ 跨账号内部划转识别**（源自 V1.5 后"验准头"：16单边+30漏账里近半是集团调拨；用户指出"虽是内部划转，但金蝶那头没做账，仍是未入账"，故不消噪、而是精确标记）。`reconcile.py` 十态：`reconcile()` 改为**全局收集各账户剩余**后跑跨账号 pass `_flag_internal`——银行某户有资金动、金蝶该户查无，但**同额(4位)在别账户以划转特征(转入/转出/调拨/往来/归集/划转)入账** → 判「内部往来·未做账」(`xfer_unbooked`)，附**对方金蝶凭证+账户**(`内部往来对应`，供核算组补做账定位)，只标不核销。Rec 加 `xfer/xfer_ref`；_ORDER/_CN/summarize/_row 补新态；单测 21→**23**(加 t_internal_unbooked：跨账号同额+划转→内部往来，且不误判外部漏账)。前端 Reconcile 补 ST/CNT/KPI 切片 + 表格显示"内部往来 → 记-X 对方账户…"。**真数据效果**：疑似漏账 30→**21**、新出「内部往来·未做账」**9 笔**(社保/公积金/往来款等集团代付调拨，各带对方凭证如 记-265 对方账户…410308)、金蝶单边仍16。浏览器验证切片9笔+表格对应凭证。新包 index-CbAbpXBV.js。**已同步 01**。**验准头结论：引擎判得准，未误配**；剩真待核≈22笔=金蝶滞后入账的利息/手续费/结息/小额打款(月底调整项，工具准确拎出)。待核清单 Excel `D:\银行金蝶稽核\待核清单_单边漏账_202606.xlsx`。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.5** | 2026-07-04 | **导航 IA 重构：银行对账 → 「ERP对账程序」可下拉 + 资金看板并入四部曲**（按用户要求）。侧栏「银行对账」改名 **ERP对账程序**（可点展开/收起 ▾），下含两项：**对账程序**（进四部曲工作流，落地在逐笔稽核）+ **账户台账**；资金看板/逐笔稽核不再单独占侧栏。**资金看板并入四部曲第 3 步「余额调节」**（`Steps.jsx` 加 `VIEW` 映射 balance→fund 视图、余额调节步可点；`FundDashboard` 加四部曲条 current=balance、标题改「余额调节 · 资金看板」、副标注明三栏对平为二期）。App 默认视图改 `reconcile`；`WORKFLOW=[import,fund,reconcile,result]` 属"对账程序"高亮集。四部曲(数据导入/逐笔稽核/余额调节)三步已可互点跳转，结果出具二期。**浏览器点击流验证**：侧栏 ERP对账程序▾ 下 对账程序/账户台账、四部曲余额调节 ›可点→「余额调节·资金看板」(¥319M总览/四科目卡/余额表)、对账程序保持高亮。新包 index-C_JFvWv8.js。**已同步 01**，双击启动.bat 即见。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.4** | 2026-07-04 | **数据接入工作流三段式：上传流水包 + 金蝶总闸取数 + 覆盖对照**（按用户设想的月底流程）。①**上传资金流水**：出纳把当月整个流水文件夹打成 .zip 上传（`POST /api/bank-import/upload`，原始 zip 走请求体免 multipart 依赖 → `bank_import.extract_zip` 解压并**修 Windows zip 中文名乱码**（无 UTF-8 标志位按 GBK 还原）+ 防路径穿越 → 设为导入目录 → 返回解析清单）；`load_bank_dir` 改**递归 os.walk**兼容解压后嵌套子目录；保留"填本机文件夹路径"为折叠的本地调试备选。②**金蝶取数**：新增「从金蝶更新」总闸按钮（`POST /api/kingdee/refresh` 清缓存 + 重拉 **科目余额表**(给资金看板/余额调节) + **银行存款序时账1002**(给逐笔稽核)），`_data_sources` 加 `balance_count`；**按用户要求撤掉资金看板的「一键接入金蝶」按钮**，金蝶取数统一由数据接入页当总闸（数据接入=备料，逐笔稽核只看结果）。③数据源覆盖表沿用。**端到端验证**（真数据）：打 53.7MB 真流水包上传→解压中文名/HISQRY完好→递归解析 596 笔（与文件夹一致）；金蝶刷新 序时账610+科目余额400；浏览器验证 数据接入页三段（上传框/从金蝶更新/覆盖表 宁波435-450✓等）、资金看板已无接入按钮、四部曲导航正常。新包 index-aFvNQ24x.js。**已同步 01**（robocopy 排除 bank_uploads 工作目录），双击启动.bat 即见。**（补）数据接入页精修**：②金蝶取数显示四类资金科目行数（库存现金/银行存款/其它货币资金/交易性金融资产，来自 `_data_sources.balance_by_subject`）；顶部注明**数据更新时间**（`_now()`，各刷新动作更新）；DataImport 页**响应式适配**（页宽 1120、manifest 行 flexWrap 换行、覆盖表 overflow-x 滚动，375px 窄屏验证通过）。新包 index-Jj9WsObM.js，已同步 01。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.3** | 2026-07-04 | **数据接入独立成页 + 四部曲进度条做成可点导航**（按用户要求，从逐笔稽核页拆出；且**不放侧栏**，改为点逐笔稽核页里的四部曲"数据导入"步进入）。新增 `views/DataImport.jsx`「数据接入」页（四步工作流第 1 步）+ 共享组件 `components/Steps.jsx`（数据导入→逐笔稽核→余额调节→结果出具，已实现的两步可点跳转：稽核页点"数据导入 ›"进数据接入页、数据接入页点"逐笔稽核"回稽核页；余额/结果二期）。**侧栏「银行对账」仍是 资金看板/账户台账/逐笔稽核 三子视图（数据接入不在侧栏）**。数据接入页含三块：①银行流水来源（导入目录路径输入 + [重新解析] + 解析清单：▶并入 财资平台589/中行HISQRY 5+2·自校验OK，跳过 支付宝/微信/抖音第三方走渠道总额、其余29个PDF/余额/回单）②金蝶序时账（1002·610笔）③数据源覆盖（每家银行 金蝶↔银行 对照：宁波435/450✓、招商142/139✓、中行5/7✓、花旗27/待补、建行1/待补）。后端新增 `/api/data-sources`(+/sync)——抽出共享 `_real_bank_kd()`，出 manifest+每家银行覆盖，不跑逐笔；加 `_DS_CACHE`。**导入目录路径从设置页移到数据接入页**（设置页只留 数据源/期间/连接测试）；**逐笔稽核页移除"数据源明细"折叠面板**（页脚改指「数据接入」页 + 保留余额稽核户数）。npm build(新包 index-DON4V4H3.js)。**浏览器验证**：侧栏3子视图(无数据接入)、稽核页四部曲"数据导入 ›"可点→进数据接入页、数据接入页点"逐笔稽核"→回稽核页、数据接入页3卡片+覆盖表5行、设置页无导入卡片、稽核页无数据源面板、/api/data-sources 与 /api/reconcile(已匹配529)均正常。新包 index-yXUIocgj.js。**已同步 01**，双击启动.bat 即见。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/（续 V1.2 修订夹） |
| **V1.2** | 2026-07-04 | **逐笔稽核接真数据（银行×金蝶1002 全链路打通）**。**数据源口径落定（用户认可）**：逐笔只做真实资金进出 = 银行对账单(1002) + 其他货币资金(1012)；理财(1101/1012理财腿)走单独对账流、公允价值变动等纯估值分录排除；支付宝/微信/抖音第三方（5.5万笔微交易 vs 金蝶22条汇总）不逐笔、走渠道总额（推迟到"做凭证"功能再逐笔）。**数据源全景锁定**：金蝶1002=21户610笔 → 宁波5户+招商8户=**财资平台"一键下载明细"**`宁波银行+招商银行.xlsx`（招商另有`交易查询139笔`双份互证）；中行2户=**HISQRY**多块CSV（自带借贷发生总额可自校验）；花旗5户27笔/建行1户=PDF（待核算组）；582/610=95%被干净结构化源覆盖。**①引擎**`reconcile.py` 七态→**九态**：新增「账户缺银行流水/账户缺金蝶数据」覆盖预检（整户只一侧有数据单列，不涌进漏账/单边），单测 17→**21/21**。**②`kernels/bank_import.py`**（新建）：财资平台解析器 + 中行HISQRY解析器（来账=收/往账=支、借贷总额自校验全过）+ 目录分派 `load_bank_dir`（财资/中行并入逐笔；支付宝/微信/抖音登记不并入；PDF/理财/余额自动跳过；含 manifest 可审计）。**③金蝶字段自愈**：`download_bank_accounts.py` 的 QueryBusinessInfo认列/resolve_fields/fetch_resilient错列自愈/组织遍历去重 移进 `kingdee_client.fetch_bank_accounts`，**删写死的 FBankAccountNumber bug**（本账套认到 FNumber）；实测真金蝶自动查到9组织、取回**153账号**（与权威台账一致）。**④稽核方案联动**：`_reconcile` 只对"明细"账户逐笔，"余额"账户（台账可手工点调）排除、响应带「余额稽核账户」清单。**⑤数据导入 UI**：设置页加"银行流水导入"目录卡片(`bank_import_dir`)；稽核页加"数据源明细"折叠面板（哪些文件并入/跳过、HISQRY自校验、余额稽核户）+ 两个新状态切片。**端到端验证**（真金蝶610×真银行596）：已匹配529/晚记37/疑似漏账30/金蝶单边16/账户缺银行流水28/账号对不上台账**0**/组合候选6，护栏笔数两边对平；浏览器 DOM+截图验证 UI。**已同步 01（config 设金蝶+导入目录），双击启动.bat 即见接真数据版**。剩余：花旗/建行 PDF（待核算组）、可选拉 16单边+30漏账逐笔清单验准头。 | Claude / c | Synced | 02_Revision_History/20260703_V1.2_接真数据/ |
| **V1.1** | 2026-07-03 | **对账引擎 v2 + 前端冷灰重构 + 性能缓存**（按需求确认书 v1.3）。`reconcile.py` 重写为**七态**（疑似漏账/做错·金额/晚记·本月/跨期晚记/金蝶单边·待查/账号对不上台账/已匹配）+ **金额比对到 4 位小数**（废旧「精度异常」）+ **晚记以当天为准** + **置信度**(高/中/低) + **组合候选**(1:N/N:1)侦测 + 数字护栏，**单测 17/17**。`app.py` 逐笔稽核接 v2 引擎与匹配桥；资金看板/稽核**加缓存**（GET 读缓存**秒开** / 仅 sync 重取，**修"每次进入实时打金蝶"卡顿**）。前端**冷灰 Linear 重构**（styles 令牌重写 / Sidebar 内联 SVG 图标离线安全 / Reconcile 七态屏：四步链路条+可信度带+置信度条+行内配对下钻+组合候选标 / FundDashboard 缓存不清屏），Vite 构建产物入 `backend/static`。**截图验证观感 + 端到端 serve 验证缓存与七态**。**D3 接真银行流水（孝感九 213/213）为剩余环**——逐笔稽核银行流水仍样例，匹配桥沿用 V1.17 不变，列后续 V1.2。**已同步覆盖 `01_Current_Deliverables/app`（数据源设回金蝶），live app 上线**。**上线后修**：给 index.html 加 `Cache-Control: no-cache` 响应头（避免浏览器缓存旧前端→旧UI+点稽核白屏）+ 删无用 Tabler 图标 CDN（离线更快），已重构建+重同步 01。**切片器改造**：稽核屏 KPI 改为 **8 个可点「切片器」**（全部 + 七态），点击即筛选、与表格联动、选中高亮（替代原独立筛选 chips），DOM 验证 8 个/点漏账筛到 2 行全漏账/复原正常，已重构建+同步 01。**导航 IA 重构**：账户台账（原「基础数据」）从顶层工具**降为「银行对账」工具的子视图**；资金看板/账户台账/逐笔稽核归入「银行对账」一个工具下（缩进子视图），电商三方核对为独立工具。DOM 验证工具头+3 子视图+切换高亮正常，已重构建+同步 01。**页面统一打磨**：账户台账/设置/电商占位统一到冷灰新版式（新抬头 `.h-title`/`.h-sub`、去失效 Tabler 图标、卡片用 `.cat`、banner 用新类、账户台账加缓存不清屏），DOM 验证抬头/卡片/无报错正常，已重构建+同步 01。账户台账加**家底汇总带**（账户总数/银行账户·进匹配桥/理财/电商/主体/生效 6 卡片），真金蝶数据 153 户/95/40/18/8，截图验证。**账户台账列重定义**：列改为 主体/开户行·渠道/账号/**稽核方案**(明细·余额，银行账户→明细逐笔/其余→余额，由类别推)/**状态**(生效·失效·已销户)/操作/来源；新增**手工标失效**（金蝶不维护此步→本地 `account_overrides.json` 持久化 + `POST /api/account-ledger/override`，不写金蝶；标失效即从生效排除，勾选含失效可查看/恢复）。DOM 验证：7 列全对 / 标失效 生效 153→152 / 恢复回 153 / 零报错。**修接口缓存 bug**：前端 fetch `cache:'no-store'` + 服务器端 no-store 中间件（所有 /api/ 响应禁缓存），根除"浏览器拿旧接口数据致 生效0/字段对不上"，验证 `Cache-Control: no-store` 头 + 生效 153。**发现金蝶同步字段 bug**：kingdee_client 用错字段 `FBankAccountNumber`（应按 `download_bank_accounts.py` 字段自愈候选 FBANKACCOUNTNUMBER/FACCTNUMBER/FNumber + 按组织遍历），台账已预建可用，此同步修复列 **V1.2**。**根因修复：`启动.bat` 加 `--reload` 自动热加载**（watchfiles 1.2.0）——此前"改了没生效"的真因是 uvicorn 老进程不会自动加载新代码，需重启；加 --reload 后后端更新无需重启。**稽核方案改为可点调整**（明细⇄余额，手工覆盖持久化 `account_overrides.json`；余额=不走逐笔、只需金蝶余额=银行余额；默认按类别推）。验证：watchfiles OK / 改余额生效 / 清理复原 / 生效 153。已同步 01 | Claude / c | Synced | 02_Revision_History/20260703_V1.1_对账引擎v2与前端重构/ |
| **V1.0** | 2026-07-03 | **项目交接初始化**：从隔壁「财务核算工作台」交接（其 V1.0–V1.17 台账已归档 `03_Source_Materials/隔壁项目台账_至V1.17_归档.md` 只读留存）；建治理四目录骨架；**只读复制** V1.9 活的应用（含 V1.17《账户台账》匹配引擎）→ `01_Current_Deliverables/app/`；一键启动器 `启动.bat` 落**项目根**（指向 01 的 app，不埋版本文件夹）；需求确认书 v1.3(Approved Baseline) 归 `01_Current_Deliverables/`、v1.0–v1.2 草稿+需求梳理+样机 v3 归 `03_Source_Materials/`；《V1.0 建设方案与影响分析》入本修订文件夹。**金蝶配置迁移**（2026-07-03 补）：新建 `00_金蝶配置文件/`（conf.ini + 取数脚本 + 取数输出参考），conf.ini 另置 `app/backend/conf.ini` 供应用读取（`conf_path()` 已命中）。启动器改为**轮询端口 8000 就绪即开浏览器**（原固定 6 秒等待）。**隔壁项目全程只读、零改动**。下一步 V1.1 起做对账引擎 v2 + 前端重构 + 性能缓存（按需求确认书 v1.3） | Claude / c | Synced | 02_Revision_History/20260703_V1.0_交接初始化/ |

---

*本台账自 V1.0（2026-07-03 交接初始化）起为本项目权威变更记录；隔壁历史见 `03_Source_Materials/` 归档，只读不续写。*
