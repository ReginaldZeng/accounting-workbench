# 操作手册：核算工作台 · 服务器 git 部署 + 自动部署（腾讯云宝塔 111.229.72.116）

> 目标：把核算工作台从「ZIP 上传」升级成「git 拉取即部署」，并配成**自动部署**（本机推 main → 服务器 ≤5 分钟自动上线）——和 BP 那套一样。
> ⚠ **先看第 6 步开头的「服务器实况」表**：真正在跑的是 `/root/deploy_acc.sh` + 每 5 分钟 cron，不是本仓库的 `deploy.sh`。
> 在**服务器上**做（碰生产 + 真库）。凭据只写服务器、绝不提交、绝不外发。仓库：`ReginaldZeng/accounting-workbench`（main 已是最新）。

## 架构（与 BP 一致：源/运行分离 + rsync 衔接）
- **前端构建物已进 git**（`01_Current_Deliverables/app/backend/static/`）——服务器**没有 node 也不用 build**。
- **两个目录，各司其职**：
  - **克隆目录（源）** `/www/wwwroot/accounting-workbench`：含 `.git`，只 `git pull`、绝不在此改。
  - **运行目录（生产）** `/www/wwwroot/finance_workbench/backend`：扁平、无 `.git`，宝塔 Python 项目就跑这个（端口 8000）。运行期数据（`conf.ini`/`.env`/`*.db`/`*_uploads/`/`version_stamp.json`）都只在这里。
- **部署 = 克隆里 `git pull` → `rsync` 源码进运行目录（不带 `--delete`）→ 写版本戳 → 重启**。`rsync` 明确 `--exclude` 掉全部运行期数据，绝不覆盖真库/凭据/上传件；`deploy.sh` 已封装这一整套。
- ⚠ 历史坑：曾以为「直接从克隆里跑、不用 rsync」，但生产实际跑在独立的 `finance_workbench/backend`——克隆 pull 了、没 rsync 进运行目录，代码就没生效（页脚版本号还停在旧号，正是这么来的）。

---

## 第 0 步：备份（宝塔·文件）
- 现在跑生产的那个目录（ZIP 解出来的）整个压 zip 存好。
- **单独复制真库** `.../app/backend/sample_data/workbench.db` 到安全处。
- 复制 `.../app/backend/conf.ini`（金蝶/钉钉凭据）、`.env`（若有）。
- Nginx 站点配置全选复制存记事本。
> 回滚就靠这几样。

## 第 1 步：只读 Deploy Key（拉私有仓库）
```bash
ssh-keygen -t ed25519 -f ~/.ssh/acc_deploy -N "" -C "deploy-accounting-server"
cat ~/.ssh/acc_deploy.pub          # 复制这一整行公钥
```
GitHub：仓库 `accounting-workbench` → **Settings → Deploy keys → Add deploy key** → 粘贴公钥，**不要勾 Allow write**（只读，服务器只拉不推）。
```bash
cat >> ~/.ssh/config <<'EOF'

Host github-acc
  HostName github.com
  User git
  IdentityFile ~/.ssh/acc_deploy
  IdentitiesOnly yes
EOF
ssh -T git@github-acc   # 期望：Hi ReginaldZeng/accounting-workbench! You've successfully authenticated...
```

## 第 2 步：克隆仓库（只作源，不在此跑生产）
```bash
git clone git@github-acc:ReginaldZeng/accounting-workbench.git /www/wwwroot/accounting-workbench
cd /www/wwwroot/accounting-workbench && git log -1 --oneline    # 应为最新 V 号
```

## 第 3 步：运行期数据不用动（本来就在运行目录里）
生产运行目录 `/www/wwwroot/finance_workbench/backend` 里的真库/凭据/上传件**原地不动**——`deploy.sh` 的 `rsync` 不带 `--delete` 且逐条 `--exclude`，只会往里灌新代码、绝不覆盖这些。所以**这一步无需搬任何数据**。
> 若你是全新机、运行目录还不存在：先把现生产 backend 整个 `cp -r` 成 `/www/wwwroot/finance_workbench/backend`，再进第 4 步。

## 第 4 步：装依赖 + 测试端口先验（别碰生产）
```bash
# 依赖装在【运行目录】用的那个 venv（宝塔项目配的解释器），确保新 requirements 生效
cd /www/wwwroot/finance_workbench/backend
pip install -r /www/wwwroot/accounting-workbench/01_Current_Deliverables/app/backend/requirements.txt
python -m uvicorn app:app --host 127.0.0.1 --port 8099 &   # 临时测试端口
sleep 3
curl -s -o /dev/null -w "首页 %{http_code}\n"  http://127.0.0.1:8099/
curl -s -o /dev/null -w "登录 %{http_code}\n"  http://127.0.0.1:8099/api/login
kill %1
```
- 页面能开、接口不 500 → 进第 5 步。若某接口 500（多半缺 DB 迁移列）→ 先在真库跑对应迁移再切。

## 第 5 步：首次 rsync 进运行目录 + 重启（生产目录不动，只灌新代码）
生产继续跑在原运行目录 `/www/wwwroot/finance_workbench/backend`（宝塔 Python 项目、端口 8000 保持不变，**不要改项目目录**）。第一次手动跑一遍 `deploy.sh` 把克隆里的代码 rsync 进去：
```bash
export ACC_REPO=/www/wwwroot/accounting-workbench
export ACC_RUN_DIR=/www/wwwroot/finance_workbench/backend      # 生产 backend（就是宝塔项目跑的那个目录）
export ACC_RESTART="<你的重启命令>"                              # 见第 6 步，例如宝塔命令行重启 / systemctl
bash /www/wwwroot/accounting-workbench/deploy.sh
```
- `deploy.sh` 会：pull → rsync（不带 --delete，护住 conf.ini/.env/*.db/*_uploads/）→ 写版本戳到运行目录 → 重启。
- 验收：`curl -s http://127.0.0.1:8000/` 能开；开 `http://111.229.72.116/`（或你的域名/路径），**页脚应显最新 V 号**。
- **AI 网关**（若在用）：其 backend 也在同一运行目录，rsync 已一并更新；`gateway:app` 端口 8020 那个项目单独重启即可。

---

## 第 6 步：自动部署 —— ⚠ 服务器实况（2026-09-05 核对）

> **服务器上实际跑的不是仓库里的 `deploy.sh`，而是 `/root/deploy_acc.sh`**（一个自带全部逻辑的包装脚本，不在 git 里）。下面 6.1/6.2 的 `restart_acc.sh` 与 2 分钟 cron 是早期方案、**服务器上不存在**，留作参考；以本节为准。

| 项 | 实况 |
|---|---|
| cron | `*/5 * * * * flock -n /tmp/acc_deploy.lock /root/deploy_acc.sh >/dev/null 2>&1` —— **每 5 分钟**，`flock` 防重叠 |
| 手动触发 | `bash /root/deploy_acc.sh`（推完不想等就敲这个） |
| 源 / 目标 | `/www/wwwroot/accounting-workbench`（clone）→ `rsync -a`（**无 --delete**）→ `/www/wwwroot/finance_workbench/backend/` |
| 判定有无更新 | `git fetch origin main`，`HEAD == origin/main` 就退出；有新提交才 `git merge --ff-only origin/main` + rsync |
| 日志 | **`/root/acc_deploy.log`**（不是 `/var/log/acc-deploy.log`——那个文件只有一条误贴 cron 行留下的报错，没用） |
| 版本戳 | 脚本内嵌 python 写 `backend/version_stamp.json`：**`ver` ＝ `00_Change_Log/` 目录下文件名前缀 `V2.xxx_` 的最大号**（不是 git log）。⚠ 所以**每个版本必须有一份 Change_Log 片段**，否则页脚不会前进 |
| 重启 | **只在后端核心 `.py` 有改动时才重启**（排除 `static/`、`tools/`、`templates/`）；纯前端构建物更新走 StaticFiles 读盘，即时生效不重启。重启命令＝**宝塔 Python 项目管理器**：`/usr/bin/btpython /www/server/panel/script/restart_project.py python 财务核算工作台`（项目名「财务核算工作台」，8000 由宝塔托管），随后 `sleep 4` 打 `/api/health` 自检 |
| 手动重启 | 宝塔面板 → Python 项目 → 「财务核算工作台」→ 重启；或命令行敲上面那条 |
| 运行期数据 | rsync 无 `--delete`，`sample_data/`、`bank_uploads/`、`conf.ini`、`.env`、`gateway.db` 等分毫不动 |

**推了之后怎么验**：最多 5 分钟后页脚版本号 = 你刚推的 Change_Log 最大号；或 `tail -5 /root/acc_deploy.log` 看「检测到新版本 → 版本戳 → 重启」三行。

### 6.1（历史方案，服务器未采用）重启方式：自适应重启脚本
本服务器上 8000 进程是 `uvicorn app:app --host 0.0.0.0 --port 8000`（系统 `/usr/bin/python3`，CWD=运行目录），**没有 supervisor、没有 systemd 守护**，宝塔也不自动拉起。用一个**自适应重启脚本**：先杀、等 4 秒，**只在端口确实没进程时才由我们拉起**——宝塔若自带守护会先拉起、我们就不动，绝不双开抢端口。放运行区（不进 git，避免和克隆冲突）：
```bash
cat > /www/wwwroot/finance_workbench/restart_acc.sh <<'EOF'
#!/bin/bash
# 重启核算工作台(8000)：先杀等4秒；有守护会自己拉起，没有就我们拉——只在端口无进程时拉，绝不双开
pkill -f 'uvicorn app:app.*--port 8000' || true
sleep 4
if ! pgrep -f 'uvicorn app:app.*--port 8000' >/dev/null; then
  cd /www/wwwroot/finance_workbench/backend
  setsid /usr/bin/python3 /usr/local/bin/uvicorn app:app --host 0.0.0.0 --port 8000 >> /var/log/acc-8000.log 2>&1 < /dev/null &
fi
EOF
chmod +x /www/wwwroot/finance_workbench/restart_acc.sh
# 测一次（约4秒小闪断）：应打印新 PID + 8000:200
bash /www/wwwroot/finance_workbench/restart_acc.sh; sleep 3
pgrep -af 'uvicorn app:app.*--port 8000'; curl -s -o /dev/null -w "8000: %{http_code}\n" http://127.0.0.1:8000/
```
> 若哪天换成 systemd 托管，把 `ACC_RESTART` 改成 `systemctl restart <服务名>` 即可，别的不用动。

### 6.2（历史方案，服务器未采用）挂 cron（每 2 分钟自动拉+部署+重启）
```bash
crontab -e
#   加一行（三个变量都带上，ACC_RESTART 指向上面的脚本）：
#   */2 * * * * ACC_REPO=/www/wwwroot/accounting-workbench ACC_RUN_DIR=/www/wwwroot/finance_workbench/backend ACC_RESTART="bash /www/wwwroot/finance_workbench/restart_acc.sh" /www/wwwroot/accounting-workbench/deploy.sh >> /var/log/acc-deploy.log 2>&1
```
> ⚠ **bootstrap 一次性坑**：给 deploy.sh 补 rsync 的那个提交，本身要靠「带 rsync 的新 deploy.sh」才能 rsync——但那趟跑的还是磁盘上的旧脚本（bash 已把旧版读进内存）。表现＝新码只到克隆、没进运行目录、页脚不变。**只需手动补一刀**（`rsync 源→运行目录` + 写戳 + 重启）即可，之后磁盘上已是新脚本，永不再犯。
> 想"推了立刻上线"而非等 2 分钟：可改 GitHub webhook → 服务器小监听触发 `deploy.sh`（需公网端口+密钥，较复杂）；cron 更省心、够用。

## 今后的流程（替代 ZIP）
- **本机**：改代码 →（前端改了就 `npm run build`）→ **写 `00_Change_Log/V2.xxx_….md`**（页脚版本号从它来）→ `git commit` → `git push`（推 origin/main，**不强推**：服务器 `--ff-only`）。
- **服务器**：cron 每 5 分钟跑 `/root/deploy_acc.sh`（或手动 `bash /root/deploy_acc.sh`）→ 上线。
- **对版**：页脚版本号；或服务器 `cd /www/wwwroot/accounting-workbench && git log -1`；或 `tail -5 /root/acc_deploy.log`。
- **回滚某版**：在本机 `git revert` 出问题的提交再推（服务器只快进，不接受回退历史）。万不得已才在服务器 `git checkout <SHA>` 后手动 `bash /root/deploy_acc.sh`——但脚本会因 `HEAD != origin/main` 直接 merge 回最新，**所以回滚必须走 revert**。

## 运行期文件清单（只在【运行目录】、绝不进 git、rsync 明确 --exclude）
运行目录 `/www/wwwroot/finance_workbench/backend/` 下：`sample_data/workbench.db`、`conf.ini`、`.env`、`gateway.db`、`bank_uploads/`、`ec_uploads/`、`tempatt_uploads/`、`bom_uploads/`、`version_stamp.json`、`sample_data/` 里那几个 `*.json`（ledger/claims/config 等，见 .gitignore）。
> 这些在克隆里就 `.gitignore`（`git pull` 不碰），`deploy.sh` 的 `rsync` 又逐条 `--exclude`（不带 `--delete`）——双保险，绝不覆盖、更不会删。

## 注意
- Deploy key 只读；任何凭据只写服务器，别提交、别外发。
- 端口只留内网，公网只放 80/443（Nginx 反代）。
- `deploy.sh` 用 `git pull --ff-only`：只快进，绝不会用远端覆盖你在服务器上的本地改动（若服务器上误改了 git 跟踪文件导致不能快进，会报错停下，让你先处理，不会硬覆盖）。
