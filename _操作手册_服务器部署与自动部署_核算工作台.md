# 操作手册：核算工作台 · 服务器 git 部署 + 自动部署（腾讯云宝塔 111.229.72.116）

> 目标：把核算工作台从「ZIP 上传」升级成「git 拉取即部署」，并配成**自动部署**（本机推 main → 服务器 1–2 分钟内自动上线）——和 BP 那套一样。
> 在**服务器上**做（碰生产 + 真库）。凭据只写服务器、绝不提交、绝不外发。仓库：`ReginaldZeng/accounting-workbench`（main 已是最新）。

## 为什么核算工作台比 BP 简单
- **前端构建物已进 git**（`01_Current_Deliverables/app/backend/static/`）——服务器**没有 node 也不用 build**。
- **运行期数据全在 backend 目录、且全被 gitignore**（`sample_data/*.db`、`conf.ini`、`.env`、`*_uploads/`、`version_stamp.json` 等）——所以**后端可以直接从 git 克隆里跑**，`git pull` 永远不碰这些数据，**不需要 BP 那样 rsync 到扁平目录**。

---

## 第 0 步：备份（宝塔·文件）
- 现在跑生产的那个目录（ZIP 解出来的）整个压 zip 存好。
- **单独复制真库** `.../app/backend/sample_data/workbench.db` 到安全处。
- 复制 `.../app/backend/conf.ini`（金蝶/钉钉凭据）、`.env`（若有）。
- Nginx 站点配置全选复制存记事本。
> 回滚就靠这几样。

## 第 1 步：只读 Deploy Key（拉私有仓库）
```bash
ssh-keygen -t ed25519 -f ~/.ssh/acc_deploy -N "" -C "deploy-accounting-server"
cat ~/.ssh/acc_deploy.pub          # 复制这一整行公钥
```
GitHub：仓库 `accounting-workbench` → **Settings → Deploy keys → Add deploy key** → 粘贴公钥，**不要勾 Allow write**（只读，服务器只拉不推）。
```bash
cat >> ~/.ssh/config <<'EOF'

Host github-acc
  HostName github.com
  User git
  IdentityFile ~/.ssh/acc_deploy
  IdentitiesOnly yes
EOF
ssh -T git@github-acc   # 期望：Hi ReginaldZeng/accounting-workbench! You've successfully authenticated...
```

## 第 2 步：克隆仓库（就在这里直接跑，不搞第二份）
```bash
git clone git@github-acc:ReginaldZeng/accounting-workbench.git /www/wwwroot/accounting-workbench
cd /www/wwwroot/accounting-workbench && git log -1 --oneline    # 应为最新 V2.425
```

## 第 3 步：把运行期数据搬进克隆的 backend 目录（这些 git 里没有）
```bash
BK=/www/wwwroot/accounting-workbench/01_Current_Deliverables/app/backend
# 把现生产目录里的真库/凭据/上传件放进来（按你现目录实际路径改左边）：
cp  /path/现生产/app/backend/sample_data/workbench.db  "$BK/sample_data/workbench.db"
cp  /path/现生产/app/backend/conf.ini                  "$BK/conf.ini"
cp  /path/现生产/app/backend/.env                      "$BK/.env"            2>/dev/null || true
# 上传件目录（有就搬，没有跳过）：
for d in bank_uploads ec_uploads tempatt_uploads bom_uploads; do
  [ -d "/path/现生产/app/backend/$d" ] && cp -r "/path/现生产/app/backend/$d" "$BK/"
done
```

## 第 4 步：装依赖 + 测试端口先验（别碰生产）
```bash
cd /www/wwwroot/accounting-workbench/01_Current_Deliverables/app/backend
pip install -r requirements.txt          # 用生产那个 venv
python -m uvicorn app:app --host 127.0.0.1 --port 8099 &   # 临时测试端口
sleep 3
curl -s -o /dev/null -w "首页 %{http_code}\n"  http://127.0.0.1:8099/
curl -s -o /dev/null -w "登录 %{http_code}\n"  http://127.0.0.1:8099/api/login
kill %1
```
- 页面能开、接口不 500 → 进第 5 步。若某接口 500（多半缺 DB 迁移列）→ 先在真库跑对应迁移再切。

## 第 5 步：把生产切到这个克隆（宝塔 Python 项目）
- 宝塔 → Python 项目管理器 → 把跑生产那个项目的**目录改成** `/www/wwwroot/accounting-workbench/01_Current_Deliverables/app/backend`，启动文件 `app:app`，端口保持原样（如 8000）。
- **AI 网关**（若在用）：`gateway:app` 端口 8020，同样指到上面这个 backend 目录，单独一个项目。
- 重启。`curl -s http://127.0.0.1:8000/` 能开即成功。开 `http://111.229.72.116/`（或你的域名/路径）验收，**页脚应显 V2.425**。

---

## 第 6 步：配自动部署（= 你要的"和 BP 一样"）
仓库根已带 `deploy.sh`（git pull 有新提交才重启，无变化跳过）。配置：
```bash
# 1) 让 deploy.sh 知道怎么重启后端（二选一）
#    a. 若后端以 --reload 跑：不用配，git pull 后自动热更 → ACC_RESTART 留空即可
#    b. 若非 --reload：给出重启命令，例如宝塔命令行重启，或 systemd：
export ACC_RESTART="systemctl restart acc-workbench"      # ← 改成你的实际重启方式
# 2) 手动先跑一次验证
bash /www/wwwroot/accounting-workbench/deploy.sh
# 3) 挂 cron，每 2 分钟自动拉+部署（推 main 后最多 2 分钟自动上线）
crontab -e
#   加一行（把 ACC_RESTART 也带上）：
#   */2 * * * * ACC_RESTART="systemctl restart acc-workbench" /www/wwwroot/accounting-workbench/deploy.sh >> /var/log/acc-deploy.log 2>&1
```
> 想要"推了立刻上线"而不是等 2 分钟，可改用 GitHub webhook → 服务器小监听脚本触发 `deploy.sh`（需公网端口+密钥，较复杂）；cron 更省心、够用。

## 今后的流程（替代 ZIP）
- **本机**：改代码 →（前端改了就 `npm run build`）→ `git commit` → `git push`（推 origin/main）。
- **服务器**：cron 自动 `deploy.sh`（或手动跑一次）→ 上线。
- **对版**：服务器 `cd /www/wwwroot/accounting-workbench && git log -1`，或看页脚版本号。
- **回滚某版**：`cd 该目录 && git checkout <SHA> && bash deploy.sh`。

## 运行期文件清单（只在服务器、绝不进 git、部署不碰）
`app/backend/` 下：`sample_data/workbench.db`、`conf.ini`、`.env`、`bank_uploads/`、`ec_uploads/`、`tempatt_uploads/`、`bom_uploads/`、`version_stamp.json`、`sample_data/` 里那几个 `*.json`（ledger/claims/config 等，见 .gitignore）。
> 这些都在 `.gitignore` 里，`git pull` 不会覆盖、更不会删。

## 注意
- Deploy key 只读；任何凭据只写服务器，别提交、别外发。
- 端口只留内网，公网只放 80/443（Nginx 反代）。
- `deploy.sh` 用 `git pull --ff-only`：只快进，绝不会用远端覆盖你在服务器上的本地改动（若服务器上误改了 git 跟踪文件导致不能快进，会报错停下，让你先处理，不会硬覆盖）。
