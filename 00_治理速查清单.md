[Change Log]
Date: 2026-06-25
Author: Reginald Zeng
Version: V1.0
Description: 项目治理一页速查清单（Cheat Sheet）。

# 项目治理速查清单 / Governance Cheat Sheet

> 一页看懂 `00_项目治理中心.md`。冲突时一律以治理原文为准。

## 🔑 一句话原则
**先想后做 · 版本留痕 · 成果隔离 · 改动审批。**
收到需求先分析设计，禁止直接写代码。

## 📁 四大目录
| 目录 | 放什么 | 能否在此开发 |
|------|--------|--------------|
| `01_Current_Deliverables/` | 仅最新已批准成果：`app/`（唯一应用）+ `docs/<YYYYMMDD_工具名>/`（各工具现行签署版文档） | ❌ 禁止 |
| `02_Revision_History/` | 所有开发 + 临时文件（一个 Major 时代一个开发夹） | ✅ 唯一开发区 |
| `03_Source_Materials/` | `00_平台通用/` + 各工具格 `<YYYYMMDD_工具名>/`（该工具全部草稿/历史版/素材） | ❌ 仅参考 |
| `00_Change_Log.md` | 总变更台账**历史正文（到 V2.169 止，只读）** | ❌ 不再改 |
| `00_Change_Log/` | **V2.170 起的新条目：一改动一文件**（`V版本号_工具线_简述.md`） | — 每次必增一个文件 |

> **工具线分格（V2.51 起）**：一工具一格，前缀=需求确认书 v1.0 出稿日；`00_平台通用` 永远置顶。
> 现有格：20260703_银行对账（含理财对账）· 20260705_物流计提 · 20260708_物流对账 · 20260709_成本台账。
> 一句话："现在算数的在 01/docs，来龙去脉在 03 的工具格子里。"

> **`04_部署包/`（非治理层级，2026-08-01 起）**：部署包（后端 zip）本地暂存区，整夹 gitignore、不入库、克隆里没有。部署包＝构建产物（源码在库随时可重打），部署历史看 `00_Change_Log.md`。

> **台账分片化（V2.170 起，2026-08-05）**：`00_Change_Log.md` 冻结为历史，新条目一律在 `00_Change_Log/` 新增一个文件。
> 原因：老规则「新条目插到大表最顶上」在**多需求并行**时每条分支必撞车。改成一改动一文件后 git 天然零冲突。
> 看全量：`python 00_Change_Log/_合并视图.py`（生成物、不入库）。格式与防撞号规则见 `00_Change_Log/README.md`。

## 🔄 开发九步（一步不可跳）
1. 读 `00_Change_Log.md`（历史）+ 扫 `00_Change_Log/`（近期）→ 2. 定当前版本 → 3. 提新版本 →
4. 用当前 Major 时代开发夹（现为 `20260703_V1.2_接真数据`；新 Major 才建新夹；纯文档任务无修订夹）→
5. 确认开发夹与正式成果一致 →
6. 开发 → 7. 产出 → 8. 同步进正式目录 → 9. **在 `00_Change_Log/` 新增**一个片段文件（不改 `00_Change_Log.md`）。

## 🔢 版本号
`主.次`，开工前先声明 `Current:` / `New:` 并确认。
Major = 架构级/不兼容（需先做影响分析）；Minor = 常规迭代。

## 🧭 工作流六步
分析需求 → 识别受影响文件 → 出开发计划 → 评估风险 → **等批准** → 实现。

## 📤 每个任务必交六件套
版本提案 · 受影响文件 · 开发计划 · 风险 · 交付物 · 变更记录。

## 🏷️ 文件头规范
每个生成文件开头必须带：
```
[Change Log]
Date: / Author: / Version: / Description:
```
适用 Python / TS / JS / React / Vue / SQL / VBA / Markdown / YAML / 配置文件。

## 🛡️ 红线（未经明确批准禁止）
删文件 · 改名 · 动目录结构 · 改 DB schema · 改环境变量 · 删既有 API。
→ 如确需，**先出《影响分析》**。
保留既有功能/流程/数据结构/API 契约/UI 规范，禁止因一个需求重做整个系统。

## 🎨 UI 前置
UX → 信息架构 → 线框图 → UI 方案 → 再实现。禁止上来就写 UI 代码。

## ✅ 完工清单（全勾才算交付）
☐ 版本确认 ☐ 修订文件夹 ☐ 开发完成 ☐ 交付物
☐ Change Log 更新 ☐ 文档更新 ☐ 临时文件隔离 ☐ 最终复审

## 📊 治理优先级与文件索引（高→低）
| 优先级 | 文件 | 类型 |
|--------|------|------|
| ① | `00_项目治理中心.md` | 🌐 通用 · 可复用/可share |
| ② | 需求文档（存 `03_Source_Materials/`） | 🔒 项目专属 · 每项目单独写 |
| ③ | `02_AI开发执行规范（Codex）.md` | 🌐 通用 · 可复用/可share |
| ④ | `03_AI产品设计规范（Claude）.md` | 🌐 通用 · 可复用/可share |
| ⑤ | 临时任务要求 | — 随任务 |

> 冲突时服从更高优先级文件。
> 🌐 通用 = 跨项目复用、可分享同事；🔒 项目专属 = 一项目一份。
> 配套通用文件：`00_Change_Log.md`、`00_治理速查清单.md` + 四个目录骨架。
