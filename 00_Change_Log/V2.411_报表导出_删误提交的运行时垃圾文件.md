# V2.411 · 删掉 main 上误提交的运行时垃圾文件 `System.Collections.Hashtable`

**日期**：2026-08-30　**分支**：main

清理旧账：`01_Current_Deliverables/app/backend/System.Collections.Hashtable` 一直躺在库里，
是 V2.246 修掉的 `$STATE` 大小写 bug 的产物——取件机脚本试跑时 `Out-File` 把哈希表的
**对象类型名当成了文件名**，随 V2.240 自查一起误提交入库。内容核实为开发机临时状态
（`{ "…108_深圳市星期十…财务报表.xlsx": "15782|2026-08-07 17:49:25" }`），非线上记录，
线上取件状态由 V2.246 + 固定工作目录兜住，故直接删。

这条清理其实 V2.278 就在 `eloquent-rhodes` 分支做过，但那条一直没并进 main，main 至今
还背着这个文件。本次直接在 main 上补做，不并那条老分支。

## 同时 .gitignore 补挡运行时文件

- `System.Collections.Hashtable`——挡住这类"对象类型名当文件名"的产物再误入库。
- 取件机运行时文件 `pull_state.json` / `pull_reports.log` / `pull_reports.ini`
  （`pull_reports.ini` 含取件码，敏感度同 conf.ini；`.example` 模板不受影响、照常入库）。

## 校验

只删了一个非代码、非配置的垃圾文件 + 加 gitignore 忽略规则，不动任何应用逻辑；
后端 import、路由、下游一律不受影响。
