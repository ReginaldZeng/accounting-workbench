# V2.586 · BOM报价审核：OA「回退」识别按 27725 真实数据修正——REDIRECT_PROCESS + 早节点冒出新任务 ＝ 回退

**日期**：2026-09-15　**业务方给单**：202609011150000027725（你 09-04 10:13 在财务经理节点回退过一次）。

## 27725 里"回退"长什么样（与 V2.584 预设不同）

- 操作记录：`REDIRECT_PROCESS`（uid=曾，备注空）；
- 财务经理节点的任务变成 **CANCELED / NONE**（不是 COMPLETED/REDIRECTED，也没有 finish_time）——V2.584 只看"已完成任务"，**这一刻什么都看不到**；
- 成本核算节点**立刻多一个新任务**（create 09-04 10:13）→ V2.584 的「退回重到」那条路能兜住，但拿不到是谁退的、也拿不到备注；
- 回退人（曾）当时**不是**该节点任务的持有人（持有人是或签的另一位）→ 不能按"持有人"过滤。

## 改（`kernels/dingtalk_bom.py`）

- 抽出纯函数 `final_state_from_inst(inst, node_ids)`（可离线测），`final_node_state` 只做取数。
- 两种信号取**较晚者**：①本节点已完成任务 `task_result ∈ {AGREE, REFUSE}`；②**回退**＝`REDIRECT_*` 操作发生时**流程正停在本节点**（本节点有创建时间 ≤ 操作时间、且当时未完成的任务）**且之后在非本节点出现新任务**（同节点转交/加签不算；更早节点的人回退不算本节点动作）。回退返回 `result=REDIRECTED`、`taskId=redirect:<时间>:<uid>`、`userid`、`finishTime`、`remark`（备注）。
- 由此 `_oa_final_sync_once` 在你回退后 5 分钟内即按「终审退回」打回已初审产品（原因＝备注，无则「OA 退回（未写原因）」，退回人＝你）；`_oa_rearrival` 也能带出回退人与备注。

## 测（27725 真实时间线切片）
全量→AGREE 09-11；截到 09-04 10:20（回退当刻）→ REDIRECTED 09-04 10:13 by 曾；截到 09-03 17:00（首次到节点）→ 无动作、节点在办；截到 09-04 19:00（志鹏再审后你未点）→ 仍是上次回退（同任务号不重复处理）；伪造同节点转交→不算回退；伪造早节点回退→不算本节点动作。ALL PASS；V2.584 功能回归 ALL PASS。

## 结论
OA 终审同步的三种动作（同意 / 拒绝 / 回退）至此都有真实数据或桩验证；回退无备注时提醒里只能写「未写原因」——**建议回退时在钉钉备注里写 CP 码 + 原因**，工具就只退那几个产品并把原因带给志鹏。
