# V2.635 物流账单复核台：右上角月份组件 + 承运商改「本月有计提的」

[Change Log] Date: 2026-09-26 | Author: Claude Opus 4.8 | Version: V2.635

用户看上线页两处反馈：
- **右上角改标准月份组件**：从只读账期文本改成全项目共用的 `PeriodPicker`（年度+12 彩色状态胶囊），随其它页体例；账期切换联动复核结果与承运商列表。App.jsx 给 LogisticsReview 传 `onPeriod`。
- **承运商列表改「本月有计提的」**：不再写死 5 家，改成金蝶本月真有物流计提的承运商列出来。后端新增 `GET /api/logistics-review/carriers?period=`——复用 `logistics_recon.accrued_by_carrier(kingdee_client.fetch_gl_voucher(y,m,'2241'))` 从 2241 供应商往来·计提凭证按承运商全称汇总贷方，映射 `logi_suppliers` 简称，标注是否已配取数说明(可复核)；已配却本月无计提的（如 pilot 迅鸽）也补列。前端头部下加「本月有计提的承运商」搜索栏：胶囊显简称+计提额，未配灰示「未配」，点击切换。
