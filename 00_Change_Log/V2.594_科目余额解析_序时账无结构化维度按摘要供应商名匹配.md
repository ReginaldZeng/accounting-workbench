# V2.594 科目余额解析（物流）：下钻凭证按摘要供应商名匹配（序时账 FF100002 为空的真机修正）

真机诊断（V2.593 内嵌）确认：2221.01.07 暂估进项税的序时账里 `FDetailID.FF100002.FNumber` 为 null（该科目根本不用这个 flex 存供应商），供应商名在摘要 `FEXPLANATION` 里（如"…上海顺丰冷运供应链有限公司…暂估进项税转认证进项税…应付单号AP…"）；账簿名过滤正常。故按维度编码匹配永远 0 条。另发现 2221.01.07 非物流专用，货物采购暂估也在此科目。

- `build_voucher_lines` 加 dim_name 维度归属回退：序时账行有结构化维度编码(FF100002)就按编码严格匹配（样例、及维度非空科目不变）；为空时用**供应商核心名在摘要里匹配**。
- 新增 `supplier_core(name)`：取 '/' 前一段去公司后缀（如"上海顺丰冷运供应链有限公司/物流运输服务"→"上海顺丰冷运"、"顺丰速运有限公司"→"顺丰速运"），用于摘要模糊匹配。
- 下钻/追溯把维度名（供应商名）传入 build_voucher_lines。V2.593 的内嵌诊断保留（仍 0 条时兜底显示）。

验证：合成真金蝶风格行（FF100002=null、供应商在摘要）→ 上海顺丰冷运匹配 2 笔 借12.39/贷31.38，武汉仟佰味食品不误入；样例结构化维度仍走编码匹配 67.99/55.25 不变；py_compile/构建/EOL 均过。⚠ 摘要匹配依赖摘要含供应商名，个别摘要不含或简称可能漏/偏，合计不一定严丝合缝对上余额表——真机看追溯里凭证是否合理、合计是否贴近。

发布：沿用 main 自动部署，须重启后端。回退＝反向提交本提交。
