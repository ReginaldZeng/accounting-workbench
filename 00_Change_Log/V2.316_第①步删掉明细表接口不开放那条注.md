# V2.316 · 第①步删掉「明细表接口不开放」那条注（补记）

**日期**：2026-08-18　**作者**：Claude / c　**线**：main（存货台账）
**补记**：当时只提交了代码，漏了本分片，V2.353 一并补上。

业务方指着第①步说明末尾那行问「这还需要？」——不需要。

那是我留的**死胡同记录**：试过金蝶《存货收发存明细表（跨维度）》的接口，
调用报「此接口暂时只支持简单账表」，不对外开放；但也不需要它，
第①步取的汇总表（跨维度）已含全部维度。

这种东西是**给开发看的**，不该占会计的屏。挪进 `kingdee_client.fetch_inventory_summary`
上方作注释并标 ⛔，防止日后有人再去试一遍。
