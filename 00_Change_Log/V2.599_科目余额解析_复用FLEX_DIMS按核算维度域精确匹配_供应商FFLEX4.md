# V2.599 科目余额解析（物流）：复用 FLEX_DIMS 按核算维度域精确匹配（供应商=FFLEX4，真根治）

真机诊断到 FF100002~010 全空、FF100011+ 不存在——因为我一直用错字段名。代码里报表导出早已锁定核算维度域 FLEX_DIMS（kingdee_client 1799）：**供应商=FFLEX4**、部门=FFLEX5、费用项目=FFLEX9…（不是 FF100002 那套）。之前瞎试的 FF100003~010 其实是"办公地点/其他往来单位/供应商分组/产品项目…"，当然全空。

- 新增 `kc.flex_num_fields()`：FLEX_DIMS 全部 19 个域的【编码】字段（别名 dim0..18，供应商 FFLEX4=dim9），下钻取数一并带出。
- `_fisbal_vouchers` 改用 `extra_fields=kc.flex_num_fields()`（异常则退化为无维度列走摘要兜底）。
- `build_voucher_lines` 已有：收该行全部结构化维度值 dim* → `_dim_match` 精确匹配（单值/复合子集）；结构化优先、摘要兜底、返回结构命中。
- `probe_voucher_dims` 诊断改按 FLEX_DIMS 各域列样例值（安全网，标出供应商/费用项目落哪个域）。

验证：flex_num_fields 出 19 个域、供应商域别名 dim9；合成行 FFLEX4=物流运输服务016 → 精确配 1 笔 1455.77、结构命中 1、别的供应商027 不进；py_compile/EOL 均过；仅后端。⚠真机：重启后点顺丰，逐笔应按 FFLEX4 供应商精确到笔、与余额表本期发生严丝对上、零差额、诊断框消失（结构命中>0）。若某科目维度顺序特殊致复合对不齐，可再引 fetch_account_dim_order。

发布：沿用 main 自动部署，须重启后端。回退＝反向提交本提交。
