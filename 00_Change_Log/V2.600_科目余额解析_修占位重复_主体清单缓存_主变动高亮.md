# V2.600 科目余额解析（物流）：修占位重复渲染 + 主体清单缓存提速 + 逐笔主变动高亮

- 修「科目余额解析·二期开发」占位重复：App.jsx CODED_VIEWS 漏登记 'fisbal'，导致真页面 FiSubjectBalance 与"规划占位"同时渲染（占位叠在真页下方，看着像没接功能）。补上 'fisbal' 即消。
- 提速：`_fisbal_orgs()` 主体清单(fetch_fin_report_list)按(源,年,期)缓存——原来 orgs 端点与系统取数各拉一次金蝶报表清单，现只一次；/sync 清缓存。（全科目余额表首次取数仍较重、之后走缓存。）
- 逐笔「主变动高亮」：voucherTable 把本期金额(借/贷取大)最大的那笔标出（★+琥珀底+加粗）——回答"这笔余额主要由哪一笔形成"，本期下钻与追溯各期通用。

验证：前端构建通过；py_compile/EOL 均过；结构化根治(V2.599 FFLEX4)真机已零差额、诊断框消失。⚠真机看：占位不再出现、启动是否变快、下钻里最大那笔是否高亮。

发布：沿用 main 自动部署，须重启后端。回退＝反向提交本提交。
