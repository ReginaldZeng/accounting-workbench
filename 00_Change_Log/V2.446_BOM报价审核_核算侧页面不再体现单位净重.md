# V2.446 · 核算侧页面不再体现单位净重（业务方定 2026-09-05：「净重不体现在核算这一侧」）

V2.445 把净重从「确认+闸」降成右栏只读参考行，业务方再定：核算侧**完全不显示**。

- 右栏「规格解析净重 · 供 BP 参考」一行删除，`NetWeightRow` 组件与 `bomSetNetWeight` 前端引用移除。
- 后端不动：`/api/bomcost/final` 照带 `netWeightKg`（规格解析参考值）给 BP；`net_weight_kg` 列与 `POST /api/bom/set-net-weight` 保留（无前端入口）。
- 确认书 v1.2 §2.7、回复文档 §2 同步。前端 build 过。
