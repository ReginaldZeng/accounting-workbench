# V2.483 · 逐日明细筛选器改序（派遣方·部门·姓名）+ KPI 卡片跟着筛选联动 + 「只看异常」含待指认

**日期**：2026-09-06　**分支**：main

Reginald：逐日筛选器顺序改 派遣方·部门·姓名；筛选要**联动后面的 KPI 小卡片**；加一个「只看异常」标签。

## 改法（网页逐日明细）

1. **筛选器改序**：`PeopleFilter` 里 归属（派遣方）挪到最前，然后 部门、姓名（逐人页共用，一起生效）。
2. **KPI 卡片联动筛选**：`bandCount` 改为在「派遣方/部门/姓名」筛过的子集上算——选了锦绣，卡片就都是锦绣的数
   （原来一直是全表 5321）。表格 `rows` 也先按 df 筛再套档位，两者同一子集。
3. **「只看异常」**：把原「仅不一致」升级为「⚠只看异常」＝撑不起上报 + 报了工时无打卡 + **同名待指认**
   （原来只有前两项）；并新增「◇同名待指认」单档卡片，那些待指认的日子第一次能被筛出来。②总览的
   「只看不一致的日次」按钮文案改「只看异常的日次」。

## 校验

- 前端 build 通过；纯前端改动。
