# V2.482 · 逐日明细列序调整 + 加「手机尾号」「规则扣减」；「重算」加感叹号注明算法

**日期**：2026-09-06　**分支**：main

Reginald：逐日明细是最细的一层，调列序为——归属、部门、姓名、手机尾号、班型、日期、上班、无效卡、下班、
打卡次数、跨度、规则扣减、上报、重算、差异、单价、金额、判定；「重算」带感叹号注明怎么算的。

## 改法（网页第⑥步「逐日明细」表）

- **列序**按上面重排；**新增两列**：
  - **手机尾号**：钉钉取数才有（人力导出没这列），同名两人靠它分。逐日行加 `手机尾号 = rec["标识"]`。
  - **规则扣减**：白班扣 1h（午饭）/夜班扣 0.5h（夜宵）。`compute_shifts` 的班次字典加 `break`，逐日行加 `规则扣减 = sh["break"]`（没切出班次＝无扣减）。
- **重算**表头加 `!` + 悬浮注：「重算 ＝ 跨度（下班−上班）按 0.5 小时向下取整，再扣规则扣减（白班1h/夜班0.5h）。是量偏离的尺子，不是重发工资」。

## 未动

- 导出的 ④逐日核对 Excel 暂不动列序——它列间有公式互引（P列=键、T列=净多记，③页 SUMIFS 按列位引用），重排会打断，需另行小心处理。

## 校验

- 内核测试全过；前端 build 通过。
