[Change Log]
Date: 2026-08-06
Author: Claude / c
Version: V2.230
Description: 【物流计提】「账单上传」加④通知设置页签(业务方定,不放系统设置)——三个场景(新供应商建档/提交核算组/付款提醒)各配钉钉手机号+邮箱,改完即生效不用动服务器;留空回落conf.ini公共名单;「发送测试」真发验证。新表notify_recipients;notifier.notify加scene参数逐渠道覆盖;凭证仍只在conf.ini。

---
version: V2.230
date: 2026-08-06
line: 物流计提
author: Claude / c
status: Synced(8082 实测)
revision: 01
---

**【物流计提】通知收件人搬进前端——「账单上传」④通知设置(业务方定:就放物流部页,不进系统设置)**:

- **三个场景各配各的**:新供应商建档(→核算组)/提交核算组(→核算组)/付款提醒(→出纳/主管),每场景=钉钉手机号+邮箱两个输入框(逗号分多人)+保存+**发送测试**(按已保存名单真发一条,收到即配置正确;改了没保存会拦);显示上次谁改的;
- **回落语义(逐渠道)**:场景某渠道留空→该渠道走 conf.ini 公共名单;页面顶部明示公共名单当前是谁——**服务器上现在只有 13691683676,部署后记得在④页把核算组配进去**;
- **边界**:钉钉应用凭证(appkey/secret)与邮箱账号密码**仍只在服务器 conf.ini**,前端只管"发给谁";手机号须是钉钉注册号;
- **实现**:新表 `notify_recipients`(scene唯一/mobiles/emails/谁改的);`notifier.notify(..., scene=)` 场景解析(DB查询失败静默回落,绝不弄垮通知);四处发送点挂场景(bills-parse/long-form新供应商→建档,submit→提交核算组,pay-remind→付款提醒);接口 GET/POST notify-recipients + POST notify-test(都要 logistics_upload 权限,全 audit)。

**实测(8082)**:保存"提交核算组=138…01,138…02/hesuan@test.com"回读✓;④页三卡渲染+回填+上次改动人✓;**桩测路由三态全对**(场景配了→用场景名单/没配→公共名单/老调用不带scene→行为不变,全程未发真消息);本机测试发送回执"未配置"=测试防线继续生效。
