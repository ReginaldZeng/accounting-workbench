# V2.588 科目余额解析（物流）：财务报表新页 + 下钻反查凭证 + 期初逐期追溯

- 报表板块 › 财务报表 下新增独立页「科目余额解析」（key=fisbal，待验收、准入 enter:fisbal 默认不给＝仅主管理员可见）。定位＝科目余额表这笔数据的“生产/落地入口”，先切物流相关科目段（费用 6601/6604/6401/5101 + 其他应付款 2241 + 应交税费—进项税 2221）。
- 双模式产生：①系统取数（样例内置物流种子分录；金蝶报表口径，借−贷有符号，期末=期初+本期借−本期贷 恒成立）②手工上传《科目余额表》Excel → 泛化容错认列（两行借/贷表头、单列原币都认），只挑物流科目。
- 质检勾稽：逐科目/维度核 期末 ?= 期初+本期借−本期贷，不平标红。逐科目核对：系统数 ⟷ 上传数 按科目对差异（人眼核对双模式）。
- 下钻反查凭证：点开任意维度行 → 勾稽拆解 + 本期发生逐笔凭证（日期/凭证字号/摘要/借贷/制单人），后端接金蝶序时账 fetch_gl_voucher_subjects。
- 期初逐期追溯：点「追溯期初来源」→ 期初＝上期期末，逐期往前翻各期构成，直到某期期初为 0（建账起点）为止。
- 内核 subject_balance.py 向后兼容扩展：parse_report_xlsx(prefixes=…)、compare(cat_fn=…) 均加默认参数，银行对账（四类资金科目）旧调用零影响；新增 cat_of_logi / normalize_full_rows / build_rows_logi_sample / qc_rows / build_voucher_lines。新增 core._FISBAL_CACHE（随 _cache_clear 作废）。
- 金蝶实时全科目取数（物流，按主体）与逐期追溯的真数据取数暂未接入取数总闸，页面已给“接线中”提示；机制与本期下钻一致，样例已跑通。无新数据库迁移，未删既有文件/接口。

验证：内核自测通过（解析只挑 6 物流科目并滤除非物流噪声与合计行、质检全平、核对精确抓出 1000 差异、下钻两笔配平、追溯 3 期回到期初 0）；py_compile 通过；端到端 HTTP 登录+上传核对返回 200 且核对正确；样例模式浏览器实点：主表/质检/下钻反查凭证/追溯期初来源均正常。前端构建通过（既有 FundDashboard 重复键与大包警告照旧）。EOL 已复核：改动文件 git diff --stat 与 --ignore-space-at-eol 一致，无换行符污染。

发布：沿用 main 自动部署（deploy.sh git pull --ff-only → rsync → 版本戳 → 重启）；页面 admin-only 待验收，对同事不可见。⚠ 生产为金蝶数据源：本版系统取数/下钻/追溯为“接线中”，真数据仅上传解析+质检可用，下钻/追溯的真数据接入为下一步。回退＝以本提交反向提交（未删任何既有文件或 API）。
