# V2.590 科目余额解析（物流）：口径按真金蝶收窄——精确明细科目 + 费用按费用项目 + 丢弃各级汇总行

- 真机联调发现：用 2221/2241 整段前缀会把整棵应交税费/其他应付款树都捞进来（真机 554 行、含各级"—"空维度汇总行），不是物流口径。
- 按物流计提工具（logistics_accrual.py）入账口径精确圈定：
  - 暂估进项税 = 精确明细科目 **2221.01.07**（挂供应商）；其他应付款—供应商往来 = **2241.02**（挂供应商）——精确科目，不再整段 2221/2241。
  - 物流费用 = 6601/6604/6401/5101，但只收**费用项目维度**命中「运费/仓储/搬运/研发外购」关键字的明细行（整棵销售费用等不再全进）。
- 系统取数与逐期追溯改用新 `normalize_logi_rows`：**丢弃各级科目汇总(空维度)行**（前端按科目分组自算小计），彻底消除父级汇总污染。
- 新增内核 `is_logi(code, dim_name)` / `normalize_logi_rows(rows12)`；`LOGI_EXACT`/`LOGI_FEE_PREFIXES`/`LOGI_FEE_ITEMS`；`LOGI_PREFIXES`（上传解析用）收窄为 2221.01.07/2241.02/5101/6401/6601/6604。前端副标题按新口径改写。

验证：内核合成金蝶12列样例（含 2221 整棵树+各级汇总空维度行+2241.02/2221.01.07 供应商行+6601 费用项目行+费用非物流维度行+合计行）→ `normalize_logi_rows` 只保留 3 条正确物流明细，汇总/树/合计/非物流费用全部滤除；py_compile 通过；前端构建通过；EOL 复核 --stat 与 --ignore-space-at-eol 一致。⚠ 费用侧「按费用项目维度筛」依赖金蝶《科目余额表》报表接口对费用科目返回的维度即为费用项目——须真机确认（若费用行未出/维度是部门，调 LOGI_FEE_ITEMS 或改按 FFLEX9 取）。

发布：沿用 main 自动部署，须重启后端；页面 admin-only 待验收。联调重点：①应交税费只剩 2221.01.07、其他应付款只剩 2241.02（按供应商）②不再有"—"空维度汇总行 ③物流费用按费用项目出得来吗（出不来即维度非费用项目，反馈调整）。回退＝反向提交本提交。
