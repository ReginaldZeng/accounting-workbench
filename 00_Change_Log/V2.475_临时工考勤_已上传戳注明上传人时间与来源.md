# V2.475 · 「已上传」戳注明谁·何时上传、来源（手工上传 / 钉钉接口）

**日期**：2026-09-06　**分支**：main

紧跟 V2.473。Reginald：①「要注明是谁，什么时间上传的」；②「要注明来源，是手工上传，还是钉钉接口」。

## 改法

- **按表记上传信息**：`_save_period` 每张表存 `{上传人, 时间, 来源}`（`原表信息`）——只更新这次新给的那张，
  没重新给的沿用上一次（用回留档时不把上传人/时间改成这次的）。重跑（用留档原表）一律不改上传信息。
- **来源**：上报表恒「手工上传」；打卡表来源由前端带：`从钉钉取打卡`→「钉钉接口」，选文件→「手工上传」
  （`form()` 附 `punch_source`，`_read_two` 收）。
- **前端 UploadedStamp**：戳上加一行「由 X 于 T 上传」＋「手工上传 / 钉钉接口」彩色标签；
  老留档没按表记时退回跑批人/跑批时间。取数完成即视为该期打卡表来源＝钉钉接口。

## 校验

- 内核 124/124；前端 build 通过；router 语法检查通过。
- 只重传打卡表时，上报表的上传人/时间/来源保持不变（按表分别记）。
