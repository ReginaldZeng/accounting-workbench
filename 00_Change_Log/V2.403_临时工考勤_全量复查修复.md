# V2.403 · 临时工考勤全量复查修复（14 agent 工作流复查后逐条改）

**日期**：2026-08-30　**分支**：main

Reginald：「从头检查下有什么地方不对」。跑了 7 维度 × 对抗式核实的多 agent 复查，
确认 20 条（3 高 6 中 11 低），本次逐条修复（MED-5 留作决策，见末）。

## 高（破坏正确性 / 隐藏真异常）

1. **②总览「结算风险」行跳错页**：`to:'people'`（⑤逐人核对）→ 但风险认定卡只在④结算风险页。
   点「N 项待认」被送到⑤，那里没有可认的东西。改 `to:'risk'`（去第④步结算风险）。
2. **逐日认定键塌缩成「姓名|日」，丢了归属**：同名不同派遣方两人同一天撞键——勾一条连带勾中另一人、
   认定一条把另一人当天的真超付异常一并无声抹掉。前端 dKey / doUndo / askAck 三处、后端两处 `_mark`
   都补成「姓名|归属|日」；老格式历史认定用 `or` 兜底读回（实测新键认定生效、老键仍读得回）。
3. **④逐日导出「判定」把白夜混合无条件盖成 ◇（不分口径）**：默认 shift 口径下混合工是内核**按日切白/夜**
   正常判档的，其「打卡撑不起上报（多付钱）」网页标红、导出却被 ◇ 覆盖成无色隐藏。改：混合行在 shift 口径下
   **改写内核算好的静态值**（重算/差异/判定），随条件格式正常上色。2026-06 实测 29 行混合，现都显示真实判定。

## 中

4. **①核对概览副标题写死 punch 口径话术**（「少记全列/多记弹性」），与默认 shift 口径下的四档表自相矛盾。
   按 `_BASIS` 分支：shift 改成「上报＝排班班次时长，只问打卡撑不撑得起上报」。
6. **导出「仅1次卡」标 ◇ 无色，网页却是 △ 黄**（可认定的待查档）。导出改 △，随条件格式标黄，与网页一致。
7. **逐日单价列 tooltip 硬编码「见第④步」**，但单价不符明细在⑤逐人核对。改成 `stepNo('people')`（⑤）。

## 低（过时注释 / 死代码 / 体验）

- 注释里的旧步号：`八步`→九步、`第④步逐人核对`→第④步结算风险、`第⑤步逐日`→第⑥步、后端 3 处`页面第⑦步`→第⑨步。
- 删死代码 `const warn`（算出后无处引用）。
- ⑨用工成本的锁只判 signOk：撤销⑦会让⑧重锁但⑨还开着 → 步骤条锁与⑨页渲染都补判 `adjSignOk`。
- 逐日批量分两组调用，若后组失败也 reloadCur，让已落库那组反映到界面、并提示「已成功 N 组」。

## MED-5 留作决策（未改）

顶栏常驻「导出 Excel」不受 ⑦→⑧→⑨ 签字闸门约束——没签也能一键导出**含用工成本汇总**的整份报告，
绕过了「复核没做完别被复制走」的本意。三个选项待 Reginald 定：
(a) 顶栏导出也按 signOk&&adjSignOk 才可点；(b) 未签时导出照出、但**不含**用工成本那段；(c) 维持现状。

## 校验
内核 114 测试全过；2026-06 回归撑不起 4/待查 0/撑得住 1594 不变；判定公式 13/13 平衡；
新旧认定键都读得回；前端构建通过。
