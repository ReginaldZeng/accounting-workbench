# V2.485 · 逐日「重算」加逐行算式说明；整表按 归属·部门·姓名·手机尾号·日 排序

**日期**：2026-09-06　**分支**：main

Reginald：重算要每一行都能看出怎么算的（2.5 是怎么来的）；整表按 归属·部门·姓名·手机尾号·日期 排序。

## 改法（网页逐日明细）

1. **逐行算式**：每行「重算」单元格加悬停说明（虚线下划线提示可悬停）——
   `(下班−上班)=跨度 3.7h → 按0.5小时向下取整 3.5h → 扣规则扣减 1h ＝ 重算 2.5h`；
   没切出完整班次的行显示「缺上班或下班卡 → 重算记 0」。补上了「向下取整」这一步（原来跨度3.7、扣减1
   直接得 2.5，中间那步看不见）。
2. **排序**：整表固定按 **归属 → 部门 → 姓名 → 手机尾号 → 日** 排序（中文按拼音、日按数字），
   同一个人的日子连在一起，好逐日看。

## 校验

- 前端 build 通过；纯前端改动。
