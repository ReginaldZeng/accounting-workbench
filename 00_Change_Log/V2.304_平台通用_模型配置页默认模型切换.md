[Change Log]
Date: 2026-08-17
Author: Claude / c
Version: V2.304
Description: 【平台通用】模型配置页「默认模型」可点击切换（写回工作台 .env 热生效）；deepseek 建议芯片按官方 2026-08 枚举（v4-flash/v4-pro + 兼容别名）。配 BP V2.231 /model 端点。

---
version: V2.304
date: 2026-08-17
line: 平台通用
author: Claude / c
status: Synced
revision: 01
---

**【平台通用】模型配置页：默认模型点击即切**

## 改动

| 文件 | 内容 |
|---|---|
| `routers/llm_hub.py` | `POST /api/llm-hub/model`：代理到工作台 `POST /api/llm/model`（写它的 .env + 热生效）；管理员；audit 记「切换默认模型 provider→model」；成功失效状态缓存 |
| `ModelConfig.jsx` | ①区卡片「默认模型」变 ModelSwitcher：点击展开（虚线下划线提示可点）→ 建议芯片一键切 / 手输任意模型名（后端有意不做白名单，供应商上新不等发版）→ 保存后卡片刷新。建议清单：deepseek=官方 2026-08 枚举 `deepseek-v4-flash`/`deepseek-v4-pro` + 兼容别名 chat/reasoner；kimi/glm/dashscope 常用各三。加 `useEffect([p.model])` 兜底：状态刷新必收起编辑态 |
| `api.js` | setWorkbenchModel |

BP 侧配套：V2.231 `/api/llm/model` 端点（appSettings 闸；与 /key 同机制）。

## 验证（本机全链路）

- API：kimi 切 moonshot-v1-32k（专属变量路径）+ deepseek 切 v4-pro 再切回 chat（默认供应商老变量路径），status 立即反映、BP 不重启。
- UI 单击流：点模型 → 芯片选 v4-pro → 保存 → 卡片显示 `deepseek-v4-pro ▾` 编辑态自动收起；再切回 chat 同样通过。
- 注：合成连环点击脚本下曾出现编辑态不收（真实单击无此问题），已加 p.model 变化兜底收起。

## 关联

- 现场切换（问数/分析里临时选模型，不改默认）在 BP V2.232——本页管"默认"，AI 页面管"这一次"。
- DeepSeek V4 Pro：1M 上下文旗舰，贵且慢——日常路由/问数留 flash，深度分析临时切 pro 更划算。
