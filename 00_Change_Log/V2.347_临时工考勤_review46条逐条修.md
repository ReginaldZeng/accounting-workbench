# V2.347 · 临时工考勤 · 全量 review 46 条发现逐条修

**日期**：2026-08-22　**分支**：main　**提交**：14e0fb8

## 怎么 review 的
5 个视角（内核逻辑 / 路由与导出 / 前端运行时 / 文案一致性 / 跨层字段契约）各自通读，
每条发现再由 2 个独立验证者（一个专门反驳、一个专门复现）复核。
50 条原始 → 49 条去重 → **46 条通过（43 条两票同意）**，剔除 3 条。103 个 agent，22 分钟。

## 修掉的「高」
| 问题 | 后果 | 修法 |
|---|---|---|
| 白夜混合的人按白班逻辑硬切 | 夜班 19:45 上班那天被判「多记 11 小时·异常」，进异常金额和复核结论 | 逐日单独成档「◇ 白夜混合，待人工」，不进四档、不计异常、不进结论；页面/导出同步 |
| `settle_verdict` 只按姓名定位格子 | 同名跨派遣方时把华顺的多记记到锦绣头上：有问题那家正常、没问题那家异常 | 改按（姓名，归属）定位；同名多行两家都记一笔；明细格差异计入 issues，一正一负抵成 0 的小计不再装作正常 |
| 看完 7 月留档去传 8 月的表 | 页面残留月份优先于表标题 → 按 7 月合同价算 8 月、还覆盖 7 月留档 | 选文件即清月份；`/review` 发现所选期间≠表标题直接拦住 |
| 右上角期间选择器只改月份不换结果 | 认定/撤销/导出落到别的月 | 选到已核月份直接打开那期；没核过的清掉当前结果 |

## 修掉的「中」（摘要）
- `_band` 白班缺失造 (0,0)：只登记夜班价的行被当"白班 0 元" → 白夜对称为 None
- 登记为 0 的管理费（保洁）也要比 → `pay_check` 判「已登记」改看 `contract_band` 是否 None
- 删旧「按月保存」表回读——单价唯一来源＝登记表，来源文案不再张冠李戴
- V2.346 前的留档快照没有合同价核对却满屏「正常」 → 读出/导出时打「旧版留档」标记，结论一律待核
- 期中调价 / 登记表问题行随结果带回，对比页与导出⑤页都讲清楚
- 导出④逐日加「已认定」列（公式外层包「✓ 已认定」，③页不再数它）+ 隐藏键列，
  ③页 SUMIFS 按 姓名|归属|部门 匹配，同名两行不再互相串
- ①页加白夜混合 / 已认定 / 未计价三行；口径说明从参数取值；②页小计/合计结论格三态底色；
  无金额列时自查段不再写「全部通过」
- 复核结论横幅改以后端 `settle.合计.结论` 为准（早先页面另算一套，与下表打架）
- 对比页缺档从红改琥珀「待核」，加表头解析参考段；期次列表加「结论」列；看板第⑥段加复核结论/缺合同价
- 「待人工确认」计数不再把缺合同价 40 人算进去；403 文案改「维护合同价登记表」；删死组件 `RateRow`
- 第①步「打卡表里有同名多行」曾渲染成 `[object Object]`（对象列表当字符串 join）

## 刻意没改
- `api.js` 两个指向已删接口的死导出与注释、`db.py` 权限点标签「维护单价表（按月）」——
  这两个文件平台线正在改、未提交，不碰。下次平台线提交后再清。
- 白夜混合的真正解法（逐日按打卡形态判白/夜）等人力给切班规则（需求确认书 Q1）。

## 验证
- 内核测试 95 条全过（新增 6 条）
- 三种合同价状态 + 旧版留档，导出 Excel 均通过
- 浏览器实测（本机 8071）：②状态条三态、③对比页琥珀缺档、④应付标缺合同价、⑤「◇白夜混合」筛选、
  ⑦结论表四家待核/锦绣异常；**认定 → 撤销一整圈跑通**
