# V2.453 · 权限中枢：登记 BP 电商定价工作台两个敏感点 `logistics:confirm` / `combo:code`（BP V2.412）

**日期**：2026-09-06　**来源**：BP 线《对接需求_电商定价权限点登记_20260906》（桌面）。

- BP V2.412 新增两个 L3 敏感点：`logistics:confirm`＝物流测算行「物流确认/撤销」（物流部）；`combo:code`＝待编码组合「保存编码 / 导入回填」（IT）。进板块仍靠 `bp:board:pricingEcom`。
- 核算侧不登记 → 账号页勾不到、**主管理员也失权**（透传串 `['*'] + BP_SENSITIVE_CODES` 里没有它们，BP 对敏感码 `'*'` 不覆盖）。
- 改动（按 BP 线原文，一字未改语义）：`CAP_META_STATIC` 在 `budget:unlock` 后追加两条（ws=bp、组 定价测算、敏感、mod `bp:board:pricingEcom`）；`BP_SENSITIVE_CODES` 追加两码。
- 验证：`/api/bp-perm-drift` 应从「missing 2 / superSensitiveGap 2」变 ok；主管理员登 BP → 电商定价测算顶部出现「物流确认权限」「组合编码权限」标签；给物流同事勾 `logistics:confirm`（板块 + 此点即可，不需 bp:edit），IT 勾 `combo:code`。
- 本机核对：`bp_perm_codes(主管理员)` 含两码；`CAP_META_STATIC` 两条已在且敏感。
