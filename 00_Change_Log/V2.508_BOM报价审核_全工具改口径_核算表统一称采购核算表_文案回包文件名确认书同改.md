# V2.508 · 全工具改口径：「核算表」统一称「采购核算表」（文案 / 回包 / 导出文件名 / 确认书 v1.3）

**日期**：2026-09-07　**业务方定**：「核算表应该是采购核算表」「整个工具你可能都要调整下」→ 三选一答「全部改成采购核算表」。

## 改

- 前端 `BomPrice.jsx` 67 行、`api.js`/`App.jsx` 注释各 1 行；后端 `routers/bom_quote.py` 70 行（提示语、审计文案、导出文件名如 `重排版采购核算表_CP_名.xlsx`、`采购核算表两版_….zip`）；确认书 v1.3 md 22 行 + Word 重出。
- 规则：`成本核算表`→`采购核算表`；其余裸 `核算表`→`采购核算表`（详情、导出、看子采购核算表、上传采购核算表…）；已是「采购核算表」的不重复加前缀。
- **不动**：`kernels/`（解析器认 xlsx 里的「成本核算样表」标记与星期九原版模板，改了解析就错）；样例种子的文件名 glob；钉钉表单字段名「成本核算表（商务输出）/（商品版本）」（那是 OA 的字，改了对不上）。
- 静态包重建：bundle 内「采购核算表」61 处、裸「核算表」0 处。
