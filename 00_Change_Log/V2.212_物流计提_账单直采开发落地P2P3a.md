[Change Log]
Date: 2026-08-06
Author: Claude / c
Version: V2.212
Description: 【物流计提】账单直采 P2+P3a 开发落地:三张映射维表+种子(MAP 硬编码退役)、账单解析内核(7 家适配器)、活表 API(row-refresh/采纳入维表)、前端五步改造(上传账单主入口/映射与税率四tab/活表复核+做账去向)。全链路实测通过:7 月真账单包 208 票 212,120.96→28 行零差。

---
version: V2.212
date: 2026-08-06
line: 物流计提
author: Claude / c
status: Synced(worktree 8082 实测)
revision: 01
---

**【物流计提】重构方案 v2.1 的 P2+P3a 开发落地**(业务方 2026-08-05「先开工」授权;金蝶录入接口链路未动、留人工实测)。

**P2 · 映射外置**(db.py):
- 三张新维表+CRUD+种子(空表幂等播):`logistics_fee_map`(费用归属映射,**四级取数** 费用归属×主体×业务线 精确→主体例外→业务线例外→默认;种子=13 默认行+3 例外行[深九出库→永续供应中心/小料原料入库→茶饮小料部/孝感线下仓仓储→仓储物流部],设备调拨/其它 manual=1)、`logistics_bizline`(10 业务线+CPFL+CPXM,TO C 规则=cpxm 非空即挂,kikiherb=CPFL013+CPXM022)、`logistics_type_map`(23 键账单标注翻译种子);
- 税率维表种子 15 条(v1.8 矩阵口径:全名×13 类,易风达/极鲜达仓储 6% 特例)。

**P3a · 账单直采内核**(`kernels/logistics_bills.py` 新):7 家适配器(丰源/易风达/极鲜达/跨越/链盟/顺丰冷运/顺丰速运,文件名认商)→标注翻译(维表精确→规则兜底→待人工)→聚合(主体×商×费用归属×业务线×描述,一行一凭证)→维度预填(四级取数+产品维度仅 6601/6401 挂)→活表行(voucher 同构)。`finalize_row`=行重算唯一事实源(税率/摘要/分录/可录入)。**解析坑修复**:易风达仓储 sheet 按账期月过滤(账单混补账月,曾多抓 57,347.90)/极鲜达零担合计行 break(continue 会捡尾部杂行)。

**P3a · API**(routers/logistics_accrual.py,+11 条):bills-parse(多文件)/row-refresh(改维度服务端重算;待人工占位描述补齐后自动清)/adopt(采纳入维表)/三维表 CRUD×9。post 接口:账单行服务端**重拼摘要**(防重幂等键唯一事实源);build_kd_model 产品分类编码字段直通(行值优先,BIZLINE_CODE 兜底+补 kikiherb)。

**P3a · 前端**(LogisticsAccrual.jsx+api.js):步①「上传账单」=账单包多文件主入口(计提月+per_file 状态卡+unknown 提示)+手填表降兜底;步②「映射与税率」四 tab(费用归属映射[例外行蓝底可删]/业务线[编码可改]/标注翻译[可增删]/税率);步③活表=点行展开维度编辑器(主体/费用归属/业务线→requery 重查映射,科目/部门/费用项目→人工覆盖;改动行琥珀+「采纳入维表」;红行=待人工)+**做账去向汇总**(主体×科目×部门×业务线,随改动实时变,含🖐待定行);步④⑤录入/打印零改动(行同构直接吃)。

**实测**(worktree 8082,launch.json 已建 wt-logistics-accrual):
- 内核基线:7 文件 208 票 212,120.96 → 28 行零差、待人工 2 行 1,786.08(=POC);山姆送仓 9% 未税 131,167.55、摘要逐字复刻真凭证;
- HTTP 全链:登录→bills-parse→row-refresh(丰源补维度→默认层级命中+摘要"计提湖北丰源物流供应链管理有限公司7月线下零售出库运费")→人工改部门→adopt 例外行落库(操作人留痕)→删除清理;
- UI 走查:步①主入口/步②四 tab 实数据渲染(映射 16·业务线 10·翻译 23·税率 15)、侧栏版本自报 V2.211+分支+8082。
- ⚠登录 body 字段=`password`(非 pwd);React 受控输入 form_input 不触发 onChange 须 computer.type;worktree 前端依赖=junction 主 repo node_modules。

**未含**:真录金蝶(链路未动,防重双闸原样);P3b 兜底长表解析(手填新模板);P4 回翻;按月签发模板下载。
