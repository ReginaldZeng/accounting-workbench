# V2.595 科目余额解析（物流）：逐笔对账找差——显差额 + 看本科目本期全部凭证

因 2221.01.07/2241.02 供应商藏在摘要、只能模糊匹配，逐笔合计可能跟余额表本期发生对不上。加"找是哪一笔"的对账能力：

- 下钻面板：逐笔合计 vs 余额表本期发生（借/贷各自）差额一旦≠0，红条标出"借差X、贷差Y —— 多半某笔摘要没写供应商名没归进来"。
- 「看本科目本期全部凭证 → 找是哪一笔」按钮：detail 加 full=1，`build_voucher_lines` 加全科目模式（dim/dim_name 都不传＝该科目本期全部凭证，不按供应商过滤），列出含各供应商的全部逐笔，用户在里面找摘要没写本供应商、金额≈差额的那一笔。
- 结构化字段根治备注：本想给序时账加"往来单位"候选字段(FASSTACTID 等)走精确匹配以彻底消差，但 _fetch_resilient 按前缀丢字段会连带丢掉同前缀的 FDetailID.* 列，风险大，暂缓；先给可靠的人眼对账。

验证：内核全科目模式（不传维度=全收该科目）已加；py_compile/构建/EOL 均过；样例模式逐笔严丝对上、不出差额条。⚠差额与全部凭证须真机看。

发布：沿用 main 自动部署，须重启后端。回退＝反向提交本提交。
