# V2.592 科目余额解析（物流）：修下钻取不到凭证——放宽账簿匹配 + 加真机诊断接口

真机反馈：下钻/追溯每期都"本期无逐笔凭证"（连有发生额的月份也空），一串数字没有凭证明细＝不知道余额怎么来的。定位：序时账逐笔按「账簿名==核算组织名」严格过滤，金蝶里 FAccountBook.FName 与 FOrgId.FName 常不完全相同 → 全被过滤掉（疑因）；也可能是序时账维度字段(FF100002)与科目余额表维度(FDETAILNUMBER)不是同一个 flex。

- 放宽账簿匹配 `_book_match`：空组织名＝不过滤；否则相等或互相包含即命中（先解最可能的单点原因）。
- 新增诊断接口 `GET /api/fi-subject-balance/_probe?code=2221.01.07&org=<可选>`（仅「上传科目余额表」权限可看）：返回 报表侧该科目维度编码样本 + org_name（账簿过滤用）+ 序时账原始行数/distinct账簿/前5行原样 —— 供真机对照，看清序时账到底把供应商放在哪个字段、账簿名叫什么，据此精确修维度匹配。

验证：py_compile 通过；EOL --stat 与 --ignore-space-at-eol 一致；样例模式不受影响（_probe 直接返回"样例模式无需诊断"）。⚠ 本版是"放宽账簿+诊断"，序时账维度字段是否还需改（FF100002→供应商 flex）待真机 _probe 结果确认，下一版据此定修。

发布：沿用 main 自动部署，须重启后端；仅后端改动（无前端/静态包变化）。真机步骤：重启后打开 `.../api/fi-subject-balance/_probe?code=2221.01.07`，把返回 JSON 贴回以定位。回退＝反向提交本提交。
