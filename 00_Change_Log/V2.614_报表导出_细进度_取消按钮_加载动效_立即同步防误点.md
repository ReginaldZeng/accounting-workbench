# V2.614 报表导出：细进度（阶段+行数）+ 取消按钮 + 加载动效 + 「立即同步」防误点

接 V2.613。2026-09-23 真机两个坑：①业务方把「立即同步」当成导出按钮点，等了半天没东西；②107 号跑了一个多小时，页面只有「正在 107」，看不出在跑还是卡死，也没法停。

## 后端
- `_JOB` 加 `stage / rows / rows_total`：`export_one(progress=)` 逐阶段上报——取三大报表 → 取科目余额 → 取序时账簿（每页报已取行数）→ 写入 Excel（每 2 万行报一次，带总行数）→ 存盘。`fetch_journal_full(on_page=)`、`build_workbook/_write_journal(progress=)` 各加一个可选回调，不传时行为不变。
- 取消：`POST /api/rptexport/cancel`（需 rpt_export）只打标记；进度回调里看到标记就抛 `_Canceled`，当前主体不落盘（本来就先写 .part 再改名），已导好的保留；不发回执，留审计。本机真跑 107：第 60 秒点取消，**0.8 秒**停下，无残留文件。
- 本机真跑 101+108 正常路径：阶段依次上报、两个文件落盘。

## 前端（RptExport.jsx / styles.css）
- 导出按钮：点下去立刻「启动中…」+ 小转圈；请求失败（断网等）把原因写出来——原来不 catch，静默失败＝"点了没反应"。
- 进度区：阶段行「● 取序时账簿 · 已取 17.5 万行」/「写入 Excel · 42.0 万行 / 42.3 万行」；「取消导出」按钮（确认框写清楚保留什么、作废什么）；有人点了取消显示「xx 已请求取消」。
- 加载动效（只表达"还在动"，不假装进度）：进度条跑时带流动斜纹、跑完收成实色；写 Excel 阶段把当前主体完成比例并进进度条；阶段行复用导航的呼吸绿灯；按钮小转圈。`prefers-reduced-motion` 下全部静止。
- 「立即同步」：当期服务器上一个文件都没有时不再回"已通知取件机"，直接提示「还没导出过……请先点上面蓝色的导出」；加悬停说明"不会去金蝶取数"。
- 修：打开页面时已有导出在跑（别人点的/自己刷新了），原来进度只取一次就定格、像卡死；现在自动接着轮询。

验证：py_compile / 前端构建通过；后端用真金蝶跑正常路径+取消；前端用模拟接口渲染真实组件，逐状态核对（启动中→导出中+取消+斜纹+呼吸灯→行数递增→取消后静止、提示正确），截图确认。

发布：改后端 .py，自动部署会**重启后端**（正在跑的导出会中断）。回退＝反向提交本提交。
