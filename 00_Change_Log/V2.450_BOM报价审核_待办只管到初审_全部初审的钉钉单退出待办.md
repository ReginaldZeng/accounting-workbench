# V2.450 · 「待办与复核」只管到初审：一单产品全部初审（且无待修）＝成本会计任务完成，退出待办

**日期**：2026-09-05　**业务方定**：「定稿了的就不需要列在这个待办啦」「这里只处理初审的动作，到初审结束，成本会计就任务完成了」。

- 待办列表默认**隐藏**已完成的钉钉单（`pending==0 && blocked==0`）；筛选栏加「显示已完成 N」切换回看。
- 顶部统计「待办单号 / 组 / 待复核」只算未完成的单，副标注「另 N 单已完成初审」。
- 进度标签「全部已定稿」→「全部已初审 · 已完成」；有待修批次的单显「待修 N」不算完成；混合态同时显「已初审 N」「待修 N」。
- 待办清空时的空态文案讲清：成本会计任务完成，终审由财务BP在「标准成本台账」做。
- 纯前端；后端 `_approval_summ` 不动。
