# V2.627 发票管家：审核页发票号码显示全＋一键复制

用户：审核页发票号码只显示后 8 位（…52467747），会计要整串复制去金蝶做账，希望显示全或给复制按钮。

- 「已收发票」表的发票号码列改为**显示完整号码**（20 位数电号码整串），号码可直接选中。
- 号码后加「复制」按钮，点一下复制整串、显示「已复制」，点它不会打开核对弹窗。
- 复制走 `navigator.clipboard`，被内嵌浏览器/未聚焦拒绝时退回 `execCommand`，尽量都能复制上。

顺带记录：**纯发票、没有付款单的情况**（如广告费投放）本来就支持——在「收票工作台」点「手工票夹」，单据金额留空即可，票照常进「发票审核」「发票台账」；审核页对没有付款金额的单会写明「不做差额核对」，不会误报差额。本版不改这块。

验证：前端浏览器回归 33 全过（发票号码显示全、复制按钮、点复制不打开弹窗）。

发布：只改前端。回退＝反向提交本提交。
