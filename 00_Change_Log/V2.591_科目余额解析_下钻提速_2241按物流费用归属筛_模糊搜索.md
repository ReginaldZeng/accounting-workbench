# V2.591 科目余额解析（物流）：下钻提速 + 2241.02 按物流费用归属再筛 + 模糊搜索

真机联调反馈：慢，且 2241.02 混进大量非物流供应商（/专业服务、/市场推广、/装修服务、/资产采购…）。修：

- **下钻提速**：原来每点一次维度行都把 6601/6604/6401/5101 全部费用科目整月序时账全拉一遍（真机"加载明细中…"卡的根因）。改为**只查被点的那一个科目**（`fetch_gl_voucher_subjects(prefix=该科目码)`）+ 按 (源,年,期,科目,账簿) **缓存**；`/sync` 时清缓存。追溯同样按科目定向取。
- **2241.02 再筛**：其他应付款—供应商往来是全供应商共用账，维度=供应商/费用归属。只保留**费用归属为物流类**（关键字：物流/运输/运费/仓储/搬运，取维度名最后一段匹配，避免误命中供应商名）；2221.01.07 暂估进项税＝物流计提专用仍全收；费用科目仍按费用项目关键字。
- **模糊搜索**：主表上方加搜索框，前端即时按 科目编码/名称、供应商、费用项目 子串筛，显示"筛出 X / 共 Y 行"+清除；空结果友好提示。

验证：样例模式浏览器实点——搜"顺丰"筛出 5/9 行、只剩顺丰相关三科目各自维度，质检 9 行全平、下钻/追溯照常；py_compile 通过；前端构建通过；EOL 复核 --stat 与 --ignore-space-at-eol 一致。⚠ 金蝶真数据的提速与 2241.02 再筛须真机确认（尤其：2241.02 是否只剩物流运输服务供应商、下钻是否变快、物流费用 6601 等能否按费用项目出来）。

发布：沿用 main 自动部署，须重启后端；admin-only 待验收。回退＝反向提交本提交。
