# V2.593 科目余额解析（物流）：下钻/追溯凭证为空时内嵌真机诊断

V2.592 放宽账簿匹配后真机仍"本期无逐笔凭证"——大概率是序时账维度字段(FF100002)≠科目余额表维度(供应商)。为免用户手动开 _probe 网址，把诊断直接嵌进页面：

- 下钻详情 / 追溯每期，当【本期有发生却匹配到0条凭证】时，返回 `_debug`：序时账未过滤行数、账簿过滤org_name、distinct账簿、样例行前3原样（金蝶字段原样）；前端在该行下方以等宽虚线框打印。追溯侧最多附2处诊断（限金蝶调用）。
- 用户照常点开某维度行 / 追溯，截图诊断框即可，无需手动构造 URL。据其中"样例行前3原样"的字段名/值定位供应商到底在哪个 flex、账簿实际叫什么，下一版精确修维度匹配。

验证：py_compile 通过；前端构建通过；EOL --stat 与 --ignore 一致；样例模式不出 _debug（笔数>0）。纯诊断，不改口径与取数逻辑。

发布：沿用 main 自动部署，须重启后端。回退＝反向提交本提交。
