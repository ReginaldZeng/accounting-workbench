# V2.433 · 替换留痕 / 历史留痕默认折叠（点开展开）

**日期**：2026-09-05　**分支**：main

Reginald：组三（历史留痕块）和替换留痕能不能缩起来。

## 改动

处理页两处历史列表改用原生 `<details>/<summary>`，**默认折叠、点标题展开**，不占版面：
- 每组内的「替换留痕 · 审核历史（N）」。
- 单末尾的「历史留痕（已全部作废 / 被替换的旧版）」块（V2.432 的 deadHist）。

标题带「· 点开查看」提示。留痕内容一条不动，只是默认收起。前端 build 过。
