# V2.422 · 判定文案统一「撑不起上报（整期超弹性 / 整期已消化）」

**日期**：2026-09-02　**分支**：main

Reginald：「撑不起上报的叫什么现在？」——查出 V2.420 改判定时**只改了导出、没改内核**，网页/导出/筛选 tab 三处叫法不一：
网页逐日写「打卡撑不起上报,异常」、导出写「整期超弹性,异常」、tab 写「撑不起上报」、消化的天网页写「不单独判」导出写「已消化」。

## 统一

类别统一叫**「撑不起上报」**（＝在厂时长撑不起上报的班次，与 tab 一致），后缀整期结论：

- 整期超弹性（真异常，红）：`⚠ 多记 X 小时(撑不起上报，整期超弹性)`
- 整期没超（降级中性）：`○ 多记 X 小时(撑不起上报，整期已消化)`

改齐三处：内核 compute 判定字串（网页逐日）、④导出判定公式、前端 BAND 档标签；
①概览四档表「⚠撑不起上报（整期超弹性）」列头改「日次 / 人数」、数字用超弹性人数、小时/金额用整期净多记。
测试断言同步（整期在弹性内→整期已消化）。内核 116/116 全过、前端 build 过。
