# V2.596 科目余额解析（物流）：探测序时账核算维度槽，定位供应商在哪个槽（奔零差额根治）

用户指出：供应商在核算维度里、是结构化的（不只摘要）。上一版只取了 FF100002 槽（对 2221.01.07/2241.02 是空的），供应商在另一个维度槽。

- 新增 `kc.probe_voucher_dims(year,period,code)`：逐个单独查序时账候选核算维度槽（FASSTACTID.FNumber/FName、FF100001/3/4/5/6/7.FNumber，各自 _query_raw 互不影响、无效槽只报错不连累其它），返回每槽的非空样例值。据某槽样例值＝「物流运输服务016」即可认定它。
- 下钻详情：kingdee 且【没匹配到凭证 或 逐笔合计与余额表本期发生有差】时，_debug 附上该探测结果（前端已有等宽框渲染）。用户点开某供应商即见，截图即可定位字段。
- 下一版据此把匹配改为按该正确维度槽精确匹配 → 零差额根治。

验证：py_compile 通过；EOL --stat 与 --ignore 一致；纯诊断（仅金蝶且有问题时触发），样例/正常匹配不受影响；仅后端改动。

发布：沿用 main 自动部署，须重启后端。回退＝反向提交本提交。
